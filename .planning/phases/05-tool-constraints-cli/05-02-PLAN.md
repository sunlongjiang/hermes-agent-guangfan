---
phase: 05-tool-constraints-cli
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - evolution/tools/evolve_tool_descriptions.py
  - tests/tools/test_evolve_tool_descriptions.py
autonomous: true
requirements:
  - TOOL-11

must_haves:
  truths:
    - "python -m evolution.tools.evolve_tool_descriptions --help 显示 --iterations, --eval-source, --hermes-repo, --dry-run 四个选项"
    - "evolve() 函数按 discover->extract->module->dataset->GEPA->constraints->evaluate->save 顺序编排"
    - "dry-run 模式验证 hermes-agent 可达、工具可加载，展示工具列表，不调用 GEPA"
    - "约束门禁在 GEPA 之后、holdout 评估之前执行，包含 size + factual + regression 三类检查"
    - "结果保存到 output/tools/ 目录，包含进化描述、metrics.json、diff 文本"
  artifacts:
    - path: "evolution/tools/evolve_tool_descriptions.py"
      provides: "Click CLI 入口 main() + 业务逻辑 evolve() + _generate_diff()"
      exports: ["main", "evolve"]
      min_lines: 200
    - path: "tests/tools/test_evolve_tool_descriptions.py"
      provides: "CLI 参数测试 + dry-run 测试 + evolve 编排测试"
      min_lines: 60
  key_links:
    - from: "evolution/tools/evolve_tool_descriptions.py"
      to: "evolution/tools/tool_loader.py"
      via: "discover_tool_files + extract_tool_descriptions"
      pattern: "from evolution\\.tools\\.tool_loader import"
    - from: "evolution/tools/evolve_tool_descriptions.py"
      to: "evolution/tools/tool_module.py"
      via: "ToolModule 构建和 get_evolved_descriptions"
      pattern: "from evolution\\.tools\\.tool_module import ToolModule"
    - from: "evolution/tools/evolve_tool_descriptions.py"
      to: "evolution/tools/tool_constraints.py"
      via: "ToolFactualChecker 事实准确性门禁"
      pattern: "from evolution\\.tools\\.tool_constraints import ToolFactualChecker"
    - from: "evolution/tools/evolve_tool_descriptions.py"
      to: "evolution/tools/tool_metric.py"
      via: "tool_selection_metric + CrossToolRegressionChecker"
      pattern: "from evolution\\.tools\\.tool_metric import"
    - from: "evolution/tools/evolve_tool_descriptions.py"
      to: "evolution/tools/tool_dataset.py"
      via: "ToolDatasetBuilder + ToolSelectionDataset"
      pattern: "from evolution\\.tools\\.tool_dataset import"
---

<objective>
构建 `evolve_tool_descriptions.py` CLI 入口点，串联 Phase 2-5 所有组件为端到端可运行的工具描述优化管道。

Purpose: 让用户通过 `python -m evolution.tools.evolve_tool_descriptions` 一条命令运行完整的工具描述进化流程（TOOL-11）。
Output: `evolution/tools/evolve_tool_descriptions.py` 和 `tests/tools/test_evolve_tool_descriptions.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tool-constraints-cli/05-CONTEXT.md
@.planning/phases/05-tool-constraints-cli/05-RESEARCH.md
@.planning/phases/05-tool-constraints-cli/05-01-SUMMARY.md

<interfaces>
<!-- Plan 01 产出的 ToolFactualChecker 接口 -->
From evolution/tools/tool_constraints.py:
```python
def _parse_bool(value) -> bool: ...

class ToolFactualChecker:
    def __init__(self, config: EvolutionConfig): ...
    def check(self, tool_name: str, original_description: str, evolved_description: str) -> ConstraintResult: ...
    def check_all(self, original_tools: list, evolved_tools: list) -> list[ConstraintResult]: ...
```

<!-- Phase 2-4 组件接口 -->
From evolution/tools/tool_loader.py:
```python
@dataclass
class ToolDescription:
    name: str
    file_path: Path
    description: str
    params: list[ToolParam] = field(default_factory=list)
    desc_format: DescFormat = DescFormat.SINGLE_LINE
    schema_var_name: str = ""
    raw_source: str = ""

def discover_tool_files(hermes_agent_path: Path) -> list[Path]: ...
def extract_tool_descriptions(file_path: Path) -> list[ToolDescription]: ...
```

From evolution/tools/tool_module.py:
```python
class ToolModule(dspy.Module):
    def __init__(self, tool_descriptions: list[ToolDescription]): ...
    def forward(self, task_description: str) -> dspy.Prediction: ...
    def get_evolved_descriptions(self) -> list[ToolDescription]: ...
```

From evolution/tools/tool_dataset.py:
```python
class ToolDatasetBuilder:
    def __init__(self, config: EvolutionConfig): ...
    def generate(self, tool_descriptions: list) -> ToolSelectionDataset: ...

class ToolSelectionDataset:
    train: list[ToolSelectionExample]
    val: list[ToolSelectionExample]
    holdout: list[ToolSelectionExample]
    def save(self, path: Path): ...
    @classmethod
    def load(cls, path: Path) -> "ToolSelectionDataset": ...
    def to_dspy_examples(self, split: str = "train") -> list[dspy.Example]: ...
```

From evolution/tools/tool_metric.py:
```python
def tool_selection_metric(example: dspy.Example, prediction: dspy.Prediction, trace=None) -> float: ...

class CrossToolRegressionChecker:
    def __init__(self, regression_threshold: float = 0.02): ...
    def compute_per_tool_rates(self, predictions: list[tuple[str, str]]) -> dict[str, float]: ...
    def check_regression(self, baseline_rates: dict[str, float], evolved_rates: dict[str, float]) -> ToolRegressionResult: ...
```

From evolution/core/constraints.py:
```python
class ConstraintValidator:
    def __init__(self, config: EvolutionConfig): ...
    def _check_size(self, text: str, artifact_type: str) -> ConstraintResult: ...
    def _check_growth(self, text: str, baseline: str, artifact_type: str) -> ConstraintResult: ...
    def _check_non_empty(self, text: str) -> ConstraintResult: ...
```

From evolution/skills/evolve_skill.py (CLI 模式参考):
```python
@click.command()
@click.option("--skill", required=True, ...)
@click.option("--iterations", default=10, ...)
@click.option("--eval-source", default="synthetic", type=click.Choice([...]), ...)
@click.option("--hermes-repo", default=None, ...)
@click.option("--dry-run", is_flag=True, ...)
def main(skill, iterations, eval_source, ...): ...
    evolve(...)

if __name__ == "__main__":
    main()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建测试骨架并验证 CLI 参数和 dry-run 行为</name>
  <files>tests/tools/test_evolve_tool_descriptions.py</files>
  <read_first>
    evolution/skills/evolve_skill.py (CLI Click 参数模式)
    tests/tools/test_tool_module.py (existing test patterns)
  </read_first>
  <behavior>
    - Click CLI main() 命令的 --help 输出包含 "--iterations", "--eval-source", "--hermes-repo", "--dry-run"
    - --eval-source 只接受 "synthetic" 和 "load" 两个选项
    - evolve_tool_descriptions 模块可通过 python -m 运行（if __name__ == "__main__" 存在）
  </behavior>
  <action>
    **创建 `tests/tools/test_evolve_tool_descriptions.py`:**

    1. 使用 `click.testing.CliRunner` 测试 CLI 参数：
       - `test_cli_help()`: 调用 `runner.invoke(main, ["--help"])`，验证输出包含 "--iterations"、"--eval-source"、"--hermes-repo"、"--dry-run"
       - `test_eval_source_choices()`: 验证 --eval-source 只接受 "synthetic" 和 "load"
    2. 使用 `unittest.mock.patch` 测试 dry-run：
       - `test_dry_run_shows_tools()`: mock `discover_tool_files` 和 `extract_tool_descriptions` 返回假数据，调用 `evolve(dry_run=True, hermes_repo="/fake")`，验证不调用 GEPA（patch dspy.GEPA 并 assert_not_called）
    3. 使用 `unittest.mock.patch` 测试模块可导入：
       - `test_module_importable()`: `from evolution.tools.evolve_tool_descriptions import main, evolve`

    **注意：** 此时 `evolve_tool_descriptions.py` 尚未创建，测试应先写好再在 Task 2 实现。但由于 TDD 在单个 plan 内可合并执行，Task 1 的测试可以先 red 然后在 Task 2 同时 green。实际执行时，executor 应先写好测试文件（红色），再实现源代码（绿色）。
  </action>
  <verify>
    <automated>cd /Users/slj/项目/hermes-agent-self-evolution && python -c "import tests.tools.test_evolve_tool_descriptions; print('test file importable')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/tools/test_evolve_tool_descriptions.py` >= 3
    - `grep "CliRunner" tests/tools/test_evolve_tool_descriptions.py` 至少出现 1 次
    - `grep "dry_run" tests/tools/test_evolve_tool_descriptions.py` 至少出现 1 次
  </acceptance_criteria>
  <done>
    测试文件存在，包含 CLI 参数验证、dry-run 行为验证、模块可导入验证的测试函数。
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 evolve_tool_descriptions.py 端到端管道</name>
  <files>evolution/tools/evolve_tool_descriptions.py</files>
  <read_first>
    evolution/skills/evolve_skill.py (完整参考模板 -- 编排流程和 CLI 模式)
    evolution/tools/tool_constraints.py (Plan 01 产出的 ToolFactualChecker)
    evolution/tools/tool_module.py (ToolModule 和 get_evolved_descriptions)
    evolution/tools/tool_dataset.py (ToolDatasetBuilder 和 ToolSelectionDataset)
    evolution/tools/tool_metric.py (tool_selection_metric 和 CrossToolRegressionChecker)
    evolution/tools/tool_loader.py (discover_tool_files 和 extract_tool_descriptions)
    evolution/core/constraints.py (ConstraintValidator._check_size/_check_growth/_check_non_empty)
    evolution/core/config.py (EvolutionConfig)
  </read_first>
  <action>
    **创建 `evolution/tools/evolve_tool_descriptions.py`:**

    模块级 docstring（per D-07）：
    ```
    """Evolve hermes-agent tool descriptions using DSPy + GEPA.

    Usage:
        python -m evolution.tools.evolve_tool_descriptions --iterations 10
        python -m evolution.tools.evolve_tool_descriptions --eval-source load --hermes-repo /path/to/repo
    """
    ```

    **导入部分：**
    ```python
    import json, sys, time, difflib
    from pathlib import Path
    from datetime import datetime
    from typing import Optional

    import click, dspy
    from rich.console import Console
    from rich.panel import Panel
    from rich.table import Table

    from evolution.core.config import EvolutionConfig, get_hermes_agent_path
    from evolution.core.constraints import ConstraintValidator
    from evolution.tools.tool_loader import discover_tool_files, extract_tool_descriptions, ToolDescription
    from evolution.tools.tool_module import ToolModule
    from evolution.tools.tool_dataset import ToolDatasetBuilder, ToolSelectionDataset
    from evolution.tools.tool_metric import tool_selection_metric, CrossToolRegressionChecker
    from evolution.tools.tool_constraints import ToolFactualChecker
    ```

    `console = Console()`

    **`_generate_diff(original_tools, evolved_tools) -> str` 函数：**
    - 构建 `original_map = {t.name: t for t in original_tools}`
    - 遍历 evolved_tools，用 `difflib.unified_diff()` 生成 before/after diff
    - fromfile=f"{evolved.name} (original)", tofile=f"{evolved.name} (evolved)"
    - 返回拼接后的 diff 字符串（per D-12）

    **`evolve(iterations, eval_source, hermes_repo, dry_run)` 函数（per D-04, D-05）：**

    1. **配置（per D-06）：**
       ```python
       config = EvolutionConfig(iterations=iterations)
       if hermes_repo:
           config.hermes_agent_path = Path(hermes_repo)
       ```

    2. **发现工具文件 + 提取描述：**
       - `tool_files = discover_tool_files(config.hermes_agent_path)`
       - 如果为空：`console.print("[red]No tool files found...[/red]")` + `sys.exit(1)`
       - 遍历 tool_files 调用 `extract_tool_descriptions(f)`，合并为 `all_tools: list[ToolDescription]`
       - 如果 all_tools 为空：`sys.exit(1)`
       - 打印工具列表（Rich Table：工具名、描述长度、参数数量）

    3. **dry-run 模式（per D-13）：**
       ```python
       if dry_run:
           console.print("[bold green]DRY RUN -- setup validated successfully.[/bold green]")
           console.print(f"  Would optimize {len(all_tools)} tool description(s)")
           console.print(f"  Dataset source: {eval_source}")
           if eval_source == "load":
               dataset_path = Path("datasets") / "tools"
               console.print(f"  Dataset path: {dataset_path} (exists: {dataset_path.exists()})")
           console.print(f"  Would run GEPA optimization ({iterations} iterations)")
           return
       ```

    4. **构建 ToolModule（保存 baseline 引用）：**
       - `original_tools = list(all_tools)` 保存原始工具列表的副本
       - `baseline_module = ToolModule(all_tools)`

    5. **生成/加载数据集：**
       - `eval_source == "synthetic"`: 使用 `ToolDatasetBuilder(config).generate(all_tools)`，保存到 `datasets/tools/`
       - `eval_source == "load"`: 使用 `ToolSelectionDataset.load(Path("datasets") / "tools")`，如不存在 sys.exit(1)
       - 打印 split 统计信息

    6. **GEPA 优化（per D-05）：**
       - 配置 DSPy: `lm = dspy.LM(config.eval_model); dspy.configure(lm=lm)`
       - 准备 trainset/valset: `dataset.to_dspy_examples("train")`, `dataset.to_dspy_examples("val")`
       - try GEPA:
         ```python
         optimizer = dspy.GEPA(metric=tool_selection_metric, max_steps=iterations)
         optimized_module = optimizer.compile(baseline_module, trainset=trainset, valset=valset)
         ```
       - except fallback to MIPROv2 (同 evolve_skill.py 模式)

    7. **提取进化后描述：**
       - `evolved_tools = optimized_module.get_evolved_descriptions()`

    8. **约束验证（per D-08, D-09，GEPA 之后 holdout 之前）：**
       - 8a. Size + growth + non_empty（per D-08）：
         - `validator = ConstraintValidator(config)`
         - 注意：不调用 `validate_all()`，直接逐工具逐参数调用单独方法（per RESEARCH anti-patterns）
         - 对每个 evolved_tool：
           - `validator._check_size(evolved_tool.description, "tool_description")`
           - `validator._check_non_empty(evolved_tool.description)`
           - 找到对应 original_tool，调用 `validator._check_growth(evolved_tool.description, original_tool.description, "tool_description")`
           - 对每个 param：`validator._check_size(param.description, "param_description")`
       - 8b. Factual accuracy（per D-01, D-02, D-08）：
         - `factual_checker = ToolFactualChecker(config)`
         - `factual_results = factual_checker.check_all(original_tools, evolved_tools)`
       - 8c. 打印所有约束结果（Rich 输出，icon + color）
       - 如果任一约束失败：
         - 保存 failed 结果到 `output/tools/FAILED_{timestamp}/`
         - `console.print("[red]Constraint validation FAILED -- not deploying[/red]")`
         - return

    9. **Holdout 评估（per D-05，含 baseline 和 evolved 两轮推理）：**
       - holdout_examples = dataset.to_dspy_examples("holdout")
       - 用 baseline_module 和 optimized_module 分别跑 holdout：
         ```python
         baseline_preds = []  # list of (correct_tool, selected_tool)
         evolved_preds = []
         for ex in holdout_examples:
             with dspy.context(lm=lm):
                 bp = baseline_module(task_description=ex.task_description)
                 baseline_preds.append((ex.correct_tool, bp.selected_tool))
                 ep = optimized_module(task_description=ex.task_description)
                 evolved_preds.append((ex.correct_tool, ep.selected_tool))
         ```
       - 计算 overall accuracy
       - 8d. Cross-tool regression（per D-10，这里放在 holdout 推理之后因为需要 per-tool rates）：
         ```python
         regression_checker = CrossToolRegressionChecker()
         baseline_rates = regression_checker.compute_per_tool_rates(baseline_preds)
         evolved_rates = regression_checker.compute_per_tool_rates(evolved_preds)
         regression_result = regression_checker.check_regression(baseline_rates, evolved_rates)
         if not regression_result.passed:
             console.print("[red]Cross-tool regression detected -- not deploying[/red]")
             # 保存 failed 结果
             return
         ```

    10. **报告结果（Rich Table）：**
        - Holdout Score (baseline vs evolved)
        - Tool count
        - Elapsed time
        - Iterations

    11. **保存结果（per D-11, D-12）：**
        - `timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")`
        - `output_dir = Path("output") / "tools" / timestamp`
        - `output_dir.mkdir(parents=True, exist_ok=True)`
        - 保存进化后描述文本：每个工具一个文件 `{tool_name}.txt`，或全部写入一个 `evolved_descriptions.json`（JSON 数组，每项包含 name + description）
        - 保存 `metrics.json`（per D-12）：timestamp, iterations, baseline_score, evolved_score, improvement, tool_count, constraints_passed
        - 保存 `diff.txt`（per D-12）：调用 `_generate_diff(original_tools, evolved_tools)`

    **Click CLI（per D-06, D-07）：**
    ```python
    @click.command()
    @click.option("--iterations", default=10, help="Number of GEPA iterations")
    @click.option("--eval-source", default="synthetic", type=click.Choice(["synthetic", "load"]),
                  help="Source for evaluation dataset")
    @click.option("--hermes-repo", default=None, help="Path to hermes-agent repo")
    @click.option("--dry-run", is_flag=True, help="Validate setup without running optimization")
    def main(iterations, eval_source, hermes_repo, dry_run):
        """Evolve hermes-agent tool descriptions using DSPy + GEPA optimization."""
        evolve(iterations=iterations, eval_source=eval_source, hermes_repo=hermes_repo, dry_run=dry_run)

    if __name__ == "__main__":
        main()
    ```

    **关键注意事项：**
    - 不调用 `write_back_description()`——只保存到 output/tools/（per D-11）
    - 不调用 `validate_all()`——逐工具逐参数调用 _check_size/_check_growth/_check_non_empty（per RESEARCH anti-patterns）
    - dry-run 对 "synthetic" 不实际调用 LLM（per RESEARCH Pitfall 4），对 "load" 验证路径存在
    - 保存 baseline_module 引用用于 holdout baseline 推理（per RESEARCH Pitfall 3）
    - 参数描述不做 factual check，只做 size check（per RESEARCH Pitfall 5，参数描述未被进化）
  </action>
  <verify>
    <automated>cd /Users/slj/项目/hermes-agent-self-evolution && python -m pytest tests/tools/test_evolve_tool_descriptions.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def evolve(" evolution/tools/evolve_tool_descriptions.py` 输出 1
    - `grep -c "def main(" evolution/tools/evolve_tool_descriptions.py` 输出 1
    - `grep -c "@click.command" evolution/tools/evolve_tool_descriptions.py` 输出 1
    - `grep "__name__.*__main__" evolution/tools/evolve_tool_descriptions.py` 匹配（python -m 可运行）
    - `grep "click.Choice.*synthetic.*load" evolution/tools/evolve_tool_descriptions.py` 匹配
    - `grep "dry_run" evolution/tools/evolve_tool_descriptions.py` 至少出现 3 次
    - `grep "ToolFactualChecker" evolution/tools/evolve_tool_descriptions.py` 至少出现 1 次
    - `grep "CrossToolRegressionChecker" evolution/tools/evolve_tool_descriptions.py` 至少出现 1 次
    - `grep "ConstraintValidator" evolution/tools/evolve_tool_descriptions.py` 至少出现 1 次
    - `grep "_check_size" evolution/tools/evolve_tool_descriptions.py` 至少出现 1 次
    - `grep "output.*tools" evolution/tools/evolve_tool_descriptions.py` 至少出现 1 次
    - `grep "difflib" evolution/tools/evolve_tool_descriptions.py` 至少出现 1 次
    - `python -m pytest tests/tools/test_evolve_tool_descriptions.py -x -q` 全部通过
    - `python -m evolution.tools.evolve_tool_descriptions --help` 输出包含 --iterations, --eval-source, --hermes-repo, --dry-run
  </acceptance_criteria>
  <done>
    evolve_tool_descriptions.py 完整实现，包含 Click CLI 四参数、evolve() 端到端管道（discover->extract->module->dataset->GEPA->constraints->evaluate->save）、dry-run 模式、三类约束门禁（size+factual+regression）、结果保存到 output/tools/（描述+metrics+diff）。
    可通过 `python -m evolution.tools.evolve_tool_descriptions` 运行。
    所有测试通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hermes-agent filesystem -> tool_loader | 读取外部仓库的 Python 文件 |
| LLM API -> GEPA optimizer | 优化器的 LLM 调用结果影响进化输出 |
| LLM API -> ToolDatasetBuilder | 合成数据集的 LLM 调用 |
| CLI args -> evolve() | 用户提供的 --hermes-repo 路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T (Tampering) | evolve() output | mitigate | 只写入 output/tools/ 目录（per D-11），不调用 write_back_description()，不修改 hermes-agent 源文件 |
| T-05-05 | T (Tampering) | --hermes-repo arg | accept | 路径由本地用户提供，CLI 是本地工具不暴露网络。discover_tool_files 只读取 .py 文件 |
| T-05-06 | D (Denial of Service) | GEPA optimizer | mitigate | GEPA 有 max_steps 上限；GEPA 失败时 fallback 到 MIPROv2（同 evolve_skill.py 模式） |
| T-05-07 | I (Info Disclosure) | metrics.json | accept | 只含评估分数和配置信息，不含 API key 或敏感数据。保存在本地 output/ 目录 |
</threat_model>

<verification>
1. `python -m pytest tests/tools/test_evolve_tool_descriptions.py -x -q` 全部通过
2. `python -m evolution.tools.evolve_tool_descriptions --help` 显示四个选项
3. `python -c "from evolution.tools.evolve_tool_descriptions import main, evolve; print('import OK')"` 输出 "import OK"
4. `python -m pytest tests/tools/ -x -q` 全部通过（包括 Plan 01 的测试）
</verification>

<success_criteria>
- CLI 可通过 `python -m evolution.tools.evolve_tool_descriptions` 运行
- --help 显示 --iterations, --eval-source (synthetic/load), --hermes-repo, --dry-run
- evolve() 按 discover->extract->module->dataset->GEPA->constraints->evaluate->save 编排
- 约束门禁在 GEPA 之后 holdout 之前，包含 size + factual + regression 三类
- dry-run 不触发 GEPA 调用
- 结果保存到 output/tools/ 包含描述、metrics.json、diff.txt
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/05-tool-constraints-cli/05-02-SUMMARY.md`
</output>
