---
phase: 14-sessiondb-mining-for-tools
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/sessions/error_retry_b.json
  - tests/fixtures/sessions/user_correction_a.json
  - tests/fixtures/sessions/oracle_disagreement_c.json
  - tests/fixtures/sessions/malformed_msg.json
  - tests/fixtures/sessions/multi_signal.json
  - tests/fixtures/sessions/surface_drift.json
  - tests/fixtures/sessions/secret_in_user_msg.json
  - tests/tools/test_session_signal_extract.py
  - tests/tools/test_session_judge.py
  - tests/tools/test_session_split.py
  - tests/tools/test_session_miner.py
  - tests/tools/test_secret_patterns_v2.py
  - tests/tools/test_jsonl_skip_bad.py
  - tests/tools/test_surface_drift.py
  - tests/tools/test_mine_cli.py
  - tests/tools/test_evolve_with_session_source.py
autonomous: true
requirements: [TOOL-V2-01]
tags: [phase-14, wave-0, test-scaffolding, fixtures, tdd-red]

must_haves:
  truths:
    - "全部 9 个 Phase 14 新建测试文件存在，containing the 28 test functions named in 14-VALIDATION.md (rows 1–28)。"
    - "全部 7 个 session fixture JSON 文件存在 under tests/fixtures/sessions/ 且各自结构反映 RESEARCH §Pitfall 1 实测 schema（messages + role/content/tool_calls + 顶层 tools list）。"
    - "执行 `.venv/bin/pytest tests/tools/test_session_*.py tests/tools/test_secret_patterns_v2.py tests/tools/test_jsonl_skip_bad.py tests/tools/test_surface_drift.py tests/tools/test_mine_cli.py tests/tools/test_evolve_with_session_source.py --collect-only -q` 能成功 collect 全部 28 测试函数（即使它们都因 Import 失败而 skip/xfail，collect 必须 ≥ 28）。"
    - "session_miner does NOT call tool_loader.write_back_description or any hermes-agent write path — 本 plan 仅创建测试脚手架，不引入任何写回路径。"
    - "Phase 14 不引入新外部依赖：pyproject.toml [project] dependencies 与 commit 前一致。"
    - "tests/tools/test_secret_patterns_v2.py 顶层禁止 import _shannon_entropy（B4 belt-and-suspenders；Wave 0 写 stub 时该符号未存在 → collect-time ImportError 风险）；Plan 03 Task 3.1 也禁止此 import；test 报错信息不显示熵值。"
  artifacts:
    - path: tests/fixtures/sessions/error_retry_b.json
      provides: "B 信号 fixture：assistant tool_call(name=A) → tool error → assistant tool_call(name=B) → tool success"
      contains: '"role": "tool"'
    - path: tests/fixtures/sessions/user_correction_a.json
      provides: "A 信号 fixture：user 'should have used X / 应该用 X' 语料"
      contains: "应该用"
    - path: tests/fixtures/sessions/oracle_disagreement_c.json
      provides: "C 信号 fixture：单 successful tool_call，但 oracle 会推荐另一工具"
      contains: '"function"'
    - path: tests/fixtures/sessions/malformed_msg.json
      provides: "schema-tolerance fixture：缺 role 的 message + 非数组 tool_calls"
    - path: tests/fixtures/sessions/multi_signal.json
      provides: "B+A 双源命中 fixture（multiplier max=3）"
    - path: tests/fixtures/sessions/surface_drift.json
      provides: "工具表面漂移 fixture：assistant 调用 legacy_tool_v0（不在 current hermes）"
      contains: "legacy_tool_v0"
    - path: tests/fixtures/sessions/secret_in_user_msg.json
      provides: "隐私 gate fixture：含 JWT + SHA256 hex"
      contains: "eyJ"
    - path: tests/tools/test_session_signal_extract.py
      provides: "B/A/C extractor + parser 容错测试占位"
      exports: ["test_parse_assistant_with_tool_calls", "test_b_error_retry", "test_a_user_correction", "test_c_oracle_disagreement"]
    - path: tests/tools/test_session_judge.py
      provides: "ConfirmMisselection round-trip + LM error fail-closed"
      exports: ["test_verdict_round_trip", "test_lm_failure_drops_candidate"]
    - path: tests/tools/test_session_split.py
      provides: "hash bucket / determinism / normalize / signals union"
      exports: ["test_hash_bucket_edges", "test_hash_determinism", "test_normalize_robust", "test_signals_union"]
    - path: tests/tools/test_session_miner.py
      provides: "miner 端到端 + duplication + multiplier max + metrics schema"
      exports: ["test_duplicate_train_only", "test_multiplier_max", "test_metrics_schema", "test_mine_end_to_end"]
    - path: tests/tools/test_secret_patterns_v2.py
      provides: "Layer 1 positives + v1 不回归 + 低熵 negatives"
      exports: ["test_layer1_positives", "test_v1_regression", "test_low_entropy_negatives"]
    - path: tests/tools/test_jsonl_skip_bad.py
      provides: "skip bad line + 5% threshold warn + EvalDataset 严格不变"
      exports: ["test_skip_bad_line", "test_evaldataset_strict_unchanged"]
    - path: tests/tools/test_surface_drift.py
      provides: "drop unknown tool + report top-N truncation"
      exports: ["test_drop_unknown_tool", "test_report_truncation"]
    - path: tests/tools/test_mine_cli.py
      provides: "CLI flags + consent gate + dry-run + signals subset + multiplier override + baseline-module optional"
      exports: ["test_consent_required", "test_dry_run", "test_signal_subset", "test_multiplier_override", "test_baseline_module_optional"]
    - path: tests/tools/test_evolve_with_session_source.py
      provides: "integration: session 优先 + 不重复 duplication"
      exports: ["test_session_overrides_synth", "test_no_double_duplication"]
  key_links:
    - from: tests/tools/test_session_judge.py
      to: tests/conftest.py
      via: "fixture mock_lm_with_usage（pytest auto-discovery）"
      pattern: "def test_.*\\(.*mock_lm_with_usage"
    - from: tests/fixtures/sessions/error_retry_b.json
      to: 实测 hermes session schema
      via: "messages[].role/content/tool_calls + 顶层 tools list"
      pattern: '"function": \\{"name"'
---

<objective>
为 Phase 14 创建全部测试脚手架（9 个新测试文件 + 7 个 session fixture JSON），让 Wave 1+ 的实现任务可以红灯起步、TDD 落地，并保证 14-VALIDATION.md 的 28 个验证占位都被定位（即使函数体先用 `pytest.skip("待 Wave 1 实现")` 占位）。本 plan 不写任何生产代码，只搭测试结构。

Purpose: 解决 Wave 0 缺口；让 14-VALIDATION.md 中所有 28 行 `<automated>` 的目标测试函数名提前存在，避免后续每个 task 临时找文件。
Output: 9 测试文件 + 7 fixture 文件 + 全部测试函数定义（先用 `pytest.skip` 占位，wave 1+ 删除 skip）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sessiondb-mining-for-tools/14-CONTEXT.md
@.planning/phases/14-sessiondb-mining-for-tools/14-RESEARCH.md
@.planning/phases/14-sessiondb-mining-for-tools/14-VALIDATION.md
@.planning/phases/14-sessiondb-mining-for-tools/14-PATTERNS.md
@tests/conftest.py
@CLAUDE.md

<interfaces>
<!-- 现有可复用 fixture（pytest auto-discovery，不需要 import） -->

From tests/conftest.py:7-38:
```python
@pytest.fixture
def mock_lm_with_usage():
    def factory(*, response_text: str = "", prompt_tokens: int = 100,
                completion_tokens: int = 20, model_name: str = "openai/gpt-4.1-mini"):
        lm = MagicMock(name=f"mock_lm[{model_name}]")
        lm.model = model_name
        lm.return_value = [response_text]
        lm._usage_records = [{
            "model": model_name,
            "usage": {
                "prompt_tokens": prompt_tokens,
                "completion_tokens": completion_tokens,
                "total_tokens": prompt_tokens + completion_tokens,
            },
        }]
        return lm
    return factory
```

<!-- 实测 hermes session schema（来自 RESEARCH.md §Pitfall 1） -->
顶层结构: { "session_id": str, "messages": list[dict], "tools": list[dict] }
assistant turn: { "role": "assistant", "content": str|None, "tool_calls": list[{ "id": str, "type": "function", "function": {"name": str, "arguments": str (json-string)} }] }
tool message: { "role": "tool", "content": str (json-encoded with `output`/`exit_code`/`error`), "tool_call_id": str }
user/system: { "role": ..., "content": str }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: 创建 7 个 session fixture JSON</name>
  <files>tests/fixtures/sessions/error_retry_b.json, tests/fixtures/sessions/user_correction_a.json, tests/fixtures/sessions/oracle_disagreement_c.json, tests/fixtures/sessions/malformed_msg.json, tests/fixtures/sessions/multi_signal.json, tests/fixtures/sessions/surface_drift.json, tests/fixtures/sessions/secret_in_user_msg.json</files>
  <read_first>
    - .planning/phases/14-sessiondb-mining-for-tools/14-RESEARCH.md (§Pitfall 1 lines 348-378 — 实测 session schema 范本)
    - .planning/phases/14-sessiondb-mining-for-tools/14-PATTERNS.md (§Fixture session JSON shape lines 666-712)
    - .planning/phases/14-sessiondb-mining-for-tools/14-VALIDATION.md (§Wave 0 Requirements lines 84-103)
  </read_first>
  <action>
    创建目录 `tests/fixtures/sessions/` 并写入 7 个 JSON 文件。每个文件必须严格遵循 RESEARCH §Pitfall 1 (lines 348-378) 给出的实测 hermes session schema：顶层 `{session_id, messages, tools}`，assistant 消息可同时含 `content` 与 `tool_calls`，tool 消息 `content` 为 JSON-encoded string，cross-reference 通过 `tool_call_id` (assistant.tool_calls[i].id == tool.tool_call_id)。注意：tool 消息的 `name` 字段实测为 None/缺失（**不能依赖 tool.name 反查工具名**），canonical name 来自 `assistant.tool_calls[i].function.name`。

    具体内容（按 14-PATTERNS.md §What to diverge per fixture lines 705-711 + 14-VALIDATION.md §Wave 0 Fixtures lines 96-103）:

    **error_retry_b.json**: messages = [user "list files", assistant tool_call id="tc-1" function.name="legacy_grep", tool tool_call_id="tc-1" content='{"output":"","exit_code":1,"error":"command not found"}', assistant tool_call id="tc-2" function.name="search_files", tool tool_call_id="tc-2" content='{"output":"a.py\\nb.py","exit_code":0,"error":null}']. tools=[{type:"function",function:{name:"legacy_grep",description:"...",parameters:{}}}, {type:"function",function:{name:"search_files",description:"...",parameters:{}}}].

    **user_correction_a.json**: messages = [user "find a TODO", assistant tool_call id="tc-1" function.name="terminal" arguments='{"command":"grep TODO"}', tool tool_call_id="tc-1" content='{"output":"...","exit_code":0,"error":null}', user "不对，应该用 search_files", assistant tool_call id="tc-2" function.name="search_files"]. tools=[{function.name:"terminal",...},{function.name:"search_files",...}].

    **oracle_disagreement_c.json**: messages = [user "read README.md", assistant tool_call id="tc-1" function.name="terminal" arguments='{"command":"cat README.md"}', tool tool_call_id="tc-1" content='{"output":"...","exit_code":0,"error":null}']. tools 含 read_file + terminal。预期 oracle ToolModule mock 会推荐 read_file。

    **malformed_msg.json**: messages = [user normal, {"content":"missing role"} (无 role 字段), assistant {"role":"assistant","tool_calls":"NOT-AN-ARRAY"} (tool_calls 不是 list), user normal]. tools=[]. 验证 extractor 不 crash。

    **multi_signal.json**: 同时命中 B 和 A：messages = [user "list files", assistant tool_call id="tc-1" function.name="legacy_grep", tool tool_call_id="tc-1" content='{"exit_code":1,"error":"fail"}', user "应该用 search_files", assistant tool_call id="tc-2" function.name="search_files", tool tool_call_id="tc-2" content='{"exit_code":0,"error":null}']. tools 含 legacy_grep + search_files。

    **surface_drift.json**: messages = [user normal, assistant tool_call id="tc-1" function.name="legacy_tool_v0" (不在 current hermes), tool tool_call_id="tc-1" content='{"exit_code":0,"error":null}']. tools=[{function.name:"legacy_tool_v0",...}]. **关键**：function.name 必须正好是 `"legacy_tool_v0"`（test 用此字符串验证 drop）。

    **secret_in_user_msg.json**: messages = [user content 含 JWT 字符串 "Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ1c2VyIn0.dozjgNryP4J3jVmNHl0w5N_XgL0n3I9PlFUP0THsR8U" 和 SHA256 hex "a665a45920422f9d417e4867efdc4fb8a04a1f3fff1fa07e998e86f7f7a27ae3", assistant tool_call function.name="terminal", tool ...]. tools=[{function.name:"terminal",...}]. **关键**：JWT 必须是字面量 `eyJ` 开头三段式，SHA256 必须是字面 64-hex；test_secret_patterns_v2 会从此 fixture 抽 user content 比对。

    每个 JSON 文件必须 `json.loads()` 合法（malformed_msg.json 也要顶层合法 JSON，仅在 messages 数组内放 schema 不规则项）。每个文件最少 3 条 messages，最多 8 条。所有 JSON 必须包含顶层 `tools` 字段（malformed_msg.json 除外可为空数组）。

    每个 fixture 末尾添加换行符（POSIX 文本约定）。
  </action>
  <verify>
    <automated>python -c "import json, pathlib; [json.loads(p.read_text()) for p in pathlib.Path('tests/fixtures/sessions').glob('*.json')]; print('all 7 fixtures parse')"</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在: `test -f tests/fixtures/sessions/error_retry_b.json && test -f tests/fixtures/sessions/user_correction_a.json && test -f tests/fixtures/sessions/oracle_disagreement_c.json && test -f tests/fixtures/sessions/malformed_msg.json && test -f tests/fixtures/sessions/multi_signal.json && test -f tests/fixtures/sessions/surface_drift.json && test -f tests/fixtures/sessions/secret_in_user_msg.json`
    - 全部 fixture 是合法 JSON: `python -c "import json,pathlib; [json.loads(p.read_text()) for p in pathlib.Path('tests/fixtures/sessions').glob('*.json')]"` 退出 0
    - JWT 字面量在 fixture 中: `grep -q 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9' tests/fixtures/sessions/secret_in_user_msg.json`
    - SHA256 hex 在 fixture 中: `grep -q 'a665a45920422f9d417e4867efdc4fb8a04a1f3fff1fa07e998e86f7f7a27ae3' tests/fixtures/sessions/secret_in_user_msg.json`
    - surface_drift 用 legacy_tool_v0: `grep -q '"legacy_tool_v0"' tests/fixtures/sessions/surface_drift.json`
    - user_correction 含中文纠正语: `grep -q '应该用' tests/fixtures/sessions/user_correction_a.json`
    - error_retry 含 exit_code=1 错误样本: `grep -q '"exit_code":1' tests/fixtures/sessions/error_retry_b.json` 或 `grep -q "'exit_code': 1" tests/fixtures/sessions/error_retry_b.json`
    - multi_signal 含双源标志: `grep -q '"exit_code":1' tests/fixtures/sessions/multi_signal.json && grep -q '应该用' tests/fixtures/sessions/multi_signal.json`
    - 全部 fixture 顶层有 messages 字段: `python -c "import json,pathlib; assert all('messages' in json.loads(p.read_text()) for p in pathlib.Path('tests/fixtures/sessions').glob('*.json'))"` 退出 0
  </acceptance_criteria>
  <done>7 fixture JSON 文件落盘，全部合法 JSON 且包含 RESEARCH §Pitfall 1 schema 要素。</done>
</task>

<task type="auto">
  <name>Task 1.2: 创建 9 个测试文件骨架（28 个测试函数 stub）</name>
  <files>tests/tools/test_session_signal_extract.py, tests/tools/test_session_judge.py, tests/tools/test_session_split.py, tests/tools/test_session_miner.py, tests/tools/test_secret_patterns_v2.py, tests/tools/test_jsonl_skip_bad.py, tests/tools/test_surface_drift.py, tests/tools/test_mine_cli.py, tests/tools/test_evolve_with_session_source.py</files>
  <read_first>
    - .planning/phases/14-sessiondb-mining-for-tools/14-VALIDATION.md (§Per-Task Verification Map lines 41-71 — 28 行测试矩阵，每行的 `pytest ...::test_xxx` 命令决定函数名)
    - .planning/phases/14-sessiondb-mining-for-tools/14-PATTERNS.md (§Reusable mock LM fixture lines 631-660 — pytest auto-discovery 模式)
    - tests/conftest.py (mock_lm_with_usage 签名)
    - tests/tools/test_param_consistency.py (现有测试文件风格 — 同目录已有 13 个测试文件，按 codebase 风格保持一致)
  </read_first>
  <action>
    创建 9 个测试文件，每个文件包含 14-VALIDATION.md 矩阵行 1–28 中映射到该文件的全部 test 函数定义。函数体使用 `pytest.skip("Wave 1+ 实现 — 见 14-NN-PLAN.md")` 占位（不抛 NotImplementedError、不留空 body 否则 collect 也算 pass 失真）。

    所有文件必须遵循 codebase 现有 tests/tools/test_*.py 风格：
    - 以 docstring 开头说明该文件的目标 phase / Wave
    - `import pytest` 顶部
    - 每个 test 函数有 docstring 说明断言什么行为
    - 使用 `pathlib.Path` 引用 fixture: `FIXTURE_DIR = Path(__file__).parent.parent / "fixtures" / "sessions"`

    精确测试函数清单（每行对应 14-VALIDATION.md 的一行）：

    **tests/tools/test_session_signal_extract.py** (rows 1-4):
    - `test_parse_assistant_with_tool_calls` (row 1) — assert parser tolerates `reasoning_details` + multi `tool_calls` + missing `role` (reads malformed_msg.json + error_retry_b.json fixtures)
    - `test_b_error_retry` (row 2) — uses error_retry_b.json fixture
    - `test_a_user_correction(mock_lm_with_usage)` (row 3) — uses user_correction_a.json + mock_lm_with_usage fixture
    - `test_c_oracle_disagreement(mock_lm_with_usage)` (row 4) — uses oracle_disagreement_c.json

    **tests/tools/test_session_judge.py** (rows 5-6):
    - `test_verdict_round_trip(mock_lm_with_usage)` (row 5) — verdict ∈ {confirm_misselection, false_positive} parses correctly
    - `test_lm_failure_drops_candidate(mock_lm_with_usage)` (row 6) — Exception → false_positive

    **tests/tools/test_session_split.py** (rows 7-10):
    - `test_hash_bucket_edges` (row 7) — buckets 69→train, 70→val, 84→val, 85→holdout (constructed task strings)
    - `test_hash_determinism` (row 8) — same task → same bucket
    - `test_normalize_robust` (row 9) — `"Read   FILE"` collapses to same hash as `"read file"`
    - `test_signals_union` (row 10) — same hash, multiple signals → 1 example with `misselection_signals` union

    **tests/tools/test_session_miner.py** (rows 11, 12, 25, 26):
    - `test_duplicate_train_only` (row 11)
    - `test_multiplier_max` (row 12)
    - `test_metrics_schema` (row 25) — verifies all CONTEXT specifics keys: `total_candidates_by_signal`, `judge_confirmed_by_signal`, `judge_false_positives_by_signal`, `surface_drift_dropped`, `surface_drift_tools`, `final_examples_by_split`, `final_train_after_duplication`, `multiplier_used`, `secret_filter_skipped`, `jsonl_skipped_lines`, `cost_usd_spent`, `judge_calls`, `judge_calls_by_signal`
    - `test_mine_end_to_end(mock_lm_with_usage, tmp_path)` (row 26)

    **tests/tools/test_secret_patterns_v2.py** (rows 13-15):
    - `test_layer1_positives` (row 13) — JWT (using exact fixture string from secret_in_user_msg.json) + AWS pattern + high-entropy token all detected
    - `test_v1_regression` (row 14) — sk-ant-api / ghp_ / xoxb- / AKIA / sk-or-v1- / Bearer / -----BEGIN PRIVATE KEY----- / password=xxx / ANTHROPIC_API_KEY 仍被 detected (subset of existing v1 strings)
    - `test_low_entropy_negatives` (row 15) — Chinese prose ("这是一段中文测试") / short English / SHA256 hex (the one in secret_in_user_msg.json fixture, length 64) NOT flagged at threshold ≥4.3 (threshold 待 Wave 1 calibrate; test 注释说明 "若 calibration 后阈值改变则更新此测试")

    **tests/tools/test_jsonl_skip_bad.py** (rows 18-19):
    - `test_skip_bad_line(tmp_path, capsys)` (row 18) — write file with 100 lines, 1 invalid → skip + counter incremented; ≥6% bad lines triggers warn output
    - `test_evaldataset_strict_unchanged(tmp_path)` (row 19) — `EvalDataset.load` 仍单行 abort（与 D-18 范围一致，不动）

    **tests/tools/test_surface_drift.py** (rows 16-17):
    - `test_drop_unknown_tool` (row 16) — fixture surface_drift.json + mock current tool set without `legacy_tool_v0` → dropped, counter incremented
    - `test_report_truncation` (row 17) — > 10 unknown tools → Rich table 截断 top-10，metrics.json 完整

    **tests/tools/test_mine_cli.py** (rows 20-24):
    - `test_consent_required` (row 20) — 缺 --i-have-consent → CliRunner exit_code == 1，stderr 含字符串 "i-have-consent" 或 "consent"
    - `test_dry_run` (row 21) — --dry-run 不调 LLM
    - `test_signal_subset` (row 22) — --signals=error_retry,user_correction 跳过 oracle path
    - `test_multiplier_override` (row 23) — --misselection-multiplier "error_retry=5,user_correction=2" 解析为 {"error_retry":5,"user_correction":2}
    - `test_baseline_module_optional` (row 24) — --baseline-module 缺省时 C 信号自动跳过 + warn

    **tests/tools/test_evolve_with_session_source.py** (rows 27-28):
    - `test_session_overrides_synth(tmp_path)` (row 27) — 同 hash 时 session-side 值留下
    - `test_no_double_duplication(tmp_path)` (row 28) — train.jsonl 已预复制 → evolve load 时不再加倍

    每个测试函数体（占位）：
    ```python
    def test_xxx():
        """断言 ...（一句话说明该测试断什么）。"""
        pytest.skip("Wave 1+ 实现 — 见 14-NN-PLAN.md")
    ```

    禁止做任何会让 collect 阶段失败的事（`from evolution.tools.session_miner import ...` 等待实现的模块 — 这些 import **不能**放在文件顶层，否则 collect 时 ImportError）。如确需 import session_miner，使用函数内 lazy import（在 skip 之后）。

    `__init__.py` 已存在 (tests/tools/__init__.py confirmed)；不需要新建。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_session_signal_extract.py tests/tools/test_session_judge.py tests/tools/test_session_split.py tests/tools/test_session_miner.py tests/tools/test_secret_patterns_v2.py tests/tools/test_jsonl_skip_bad.py tests/tools/test_surface_drift.py tests/tools/test_mine_cli.py tests/tools/test_evolve_with_session_source.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 9 测试文件存在: `for f in test_session_signal_extract test_session_judge test_session_split test_session_miner test_secret_patterns_v2 test_jsonl_skip_bad test_surface_drift test_mine_cli test_evolve_with_session_source; do test -f tests/tools/$f.py || exit 1; done`
    - collect 返回 ≥ 28 测试: `.venv/bin/pytest tests/tools/test_session_*.py tests/tools/test_secret_patterns_v2.py tests/tools/test_jsonl_skip_bad.py tests/tools/test_surface_drift.py tests/tools/test_mine_cli.py tests/tools/test_evolve_with_session_source.py --collect-only -q 2>&1 | grep -E '^[0-9]+ tests collected' | awk '{print $1}' | xargs -I{} test {} -ge 28`
    - 全部 28 测试函数名存在: `for fn in test_parse_assistant_with_tool_calls test_b_error_retry test_a_user_correction test_c_oracle_disagreement test_verdict_round_trip test_lm_failure_drops_candidate test_hash_bucket_edges test_hash_determinism test_normalize_robust test_signals_union test_duplicate_train_only test_multiplier_max test_layer1_positives test_v1_regression test_low_entropy_negatives test_drop_unknown_tool test_report_truncation test_skip_bad_line test_evaldataset_strict_unchanged test_consent_required test_dry_run test_signal_subset test_multiplier_override test_baseline_module_optional test_metrics_schema test_mine_end_to_end test_session_overrides_synth test_no_double_duplication; do grep -rq "def $fn" tests/tools/ || { echo "missing $fn"; exit 1; }; done`
    - mock_lm_with_usage 复用而不重新定义: `! grep -E 'def mock_lm_with_usage' tests/tools/test_session_*.py tests/tools/test_secret_patterns_v2.py tests/tools/test_jsonl_skip_bad.py tests/tools/test_surface_drift.py tests/tools/test_mine_cli.py tests/tools/test_evolve_with_session_source.py 2>/dev/null`
    - 没有顶层 import 引用尚未存在的 session_miner: `! grep -E '^from evolution.tools.session_miner' tests/tools/test_session_*.py tests/tools/test_mine_cli.py tests/tools/test_evolve_with_session_source.py 2>/dev/null`
    - 没有顶层 import _shannon_entropy（B4 防回归 belt-and-suspenders；该符号 Wave 1 才存在）: `! grep -E '^(from evolution\.tools\.session_miner|from evolution\.core\.external_importers import _shannon_entropy)' tests/tools/test_secret_patterns_v2.py 2>/dev/null`
  </acceptance_criteria>
  <done>9 测试文件含 28 测试函数 stub，pytest collect 全部通过且不影响现有测试。</done>
</task>

<task type="auto">
  <name>Task 1.3: 验证不回归现有测试套件</name>
  <files>(no source change — verification only)</files>
  <read_first>
    - .planning/phases/14-sessiondb-mining-for-tools/14-VALIDATION.md (§Test Infrastructure lines 18-24 — 完整测试命令)
    - pyproject.toml ([tool.pytest.ini_options] 部分)
  </read_first>
  <action>
    跑两条命令并保存输出（不需要持久化文件，只在 task summary 报告）：

    1. `.venv/bin/pytest tests/ -q --tb=no 2>&1 | tail -10` — 验证现有 385 passed + 1 xfailed 仍 GREEN，加 28 skipped (本 plan 新增 stub 全部 skip)。**不允许**有 errors（collect 失败）或 failed。

    2. `git status --short tests/ tests/fixtures/` — 确认只有 14-01-PLAN 引入的 16 个新文件（9 测试 + 7 fixture），无其他改动。

    若 1 出现 errors 或 failed，回到 Task 1.2 修测试 stub（最常见原因：顶层 import 了 session_miner / mine_tool_sessions 等待实现的模块）。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/ -q --tb=no 2>&1 | tail -3 | grep -E 'passed|skipped' | grep -v 'failed' | grep -v 'error'</automated>
  </verify>
  <acceptance_criteria>
    - 现有 385 测试不回归: `.venv/bin/pytest tests/ -q --tb=no 2>&1 | grep -E '^(FAILED|ERROR)' | wc -l | xargs test 0 -eq` 即 grep 出 0 行
    - 新增测试全部 skip 或 collected: `.venv/bin/pytest tests/tools/test_session_*.py tests/tools/test_secret_patterns_v2.py tests/tools/test_jsonl_skip_bad.py tests/tools/test_surface_drift.py tests/tools/test_mine_cli.py tests/tools/test_evolve_with_session_source.py -q --tb=no 2>&1 | grep -E '[0-9]+ skipped' | wc -l | xargs test 1 -le`
    - 没有意外的源代码改动: `git diff --name-only evolution/ 2>/dev/null | wc -l | xargs test 0 -eq`
    - pyproject.toml 未被修改: `! git diff --name-only 2>/dev/null | grep -q '^pyproject.toml$'`
  </acceptance_criteria>
  <done>整套 pytest 套件 GREEN（385 passed + 1 xfailed + 28 new skipped），git diff 仅见 16 新文件。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → test runner | tests/fixtures/sessions/*.json 读入测试进程 — fixture 内含模拟 secret，但仅本地测试用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (本 plan owns no incremental Phase 14 threats) | — | — | — | 本 plan 仅创建测试脚手架，不引入新 attack surface。Phase 14 主要威胁 T-14-01..T-14-04 由后续 plan owns（T-14-01 by Plan 04 LLM judge；T-14-02 by Plan 03 secret patterns；T-14-03 by Plan 02 JSONL helper；T-14-04 by Plan 05 CLI）。本 plan 仅为这些威胁占测试位（test_lm_failure_drops_candidate, test_layer1_positives, test_skip_bad_line, test_consent_required）。 |

注意：fixture `secret_in_user_msg.json` 含字面 JWT 与 SHA256 hex 字符串。这些是合成示例值，不是真实凭证。已经通过 .gitignore（datasets/）覆盖不到 tests/fixtures/，但因为是合成值且单元测试需要确定字面量，接受 commit 进 git。
</threat_model>

<verification>
- 整体 pytest 套件 (`.venv/bin/pytest tests/ -q --tb=no`) 返回 0；新增 28 个测试全部 skipped；无 errors / failed
- 7 个 fixture JSON 全部 `json.loads()` 合法
- 9 个测试文件 + 7 个 fixture 文件存在
- 现有 evolution/ 源代码无改动（`git diff --name-only evolution/` 为空）
</verification>

<success_criteria>
- 9 个测试文件全部包含 14-VALIDATION.md 矩阵中映射到该文件的测试函数定义（共 28 个）
- 7 个 session fixture JSON 全部含 RESEARCH §Pitfall 1 实测 schema 要素
- pytest collect 返回 ≥ 28 新测试且不影响现有 385 + 1 xfailed
- 无 evolution/ 源码改动；无 pyproject.toml 改动
</success_criteria>

<output>
After completion, create `.planning/phases/14-sessiondb-mining-for-tools/14-01-SUMMARY.md` describing fixtures + test files created, total stub count, baseline pytest result.
</output>
