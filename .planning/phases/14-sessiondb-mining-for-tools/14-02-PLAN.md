---
phase: 14-sessiondb-mining-for-tools
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - evolution/tools/tool_dataset.py
autonomous: true
requirements: [TOOL-V2-01]
tags: [phase-14, wave-1, data-contract, dataclass, jsonl-tolerance]

must_haves:
  truths:
    - "ToolSelectionExample 实例可以携带 misselection_signals 列表，向后兼容旧 JSONL（无字段 → 空 list 默认）"
    - "新加 misselection_signals 字段被 to_dict 序列化、from_dict 反序列化、JSONL save/load 正确 round-trip"
    - "ToolSelectionDataset.load 行为不变（D-18 严格不动）—— EvalDataset.load / GoldenDatasetLoader.load 同样不被本 plan 触及"
    - "Plan 02 Task 2.3 的 tests/tools/ 回归扫描使用 --ignore=tests/tools/test_secret_patterns_v2.py（B4）：Plan 03 在同 Wave 1 并行修改该文件，ignore 防止 cross-plan race 误报；两 plan 各自只改自己的目标文件，无 files_modified 冲突。"
    - "session_miner does NOT call tool_loader.write_back_description or any hermes-agent write path — 本 plan 不触及任何 hermes-agent 路径"
    - "Phase 14 不引入新外部依赖：pyproject.toml [project] dependencies 与 commit 前一致"
  artifacts:
    - path: evolution/tools/tool_dataset.py
      provides: "ToolSelectionExample.misselection_signals 字段 + to_dict/from_dict 兼容"
      contains: "misselection_signals: list[str]"
  key_links:
    - from: evolution/tools/tool_dataset.py
      to: tests/tools/test_tool_dataset.py
      via: "to_dict/from_dict round-trip + 现有测试不回归"
      pattern: "misselection_signals"
---

<objective>
按 D-02 给 `ToolSelectionExample` 新增 `misselection_signals: list[str] = field(default_factory=list)` 字段，并将其纳入 `to_dict()` 序列化与 `from_dict()` 反序列化（从 codebase 现有的 `__dataclass_fields__` 过滤式 from_dict 自动获得旧 JSONL 兼容）。

Purpose: D-02 要求每个 mined example 携带其命中的信号源列表（{"error_retry","user_correction","oracle_disagreement"} 子集），用于后续按信号 multiplier 复制（D-11）以及加权审计。
Output: tool_dataset.py 修改后旧 Phase 4 JSONL 仍能 load（默认 misselection_signals=[]），新例可正常 save/load round-trip。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sessiondb-mining-for-tools/14-CONTEXT.md
@.planning/phases/14-sessiondb-mining-for-tools/14-RESEARCH.md
@.planning/phases/14-sessiondb-mining-for-tools/14-PATTERNS.md
@evolution/tools/tool_dataset.py
@tests/tools/test_tool_dataset.py

<interfaces>
<!-- 现有 ToolSelectionExample 字段集（lines 32-71） -->

From evolution/tools/tool_dataset.py:32-71:
```python
@dataclass
class ToolSelectionExample:
    task_description: str
    correct_tool: str
    correct_params: dict = field(default_factory=dict)
    difficulty: str = "medium"  # easy, medium, hard
    confuser_tools: list[str] = field(default_factory=list)
    reason: str = ""
    source: str = "synthetic"

    def to_dict(self) -> dict:
        return {
            "task_description": self.task_description,
            "correct_tool": self.correct_tool,
            "correct_params": self.correct_params,
            "difficulty": self.difficulty,
            "confuser_tools": self.confuser_tools,
            "reason": self.reason,
            "source": self.source,
        }

    @classmethod
    def from_dict(cls, d: dict) -> "ToolSelectionExample":
        # 关键：line 71 已用 __dataclass_fields__ 过滤 — 新字段会自动取默认值
        return cls(**{k: v for k, v in d.items() if k in cls.__dataclass_fields__})
```

<!-- 关键不变量（D-18 scope guard） -->
ToolSelectionDataset.save / load (lines 90-128) **本 plan 不触及**。
EvalDataset.load / GoldenDatasetLoader.load 同样不动。
JSONL bad-line tolerance 走 Plan 04 新建的 `_load_jsonl_skip_bad` helper。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: 解除 Plan 01 中 ToolSelectionExample 相关 stub，写 RED 验收测试</name>
  <files>tests/tools/test_session_split.py</files>
  <read_first>
    - tests/tools/test_session_split.py (Plan 01 创建的 stub)
    - evolution/tools/tool_dataset.py (lines 32-71 — ToolSelectionExample 当前字段集)
    - .planning/phases/14-sessiondb-mining-for-tools/14-VALIDATION.md (row 10 — test_signals_union)
  </read_first>
  <action>
    实现 `tests/tools/test_session_split.py::test_signals_union` (row 10) — 这是 Phase 14 唯一直接验证 D-02 字段语义的单元测试位。

    把 stub:
    ```python
    def test_signals_union():
        pytest.skip("Wave 1+ 实现 — 见 14-NN-PLAN.md")
    ```

    替换为真测试（注意：`union` 由 session_miner 内部完成，但 ToolSelectionExample 必须**能**承载多 signal —— 本 task 只测 dataclass 字段层面）：
    ```python
    def test_signals_union():
        """ToolSelectionExample 的 misselection_signals 字段支持多源 union 后的列表内容。

        由 session_miner（Plan 04）在 mine() 末尾对同 hash 的 candidate union signals,
        然后写入 ToolSelectionExample.misselection_signals。本测试只验证字段能承载,
        不调用 session_miner（那是 Plan 04 的职责）。
        """
        from evolution.tools.tool_dataset import ToolSelectionExample
        ex = ToolSelectionExample(
            task_description="list files",
            correct_tool="search_files",
            misselection_signals=["error_retry", "user_correction"],
        )
        assert ex.misselection_signals == ["error_retry", "user_correction"]

        # 序列化-反序列化 round-trip
        d = ex.to_dict()
        assert d["misselection_signals"] == ["error_retry", "user_correction"]

        ex2 = ToolSelectionExample.from_dict(d)
        assert ex2.misselection_signals == ["error_retry", "user_correction"]

        # 旧 JSONL 兼容：无 misselection_signals 字段 → default []
        old_dict = {
            "task_description": "old example",
            "correct_tool": "terminal",
            "correct_params": {},
            "difficulty": "medium",
            "confuser_tools": [],
            "reason": "",
            "source": "synthetic",
        }
        ex_old = ToolSelectionExample.from_dict(old_dict)
        assert ex_old.misselection_signals == []
    ```

    确认此测试此刻 RED（尚未加字段）：
    `.venv/bin/pytest tests/tools/test_session_split.py::test_signals_union -x` 应 FAIL 或 ERROR（AttributeError: unexpected keyword argument 'misselection_signals' / 字段不存在）。

    不要为此 plan 实现任何 union 行为（那是 Plan 04 在 SessionToolMiner._reduce_candidates_by_hash 中做的）。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_session_split.py::test_signals_union -x 2>&1 | grep -qE 'FAILED|ERROR|TypeError'</automated>
  </verify>
  <acceptance_criteria>
    - test_signals_union 不再 skip: `! grep -A 5 'def test_signals_union' tests/tools/test_session_split.py | grep -q 'pytest.skip'`
    - test 含字段断言: `grep -q 'misselection_signals == \\["error_retry", "user_correction"\\]' tests/tools/test_session_split.py`
    - test 含旧 JSONL 兼容断言: `grep -q "misselection_signals == \\[\\]" tests/tools/test_session_split.py`
    - 测试当前 RED（字段未加）: `.venv/bin/pytest tests/tools/test_session_split.py::test_signals_union -x 2>&1 | grep -qE 'FAILED|TypeError|unexpected keyword'`
  </acceptance_criteria>
  <done>test_signals_union 替换 stub，断言 misselection_signals 字段语义；当前测试 RED。</done>
</task>

<task type="auto">
  <name>Task 2.2: ToolSelectionExample 新增 misselection_signals 字段（D-02）</name>
  <files>evolution/tools/tool_dataset.py</files>
  <read_first>
    - evolution/tools/tool_dataset.py (lines 32-71 — 当前 dataclass 与 to_dict)
    - .planning/phases/14-sessiondb-mining-for-tools/14-CONTEXT.md (D-02 — 字段命名 misselection_signals: list[str], 取值集合 {error_retry, user_correction, oracle_disagreement}, 默认 field(default_factory=list))
    - .planning/phases/14-sessiondb-mining-for-tools/14-PATTERNS.md (§Pattern E lines 819-839 — Dataclass + to_dict + filtered from_dict)
  </read_first>
  <action>
    精确编辑 evolution/tools/tool_dataset.py：

    1) **dataclass 字段（line 54 之后插入）** — 在 `source: str = "synthetic"` 后追加一行（per D-02）:
    ```python
        misselection_signals: list[str] = field(default_factory=list)
    ```
    保持 `@dataclass` 装饰、字段对齐、4-space indent 与现有风格一致。

    2) **更新 docstring（lines 39-46）** — 在现有 Args 列表后追加一行（保持 Google-style 与上文一致）:
    ```python
        misselection_signals: List of signal sources that flagged this example as
            a misselection candidate. Subset of {"error_retry", "user_correction",
            "oracle_disagreement"} (D-02). Empty for synthetic / golden examples.
    ```

    3) **更新 to_dict（lines 56-66）** — 在返回 dict 末尾（`"source": self.source,` 之后）追加一行：
    ```python
            "misselection_signals": self.misselection_signals,
    ```
    保持 dict 键序与字段定义顺序一致。

    4) **不动 from_dict（line 68-71）** — 现有 `cls(**{k: v for k, v in d.items() if k in cls.__dataclass_fields__})` 已自动过滤未知键，新字段会从 dataclass 默认值读 `[]`。**禁止**改 from_dict（PATTERNS.md §Pattern E lines 833-839 + 14-PATTERNS.md lines 432-435 强调）。

    5) **不动其他类**：
    - **不**动 `ToolSelectionDataset.save / load`（line 90-128，D-18 显式不允许）
    - **不**动 `ToolDatasetBuilder`
    - **不**动 `to_dspy_examples`

    禁止：添加任何 import；新增任何 helper 函数；改动其他 dataclass。

    若 IDE 自动 sort imports 把 file 改了无关行，撤销这些改动只保留上述 4 处。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_session_split.py::test_signals_union tests/tools/test_tool_dataset.py -x --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 字段已加: `grep -q "misselection_signals: list\\[str\\] = field(default_factory=list)" evolution/tools/tool_dataset.py`
    - to_dict 含新键: `grep -q '"misselection_signals": self.misselection_signals' evolution/tools/tool_dataset.py`
    - from_dict 行号 70-72 不变（仍单行 cls(**{...}) 模式）: `grep -A 1 'def from_dict' evolution/tools/tool_dataset.py | grep -q '__dataclass_fields__'`
    - test_signals_union 通过: `.venv/bin/pytest tests/tools/test_session_split.py::test_signals_union -x 2>&1 | grep -qE '1 passed'`
    - 现有 tool_dataset 测试不回归: `.venv/bin/pytest tests/tools/test_tool_dataset.py -q --tb=no 2>&1 | tail -3 | grep -E 'passed' | grep -v 'failed'`
    - 没有改 ToolSelectionDataset.save/load 的方法体（W6 hunk-header check — anchor 在 def save/load 上时即拒绝；不依赖单个 +/- 行）: `! git diff -U0 evolution/tools/tool_dataset.py | grep -B 1 '^@@' | grep -E '\b(def save|def load)\b'`
    - 没新增 import: `git diff evolution/tools/tool_dataset.py | grep -E '^\\+import|^\\+from' | wc -l | xargs test 0 -eq`
    - pyproject.toml 未改: `! git diff --name-only 2>/dev/null | grep -q '^pyproject.toml$'`
  </acceptance_criteria>
  <done>misselection_signals 字段 GREEN，旧 Phase 4 JSONL 自动兼容（field default_factory=list），现有测试不回归。</done>
</task>

<task type="auto">
  <name>Task 2.3: 验证向后兼容 — 重新加载真实 Phase 4 dataset 不破坏</name>
  <files>(no source change — verification only)</files>
  <read_first>
    - evolution/tools/tool_dataset.py (修改后)
    - datasets/tools/ (检查现有 train.jsonl / val.jsonl / holdout.jsonl 是否在 repo)
  </read_first>
  <action>
    跑两条独立验证命令，确认 D-02 加字段不破坏既有数据：

    1) **真实 Phase 4 数据 reload smoke test**（如 datasets/tools/{train,val,holdout}.jsonl 存在）:
    ```bash
    test -f datasets/tools/train.jsonl && \
      .venv/bin/python -c "
    from pathlib import Path
    from evolution.tools.tool_dataset import ToolSelectionDataset
    ds = ToolSelectionDataset.load(Path('datasets/tools'))
    print(f'train={len(ds.train)} val={len(ds.val)} holdout={len(ds.holdout)}')
    # 旧 JSONL 无 misselection_signals 字段 → 必须默认 []
    if ds.train:
        assert ds.train[0].misselection_signals == [], f'expected [], got {ds.train[0].misselection_signals!r}'
        print('backward-compat OK')
    " || echo "datasets/tools/train.jsonl 不存在，跳过 reload smoke"
    ```

    2) **完整 tools 测试套件**（B4：忽略 test_secret_patterns_v2.py，Plan 03 在同 Wave 内并行修改该文件）：`.venv/bin/pytest tests/tools/ --ignore=tests/tools/test_secret_patterns_v2.py -q --tb=no` 应只有 1 个新 test 转 pass（test_signals_union），其余 stub 仍 skip，老测试 PASS。允许 expected output: 现有 tool_dataset 测试 + 新 1 passed (signals_union) + skipped 数若干。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/ --ignore=tests/tools/test_secret_patterns_v2.py -q --tb=no 2>&1 | tail -3 | grep -qE '[0-9]+ passed' && ! .venv/bin/pytest tests/tools/ --ignore=tests/tools/test_secret_patterns_v2.py -q --tb=no 2>&1 | grep -qE 'failed|error'</automated>
  </verify>
  <acceptance_criteria>
    - tests/tools/ 整体不回归（无 failed/error），**忽略** test_secret_patterns_v2.py（B4 — 该文件由 Plan 03 同 Wave 内并行修改，避免 race 误报）: `! .venv/bin/pytest tests/tools/ --ignore=tests/tools/test_secret_patterns_v2.py -q --tb=no 2>&1 | grep -qE '[0-9]+ failed|[0-9]+ error'`
    - test_signals_union 是新 pass: `.venv/bin/pytest tests/tools/test_session_split.py::test_signals_union -q 2>&1 | grep -q '1 passed'`
    - Plan 01 的 28 stub 仍未实现（除 signals_union）: `.venv/bin/pytest tests/tools/test_session_*.py tests/tools/test_secret_patterns_v2.py tests/tools/test_jsonl_skip_bad.py tests/tools/test_surface_drift.py tests/tools/test_mine_cli.py tests/tools/test_evolve_with_session_source.py -q --tb=no 2>&1 | grep -E '[0-9]+ skipped' | grep -qE '2[0-9]'`
  </acceptance_criteria>
  <done>backward-compat 验证 GREEN，全 tests/tools/ 套件通过，仅 misselection_signals 一处变更。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no incremental boundary) | 本 plan 仅扩展现有 dataclass，不引入新 trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (no incremental Phase 14 threats) | — | — | — | 本 plan 仅 D-02 数据契约扩展，不触及 LLM judge / secret 过滤 / JSONL 容错 / consent flag。继承 ToolSelectionExample 现有威胁面（dataclass 不引入新 trust boundary）。 |

向后兼容性是本 plan 的关键不变量：from_dict 通过 `cls.__dataclass_fields__` 过滤——这是仓库**现有**模式（line 71），未由本 plan 引入。
</threat_model>

<verification>
- ToolSelectionExample 实例可创建带/不带 misselection_signals 字段
- 旧 Phase 4 JSONL（如 datasets/tools/train.jsonl 存在）reload 后 ds.train[i].misselection_signals == []
- to_dict / from_dict round-trip 保持新字段值
- ToolSelectionDataset.save/load 的源代码 NOT 修改（git diff 仅 line 范围 32-71）
- 现有 tests/tools/ 套件不回归
</verification>

<success_criteria>
- D-02 字段已加并参与序列化
- 旧 Phase 4 JSONL 自动兼容（默认空 list）
- 唯一新通过测试为 test_signals_union；其余 27 stub 保持 skip
- 整套 pytest tests/ 不回归
</success_criteria>

<output>
After completion, create `.planning/phases/14-sessiondb-mining-for-tools/14-02-SUMMARY.md` describing field added, backward-compat verified, lines changed.
</output>
