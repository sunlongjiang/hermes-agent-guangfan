---
phase: 14-sessiondb-mining-for-tools
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - evolution/tools/mine_tool_sessions.py
autonomous: true
requirements: [TOOL-V2-01]
tags: [phase-14, wave-3, cli, click, rich, consent-gate, asvs-v4, threat-T-14-04]

must_haves:
  truths:
    - "D-08 — 输出目录结构：datasets/tools/sessions/<YYYYMMDD_HHMMSS>/{train,val,holdout}.jsonl + metrics.json + miner_log.jsonl；CLI 末尾打印 Rich table 总结 candidate/judge 通过/各 signal 命中/surface drift dropped/final example 数"
    - "D-12 — mine_tool_sessions CLI flag `--misselection-multiplier \"error_retry=3,user_correction=3,oracle=2\"` 以 key=value 覆盖默认；GEPA 通过均匀采样消化加权"
    - "D-16 — `--i-have-consent` mandatory flag（Layer 3）；缺省 raise SystemExit(1) 含明确错误消息；evolve_* CLI 通过 `--session-source` 加载已清洗 JSONL 时不需要此 flag"
    - "python -m evolution.tools.mine_tool_sessions 可执行；--help 列出全部 D-07 必需 flags"
    - "缺 --i-have-consent 时 CLI exit_code == 1 且 stderr 含 'consent'（T-14-04 mitigation）"
    - "--dry-run 模式不调任何 LLM API（_judge_candidate 不被调用）"
    - "--signals=error_retry,user_correction 跳过 oracle 路径（self.signals 不包含 oracle_disagreement）"
    - "--misselection-multiplier 'error_retry=5,user_correction=2' 解析为 dict 正确传给 SessionToolMiner.multiplier_override"
    - "--baseline-module 缺省时 oracle 信号自动 skip + console warn"
    - "成功路径写出 datasets/tools/sessions/<YYYYMMDD_HHMMSS>/{train,val,holdout}.jsonl + metrics.json + miner_log.jsonl"
    - "失败路径（缺 sessions 文件 / 0 candidate）写 datasets/tools/sessions/FAILED_<ts>/ + sys.exit(1)"
    - "metrics.json 含 14-PATTERNS.md lines 308-326 列出的全部 13 个键（B2 — dry-run 与 wet-run 两路径均实测成立；wet-run 用 mock_lm_with_usage + 1 fixture session）"
    - "W4 — dry-run 路径调 `miner.enumerate_candidates(sessions_path, current_tools, limit=limit)` 公共 API，不再 poke 私有 `_extract_*`"
    - "Rich Table 总结打印每信号的 candidate / confirmed / false_positive / train_after_dup 统计"
    - "session_miner does NOT call tool_loader.write_back_description or any hermes-agent write path — CLI 仅读 sessions_dir + read 接口 extract_tool_descriptions"
    - "Phase 14 不引入新外部依赖：pyproject.toml [project] dependencies 与 commit 前一致"
  artifacts:
    - path: evolution/tools/mine_tool_sessions.py
      provides: "Click CLI entry point for SessionToolMiner; parses 8 flags + writes JSONL + metrics + miner_log"
      exports: ["main"]
      contains: "def main"
      min_lines: 200
  key_links:
    - from: evolution/tools/mine_tool_sessions.py::main
      to: evolution/tools/session_miner.py::SessionToolMiner
      via: "miner.mine(sessions_dir, current_tools) → miner.split_and_duplicate(examples)"
      pattern: "SessionToolMiner"
    - from: evolution/tools/mine_tool_sessions.py
      to: evolution/tools/tool_loader.py::extract_tool_descriptions
      via: "current_tools 来源（read-only）"
      pattern: "extract_tool_descriptions"
    - from: evolution/tools/mine_tool_sessions.py
      to: datasets/tools/sessions/<ts>/metrics.json
      via: "json.dumps with indent=2"
      pattern: "metrics.json"
---

<objective>
新建 `evolution/tools/mine_tool_sessions.py` —— Phase 14 的 mining CLI 入口。Wire D-07 全部 8 个 flags、D-13 输出目录结构、D-16 强制 consent gate、D-17 surface drift 报告、D-18 JSONL skip 计数。让用户可以一行命令离线挖矿全 ~/.hermes/sessions/ 转录得到 train/val/holdout JSONL + metrics.json。

Purpose: D-06 + D-07 + D-08 + D-12 + D-16 全部映射到本文件。CLI 是用户唯一接触面；T-14-04（操作员未授权挖矿）由本 plan 拦截。
Output: 单文件 mine_tool_sessions.py + 5 个测试 stub 解开（test_consent_required, test_dry_run, test_signal_subset, test_multiplier_override, test_baseline_module_optional）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sessiondb-mining-for-tools/14-CONTEXT.md
@.planning/phases/14-sessiondb-mining-for-tools/14-RESEARCH.md
@.planning/phases/14-sessiondb-mining-for-tools/14-PATTERNS.md
@evolution/tools/session_miner.py
@evolution/tools/evolve_tool_descriptions.py
@evolution/tools/evolve_tool_params.py
@evolution/core/config.py

<interfaces>
<!-- Plan 04 完成的 SessionToolMiner API -->
From evolution/tools/session_miner.py:
```python
class SessionToolMiner:
    def __init__(self, config: EvolutionConfig, signals=None, multiplier_override=None, baseline_module=None): ...
    def mine(self, sessions_dir: Path, current_tools: list, limit: int = 0) -> list[ToolSelectionExample]: ...
    def split_and_duplicate(self, examples) -> dict[str, list[ToolSelectionExample]]: ...
    metrics: dict  # filled after mine() + split_and_duplicate()

DEFAULT_MULTIPLIER: dict[str, int]  # {"error_retry":3,"user_correction":3,"oracle_disagreement":2}
VALID_SIGNALS: frozenset[str]
```

<!-- Phase 5 CLI 模板（evolve_tool_descriptions.py:400-417） — 复刻 4-flag standard set -->
Standard 4-flag set per D-07: --hermes-repo, --model, --api-base, --dry-run

<!-- Phase 14 新增 8 flags from D-07 (CONTEXT.md lines 38-45) -->
--sessions-dir <path>           # 默认 None → Path.home() / ".hermes" / "sessions"
--output <dir>                  # 默认 None → datasets/tools/sessions/<YYYYMMDD_HHMMSS>/
--limit <int>                   # 0 = all
--i-have-consent                # is_flag=True; REQUIRED — 缺则 SystemExit(1)
--signals <csv>                 # 默认 "error_retry,user_correction,oracle_disagreement"
--baseline-module <path>        # 默认 None → C 信号 skip
--judge-model <name>            # 默认 None → 走 EvolutionConfig.judge_model
--misselection-multiplier <kv>  # 默认 None；格式 "error_retry=3,user_correction=3,oracle_disagreement=2"

<!-- 输出目录约定（D-08） -->
datasets/tools/sessions/<ts>/
├── train.jsonl
├── val.jsonl
├── holdout.jsonl
├── metrics.json
└── miner_log.jsonl   # one line per (candidate, verdict)

<!-- 失败路径（PATTERNS §FAILED_/ABORTED_ lines 363-374） -->
datasets/tools/sessions/FAILED_<ts>/   # 写部分 metrics + sys.exit(1)
datasets/tools/sessions/ABORTED_<ts>/  # 用户 Ctrl+C / --limit 早停

<!-- Tool surface for D-17 -->
from evolution.tools.tool_loader import discover_tool_files, extract_tool_descriptions
current_tools = []
for file_path in discover_tool_files(config.hermes_agent_path):
    current_tools.extend(extract_tool_descriptions(file_path))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: 实现 mine_tool_sessions.py CLI 主入口（D-07 全部 flags + 编排 + metrics + Rich Table）</name>
  <files>evolution/tools/mine_tool_sessions.py</files>
  <read_first>
    - evolution/tools/evolve_tool_descriptions.py (lines 1-31, 350-417 — imports / metrics.json 写入 / Click 模板)
    - evolution/tools/evolve_tool_params.py (lines 30-34 — FAILED_/ABORTED_ docstring contract)
    - evolution/tools/session_miner.py (Plan 04 — SessionToolMiner API)
    - .planning/phases/14-sessiondb-mining-for-tools/14-CONTEXT.md (D-06, D-07, D-08, D-12, D-16, D-17)
    - .planning/phases/14-sessiondb-mining-for-tools/14-RESEARCH.md (§Code Examples Example 3 lines 619-644)
    - .planning/phases/14-sessiondb-mining-for-tools/14-PATTERNS.md (lines 198-388 — mine_tool_sessions.py 完整模板)
  </read_first>
  <action>
    创建 evolution/tools/mine_tool_sessions.py。结构对齐 evolve_tool_descriptions.py（imports + module-level console + helper functions + Click main + `if __name__ == "__main__": main()`）。

    **文件头**：
    ```python
    """SessionDB tool-misselection mining CLI — Phase 14 (TOOL-V2-01).

    Reads ~/.hermes/sessions/*.json transcripts and produces ToolSelectionExample
    JSONL files suitable for unioning with synthetic Phase 4 datasets.

    Usage:
        python -m evolution.tools.mine_tool_sessions \\
            --i-have-consent \\
            --sessions-dir ~/.hermes/sessions \\
            --signals error_retry,user_correction,oracle_disagreement \\
            --baseline-module output/tools/<latest> \\
            --output datasets/tools/sessions/<ts>

    Output topology (D-08):
        datasets/tools/sessions/<YYYYMMDD_HHMMSS>/
            ├── train.jsonl / val.jsonl / holdout.jsonl
            ├── metrics.json
            └── miner_log.jsonl

    Failure paths:
        FAILED_<ts>/   — sessions empty / consent missing / 0 candidate after filter
        ABORTED_<ts>/  — Ctrl+C / --limit early stop

    READ-ONLY guarantee: this CLI never calls tool_loader.write_back_description
    or any hermes-agent mutation path. It only reads session JSON + the current
    tool surface via discover_tool_files / extract_tool_descriptions.
    """

    import json
    import sys
    from datetime import datetime
    from pathlib import Path
    from typing import Optional

    import click
    from rich.console import Console
    from rich.panel import Panel
    from rich.table import Table

    from evolution.core.config import EvolutionConfig, get_hermes_agent_path
    from evolution.tools.tool_loader import discover_tool_files, extract_tool_descriptions
    from evolution.tools.session_miner import (
        SessionToolMiner,
        DEFAULT_MULTIPLIER,
        VALID_SIGNALS,
    )

    console = Console()
    ```

    **Helpers**（4 个私有 helper）：

    ```python
    def _parse_signals(value: str) -> list[str]:
        """Parse '--signals' CSV into a deduped list. Unknown signals → click.UsageError."""
        items = [s.strip() for s in (value or "").split(",") if s.strip()]
        bad = [s for s in items if s not in VALID_SIGNALS]
        if bad:
            raise click.UsageError(
                f"--signals contains unknown signal(s): {bad}. Valid: {sorted(VALID_SIGNALS)}"
            )
        if not items:
            raise click.UsageError("--signals is empty after parsing")
        return list(dict.fromkeys(items))  # dedupe, preserve order


    def _parse_multiplier_override(value: Optional[str]) -> dict[str, int]:
        """Parse '--misselection-multiplier' kv string into dict[str, int].

        Format: 'error_retry=3,user_correction=3,oracle_disagreement=2'.
        Empty / None returns {}. Unknown signal keys raise click.UsageError.
        """
        if not value:
            return {}
        out: dict[str, int] = {}
        for part in value.split(","):
            if not part.strip():
                continue
            if "=" not in part:
                raise click.UsageError(
                    f"--misselection-multiplier item {part!r} missing '='"
                )
            k, v = part.split("=", 1)
            k = k.strip()
            if k not in VALID_SIGNALS:
                raise click.UsageError(
                    f"--misselection-multiplier unknown signal {k!r}; valid: {sorted(VALID_SIGNALS)}"
                )
            try:
                out[k] = int(v.strip())
            except ValueError:
                raise click.UsageError(
                    f"--misselection-multiplier value for {k!r} must be int, got {v!r}"
                )
        return out


    def _load_baseline_module(output_dir: Path, hermes_repo: Path):
        """Reconstruct a ToolModule from a Phase 5 / 13 output dir (Pitfall 4).

        Reads <output_dir>/evolved_descriptions.json. Falls back to current
        hermes-agent toolset for any tool not present in the artifact.
        Returns ToolModule or raises click.UsageError.
        """
        from evolution.tools.tool_module import ToolModule
        evolved_path = output_dir / "evolved_descriptions.json"
        if not evolved_path.exists():
            raise click.UsageError(
                f"--baseline-module {output_dir} missing evolved_descriptions.json"
            )
        payload = json.loads(evolved_path.read_text())
        # Build current tools and overlay artifact descriptions
        current_tools: list = []
        for fp in discover_tool_files(hermes_repo):
            current_tools.extend(extract_tool_descriptions(fp))
        desc_map = {item.get("name"): item.get("description", "") for item in payload}
        params_map = {
            item.get("name"): {p.get("name"): p.get("description", "") for p in item.get("params", [])}
            for item in payload
        }
        for t in current_tools:
            if t.name in desc_map:
                t.description = desc_map[t.name]
                pm = params_map.get(t.name, {})
                for p in getattr(t, "params", []):
                    if p.name in pm:
                        p.description = pm[p.name]
        return ToolModule(current_tools)


    def _write_miner_log(out_dir: Path, miner: SessionToolMiner) -> None:
        """Write miner_log.jsonl: one line per metric snapshot for audit.

        Phase 14 keeps this minimal: dump miner.metrics as one JSON object on
        a single line. Future phases can extend per-candidate audit lines.
        """
        log_path = out_dir / "miner_log.jsonl"
        with open(log_path, "w") as f:
            f.write(json.dumps({"event": "metrics_snapshot", "metrics": miner.metrics}) + "\n")
    ```

    **主 evolve / mine 函数**（被 CLI 调用）：

    ```python
    def mine(
        sessions_dir: Optional[str],
        output: Optional[str],
        limit: int,
        i_have_consent: bool,
        signals: str,
        baseline_module: Optional[str],
        judge_model: Optional[str],
        misselection_multiplier: Optional[str],
        hermes_repo: Optional[str],
        model: Optional[str],
        api_base: Optional[str],
        dry_run: bool,
    ) -> int:
        """Run the mining pipeline. Returns exit code (0 success, 1 failure)."""
        # ── 0. Consent gate (D-16, T-14-04) ──────────────────────────────
        if not i_have_consent:
            click.echo(
                "--i-have-consent is REQUIRED — refusing to read session data without explicit consent.\n"
                "Pass --i-have-consent to proceed.",
                err=True,
            )
            return 1

        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        # ── 1. Resolve config + paths ────────────────────────────────────
        config = EvolutionConfig.load(
            api_base=api_base,
            api_key=None,
            model=model,
            hermes_repo=hermes_repo,
        )
        if judge_model:
            config.judge_model = judge_model

        sessions_path = Path(sessions_dir).expanduser() if sessions_dir else (Path.home() / ".hermes" / "sessions")
        if output:
            out_dir = Path(output)
        else:
            out_dir = Path("datasets") / "tools" / "sessions" / timestamp

        signals_list = _parse_signals(signals)
        multiplier_override = _parse_multiplier_override(misselection_multiplier)

        console.print(Panel.fit(
            f"[bold cyan]Phase 14 SessionDB Mining[/]\n"
            f"sessions={sessions_path}  output={out_dir}\n"
            f"signals={signals_list}  judge_model={config.judge_model}\n"
            f"limit={'all' if limit == 0 else limit}  dry_run={dry_run}",
        ))

        # ── 2. Sessions dir validation ──────────────────────────────────
        if not sessions_path.exists() or not sessions_path.is_dir():
            console.print(f"[red]✗ sessions-dir {sessions_path} does not exist or is not a directory[/red]")
            failed = Path("datasets") / "tools" / "sessions" / f"FAILED_{timestamp}"
            failed.mkdir(parents=True, exist_ok=True)
            (failed / "metrics.json").write_text(
                json.dumps({"error": "sessions_dir_missing", "path": str(sessions_path)}, indent=2)
            )
            return 1

        # ── 3. Current tool surface ────────────────────────────────────
        current_tools: list = []
        for fp in discover_tool_files(config.hermes_agent_path):
            current_tools.extend(extract_tool_descriptions(fp))
        if not current_tools:
            console.print(f"[red]✗ No tools discovered under {config.hermes_agent_path}[/red]")
            failed = Path("datasets") / "tools" / "sessions" / f"FAILED_{timestamp}"
            failed.mkdir(parents=True, exist_ok=True)
            (failed / "metrics.json").write_text(json.dumps({"error": "no_tools_found"}, indent=2))
            return 1

        # ── 4. Optional baseline module for C signal ───────────────────
        baseline_mod = None
        if baseline_module:
            baseline_mod = _load_baseline_module(Path(baseline_module), config.hermes_agent_path)
        elif "oracle_disagreement" in signals_list:
            console.print(
                "[yellow]⚠ --baseline-module not provided; oracle_disagreement signal will be skipped[/yellow]"
            )

        # ── 5. Mining ───────────────────────────────────────────────────
        miner = SessionToolMiner(
            config=config,
            signals=signals_list,
            multiplier_override=multiplier_override,
            baseline_module=baseline_mod,
        )

        if dry_run:
            console.print("[bold yellow]DRY-RUN[/]: enumerating candidates only — no LLM calls")
            # W4 修订: 走 SessionToolMiner.enumerate_candidates 公共 API（避免 poke 私有 _extract_*）。
            # B2 修订: enumerate_candidates 调 _fresh_metrics() 已初始化全 13 keys，
            # 包括 final_examples_by_split / final_train_after_duplication / multiplier_used，
            # dry-run metrics.json 与 wet-run schema 一致。
            cands = miner.enumerate_candidates(sessions_path, current_tools, limit=limit)
            _print_summary_table(miner.metrics, len(cands))
            out_dir.mkdir(parents=True, exist_ok=True)
            (out_dir / "metrics.json").write_text(json.dumps(miner.metrics, indent=2))
            console.print(f"\n[bold green]Dry-run complete[/]: {out_dir}/metrics.json")
            return 0

        examples = miner.mine(sessions_path, current_tools, limit=limit)
        if not examples:
            console.print("[yellow]⚠ No misselection examples after judge; writing FAILED_<ts>/[/yellow]")
            failed = Path("datasets") / "tools" / "sessions" / f"FAILED_{timestamp}"
            failed.mkdir(parents=True, exist_ok=True)
            (failed / "metrics.json").write_text(json.dumps(miner.metrics, indent=2))
            _print_summary_table(miner.metrics, 0)
            return 1

        split = miner.split_and_duplicate(examples)

        # ── 6. Write outputs (D-08) ─────────────────────────────────────
        out_dir.mkdir(parents=True, exist_ok=True)
        for split_name in ("train", "val", "holdout"):
            with open(out_dir / f"{split_name}.jsonl", "w") as f:
                for ex in split[split_name]:
                    f.write(json.dumps(ex.to_dict(), ensure_ascii=False) + "\n")
        (out_dir / "metrics.json").write_text(json.dumps(miner.metrics, indent=2))
        _write_miner_log(out_dir, miner)

        _print_summary_table(miner.metrics, len(examples))
        console.print(f"\n[bold green]Mining complete[/]: {out_dir}/")
        return 0


    def _print_summary_table(metrics: dict, total_examples: int) -> None:
        """Rich Table summary (D-08 末)."""
        t = Table(title="SessionDB Mining Summary", show_header=True, header_style="bold cyan")
        t.add_column("Signal", style="bold")
        t.add_column("Candidates", justify="right")
        t.add_column("Confirmed", justify="right")
        t.add_column("False Positives", justify="right")
        for s in ("error_retry", "user_correction", "oracle_disagreement"):
            t.add_row(
                s,
                str(metrics["total_candidates_by_signal"].get(s, 0)),
                str(metrics["judge_confirmed_by_signal"].get(s, 0)),
                str(metrics["judge_false_positives_by_signal"].get(s, 0)),
            )
        t.add_row(
            "[bold]TOTAL[/]",
            str(sum(metrics["total_candidates_by_signal"].values())),
            str(sum(metrics["judge_confirmed_by_signal"].values())),
            str(sum(metrics["judge_false_positives_by_signal"].values())),
        )
        console.print(t)
        console.print(f"  surface_drift_dropped: {metrics['surface_drift_dropped']}")
        console.print(f"  secret_filter_skipped: {metrics['secret_filter_skipped']}")
        console.print(f"  judge_calls: {metrics.get('judge_calls', 0)}")
        # Pitfall 8: top-10 surface drift tools to console; full dict in metrics.json
        drift = metrics.get("surface_drift_tools") or {}
        if drift:
            top = sorted(drift.items(), key=lambda kv: kv[1], reverse=True)[:10]
            console.print(f"  surface_drift_tools (top-{len(top)}): {dict(top)}")
        console.print(f"  total_examples_post_judge: {total_examples}")
    ```

    **Click CLI**：

    ```python
    @click.command()
    @click.option("--sessions-dir", default=None, type=click.Path(),
                  help="Directory containing session_*.json (default ~/.hermes/sessions)")
    @click.option("--output", default=None, type=click.Path(),
                  help="Output directory (default datasets/tools/sessions/<YYYYMMDD_HHMMSS>/)")
    @click.option("--limit", default=0, type=int, help="0 = scan all sessions")
    @click.option("--i-have-consent", is_flag=True,
                  help="REQUIRED — explicit consent to read session data (Layer 3 privacy gate)")
    @click.option("--signals", default="error_retry,user_correction,oracle_disagreement",
                  help="Comma-separated subset of {error_retry, user_correction, oracle_disagreement}")
    @click.option("--baseline-module", default=None, type=click.Path(),
                  help="Path to a Phase 5/13 output dir for oracle (C signal); omit → skip C")
    @click.option("--judge-model", default=None,
                  help="Override config.judge_model for ConfirmMisselection LLM judge")
    @click.option("--misselection-multiplier", default=None,
                  help='Override defaults, format "error_retry=3,user_correction=3,oracle_disagreement=2"')
    @click.option("--hermes-repo", default=None,
                  help="Path to hermes-agent repo (overrides HERMES_AGENT_REPO env)")
    @click.option("--model", default=None, help="Override LLM model for non-judge calls")
    @click.option("--api-base", default=None, help="Override API base URL")
    @click.option("--dry-run", is_flag=True,
                  help="Skip LLM judge; enumerate candidates and exit")
    def main(
        sessions_dir: Optional[str],
        output: Optional[str],
        limit: int,
        i_have_consent: bool,
        signals: str,
        baseline_module: Optional[str],
        judge_model: Optional[str],
        misselection_multiplier: Optional[str],
        hermes_repo: Optional[str],
        model: Optional[str],
        api_base: Optional[str],
        dry_run: bool,
    ) -> None:
        """Mine hermes session JSONL transcripts for tool misselection patterns."""
        sys.exit(mine(
            sessions_dir=sessions_dir,
            output=output,
            limit=limit,
            i_have_consent=i_have_consent,
            signals=signals,
            baseline_module=baseline_module,
            judge_model=judge_model,
            misselection_multiplier=misselection_multiplier,
            hermes_repo=hermes_repo,
            model=model,
            api_base=api_base,
            dry_run=dry_run,
        ))


    if __name__ == "__main__":
        main()
    ```

    **解 stubs**（5 个 mine_cli 测试 row 20-24）：

    在 tests/tools/test_mine_cli.py 中替换 5 个 stub 用 `click.testing.CliRunner`：
    - `test_consent_required`: invoke main with no flag → exit_code == 1, "consent" 在 stderr
    - `test_dry_run`: tmp_path 含一个 minimal session JSON, --i-have-consent --dry-run --sessions-dir tmp_path --output tmp_path/out → exit_code == 0; metrics.json 写入；judge_calls == 0
    - `test_signal_subset`: --signals error_retry,user_correction → metrics["judge_calls_by_signal"]["oracle_disagreement"] == 0
    - `test_multiplier_override`: 调 _parse_multiplier_override（直接 import + call）assert {"error_retry":5,"user_correction":2}
    - `test_baseline_module_optional`: --signals error_retry,oracle_disagreement 但无 --baseline-module → console output 含 "skipped" 或 "skip"; oracle 信号实际 0 candidate

    禁止：在 CLI 直接 import write_back_description / 任何 hermes-agent 写回路径；新增依赖。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_mine_cli.py -v --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在: `test -f evolution/tools/mine_tool_sessions.py`
    - main + mine 函数: `grep -q "@click.command()" evolution/tools/mine_tool_sessions.py && grep -q "^def main(" evolution/tools/mine_tool_sessions.py && grep -q "^def mine(" evolution/tools/mine_tool_sessions.py`
    - 8 个新增 flags + 4 个 standard flags 全部存在: `for flag in 'sessions-dir' 'output' 'limit' 'i-have-consent' 'signals' 'baseline-module' 'judge-model' 'misselection-multiplier' 'hermes-repo' 'model' 'api-base' 'dry-run'; do grep -q -- "--$flag" evolution/tools/mine_tool_sessions.py || { echo "missing flag $flag"; exit 1; }; done`
    - --i-have-consent help 字面: `grep -q "REQUIRED" evolution/tools/mine_tool_sessions.py`
    - 没有 hermes-agent 写回 import: `! grep -E 'write_back_description|write_back_param' evolution/tools/mine_tool_sessions.py`
    - SessionToolMiner imported: `grep -q "from evolution.tools.session_miner import" evolution/tools/mine_tool_sessions.py`
    - --help 实际可执行: `.venv/bin/python -m evolution.tools.mine_tool_sessions --help 2>&1 | grep -q -- '--i-have-consent'`
    - --help 列出 12 flags: `.venv/bin/python -m evolution.tools.mine_tool_sessions --help 2>&1 | grep -cE '^\\s+--' | xargs test 12 -le`
    - 5 个 CLI stubs 解开 GREEN（W3 — 用 file::function 取代 -k 模糊匹配）: `for fn in test_consent_required test_dry_run test_signal_subset test_multiplier_override test_baseline_module_optional; do .venv/bin/pytest tests/tools/test_mine_cli.py::$fn -q --tb=no 2>&1 | grep -qE "1 passed" || { echo "fail $fn"; exit 1; }; done`
    - 缺 consent 时 exit 1: `.venv/bin/python -m evolution.tools.mine_tool_sessions 2>&1 | head -5 | grep -qi 'consent'`
    - 缺 consent 时 exit code = 1: `.venv/bin/python -m evolution.tools.mine_tool_sessions; test $? -eq 1`
    - 整套 tests/tools/ 不回归: `! .venv/bin/pytest tests/tools/ -q --tb=no 2>&1 | grep -qE '[0-9]+ failed|[0-9]+ error'`
    - pyproject.toml 未改: `! git diff --name-only 2>/dev/null | grep -q '^pyproject.toml$'`
  </acceptance_criteria>
  <done>CLI 落地，5 个 mine_cli 测试 GREEN，consent gate 拦截无 flag 调用，--help 列出全部 flags。</done>
</task>

<task type="auto">
  <name>Task 5.2: 解 stub - test_metrics_schema 完整字段断言</name>
  <files>tests/tools/test_session_miner.py</files>
  <read_first>
    - tests/tools/test_session_miner.py (test_metrics_schema 现状 — Plan 04 已部分实现)
    - tests/tools/test_session_miner.py::test_mine_end_to_end (Plan 04 line 843 附近 — `mock_lm_with_usage` + fixture session 套路参考)
    - tests/tools/conftest.py (mock_lm_with_usage fixture + fixture sessions 位置)
    - .planning/phases/14-sessiondb-mining-for-tools/14-PATTERNS.md (lines 308-326 — metrics.json 完整字段集)
    - evolution/tools/mine_tool_sessions.py (Task 5.1 完成 — 现在 metrics 包含 final_examples_by_split / multiplier_used / final_train_after_duplication / cost_usd_spent)
  </read_first>
  <action>
    扩展 test_metrics_schema 让它在 **dry-run 与 wet-run 两条路径**都实测断言全 13 keys（B2 修订核心——W-B2-partial 回复：取消 wet-run 注释块，实测而非仅靠 _fresh_metrics 结构推断）。Plan 04 已让 `_fresh_metrics()` 预初始化 final_examples_by_split / final_train_after_duplication / multiplier_used，wet-run 走 mine + split_and_duplicate 更新这些 keys 的值，两路径都应持有 13 keys。

    **wet-run 构造参考**：直接对照 Plan 04 `test_mine_end_to_end`（~line 843）的 mock_lm_with_usage + 1 fixture session 套路；fixture session 文件与 fixture tool 列表从 `tests/fixtures/sessions/` 与 conftest 现成资源复用。

    ```python
    def test_metrics_schema(mock_lm_with_usage, tmp_path, monkeypatch):
        """metrics.json schema 含 14-PATTERNS.md lines 308-326 的全部 13 个字段；
        在 dry-run（仅 enumerate_candidates）与 wet-run（mine + split_and_duplicate）
        两路径均**实测**成立（B2 / W-B2-partial fix）。
        """
        import dspy
        from pathlib import Path
        from evolution.tools.session_miner import SessionToolMiner
        from evolution.core.config import EvolutionConfig

        expected_keys = {
            "total_candidates_by_signal",
            "judge_confirmed_by_signal",
            "judge_false_positives_by_signal",
            "surface_drift_dropped",
            "surface_drift_tools",
            "final_examples_by_split",
            "final_train_after_duplication",
            "multiplier_used",
            "secret_filter_skipped",
            "jsonl_skipped_lines",
            "judge_calls",
            "judge_calls_by_signal",
            "cost_usd_spent",
        }

        config = EvolutionConfig()

        # ── (a) Dry-run path: enumerate_candidates only ────────────────
        sessions_dir = tmp_path / "sessions_a"
        sessions_dir.mkdir()
        # Empty sessions_dir is fine — we just need the miner to walk it and
        # finish; metrics shape is what we assert.
        miner_dry = SessionToolMiner(config=config)
        miner_dry.enumerate_candidates(sessions_dir, current_tools=[], limit=0)
        missing_dry = expected_keys - set(miner_dry.metrics.keys())
        assert not missing_dry, f"dry-run metrics missing keys: {missing_dry}"
        assert set(miner_dry.metrics["final_examples_by_split"].keys()) == {"train", "val", "holdout"}
        assert miner_dry.metrics["multiplier_used"]["error_retry"] >= 1

        # ── (b) Wet-run path: mine + split_and_duplicate (LLM mocked) ──
        # 用与 Plan 04 test_mine_end_to_end 相同的 mock_lm_with_usage 套路；
        # fixture sessions 在 tests/fixtures/sessions/；tool 列表从同款 fixture 取。
        monkeypatch.setattr(dspy.settings, "lm", mock_lm_with_usage)
        miner_wet = SessionToolMiner(
            config=config,
            signals={"error_retry"},          # 单 signal 足够覆盖 13 keys 填充
            multiplier_override=None,
            baseline_module=None,
        )
        # 参考 test_mine_end_to_end：fixture session 目录 + minimal current_tools
        fixture_sessions = Path("tests/fixtures/sessions/")
        # tool_descs 来自与 test_mine_end_to_end 相同的 fixture 构造（1 个 tool 即可）；
        # 若 conftest 已暴露 fixture（如 `fixture_tool_descs`），直接在函数签名加入该参数。
        # 本 test 若未注入 fixture，保留最小单 tool stub：
        from evolution.tools.tool_loader import ToolDescription
        tool_descs = [ToolDescription(name="terminal", description="run shell", params=[])]

        examples = miner_wet.mine(
            sessions_dir=fixture_sessions,
            current_tools=tool_descs,
            limit=1,
        )
        miner_wet.split_and_duplicate(examples)

        missing_wet = expected_keys - set(miner_wet.metrics.keys())
        assert not missing_wet, f"wet-run metrics missing keys: {missing_wet}"

        # signal-bucketed dicts have all 3 signals (两路径共同断言)
        for miner in (miner_dry, miner_wet):
            for k in ("total_candidates_by_signal", "judge_confirmed_by_signal",
                      "judge_false_positives_by_signal", "judge_calls_by_signal"):
                assert set(miner.metrics[k].keys()) == {"error_retry","user_correction","oracle_disagreement"}, \
                    f"{k} keys = {miner.metrics[k].keys()}"
    ```

    **实现备注（给 executor）**：
    - 若 `tests/tools/conftest.py` 已经有 `fixture_tool_descs` / `fixture_sessions_dir` 等可复用 fixture，优先改成 fixture 参数注入，避免 inline `ToolDescription(...)` 构造偏离既有 test 风格。
    - fixture session 文件需至少包含 1 个 error_retry 信号 candidate，否则 `examples` 可能为空；split_and_duplicate 即使 examples=[] 也会更新 final_examples_by_split / final_train_after_duplication（写 0），断言依然成立。
    - mock_lm_with_usage 已在 Plan 04 test_mine_end_to_end 中使用；判决 ConfirmMisselection 时会走该 mock，不产生真实 API 调用。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_session_miner.py::test_metrics_schema -x --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test_metrics_schema 含 13 keys 集合断言: `grep -q '"final_examples_by_split"' tests/tools/test_session_miner.py && grep -q '"multiplier_used"' tests/tools/test_session_miner.py && grep -q '"final_train_after_duplication"' tests/tools/test_session_miner.py`
    - B2 — test 同时断言 dry-run（enumerate_candidates）路径含 13 keys: `grep -q 'enumerate_candidates' tests/tools/test_session_miner.py && grep -q 'missing_dry' tests/tools/test_session_miner.py`
    - W-B2-partial fix — wet-run 路径实测（非注释）：`grep -q 'miner_wet' tests/tools/test_session_miner.py && grep -q 'missing_wet' tests/tools/test_session_miner.py && grep -q 'miner_wet.mine(' tests/tools/test_session_miner.py`
    - W-B2-partial fix — wet-run 使用 mock_lm_with_usage 而非真 LLM: `grep -q 'mock_lm_with_usage' tests/tools/test_session_miner.py && grep -q 'monkeypatch.setattr(dspy.settings' tests/tools/test_session_miner.py`
    - W-B2-partial fix — 最终循环含两路径（not `(miner_dry,)`）: `grep -qE 'for miner in \\(miner_dry, miner_wet\\)' tests/tools/test_session_miner.py`
    - test 通过（W3 — explicit file::function 取代 -k）: `.venv/bin/pytest tests/tools/test_session_miner.py::test_metrics_schema -q --tb=no 2>&1 | grep -q '1 passed'`
  </acceptance_criteria>
  <done>test_metrics_schema 完整断言所有 14-PATTERNS.md 字段，**dry-run + wet-run 两路径均实测含 13 keys**（B2 / W-B2-partial），wet-run 走 mock_lm_with_usage + 1 fixture session，GREEN。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户终端 → CLI 进程 | 必须经 --i-have-consent 显式 opt-in 才允许读 session 数据 |
| CLI 进程 → 文件系统 (~/.hermes/sessions/) | 进程仅读取，绝不写回；hermes-agent repo 同样仅 read 接口 |
| CLI 进程 → datasets/tools/sessions/<ts>/ | 输出目录写入；包含 metrics.json + miner_log.jsonl + 三 split JSONL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-14-04** (owns) | Spoofing / Repudiation | `evolution/tools/mine_tool_sessions.py::mine()` 第 0 步 consent gate | mitigate | 缺 --i-have-consent 时 click.echo(...err=True) → return 1 → sys.exit(1)。test_consent_required 验证 exit_code==1 + stderr 含 "consent"。CLI **不**回退到默认 yes；不可被 env var 旁路（D-16 显式：必填 flag）。 |
| **T-14-02** (consumer) | Information disclosure | mine() 走 SessionToolMiner.mine → secret filter（Plan 03） | mitigate (Plan 03) | 不重新实现 secret 过滤。 |
| **T-14-01** (consumer) | Tampering | mine() 走 SessionToolMiner._judge_candidate（Plan 04） | mitigate (Plan 04) | 不重新实现 judge fail-closed。 |
| **T-14-03** (consumer) | DoS | dry-run 路径直接 json.loads(sp.read_text())；非合法 session JSON 走 try/except → continue + jsonl_skipped_lines++ | mitigate (uses Plan 04 helper) | mine() 路径已由 SessionToolMiner.mine 内部 try/except 保护。dry-run 路径在 Task 5.1 显式写了 try/except + 计数。 |
| Output dir traversal via --output | Tampering | --output 取 click.Path() | accept | click.Path() 接受任意路径；这是单用户离线工具，与 deploy_mode gate 无关；rejected for Phase 14 scope。 |
| --baseline-module pointing to forged json | Spoofing | _load_baseline_module → json.loads → 若不含 evolved_descriptions.json 抛 click.UsageError | mitigate | _load_baseline_module 抛 UsageError → click 退出 2；不会静默 fallback（Pitfall 4）。 |
</threat_model>

<verification>
- mine_tool_sessions.py ≥ 200 行 source code
- 12 个 Click flags（4 standard + 8 phase-14 specific）
- python -m evolution.tools.mine_tool_sessions --help 列出全部 flags
- 5 个 mine_cli 测试 GREEN（consent_required + dry_run + signal_subset + multiplier_override + baseline_module_optional）
- test_metrics_schema GREEN（**dry-run + wet-run 两路径**均实测含 final_examples_by_split / multiplier_used / final_train_after_duplication / cost_usd_spent 字段）
- session_miner.py 与 tool_dataset.py 的 D-02 字段未被本 plan 修改
- pyproject.toml 未改
- 整套 tests/ GREEN 不回归
</verification>

<success_criteria>
- 缺 --i-have-consent → exit 1 + "consent" 在 stderr
- --dry-run 不调 LLM（judge_calls 保持 0）
- --signals=error_retry,user_correction 跳过 oracle path
- --misselection-multiplier "error_retry=5,user_correction=2" 正确解析
- --baseline-module 缺省 → C 信号自动 skip + console warn
- metrics.json schema 完整（14-PATTERNS.md lines 308-326 全部字段）—— dry-run + wet-run 两路径实测
- read-only 守卫不破（无 write_back_description 引用）
</success_criteria>

<output>
After completion, create `.planning/phases/14-sessiondb-mining-for-tools/14-05-SUMMARY.md` listing CLI flags wired, consent-gate behavior, summary table layout, output directory topology.
</output>
</output>
