---
phase: 14-sessiondb-mining-for-tools
plan: 06
type: execute
wave: 4
depends_on: [04, 05]
files_modified:
  - evolution/tools/evolve_tool_descriptions.py
  - evolution/tools/evolve_tool_params.py
autonomous: false
requirements: [TOOL-V2-01]
tags: [phase-14, wave-4, evolve-cli-integration, session-source, calibration, manual-verification]

must_haves:
  truths:
    - "D-14 — evolve_* CLI 在 union 合成 + session 数据集时：先各自按 task hash 去重，再两路 union；同 hash 例子 session 优先（误选信号在 misselection_signals 字段保留）；最终 train/val/holdout 跨数据源 hash 去重"
    - "evolve_tool_descriptions 与 evolve_tool_params 接 --session-source <path> flag（exists=True, dir_okay=True）"
    - "传入 --session-source 时 CLI 经 _load_jsonl_skip_bad 加载三个 split jsonl，与 ToolDatasetBuilder 合成集 union（synthetic 入桶在前，session 入桶在后；同 hash session 优先；Pitfall 10）"
    - "B1 — union 通过模块级 helper `_union_session_into_dataset(dataset, session_source) -> None`（**接收 in-memory ToolSelectionDataset 对象**）封装；evolve_tool_descriptions.py 在 inline load 后调用，evolve_tool_params.py 在 _load_dataset() 内部 dataset 已存在的位置调用（thread `session_source` 参数）—— 不在 _evolve_impl 内部引用未存在的 dataset 局部，避免 NameError"
    - "B1 — 两 CLI 文件**各自**定义一份 `_union_session_into_dataset` helper（不跨模块 import），语义/签名/不做二次复制 (Pitfall 5) 完全一致；W5 验证两份拷贝均不引用 `_multiplier_for` 或 `DEFAULT_MULTIPLIER`"
    - "train.jsonl 已由 mine_tool_sessions 预复制；evolve CLI 在 union 路径不再做二次复制（Pitfall 5）"
    - "W5 — `_union_session_into_dataset` 两份 helper body 均不引用 `_multiplier_for` 或 `DEFAULT_MULTIPLIER`；test_no_double_duplication 用 `inspect.getsource(_union_session_into_dataset)` 双管验证（行为代理 + source 检查）"
    - "不传 --session-source 时 CLI 行为完全不变（既有 Phase 5 / Phase 13 测试不回归）"
    - "metrics.json 增加 session_source: str | null 字段为 traceability"
    - "Plan 06 不修改 EvalDataset.load / GoldenDatasetLoader.load / ToolSelectionDataset.load（D-18 范围保持）"
    - "session_miner does NOT call tool_loader.write_back_description or any hermes-agent write path — 本 plan 也只读 session JSONL"
    - "Phase 14 不引入新外部依赖：pyproject.toml [project] dependencies 与 commit 前一致"
    - "Manual verification checkpoint：44-session 真数据 dry-run 通过（人工确认 candidate 数 ≥ 1 per 启用 signal、metrics.json 完整、不超预算）"
    - "熵阈值 calibration checkpoint：dry-run 输出 secret_filter_skipped 占比 ≤ 5%；若 > 5% 则把 _SECRET_ENTROPY_THRESHOLD 提到 4.3 并重跑"
  artifacts:
    - path: evolution/tools/evolve_tool_descriptions.py
      provides: "--session-source flag + union load branch + metrics.json session_source 字段"
      contains: "session-source"
    - path: evolution/tools/evolve_tool_params.py
      provides: "--session-source flag + union load branch + metrics.json session_source 字段"
      contains: "session-source"
  key_links:
    - from: evolution/tools/evolve_tool_descriptions.py
      to: evolution/tools/session_miner.py
      via: "session JSONL 容错加载（D-18） + normalize hash"
      pattern: "_load_jsonl_skip_bad"
    - from: evolution/tools/evolve_tool_params.py
      to: evolution/tools/session_miner.py
      via: "同上"
      pattern: "_load_jsonl_skip_bad"
    - from: evolve_tool_*.py union branch
      to: evolution/tools/tool_dataset.py::ToolSelectionExample.from_dict
      via: "session JSONL 行 → ToolSelectionExample（带 misselection_signals）"
      pattern: "ToolSelectionExample.from_dict"
---

<objective>
把 mine_tool_sessions 产出的 JSONL 接进现有两个 evolve CLI（D-09/D-14）：在 evolve_tool_descriptions.py 与 evolve_tool_params.py 上各加一个 --session-source flag；传入时通过 _load_jsonl_skip_bad 读三个 split jsonl，按 hash 在 split 内 union（session 优先），不二次复制。然后跑 manual verification checkpoint：用真实 ~/.hermes/sessions/ 跑 dry-run smoke + entropy threshold calibration。

Purpose: 闭合 Phase 14 端到端管道；让用户跑 mine_tool_sessions ... 后再 evolve_tool_descriptions --session-source datasets/tools/sessions/<ts> 一气呵成。Manual checkpoint 验证 RESEARCH §A2 熵阈值假设 + 实际 cost 估算。
Output: 两个 evolve CLI 文件加 flag + 集成 27/28 测试 row GREEN（test_session_overrides_synth + test_no_double_duplication）。Manual verification block 由人工执行并签字。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sessiondb-mining-for-tools/14-CONTEXT.md
@.planning/phases/14-sessiondb-mining-for-tools/14-RESEARCH.md
@.planning/phases/14-sessiondb-mining-for-tools/14-PATTERNS.md
@evolution/tools/evolve_tool_descriptions.py
@evolution/tools/evolve_tool_params.py
@evolution/tools/session_miner.py
@evolution/tools/tool_dataset.py

<interfaces>
Plan 04 export — _load_jsonl_skip_bad helper:
  def _load_jsonl_skip_bad(path: Path) -> tuple[list[dict], int]
  def _normalize_task_hash(task: str) -> str

Plan 02 - ToolSelectionExample 已支持 misselection_signals。
ToolSelectionDataset.load 不动（D-18 守卫）。

现有 evolve CLI 入口（修改前）:
  evolve_tool_descriptions.py: lines 400-417 — Click 命令 + main() 转 evolve()
  evolve_tool_params.py: lines 509-617 — Click 命令 + evolve() 转 _evolve_impl()

Pitfall 10 union 顺序铁律: synth 先入 by_hash dict, session 后入 — 同 hash session 覆写 synth。
**不再** apply train multiplier — train.jsonl 已由 mine_tool_sessions 预复制（Pitfall 5）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: evolve_tool_descriptions 加 --session-source flag + union load 分支</name>
  <files>evolution/tools/evolve_tool_descriptions.py</files>
  <read_first>
    - evolution/tools/evolve_tool_descriptions.py (lines 1-31, 350-417 — current imports + metrics.json 写入 + Click 命令)
    - .planning/phases/14-sessiondb-mining-for-tools/14-CONTEXT.md (D-09 + D-14)
    - .planning/phases/14-sessiondb-mining-for-tools/14-RESEARCH.md (§Pitfall 5 lines 480-492 + §Pitfall 10 lines 555-573)
    - .planning/phases/14-sessiondb-mining-for-tools/14-PATTERNS.md (lines 540-577)
    - tests/tools/test_evolve_tool_descriptions.py (现有 CLI 集成测试)
  </read_first>
  <action>
    在 evolution/tools/evolve_tool_descriptions.py 做四处增量修改。**不**重写整个文件——只插入这些代码块。

    改动 A — imports（顶部 import 区）：在现有 `from evolution.tools.tool_constraints import ToolFactualChecker` 之后追加：
    `from evolution.tools.session_miner import _load_jsonl_skip_bad, _normalize_task_hash`

    改动 B — Click flag（在 `@click.option("--api-base", ...)` 之后插入）：
    `@click.option("--session-source", default=None, type=click.Path(exists=True, file_okay=False, dir_okay=True), help="Directory with train/val/holdout.jsonl from mine_tool_sessions; union with synthetic dataset (session-side wins on hash collision)")`

    改动 C — main 形参 + evolve 转发：在 `def main(iterations, eval_source, hermes_repo, dry_run, model, api_base):` 形参末尾追加 `session_source`；在 `evolve(...)` 调用中传 `session_source=session_source`。同步在 `def evolve(...)` 函数签名末尾加默认参数 `session_source: Optional[str] = None`。

    改动 D — evolve() 函数内 union 分支（**B1 修订**：必须把 union 提到一个模块级 helper `_union_session_into_dataset`，并在 inline `dataset = ToolSelectionDataset.load(dataset_path)` / 合成路径之后、`dataset.to_dspy_examples(...)` 之前调用它。这样 `dataset` 局部对象**已经存在**，避免 NameError；同时 helper 也被 Plan 06 Task 6.3 的两个集成测试直接调用）：

    1) **加 helper（模块级）—— 同一文件，与 evolve() 平级**：

    ```python
    def _union_session_into_dataset(dataset, session_source) -> None:
        """In-place union — synth first, session overrides on hash collision (D-14).

        B1 contract: caller must pass an in-memory ToolSelectionDataset object
        (NOT a path). Caller is responsible for invoking this AFTER
        `dataset = ToolSelectionDataset.load(...)` / synthesis and BEFORE
        `dataset.to_dspy_examples(...)`. Mutates dataset.train/val/holdout
        in-place. Does NOT re-apply train multiplier (Pitfall 5).
        """
        from pathlib import Path as _SessPath
        sess_dir = _SessPath(session_source)
        console.print(f"[bold cyan]Phase 14 (D-09 / D-14):[/] union session dataset from {sess_dir}")
        for split_name in ("train", "val", "holdout"):
            sess_rows, skipped = _load_jsonl_skip_bad(sess_dir / f"{split_name}.jsonl")
            if skipped:
                console.print(f"  {split_name}.jsonl skipped {skipped} bad lines")
            session_examples = [
                ToolSelectionExample.from_dict(r) for r in sess_rows
            ]
            # Pitfall 10 ordering: synth first, session overrides on hash collision.
            # Pitfall 5: NO re-duplication — mine_tool_sessions has pre-duplicated train.
            by_hash: dict = {}
            for ex in getattr(dataset, split_name):
                by_hash[_normalize_task_hash(ex.task_description)] = ex
            for ex in session_examples:
                by_hash[_normalize_task_hash(ex.task_description)] = ex
            setattr(dataset, split_name, list(by_hash.values()))
        console.print(
            f"  After union: train={len(dataset.train)} val={len(dataset.val)} holdout={len(dataset.holdout)}"
        )
    ```

    2) **在 evolve() 内 inline load 之后 + dspy_examples 转换之前调用 helper**。evolve_tool_descriptions.py 当前在 lines 146-167 inline 拥有 `dataset = ToolSelectionDataset.load(dataset_path)` 与后续 `dataset.to_dspy_examples("train"/"val")` 之间：在 `console.print(f"  Split: {len(dataset.train)} train ...")` 之后、`# ── 6. GEPA optimization ───` 之前插入：

    ```python
    if session_source:
        _union_session_into_dataset(dataset, session_source)
    ```

    这样 `dataset` 对象明确存在，B1 NameError 路径不可达。后续 `dataset.to_dspy_examples("train")` 与 `dataset.to_dspy_examples("val")` 直接读 union 后的 train/val。

    改动 E — metrics.json 字段（lines 365-378 的 metrics dict）：在 `"constraints_passed": True,` 之后追加：
    `"session_source": str(session_source) if session_source else None,`

    不动：
    - 现有 GEPA / fitness / metric 调用链
    - dry-run 早期返回逻辑
    - 任何 ToolSelectionDataset.save / load 路径
    - 任何 ToolSelectionExample 字段（已在 Plan 02 处理）

    禁止：新增依赖；改 ToolSelectionDataset / ToolSelectionExample。
  </action>
  <verify>
    <automated>.venv/bin/python -m evolution.tools.evolve_tool_descriptions --help 2>&1 | grep -q -- "--session-source"</automated>
  </verify>
  <acceptance_criteria>
    - --session-source flag 存在: `.venv/bin/python -m evolution.tools.evolve_tool_descriptions --help 2>&1 | grep -q -- '--session-source'`
    - import _load_jsonl_skip_bad: `grep -q "from evolution.tools.session_miner import" evolution/tools/evolve_tool_descriptions.py`
    - B1 — _union_session_into_dataset helper 定义存在: `grep -q "def _union_session_into_dataset" evolution/tools/evolve_tool_descriptions.py`
    - B1 — _union_session_into_dataset 被调用（dataset 已存在的位置）: `grep -q "_union_session_into_dataset(dataset" evolution/tools/evolve_tool_descriptions.py`
    - B1 — `dataset = ToolSelectionDataset.load` anchor 存在（确保 union 时 dataset 已存在）: `grep -q "dataset = ToolSelectionDataset.load" evolution/tools/evolve_tool_descriptions.py`
    - union branch 标识存在: `grep -q "Phase 14 (D-09 / D-14)" evolution/tools/evolve_tool_descriptions.py`
    - metrics.json 含 session_source: `grep -q '"session_source":' evolution/tools/evolve_tool_descriptions.py`
    - 现有 evolve_tool_descriptions 测试不回归: `.venv/bin/pytest tests/tools/test_evolve_tool_descriptions.py -q --tb=no 2>&1 | tail -3 | grep -E 'passed' | grep -v 'failed'`
    - 没有新依赖: `! git diff pyproject.toml 2>/dev/null | grep -q '^\\+\\s*"'`
    - 没有 hermes-agent 写回: 现有文件本来就没有 write_back，本 task 不引入；`! grep -E 'write_back' evolution/tools/evolve_tool_descriptions.py | grep -v "^#"`
  </acceptance_criteria>
  <done>--session-source flag 加进 evolve_tool_descriptions；不传时行为不变；现有测试通过。</done>
</task>

<task type="auto">
  <name>Task 6.2: evolve_tool_params 加 --session-source flag + union load 分支</name>
  <files>evolution/tools/evolve_tool_params.py</files>
  <read_first>
    - evolution/tools/evolve_tool_params.py (lines 1-100, 505-620, 820-870 — current imports + Click 命令 + metrics.json 字段)
    - .planning/phases/14-sessiondb-mining-for-tools/14-CONTEXT.md (D-09 + D-14)
    - .planning/phases/14-sessiondb-mining-for-tools/14-PATTERNS.md (lines 581-621)
    - tests/tools/test_evolve_tool_params_cli.py (现有 CLI 集成测试)
  </read_first>
  <action>
    在 evolution/tools/evolve_tool_params.py 做四处增量修改。Phase 13 文件较大（1000+ 行），**只**做以下修改，不动其他逻辑。

    改动 A — imports：在现有 `from evolution.tools.v1_baseline_gate import ...` （或最近的 evolution.tools 内部 import）之后追加：
    `from evolution.tools.session_miner import _load_jsonl_skip_bad, _normalize_task_hash`

    改动 B — Click flag：在现有 `@click.option("--auto", ...)` decorator 块**之后**、`def evolve(...)` 之前追加：
    `@click.option("--session-source", default=None, type=click.Path(exists=True, file_okay=False, dir_okay=True), help="Directory with train/val/holdout.jsonl from mine_tool_sessions; union with synthetic dataset (session-side wins on hash collision)")`

    改动 C — evolve 形参 + _evolve_impl 转发：
    - 在 `def evolve(iterations, ..., auto)` 形参末尾追加 `session_source: Optional[str] = None`
    - 在 evolve 内调 `_evolve_impl(...)` 处传 `session_source=session_source`
    - 在 `_evolve_impl(...)` 形参末尾同样加 `session_source: Optional[str] = None`

    改动 D — **B1 关键修订**：

    `evolve_tool_params.py::_evolve_impl` 在 line 705 是 `trainset, valset, holdout = _load_dataset(eval_source, config, all_tools)`——**没有 dataset 局部变量**！原计划在 _evolve_impl() 内 `getattr(dataset, split_name)` 会立即 NameError。

    修正：把 union 操作移到 **_load_dataset 内部**（dataset 对象在 lines 252/260 已存在）。

    1) **加模块级 helper（与 evolve() / _evolve_impl 平级）**——与 Plan 06 Task 6.1 同名同语义的 `_union_session_into_dataset(dataset, session_source) -> None`（in-place 修改 ToolSelectionDataset）。语义、签名、helper body 与 Task 6.1 完全一致；两文件各自定义一份（避免跨 CLI 模块依赖；W5 验证 helper 内部不调 _multiplier_for / DEFAULT_MULTIPLIER —— 同一约束适用两份拷贝）。

    2) **修改 _load_dataset 签名**——增加 `session_source: Optional[str] = None` 参数（**B1 thread**）：
    ```python
    def _load_dataset(
        eval_source: str,
        config: EvolutionConfig,
        all_tools: list[ToolDescription],
        session_source: Optional[str] = None,
    ) -> tuple[list[dspy.Example], list[dspy.Example], list[dspy.Example]]:
        ...
        # 现有 dataset = ToolSelectionDataset.load(dataset_path) 或 builder.generate(...)
        # 之后立即（且在 to_dspy_examples 之前）：
        if session_source:
            _union_session_into_dataset(dataset, session_source)
        return (
            dataset.to_dspy_examples("train"),
            dataset.to_dspy_examples("val"),
            dataset.to_dspy_examples("holdout"),
        )
    ```

    3) **_evolve_impl 处 thread session_source 参数**：把 line 705 改为
    ```python
    trainset, valset, holdout = _load_dataset(eval_source, config, all_tools, session_source=session_source)
    ```
    并在 `def evolve(...)` 与 `def _evolve_impl(...)` 形参末尾加 `session_source: Optional[str] = None`；evolve() 调 `_evolve_impl(...)` 时传 `session_source=session_source`。

    4) **保证不会再有 NameError**：union 现在在 `dataset` 局部明确存在的位置发生（_load_dataset 内）。**禁止**在 _evolve_impl 函数体内引用 `dataset` 变量；它从未存在于 _evolve_impl scope。

    改动 E — metrics.json：在 _evolve_impl() 写 metrics.json（line 830 附近 `metrics: dict[str, Any] = { ... }`）的 dict 末尾追加：
    `"session_source": str(session_source) if session_source else None,`

    不动：
    - GEPA / MIPROv2 fallback / cost_tracker / V1BaselineGate / persist_per_tool_rates 任何 Phase 13 路径
    - FAILED_/ABORTED_ 输出目录约定
    - 任何 evolve 暴露的 __all__ 项

    禁止：新增依赖；改 ToolSelectionDataset / ToolSelectionExample / ToolModule。
  </action>
  <verify>
    <automated>.venv/bin/python -m evolution.tools.evolve_tool_params --help 2>&1 | grep -q -- "--session-source"</automated>
  </verify>
  <acceptance_criteria>
    - --session-source flag 存在: `.venv/bin/python -m evolution.tools.evolve_tool_params --help 2>&1 | grep -q -- '--session-source'`
    - import _load_jsonl_skip_bad: `grep -q "from evolution.tools.session_miner import" evolution/tools/evolve_tool_params.py`
    - B1 — _union_session_into_dataset helper 定义存在: `grep -q "def _union_session_into_dataset" evolution/tools/evolve_tool_params.py`
    - B1 — _union_session_into_dataset 在 _load_dataset 内被调用: `grep -q "_union_session_into_dataset(dataset" evolution/tools/evolve_tool_params.py`
    - B1 — `dataset = ToolSelectionDataset.load` anchor 仍在 _load_dataset 内（确保 union 时 dataset 已存在）: `grep -q "dataset = ToolSelectionDataset.load" evolution/tools/evolve_tool_params.py`
    - B1 — _load_dataset 签名 thread session_source: `grep -E "def _load_dataset\\(" evolution/tools/evolve_tool_params.py | grep -q "session_source"`
    - B1-regression fix — _evolve_impl body 不直接 getattr(dataset, ...) （scoped 检查，避免 helper body 内合法 getattr 触发误报；与 W5 同款 awk 模式）: `! awk '/^def _evolve_impl\\(/,/^def [a-zA-Z_]+\\(/' evolution/tools/evolve_tool_params.py | grep -qE 'getattr\\(dataset, '`
    - union branch 标识存在: `grep -q "Phase 14 (D-09 / D-14)" evolution/tools/evolve_tool_params.py`
    - metrics.json 含 session_source: `grep -c '"session_source":' evolution/tools/evolve_tool_params.py | xargs test 1 -le`
    - 现有 evolve_tool_params 测试不回归: `.venv/bin/pytest tests/tools/test_evolve_tool_params_cli.py tests/tools/test_v1_baseline_gate.py tests/tools/test_constraint_failure_records.py -q --tb=no 2>&1 | tail -3 | grep -E 'passed' | grep -v 'failed'`
    - Phase 13 hard scope guard 不破: `grep -c 'write_back' evolution/tools/evolve_tool_params.py | xargs test 0 -eq` 即 0 个
    - 没有新依赖: `! git diff pyproject.toml 2>/dev/null | grep -q '^\\+\\s*"'`
  </acceptance_criteria>
  <done>--session-source flag 加进 evolve_tool_params；Phase 13 测试不回归；hard scope guard 不破。</done>
</task>

<task type="auto">
  <name>Task 6.3: 解 stub - test_session_overrides_synth + test_no_double_duplication 集成测试</name>
  <files>tests/tools/test_evolve_with_session_source.py</files>
  <read_first>
    - tests/tools/test_evolve_with_session_source.py (Plan 01 stubs)
    - evolution/tools/evolve_tool_descriptions.py (Task 6.1 后)
    - evolution/tools/evolve_tool_params.py (Task 6.2 后)
    - .planning/phases/14-sessiondb-mining-for-tools/14-VALIDATION.md (rows 27-28)
  </read_first>
  <action>
    在 tests/tools/test_evolve_with_session_source.py 替换 2 个 stub。本测试不实际跑 GEPA（cost prohibitive），只验证 union 行为：直接 import evolve_tool_descriptions / evolve_tool_params 中的 union 逻辑或调 evolve 函数 + dry-run + 检查中间 dataset 状态。

    最简单做法是测试 union helper 行为而非完整 evolve 流程——但 D-09/D-14 的 union 分支嵌入在 evolve() / _evolve_impl() 函数内部。可行的两个策略：

    Strategy 1（推荐）: 把 union 逻辑提取到模块级 helper `_union_session_into_dataset(dataset, session_source: Path)` 然后在 evolve() 中调它；测试直接调 helper（适合 Plan 06 Task 6.1/6.2 顺便重构）。

    Strategy 2（最小改动）: 用 click.testing.CliRunner 跑 evolve_tool_descriptions --dry-run --session-source <tmp> --eval-source synthetic 并 mock LLM；assert 输出中 "union" 字样 + 数据集大小。

    选 Strategy 1 — 更可测试。在 Task 6.1/6.2 已经写完后，回头把 inline union 逻辑提取到 evolve_tool_descriptions.py 的模块级 helper（同函数主体不变）：

    ```python
    def _union_session_into_dataset(dataset, session_source) -> None:
        """In-place union — synth first, session overrides on hash collision (D-14)."""
        from pathlib import Path as _SessPath
        from evolution.tools.session_miner import _load_jsonl_skip_bad, _normalize_task_hash
        sess_dir = _SessPath(session_source)
        for split_name in ("train", "val", "holdout"):
            sess_rows, skipped = _load_jsonl_skip_bad(sess_dir / f"{split_name}.jsonl")
            if skipped:
                console.print(f"  {split_name}.jsonl skipped {skipped} bad lines")
            session_examples = [ToolSelectionExample.from_dict(r) for r in sess_rows]
            by_hash: dict = {}
            for ex in getattr(dataset, split_name):
                by_hash[_normalize_task_hash(ex.task_description)] = ex
            for ex in session_examples:
                by_hash[_normalize_task_hash(ex.task_description)] = ex
            setattr(dataset, split_name, list(by_hash.values()))
    ```
    并在 evolve() / _evolve_impl() 中替换 inline 块为 `_union_session_into_dataset(dataset, session_source)`（如 Task 6.1/6.2 已用 inline 形式，本 task 把它提到模块级即可）。

    在 evolve_tool_params.py 同理提取 `_union_session_into_dataset` 模块级 helper（或 import from evolve_tool_descriptions —— 但避免跨模块依赖，每个 CLI 文件各自定义；语义相同）。

    然后写 2 个真实测试：

    ```python
    def test_session_overrides_synth(tmp_path):
        """同 hash 时 session-side example 替换 synthetic 例子（D-14）。"""
        from evolution.tools.tool_dataset import ToolSelectionDataset, ToolSelectionExample
        from evolution.tools.evolve_tool_descriptions import _union_session_into_dataset

        # 1) Build a synthetic dataset with one task in train
        synth = ToolSelectionDataset(
            train=[ToolSelectionExample(
                task_description="list files",
                correct_tool="terminal",
                source="synthetic",
            )],
            val=[],
            holdout=[],
        )

        # 2) Write a session JSONL with same task but different correct_tool + signal
        sess_dir = tmp_path / "session"
        sess_dir.mkdir()
        for split in ("train", "val", "holdout"):
            (sess_dir / f"{split}.jsonl").write_text("")
        sess_ex = ToolSelectionExample(
            task_description="list files",
            correct_tool="search_files",
            source="session",
            misselection_signals=["error_retry"],
        )
        (sess_dir / "train.jsonl").write_text(json.dumps(sess_ex.to_dict()) + "\n")

        # 3) Union and verify
        _union_session_into_dataset(synth, sess_dir)
        assert len(synth.train) == 1, "Same hash should collapse to 1 example"
        assert synth.train[0].correct_tool == "search_files", "Session-side should win on collision"
        assert synth.train[0].misselection_signals == ["error_retry"]

    def test_no_double_duplication(tmp_path):
        """Train.jsonl 已预复制 → evolve --session-source 不再复制（Pitfall 5）。

        W5 修订：双管验证 ——
            (a) 行为代理：3 行同 hash session JSONL → union 后 train 仅 1 行（by_hash dedup）。
            (b) 直接 source-level：union helper body **不**调用 `_multiplier_for`
                / `DEFAULT_MULTIPLIER`，确保它确实没有把 train 再复制一遍。
        """
        import re
        import inspect
        from evolution.tools.tool_dataset import ToolSelectionDataset, ToolSelectionExample
        from evolution.tools.evolve_tool_descriptions import _union_session_into_dataset

        # ── (a) 行为代理：dict dedup 后 1 行 ────────────────────────────
        synth = ToolSelectionDataset(train=[], val=[], holdout=[])

        # mine_tool_sessions has already written 3 copies of the same example to train.jsonl
        sess_dir = tmp_path / "session"
        sess_dir.mkdir()
        same_ex = ToolSelectionExample(
            task_description="list files",
            correct_tool="search_files",
            source="session",
            misselection_signals=["error_retry"],
        )
        (sess_dir / "train.jsonl").write_text(
            (json.dumps(same_ex.to_dict()) + "\n") * 3
        )
        for split in ("val", "holdout"):
            (sess_dir / f"{split}.jsonl").write_text("")

        _union_session_into_dataset(synth, sess_dir)
        # by_hash dedup → 1 unique entry. evolve CLI does NOT add multiplier on top.
        assert len(synth.train) == 1, f"Expected 1 (deduped), got {len(synth.train)}"

        # ── (b) Source-level：helper body 不引用 _multiplier_for ────────
        helper_src = inspect.getsource(_union_session_into_dataset)
        assert "_multiplier_for" not in helper_src, \
            "W5: union helper must NOT call _multiplier_for (Pitfall 5)"
        assert "DEFAULT_MULTIPLIER" not in helper_src, \
            "W5: union helper must NOT reference DEFAULT_MULTIPLIER (Pitfall 5)"
    ```

    注意 `import json` 顶部加上。

    若 Task 6.1/6.2 还没把 union 逻辑提取到 _union_session_into_dataset 函数，本 task 需要先做这个小重构（在 evolve_tool_descriptions.py + evolve_tool_params.py 各加 def _union_session_into_dataset 函数；inline 块替换为 call）。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_evolve_with_session_source.py -v --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - _union_session_into_dataset helper 在 evolve_tool_descriptions.py 模块级: `grep -q "^def _union_session_into_dataset" evolution/tools/evolve_tool_descriptions.py`
    - 同 helper 在 evolve_tool_params.py 模块级（或 imported from descriptions）: `grep -qE "(^def _union_session_into_dataset|from evolution.tools.evolve_tool_descriptions import _union_session_into_dataset)" evolution/tools/evolve_tool_params.py`
    - 两个测试 GREEN: `.venv/bin/pytest tests/tools/test_evolve_with_session_source.py::test_session_overrides_synth tests/tools/test_evolve_with_session_source.py::test_no_double_duplication -q 2>&1 | grep -q '2 passed'`
    - test_session_overrides_synth 含 session 优先断言: `grep -q 'correct_tool == "search_files"' tests/tools/test_evolve_with_session_source.py`
    - test_no_double_duplication 含 dedup 断言: `grep -q "len(synth.train) == 1" tests/tools/test_evolve_with_session_source.py`
    - W5 — test_no_double_duplication 含 helper-body source 断言: `grep -q "_multiplier_for" tests/tools/test_evolve_with_session_source.py && grep -q "DEFAULT_MULTIPLIER" tests/tools/test_evolve_with_session_source.py && grep -q "inspect.getsource" tests/tools/test_evolve_with_session_source.py`
    - W5 — 两 CLI 文件中的 _union_session_into_dataset helper body 不引用 _multiplier_for / DEFAULT_MULTIPLIER（grep guard，与 inspect-based test 双保险）: `for f in evolution/tools/evolve_tool_descriptions.py evolution/tools/evolve_tool_params.py; do awk '/^def _union_session_into_dataset/,/^def [a-zA-Z_]+\\(/' "$f" | grep -E "_multiplier_for|DEFAULT_MULTIPLIER" && { echo "W5 fail in $f"; exit 1; } || true; done`
    - 整套 tests/ 不回归: `! .venv/bin/pytest tests/ -q --tb=no 2>&1 | grep -qE '[0-9]+ failed|[0-9]+ error'`
  </acceptance_criteria>
  <done>2 个 integration 测试 GREEN；union helper 模块级可复用；test_no_double_duplication 双管验证 dedup（行为代理 + helper-body source 检查 W5）；全 tests/ 套件 GREEN。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.4: Manual checkpoint - 44-session real-data dry-run + entropy threshold calibration</name>
  <files>(no source change — manual verification + entropy threshold calibration)</files>
  <action>
    全部 Phase 14 自动化已落地（fixtures + 测试 + ToolSelectionExample 字段 + secret patterns v2 + SessionToolMiner + mine_tool_sessions CLI + evolve --session-source flag）。本 task 由人工执行三组验证（A/B/C），把结果写入 14-06-SUMMARY.md。

    **A. 44-session 真数据 dry-run smoke**（CONTEXT specifics + 14-VALIDATION.md §Manual-Only 行 1）:

    ```bash
    .venv/bin/python -m evolution.tools.mine_tool_sessions \
        --i-have-consent --dry-run \
        --sessions-dir ~/.hermes/sessions \
        --signals error_retry,user_correction,oracle_disagreement \
        --output /tmp/p14-dry-run-44
    ```

    若有 baseline-module（Phase 5 或 Phase 13 之前的 output）追加 `--baseline-module output/tools/<latest-success-dir>`。

    检查清单：
    1. CLI 退出码 == 0
    2. /tmp/p14-dry-run-44/metrics.json 存在
    3. metrics.json 含全部 13 keys（参见 Plan 05 test_metrics_schema 集合）
    4. 至少 error_retry 与 user_correction 信号下 candidate ≥ 1（44 sessions 数据下；如全部 = 0 说明 extractor 解析有问题，回到 Plan 04）
    5. **OPENAI / OPENROUTER billing 仪表盘**确认 dry-run 期间 0 调用（dry-run 应不调 LLM）
    6. judge_calls == 0
    7. 工具名分布合理（terminal / search_files / read_file 应在 candidates 中）
    8. surface_drift_dropped: 若全为零，说明所有 session 工具与 current hermes 完全一致；若 > 总 candidate 50%，说明 hermes-agent 工具表面变化大，需检查 baseline-module 配置

    把 metrics.json 全文与 console summary table 复制到 14-06-SUMMARY.md。

    **B. Entropy threshold calibration**（14-VALIDATION.md §Manual-Only 行 2 + RESEARCH §A2）:

    1. 从上一步 metrics.json 读 secret_filter_skipped / 总 candidate 比例：
       - 比例 ≤ 5% → 阈值 4.0 OK，无须改动
       - 比例 > 5% → 把 evolution/core/external_importers.py 中 _SECRET_ENTROPY_THRESHOLD = 4.0 改为 4.3，重跑 dry-run；若仍 > 5% 试 4.5；把最终选定值与理由写入 14-06-SUMMARY.md
    2. 人工 audit secret_filter_skipped 的若干样本（在 Plan 05 _print_summary_table 后加一行打印命中样例的 console 输出，或临时手动 grep miner_log.jsonl）：判断是否真的是 secret 还是误中。误中率 > 20% 即必须调阈值。
    3. 若调阈值，重跑 `.venv/bin/pytest tests/tools/test_secret_patterns_v2.py -v` 确认 v1 不回归 + low_entropy_negatives 仍 PASS；第二次 dry-run 验证 secret_filter_skipped 比例落到 ≤ 5%。

    **C. Cost sanity check**（14-VALIDATION.md §Manual-Only 行 3）:

    若有意愿跑 wet run（限 5 sessions）:
    ```bash
    .venv/bin/python -m evolution.tools.mine_tool_sessions \
        --i-have-consent \
        --sessions-dir ~/.hermes/sessions \
        --judge-model openai/gpt-4.1-mini \
        --limit 5 \
        --output /tmp/p14-wet-5
    ```
    确认 OpenAI 账单增量 ≤ $0.20。Extrapolate 44 sessions / gpt-4.1 成本 ~$10。

    把 A/B/C 结果（成功状态 / 阈值最终值 / 实际 cost）写到 .planning/phases/14-sessiondb-mining-for-tools/14-06-SUMMARY.md，然后回复 resume-signal。
  </action>
  <verify>
    <automated>test -f /tmp/p14-dry-run-44/metrics.json && .venv/bin/python -c "import json; m=json.loads(open('/tmp/p14-dry-run-44/metrics.json').read()); needed={'total_candidates_by_signal','judge_confirmed_by_signal','judge_false_positives_by_signal','surface_drift_dropped','surface_drift_tools','secret_filter_skipped','jsonl_skipped_lines','judge_calls','judge_calls_by_signal','cost_usd_spent'}; missing=needed-set(m.keys()); assert not missing, missing"</automated>
  </verify>
  <resume-signal>
    回复 "approved" + 抄一份 metrics.json 关键字段（total_candidates_by_signal, judge_confirmed_by_signal, secret_filter_skipped, surface_drift_dropped）；或描述 issue 并提议 fix。若需改 entropy threshold，告知最终采用的数值。
  </resume-signal>
  <done>Manual checklist A/B/C 全部完成；metrics.json 13 keys 完整；entropy threshold 最终值确定；14-06-SUMMARY.md 含人工签字。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.hermes/sessions/*.json → evolve CLI（通过 mine_tool_sessions 中转产物） | session 数据已经过 mine_tool_sessions 的 secret/surface drift 过滤 + judge gate；evolve --session-source 信任此输入（D-16 末："信任已审过的 JSONL 输出"） |
| session JSONL → ToolSelectionExample.from_dict | 本身只解析，不调 LLM，不写文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-14-03** (consumer of Plan 04 helper) | DoS | evolve_tool_descriptions.py / evolve_tool_params.py 在 union 路径调 _load_jsonl_skip_bad | mitigate (uses Plan 04) | 走 Plan 04 _load_jsonl_skip_bad helper —— 行级 try/except，5% 阈值 console warn。**不**改 ToolSelectionDataset.load。 |
| **T-14-02** (consumer) | Information disclosure | evolve --session-source 不重新 secret-filter（D-16 末：信任已审过的输出） | accept (per D-16) | 不重新做 secret filter；mining 阶段已经过了 Layer 1。若用户手动喂入未经 mine_tool_sessions 处理的 JSONL，secret 可能漏出 — 这是 known limit, 通过 PR review + .gitignore datasets/ 收口。 |
| Manual checkpoint 跳过 | Repudiation | Task 6.4 是 blocking checkpoint | mitigate | 缺 manual sign-off → execute-phase 不会进 Phase 14 完成态；resume-signal 必须包含 metrics 字段抄录或 fix 描述。 |

无新增 threat。Plan 06 是 wiring + manual verification；T-14-01/02/03/04 全部由前 plan 落地。
</threat_model>

<verification>
- evolve_tool_descriptions.py 与 evolve_tool_params.py 各加 --session-source flag
- _union_session_into_dataset helper 在两个 CLI 模块级可调用
- 27 + 28 row 的 2 个 integration 测试 GREEN
- 不传 --session-source 时既有测试不回归
- pyproject.toml / ToolSelectionDataset / EvalDataset 无改动
- Manual checkpoint Task 6.4 由人工签字
- 整套 tests/ GREEN（无 failed/error）
</verification>

<success_criteria>
- 28/28 测试 row 全部映射到代码路径并 GREEN
- D-09 + D-14 union 顺序铁律 (synth first, session overrides) 单元测试覆盖
- Pitfall 5 不二次复制经 test_no_double_duplication 验证
- Phase 5 / Phase 13 既有 CLI 测试不回归
- Manual dry-run smoke 在真 44-session 数据上得到非空 candidate 分布
- entropy threshold 经过 calibration（4.0 OK 或调到 4.3+）
</success_criteria>

<output>
After completion, create `.planning/phases/14-sessiondb-mining-for-tools/14-06-SUMMARY.md` listing flag wiring + helper extraction + manual verification results (metrics.json key fields抄录, entropy threshold final value, cost sanity check actual numbers).
</output>
</output>
