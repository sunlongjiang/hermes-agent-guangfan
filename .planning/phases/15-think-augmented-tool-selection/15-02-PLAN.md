---
phase: 15
plan: 02
type: execute
wave: 1
depends_on:
  - "01"
files_modified:
  - evolution/tools/tool_module.py
  - tests/tools/test_tool_module.py
autonomous: true
requirements:
  - TOOL-V2-03
must_haves:
  truths:
    - "ToolModule(enable_reasoning=False) 行为与 Phase 13 完全等价(默认值向后兼容)"
    - "ToolModule(enable_reasoning=True) 构造 self.reasoner: dspy.Predict(ToolReasoningSignature),其 LM max_tokens=200(D-04 双保险其一)"
    - "ToolReasoningSignature.docstring 显式声明 'Be concise (≤200 tokens). Do NOT pre-select a tool.'(D-04 双保险其二 + D-02 拒绝 selector 橡皮图章)"
    - "forward() 在 enable_reasoning=True 时先调 reasoner,把 reasoning 文本作为 selector 的 reasoning InputField 输入(默认 '')"
    - "GEPA 通过 named_predictors() 可触达 reasoner.signature.instructions(TOOL-V2-03 SC-2)"
    - "enable_reasoning=True 时 forward 返回的 dspy.Prediction 含 reasoning 与 reasoning_tokens(token=len(reasoning)/4)字段"
  artifacts:
    - path: "evolution/tools/tool_module.py"
      provides: "新 ToolReasoningSignature 类 + ToolModule.__init__ 接受 enable_reasoning kwarg + forward 双路径 + ToolSelectionWithParamsSignature 新增 reasoning InputField"
      contains: "class ToolReasoningSignature"
    - path: "tests/tools/test_tool_module.py"
      provides: "TestEnableReasoning 测试类:7 个测试,覆盖构造分支 / forward 双路径 / max_tokens=200 / named_predictors / 向后兼容默认值"
      min_lines: 100
  key_links:
    - from: "evolution/tools/tool_module.py forward()"
      to: "self.reasoner 与 self.selector"
      via: "if self.reasoner is not None: reasoning = self.reasoner(...); selector(..., reasoning=reasoning)"
      pattern: "self.reasoner\\(task_description=.*available_tools="
    - from: "ToolReasoningSignature"
      to: "GEPA named_predictors"
      via: "dspy.Predict(ToolReasoningSignature) 注册成 self.reasoner -> named_predictors 包含 'reasoner'"
      pattern: "dspy\\.Predict\\(ToolReasoningSignature\\)"
    - from: "ToolSelectionWithParamsSignature"
      to: "selector 接收 reasoning InputField"
      via: "reasoning: str = dspy.InputField(default=..., desc=...)"
      pattern: "reasoning.*=.*dspy\\.InputField"
---

<objective>
为 Phase 15 实现 ToolModule 改造的核心:把可选 reasoning 前置 Predict 引入,使 GEPA 能通过 `named_predictors()` 触达 reasoner.signature.instructions 进行优化;forward 在 think-on 路径上先调 reasoner,产物作为 selector 的额外 InputField。同时扩展 ToolSelectionWithParamsSignature 使其向后兼容 `reasoning=""` 默认值(D-05 静态分叉、D-07 构造后不可变、D-06 think-off 与 Phase 13 行为完全等价)。

Purpose:
- 锁定 TOOL-V2-03 SC-1(enable_reasoning 构造)+ SC-2(reasoner.instructions 暴露给 GEPA)的实现合约
- D-04 200-token cap 双保险:LM-level max_tokens=200 + signature docstring 显式提示
- D-02 拒绝 reasoner 越权选 tool:signature 只输出 reasoning 文本,selector 仍接收完整 tools 列表

Output:
- 修改后的 `evolution/tools/tool_module.py`
- 扩展后的 `tests/tools/test_tool_module.py` 含 TestEnableReasoning 测试类
- Plan 01 的 conftest.mock_reasoning_module fixture 转 GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-think-augmented-tool-selection/15-CONTEXT.md
@.planning/phases/15-think-augmented-tool-selection/15-RESEARCH.md
@.planning/phases/15-think-augmented-tool-selection/15-PATTERNS.md
@.planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md
@.planning/phases/15-think-augmented-tool-selection/15-01-PLAN.md
@evolution/tools/tool_module.py
</context>

<interfaces>
<!-- 当前 tool_module.py 的全部 API 与 hot spots — 改造点必须严格沿用模式 -->

From `evolution/tools/tool_module.py:21-43`:
```python
class ToolSelectionWithParamsSignature(dspy.Signature):
    """Given a task and available tools, pick the tool AND infer its params.
    ...
    """
    task_description: str = dspy.InputField(desc="The task that needs to be accomplished")
    available_tools: str = dspy.InputField(desc="Formatted list of available tools with their descriptions and parameters")
    selected_tool: str = dspy.OutputField(desc="The exact name of the most appropriate tool for this task")
    selected_params: str = dspy.OutputField(desc="JSON object mapping param_name to value, ...")
```

From `evolution/tools/tool_module.py:104-127`:
```python
def __init__(self, tool_descriptions: list[ToolDescription]):
    super().__init__()
    self.tools: dict[str, _ToolParamBundle] = {}
    self._frozen_tool_desc: dict[str, str] = {}
    self._frozen_tools: dict[str, ToolDescription] = {}
    self._tool_names: list[str] = []
    for td in tool_descriptions:
        safe_name = self._safe_key(td.name)
        self.tools[safe_name] = _ToolParamBundle(td.name, list(td.params))
        self._frozen_tool_desc[td.name] = (td.description or f"Tool: {td.name}")
        self._frozen_tools[td.name] = td
        self._tool_names.append(td.name)
    # Selector: ChainOfThought with upgraded signature (D-05).
    self.selector = dspy.ChainOfThought(ToolSelectionWithParamsSignature)
```

From `evolution/tools/tool_module.py:162-184`:
```python
def forward(self, task_description: str) -> dspy.Prediction:
    available_tools = self._format_available_tools()
    result = self.selector(
        task_description=task_description,
        available_tools=available_tools,
    )
    selected_params = getattr(result, "selected_params", "") or "{}"
    return dspy.Prediction(
        selected_tool=result.selected_tool,
        selected_params=selected_params,
    )
```

From `evolution/core/config.py:42-59`(EvolutionConfig):
```python
@dataclass
class EvolutionConfig:
    hermes_agent_path: Path
    eval_model: str = "openai/gpt-4.1-mini"
    optimizer_model: str = "openai/gpt-4.1"
    judge_model: str = "openai/gpt-4.1"
    reflection_model: Optional[str] = None
    max_param_desc_size: int = 200
    max_tool_desc_size: int = 500
    max_cost_usd: Optional[float] = None

    def get_lm_kwargs(self) -> dict[str, Any]:
        # 返回 {"max_tokens": ..., 其他 LM 配置}
```

DSPy Predict.set_lm() API(RESEARCH §1.3 Path C):
```python
predict.set_lm(dspy.LM(model_name, max_tokens=200, ...))
predict.get_lm()  # 返回已 attach 的 LM 实例
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 evolution/tools/tool_module.py — 新增 ToolReasoningSignature + reasoning InputField + enable_reasoning ctor + forward 双路径</name>
  <files>evolution/tools/tool_module.py</files>
  <read_first>
    - evolution/tools/tool_module.py(全文 — 232 行,改造在 Signature 段 + __init__ + forward)
    - .planning/phases/15-think-augmented-tool-selection/15-RESEARCH.md §1.1 + §1.3 + §1.4 + §2(file:line 表)
    - .planning/phases/15-think-augmented-tool-selection/15-PATTERNS.md §1(构造期 + forward + Signature 三段 pattern excerpt)
    - .planning/phases/15-think-augmented-tool-selection/15-CONTEXT.md(D-01..D-07 / D-17)
    - evolution/core/config.py(EvolutionConfig.get_lm_kwargs)
  </read_first>
  <behavior>
    - ToolReasoningSignature(dspy.Signature) 类存在,docstring 内含 "Be concise (≤200 tokens)" 与 "Do NOT pre-select a tool"(D-02 / D-04 双保险);字段:task_description, available_tools → reasoning
    - ToolSelectionWithParamsSignature 新增 `reasoning: str = dspy.InputField(default="", desc=...)` 字段(向后兼容,RESEARCH §9 选项 A)
    - ToolModule.__init__ 新签名 `__init__(self, tool_descriptions, *, enable_reasoning: bool = False, eval_model: str = "openai/gpt-4.1-mini", lm_kwargs: Optional[dict] = None)`(D-05/D-07)
    - enable_reasoning=False(默认)→ self.reasoner = None;Phase 13 行为完全等价
    - enable_reasoning=True → 构造 reasoning_lm: `dspy.LM(eval_model, max_tokens=200, **{k: v for k, v in (lm_kwargs or {}).items() if k != "max_tokens"})`;self.reasoner = dspy.Predict(ToolReasoningSignature);self.reasoner.set_lm(reasoning_lm)
    - forward(task_description) 在 self.reasoner is None 时与原 Phase 13 完全一致;reasoning_text 设为 ""
    - forward 在 self.reasoner is not None 时:reasoning_pred = self.reasoner(task_description=..., available_tools=...);reasoning_text = reasoning_pred.reasoning;然后 selector(task_description=..., available_tools=..., reasoning=reasoning_text)
    - forward 返回 dspy.Prediction(selected_tool=..., selected_params=..., reasoning=reasoning_text, reasoning_tokens=int(len(reasoning_text)/4))(RESEARCH §1.4 Path 1)
    - named_predictors() 在 enable_reasoning=True 时包含 reasoner(GEPA 可触达 signature.instructions)
  </behavior>
  <action>
修改 `evolution/tools/tool_module.py`。改造分 4 段:

**(A) 顶部 import 与 module docstring(保留)**

保持现有 docstring 与 imports;在文件末尾(`ToolSelectionWithParamsSignature` 之后,`_ToolParamBundle` 之前)新增 `ToolReasoningSignature` 类。

**(B) 新增 ToolReasoningSignature 类(放在 `ToolSelectionWithParamsSignature` 之后)**

```python
# ── Phase 15 Reasoning Signature ────────────────────────────────────────

class ToolReasoningSignature(dspy.Signature):
    """Briefly reason about which tool best fits this task.

    Be concise (≤200 tokens). Mention what makes the candidate tools
    different in this context. Do NOT pre-select a tool — that is the
    selector's job. Focus on disambiguating overlapping tools by their
    intent, scope, or side-effects.
    """
    task_description: str = dspy.InputField(
        desc="The task to accomplish",
    )
    available_tools: str = dspy.InputField(
        desc="Formatted listing of available tools with descriptions and params",
    )
    reasoning: str = dspy.OutputField(
        desc="Short rationale comparing top candidates, ≤200 tokens. Do not name a winner.",
    )
```

**(C) 扩展 ToolSelectionWithParamsSignature — 新增 reasoning InputField(向后兼容)**

修改现有 `ToolSelectionWithParamsSignature` 类,在 `available_tools` 之后、`selected_tool` 之前 插入:

```python
    reasoning: str = dspy.InputField(
        default="",
        desc=(
            "Optional pre-reasoning from think-on path; empty string '' on "
            "think-off path. If non-empty, treat as advisory context — do not "
            "blindly defer; reasoning may be wrong."
        ),
    )
```

**(D) 修改 ToolModule.__init__ 与 forward**

ToolModule.__init__ 新签名(关键字-only `enable_reasoning` 与可选 LM 配置):

```python
def __init__(
    self,
    tool_descriptions: list[ToolDescription],
    *,
    enable_reasoning: bool = False,
    eval_model: str = "openai/gpt-4.1-mini",
    lm_kwargs: Optional[dict] = None,
):
    super().__init__()

    # 保留 Phase 13 现有构造:tools / _frozen_tool_desc / _frozen_tools /
    # _tool_names / selector(全部不变)
    self.tools: dict[str, _ToolParamBundle] = {}
    self._frozen_tool_desc: dict[str, str] = {}
    self._frozen_tools: dict[str, ToolDescription] = {}
    self._tool_names: list[str] = []
    for td in tool_descriptions:
        safe_name = self._safe_key(td.name)
        self.tools[safe_name] = _ToolParamBundle(td.name, list(td.params))
        self._frozen_tool_desc[td.name] = (td.description or f"Tool: {td.name}")
        self._frozen_tools[td.name] = td
        self._tool_names.append(td.name)
    self.selector = dspy.ChainOfThought(ToolSelectionWithParamsSignature)

    # ── Phase 15: Optional reasoner Predict ─────────────────────────
    # D-05: opt-in flag, static branching at constructor.
    # D-07: enable_reasoning is immutable after __init__ — never set self.reasoner
    # outside this block.
    self.enable_reasoning: bool = bool(enable_reasoning)
    self.reasoner: Optional[dspy.Predict] = None
    if self.enable_reasoning:
        # D-04 双保险其一: reasoner LM max_tokens=200.
        # Filter caller-supplied lm_kwargs to avoid double-max_tokens.
        filtered_kwargs = {
            k: v for k, v in (lm_kwargs or {}).items()
            if k != "max_tokens"
        }
        reasoning_lm = dspy.LM(eval_model, max_tokens=200, **filtered_kwargs)
        # D-01: separate Predict, NOT a CoT — selector retains independence.
        self.reasoner = dspy.Predict(ToolReasoningSignature)
        # RESEARCH §1.3 Path C: per-Predict LM override for token cap.
        self.reasoner.set_lm(reasoning_lm)
```

ToolModule.forward 新实现:

```python
def forward(self, task_description: str) -> dspy.Prediction:
    """Select the best tool AND its parameters for a given task.

    Phase 15: if self.reasoner is not None (enable_reasoning=True), run the
    reasoner first to produce a short rationale, then pass it to selector as
    an InputField. Otherwise the path is identical to Phase 13.

    Returns:
        dspy.Prediction with:
            - selected_tool (str)
            - selected_params (str, JSON-encoded; may be '{}')
            - reasoning (str; '' on think-off path)
            - reasoning_tokens (int; 0 on think-off path)
    """
    available_tools = self._format_available_tools()

    # Phase 15: think-on path runs reasoner first (D-01, D-02).
    reasoning_text = ""
    if self.reasoner is not None:
        reasoning_pred = self.reasoner(
            task_description=task_description,
            available_tools=available_tools,
        )
        # Defensive: reasoner Prediction may have empty/None reasoning under
        # certain LM error states. Coerce to str.
        reasoning_text = str(getattr(reasoning_pred, "reasoning", "") or "")

    # Selector retains FULL tools listing (D-02 — no rubber-stamp). The
    # `reasoning` InputField is advisory; default "" preserves Phase 13
    # behavior on think-off path.
    result = self.selector(
        task_description=task_description,
        available_tools=available_tools,
        reasoning=reasoning_text,
    )
    selected_params = getattr(result, "selected_params", "") or "{}"

    # RESEARCH §1.4 Path 1: token estimate = len(text)/4 — good enough for
    # ThinkABGate decisioning with 200-token cap (Phase 16 dashboard can
    # upgrade to precise LM usage later).
    reasoning_tokens = int(len(reasoning_text) / 4) if reasoning_text else 0

    return dspy.Prediction(
        selected_tool=result.selected_tool,
        selected_params=selected_params,
        reasoning=reasoning_text,
        reasoning_tokens=reasoning_tokens,
    )
```

**(E) 不动其它部分**

- `_ToolParamBundle` 完全不动
- `_safe_key` / `_format_available_tools` 完全不动(_frozen_tool_desc 隔离原则保持)
- `get_evolved_descriptions()` 完全不动(本 phase 不进化 description)
- module docstring 顶部可在 Phase 13 描述后追加一行:`Phase 15: optional reasoner Predict exposed via enable_reasoning=True (D-01..D-07).`
  </action>
  <verify>
    <automated>/Users/slj/项目/hermes-agent-self-evolution/.venv/bin/python -c "
import dspy
from pathlib import Path
from evolution.tools.tool_loader import ToolDescription, ToolParam
from evolution.tools.tool_module import ToolModule, ToolReasoningSignature, ToolSelectionWithParamsSignature

# Off path: backward-compat
tools = [ToolDescription(name='t1', file_path=Path('/fake/t1.py'), description='d', params=[ToolParam(name='p', type='string', required=True, description='pd')])]
m_off = ToolModule(tools)
assert m_off.reasoner is None, f'default enable_reasoning=False must yield reasoner=None, got {m_off.reasoner}'

# On path
m_on = ToolModule(tools, enable_reasoning=True)
assert m_on.reasoner is not None, 'enable_reasoning=True must construct reasoner'
assert isinstance(m_on.reasoner, dspy.Predict), f'reasoner must be dspy.Predict, got {type(m_on.reasoner)}'

# Signature exposed
assert hasattr(ToolReasoningSignature, '__doc__')
assert 'Be concise' in (ToolReasoningSignature.__doc__ or '')
assert 'Do NOT pre-select' in (ToolReasoningSignature.__doc__ or '')

# named_predictors includes reasoner
names = dict(m_on.named_predictors()).keys()
assert any('reasoner' in n for n in names), f'reasoner missing from named_predictors: {list(names)}'

# Reasoning InputField on selector signature
fields = ToolSelectionWithParamsSignature.model_fields if hasattr(ToolSelectionWithParamsSignature, 'model_fields') else ToolSelectionWithParamsSignature.__annotations__
assert 'reasoning' in fields, f'ToolSelectionWithParamsSignature missing reasoning field: {fields}'

print('Wave 1 module smoke: PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class ToolReasoningSignature" evolution/tools/tool_module.py` 命中
    - `grep -q "Be concise" evolution/tools/tool_module.py` 命中(D-04 docstring 提示)
    - `grep -q "Do NOT pre-select" evolution/tools/tool_module.py` 命中(D-02 防 reasoner 越权)
    - `grep -q "enable_reasoning: bool = False" evolution/tools/tool_module.py` 命中(D-05 默认值)
    - `grep -c "self.reasoner" evolution/tools/tool_module.py` ≥ 4(__init__ 2 处 + forward 2 处)
    - `grep -q "max_tokens=200" evolution/tools/tool_module.py` 命中(D-04 LM 双保险其二)
    - `grep -q "self.reasoner.set_lm" evolution/tools/tool_module.py` 命中(RESEARCH §1.3 Path C)
    - `grep -q "reasoning: str = dspy.InputField" evolution/tools/tool_module.py` 命中(ToolSelectionWithParamsSignature 新字段)
    - `grep -q 'default=""' evolution/tools/tool_module.py` 命中(向后兼容)
    - `grep -q "reasoning_tokens=" evolution/tools/tool_module.py` 命中(D-17 / RESEARCH §1.4 Path 1)
    - `grep -q "int(len(reasoning_text) / 4)" evolution/tools/tool_module.py` 命中(token 估算 — RESEARCH §1.4)
    - 上述 verify smoke 退出 0,输出 "Wave 1 module smoke: PASS"
    - `.venv/bin/python -m pytest tests/tools/test_tool_module.py -x --tb=short -k "not TestEnableReasoning"` 退出 0(Phase 13 现有测试不退化)
  </acceptance_criteria>
  <done>
    tool_module.py 改造完成:ToolReasoningSignature 类定义、selector signature 新增 reasoning InputField(default="")、__init__ 接受 enable_reasoning(默认 False)与可选 lm_kwargs、enable_reasoning=True 时 construct self.reasoner 并 set_lm(max_tokens=200)、forward 双路径返回带 reasoning + reasoning_tokens 的 Prediction。向后兼容:不传 enable_reasoning 时 Phase 13 现有测试全 GREEN。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 tests/tools/test_tool_module.py — 新增 TestEnableReasoning 测试类,7 个测试覆盖构造分支 / forward 双路径 / max_tokens / named_predictors / 默认值向后兼容</name>
  <files>tests/tools/test_tool_module.py</files>
  <read_first>
    - tests/tools/test_tool_module.py(现有文件结构 — 顶部 import + fixture 风格)
    - tests/tools/test_tool_module_per_param.py(_make_3x3_tools fixture 风格)
    - .planning/phases/15-15-think-augmented-tool-selection/15-VALIDATION.md §"Per-Task Verification Map" Wave 1 行
    - .planning/phases/15-think-augmented-tool-selection/15-PATTERNS.md §7(TestEnableReasoning 测试列表)
    - evolution/tools/tool_module.py(已被 Task 1 修改 — 验证新 API)
  </read_first>
  <behavior>
    - test_constructs_reasoner: ToolModule(tools, enable_reasoning=True).reasoner 是 dspy.Predict 实例;其 signature 是 ToolReasoningSignature(检查 type(self.reasoner.signature) 或 signature class name)
    - test_disabled_reasoner_absent: ToolModule(tools, enable_reasoning=False).reasoner is None
    - test_default_enable_reasoning_is_false: ToolModule(tools) (不传 kwarg)→ reasoner is None(向后兼容守门)
    - test_off_path_no_reasoner_call: mock LM;enable_reasoning=False,调 forward;断 returned Prediction.reasoning == "" 且 reasoning_tokens == 0
    - test_on_path_reasoner_first: mock LM;enable_reasoning=True,patch self.reasoner 与 self.selector;调 forward;断 reasoner.call_count == 1 在 selector.call_count == 1 之前,且 selector kwargs 中 reasoning 非空
    - test_reasoner_lm_max_tokens_200: enable_reasoning=True;`module.reasoner.get_lm().kwargs.get("max_tokens") == 200`(或等价访问;若 .kwargs 不可用则读 `module.reasoner.lm.kwargs`)
    - test_reasoner_in_named_predictors: enable_reasoning=True;断 `dict(module.named_predictors())` 中至少有一个 key 含 "reasoner";断该 Predict.signature.instructions(或 .signature.__doc__)非空
  </behavior>
  <action>
在 `tests/tools/test_tool_module.py` 文件**末尾**追加 `TestEnableReasoning` 测试类。不改动现有测试。

⚠️ 先 Read 现有 test_tool_module.py(它当前的 import 与 fixture 结构),复用其 fixture 风格。

要追加的内容:

```python
# ── Phase 15: TestEnableReasoning ────────────────────────────────────────

class TestEnableReasoning:
    """Phase 15 Wave 1 — ToolModule(enable_reasoning=...) construction + forward
    branching + GEPA discoverability + backward compat."""

    @staticmethod
    def _tools():
        from pathlib import Path
        from evolution.tools.tool_loader import ToolDescription, ToolParam
        return [
            ToolDescription(
                name="search",
                file_path=Path("/fake/search.py"),
                description="Search for stuff",
                params=[
                    ToolParam(name="q", type="string", required=True, description="query"),
                ],
            ),
            ToolDescription(
                name="read_file",
                file_path=Path("/fake/read_file.py"),
                description="Read file",
                params=[
                    ToolParam(name="path", type="string", required=True, description="file path"),
                ],
            ),
        ]

    def test_constructs_reasoner(self):
        """enable_reasoning=True → self.reasoner is dspy.Predict(ToolReasoningSignature)."""
        import dspy
        from unittest.mock import patch, MagicMock
        from evolution.tools.tool_module import ToolModule, ToolReasoningSignature
        with patch("evolution.tools.tool_module.dspy.LM") as mock_lm:
            mock_lm.return_value = MagicMock()
            module = ToolModule(self._tools(), enable_reasoning=True)
        assert module.reasoner is not None
        assert isinstance(module.reasoner, dspy.Predict)
        # Signature class identity check (handles both Pydantic-1 and -2 dspy versions)
        sig = module.reasoner.signature
        sig_name = getattr(sig, "__name__", None) or type(sig).__name__
        assert "ToolReasoningSignature" in sig_name or sig is ToolReasoningSignature

    def test_disabled_reasoner_absent(self):
        """enable_reasoning=False → self.reasoner is None."""
        from evolution.tools.tool_module import ToolModule
        module = ToolModule(self._tools(), enable_reasoning=False)
        assert module.reasoner is None

    def test_default_enable_reasoning_is_false(self):
        """Backward compat: not passing the kwarg → Phase 13 behavior."""
        from evolution.tools.tool_module import ToolModule
        module = ToolModule(self._tools())  # no kwarg
        assert module.reasoner is None
        assert module.enable_reasoning is False

    def test_off_path_no_reasoner_call(self):
        """think-off forward: reasoner never called; returned Prediction.reasoning == ''."""
        import dspy
        from unittest.mock import MagicMock
        from evolution.tools.tool_module import ToolModule
        module = ToolModule(self._tools(), enable_reasoning=False)
        # Replace selector with mock to avoid real LM call.
        module.selector = MagicMock(return_value=dspy.Prediction(
            selected_tool="search",
            selected_params="{}",
        ))
        pred = module.forward(task_description="find files")
        assert pred.reasoning == ""
        assert pred.reasoning_tokens == 0

    def test_on_path_reasoner_first(self):
        """think-on forward: reasoner called BEFORE selector; selector kwargs include reasoning."""
        import dspy
        from unittest.mock import patch, MagicMock
        from evolution.tools.tool_module import ToolModule
        with patch("evolution.tools.tool_module.dspy.LM") as mock_lm:
            mock_lm.return_value = MagicMock()
            module = ToolModule(self._tools(), enable_reasoning=True)
        # Inject ordered mocks; assert call order via shared list.
        call_log: list[str] = []
        def reasoner_mock(**kwargs):
            call_log.append("reasoner")
            return dspy.Prediction(reasoning="comparing search vs read_file...")
        def selector_mock(**kwargs):
            call_log.append("selector")
            assert "reasoning" in kwargs, f"selector must receive reasoning kwarg; got {list(kwargs)}"
            assert kwargs["reasoning"] != "", f"reasoning must be non-empty on think-on path; got {kwargs['reasoning']!r}"
            return dspy.Prediction(selected_tool="search", selected_params="{}")
        module.reasoner = reasoner_mock
        module.selector = selector_mock
        pred = module.forward(task_description="find files containing TODO")
        assert call_log == ["reasoner", "selector"]
        assert pred.reasoning == "comparing search vs read_file..."
        assert pred.reasoning_tokens > 0  # len("comparing...")/4 > 0

    def test_reasoner_lm_max_tokens_200(self):
        """D-04: reasoner LM has max_tokens=200 hard cap."""
        import dspy
        from unittest.mock import patch
        from evolution.tools.tool_module import ToolModule
        # Use a real dspy.LM ctor recording but NOT calling the LM.
        captured_kwargs = {}
        real_LM = dspy.LM

        def capturing_LM(model_name, **kwargs):
            captured_kwargs.update(kwargs)
            return real_LM.__new__(real_LM)  # uninitialized instance is fine for kwarg check

        with patch("evolution.tools.tool_module.dspy.LM", side_effect=capturing_LM) as patched:
            ToolModule(self._tools(), enable_reasoning=True)
        # patched.call_args captures first call kwargs
        first_call_kwargs = patched.call_args.kwargs if patched.call_args else {}
        # Either side_effect captured or call_args reflects max_tokens=200
        assert (
            captured_kwargs.get("max_tokens") == 200
            or first_call_kwargs.get("max_tokens") == 200
        ), (
            f"reasoner LM must be constructed with max_tokens=200; got "
            f"captured={captured_kwargs} call_args={first_call_kwargs}"
        )

    def test_reasoner_in_named_predictors(self):
        """TOOL-V2-03 SC-2: reasoner.signature.instructions is GEPA-discoverable."""
        from unittest.mock import patch, MagicMock
        from evolution.tools.tool_module import ToolModule
        with patch("evolution.tools.tool_module.dspy.LM") as mock_lm:
            mock_lm.return_value = MagicMock()
            module = ToolModule(self._tools(), enable_reasoning=True)
        names_to_predicts = dict(module.named_predictors())
        # named_predictors paths often qualified like "reasoner" or include child paths
        reasoner_keys = [k for k in names_to_predicts.keys() if "reasoner" in k]
        assert len(reasoner_keys) >= 1, (
            f"reasoner missing from named_predictors. Got keys: {list(names_to_predicts.keys())}"
        )
        # Instructions must be the ToolReasoningSignature docstring (non-empty).
        pred = names_to_predicts[reasoner_keys[0]]
        instructions = pred.signature.instructions
        assert instructions, f"reasoner instructions empty"
        assert "Be concise" in instructions or "≤200 tokens" in instructions
```

⚠️ 注意:test_reasoner_lm_max_tokens_200 测试可能因 dspy.LM 的 patch 时序 issue 需要小幅调整;若 `patched.call_args` 在 side_effect 模式下不可访问,改读 `mock_lm.call_args_list[0].kwargs`。该测试是 D-04 LM 配置的金标准。
  </action>
  <verify>
    <automated>/Users/slj/项目/hermes-agent-self-evolution/.venv/bin/python -m pytest /Users/slj/项目/hermes-agent-self-evolution/tests/tools/test_tool_module.py::TestEnableReasoning -v --tb=short 2>&1 | tee /tmp/15-02-test.log | tail -50 && grep -E "passed|failed|error" /tmp/15-02-test.log | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class TestEnableReasoning" tests/tools/test_tool_module.py` 命中
    - `grep -c "def test_" tests/tools/test_tool_module.py` 比 Wave 0 之前多 ≥ 7
    - `grep -q "def test_constructs_reasoner" tests/tools/test_tool_module.py` 命中
    - `grep -q "def test_disabled_reasoner_absent" tests/tools/test_tool_module.py` 命中
    - `grep -q "def test_default_enable_reasoning_is_false" tests/tools/test_tool_module.py` 命中
    - `grep -q "def test_off_path_no_reasoner_call" tests/tools/test_tool_module.py` 命中
    - `grep -q "def test_on_path_reasoner_first" tests/tools/test_tool_module.py` 命中
    - `grep -q "def test_reasoner_lm_max_tokens_200" tests/tools/test_tool_module.py` 命中
    - `grep -q "def test_reasoner_in_named_predictors" tests/tools/test_tool_module.py` 命中
    - `pytest tests/tools/test_tool_module.py::TestEnableReasoning -v` 7/7 passed(全 GREEN)
    - `pytest tests/tools/test_tool_module.py -x --tb=short` 退出 0(整文件 GREEN,Phase 13 现有测试不退化)
    - `pytest tests/tools/test_tool_module_per_param.py -x --tb=short` 退出 0(Phase 13 per-param 测试不退化)
  </acceptance_criteria>
  <done>
    TestEnableReasoning 测试类追加完毕,7/7 GREEN,Phase 13 现有 ToolModule 测试不退化。`pytest tests/tools/test_tool_module*.py` 全 GREEN。Wave 0 Plan 01 的 `tests/tools/conftest.py::mock_reasoning_module` fixture 此时可被 Wave 2/3 实际使用。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 13 baseline → Phase 15 修改 | Phase 13 现有 ToolModule 是 Phase 13/14 测试 baseline 的核心;改坏可破 385 现有测试 |
| ToolReasoningSignature.docstring → GEPA reflection | GEPA 把 docstring 当 mutable instructions;过激/泄密 prompt 会被反思修改 |
| reasoner.set_lm → dspy.context 边界 | per-Predict LM 与全局 dspy.context(lm=) 的优先级关系是 DSPy 内部 API(RESEARCH §1.3 已校核) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Tampering | ToolModule.forward 双路径 | mitigate | 默认 enable_reasoning=False;test_default_enable_reasoning_is_false 守门 — 不传 kwarg 时行为完全等价 Phase 13 |
| T-15-02-02 | Repudiation | reasoner.set_lm 修改全局 LM | mitigate | per-Predict set_lm 仅影响该 Predict 实例;selector 仍走 dspy.configure / dspy.context 全局 LM(RESEARCH §1.3 Path C) |
| T-15-02-03 | Info Disclosure | ToolReasoningSignature.docstring 内容 | accept | docstring 不含 PII / secret;通用化的引导文字 |
| T-15-02-04 | DoS | reasoning_text 在 forward 中过长(LM 不遵循 max_tokens) | mitigate | LM-level max_tokens=200 + docstring 提示 ≤200 tokens 双保险(D-04);若超长 reasoning_tokens=len/4 估算上限自然界定 ThinkABGate decision |
| T-15-02-05 | Elevation of Privilege | reasoner 越权选 tool(Pitfall 4 selector 橡皮图章) | mitigate | ToolReasoningSignature 仅 OutputField=reasoning;Pitfall 4 §3 守门 + D-02 文档化拒绝;selector signature 接收 reasoning 但保留完整 tools 列表 |
</threat_model>

<verification>
**Plan-level checks:**

1. **ToolModule API 完整:** `python -c "from evolution.tools.tool_module import ToolReasoningSignature, ToolModule; m = ToolModule([]); assert m.reasoner is None"` 退出 0

2. **TestEnableReasoning 7/7 GREEN:** `pytest tests/tools/test_tool_module.py::TestEnableReasoning -v` 通过 7 个测试

3. **Phase 13/14 现有测试不退化:** `pytest tests/tools/ -x --tb=short --ignore=tests/tools/test_think_metrics.py --ignore=tests/tools/test_evolve_tool_reasoning.py` 退出 0

4. **GEPA 路径可达:** `named_predictors()` 包含 reasoner 路径,且其 `signature.instructions` 含 "Be concise"

5. **D-04 双保险:** docstring 含 "≤200 tokens" 提示文字 + LM 构造期 max_tokens=200 kwarg

6. **Plan 01 conftest.mock_reasoning_module fixture 现在可用:** `pytest tests/tools/conftest.py` collect 阶段不再 import-error

7. **TOOL-V2-03 SC-1 与 SC-2 已锁定:** SC-1 (enable_reasoning 构造) → test_constructs_reasoner / test_disabled_reasoner_absent;SC-2 (reasoning 可被 GEPA 优化) → test_reasoner_in_named_predictors 断 instructions 暴露
</verification>

<success_criteria>
1. `evolution/tools/tool_module.py` 改造完成,新增 ToolReasoningSignature 类、ToolSelectionWithParamsSignature 新 reasoning InputField(default="")、ToolModule.__init__ 接受 enable_reasoning 关键字参数(默认 False)、enable_reasoning=True 时构造 self.reasoner = dspy.Predict(ToolReasoningSignature) 并 set_lm(max_tokens=200);forward 双路径返回带 reasoning + reasoning_tokens 的 Prediction
2. `tests/tools/test_tool_module.py` 新增 TestEnableReasoning 测试类,7/7 GREEN
3. `pytest tests/tools/test_tool_module.py tests/tools/test_tool_module_per_param.py -x` 全部 GREEN(Phase 13 现有 + Phase 15 新测试)
4. D-01..D-07 + D-17 在代码 + 测试中均有覆盖:
   - D-01 dspy.Predict(ToolReasoningSignature) 而非 CoT — code:`dspy.Predict(ToolReasoningSignature)`
   - D-02 reasoner 不输出 selected_tool — code:Signature 仅 OutputField=reasoning
   - D-03 全局单 reasoner — code:self.reasoner 单实例
   - D-04 200-token cap 双保险 — code:max_tokens=200 + docstring "≤200 tokens"
   - D-05 opt-in flag — code:enable_reasoning kwarg
   - D-06 think-off baseline 与 Phase 13 等价 — test:test_default_enable_reasoning_is_false
   - D-07 ctor 后不可变 — code:`*` 强制 keyword-only,文档说明 immutable
   - D-17 reasoning_tokens 字段 — code:`int(len(reasoning_text) / 4)` 返回 Prediction
5. Pitfall 4 守门:reasoning 长度由 LM-level max_tokens=200 截断;Pitfall 4 §3 reasoner rubber-stamp 风险通过 docstring "Do NOT pre-select" + selector 保留完整 tools 列表抵消
</success_criteria>

<output>
After completion, create `.planning/phases/15-think-augmented-tool-selection/15-02-SUMMARY.md` recording:
- tool_module.py 总 LoC 变化(原 232 行 → 现 ~290 行)
- TestEnableReasoning 7/7 GREEN 结果
- Phase 13 现有测试不退化的确认命令与结果
- D-04 LM max_tokens=200 实测(test_reasoner_lm_max_tokens_200 输出)
- named_predictors() 包含 "reasoner" 的 key 路径(实测)
</output>
