---
phase: 15
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - evolution/tools/think_metrics.py
autonomous: true
requirements:
  - TOOL-V2-03
must_haves:
  truths:
    - "think_metrics.py 提供 ThinkABGate 类(类 API)与 check_think_ab_gate 函数(函数 API)双形态,镜像 v1_baseline_gate.py 双 API 模式(D-15 / RESEARCH §5.1)"
    - "三重门 AND 逻辑:full_regression_gate_passed AND ambiguous_gate_passed AND latency_gate_passed = 总 passed(D-14)"
    - "小样本保护:ambiguous_sample_size < 5 时 ambiguous_gate_skipped=True,该门视为 PASS(D-16)"
    - "默认阈值硬编码在模块级常量,EvolutionConfig 不扩(D-15)"
    - "check_think_ab_gate 返回 ConstraintResult,details 是 sort_keys json 字符串(RESEARCH §4)"
    - "sample_latency_tokens(module, examples, lm) 在 dspy.context(lm=) 内逐例 time.perf_counter 计时,失败时 try/except skip,返回 p50/p95/mean stats(D-17)"
    - "think_metrics 模块不引入任何 GEPA 5-param 签名 metric — 由测试 test_no_gepa_metric_added 守门(Pitfall 12)"
  artifacts:
    - path: "evolution/tools/think_metrics.py"
      provides: "ThinkABGate / check_think_ab_gate / sample_latency_tokens / 4 个模块级常量"
      contains: "class ThinkABGate"
  key_links:
    - from: "evolution/tools/think_metrics.py"
      to: "evolution.core.constraints.ConstraintResult"
      via: "check_think_ab_gate 返回 ConstraintResult(passed, constraint_name='think_ab_gate', message, details=json.dumps(...))"
      pattern: "from evolution.core.constraints import ConstraintResult"
    - from: "think_metrics._compute_think_ab_metrics"
      to: "ThinkABGate.check 与 check_think_ab_gate"
      via: "两个 API 内部都调用 _compute_think_ab_metrics(**kwargs) 保证语义一致"
      pattern: "_compute_think_ab_metrics"
    - from: "think_metrics.sample_latency_tokens"
      to: "dspy.context + time.perf_counter"
      via: "with dspy.context(lm=lm): for ex in examples: t0=time.perf_counter(); module(...); t1=time.perf_counter()"
      pattern: "time\\.perf_counter|dspy\\.context"
---

<objective>
实现 Phase 15 的核心 gate 与采样模块 `evolution/tools/think_metrics.py`,提供:
1. **三重 AND 门**(D-14):full regression tolerance 2pp + ambiguous improvement 3pp + latency p95 5s budget
2. **双 API 形态**(D-15 / RESEARCH §5.1):函数式 `check_think_ab_gate(...)` 返回 ConstraintResult;类式 `ThinkABGate.check(...)` 返回完整 metrics dict
3. **小样本保护**(D-16):ambiguous_sample_size < 5 时跳过 ambiguous 门
4. **latency / reasoning-token 采样**(D-17):`sample_latency_tokens(module, examples, lm)` 包裹 forward 计时与 token 估算
5. **守门测试 GREEN**:Plan 01 的 `tests/tools/test_think_metrics.py` 15+ 测试全部 GREEN

Purpose:
- 锁定 D-14 三重门作为 Phase 15 后置验证的核心契约
- 复用 v1_baseline_gate.py 双 API + ConstraintResult 模式,降低 reviewer 学习成本
- 为 Plan 04 CLI 提供 import-ready 的接口(ThinkABGate, sample_latency_tokens, 4 个 DEFAULT_* 常量)

Output:
- 新建 `evolution/tools/think_metrics.py`(~150 LoC)
- Plan 01 的 `tests/tools/test_think_metrics.py` 全部 GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-think-augmented-tool-selection/15-CONTEXT.md
@.planning/phases/15-think-augmented-tool-selection/15-RESEARCH.md
@.planning/phases/15-think-augmented-tool-selection/15-PATTERNS.md
@.planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md
@.planning/phases/15-think-augmented-tool-selection/15-01-PLAN.md
@.planning/research/PITFALLS.md
@evolution/tools/v1_baseline_gate.py
@evolution/core/constraints.py
</context>

<interfaces>
<!-- 镜像分析对象:v1_baseline_gate.py 的双 API 与 ConstraintResult 写入风格 -->

From `evolution/tools/v1_baseline_gate.py:52-86`(内部计算函数模板):
```python
def _compute_baseline_gate_metrics(
    evolved_score: float,
    baseline_score: float,
    tolerance: float = 0.02,
) -> dict:
    delta = round(float(evolved_score) - float(baseline_score), 10)
    threshold = -float(tolerance)
    passed = delta >= threshold
    if passed:
        message = f"v1 baseline gate OK: evolved={evolved_score:.4f} ..."
    else:
        message = f"v1 baseline gate FAILED: evolved={evolved_score:.4f} regressed by {abs(delta):.4f} ..."
    return {
        "passed": passed, "delta": delta, "tolerance_pp": float(tolerance),
        "evolved_score": float(evolved_score), "baseline_score": float(baseline_score),
        "message": message,
    }
```

From `evolution/tools/v1_baseline_gate.py:92-131`(函数 API 模板,返回 ConstraintResult):
```python
def check_v1_baseline_gate(
    evolved_score: float,
    baseline_score: float,
    tolerance: float = 0.02,
) -> ConstraintResult:
    metrics = _compute_baseline_gate_metrics(
        evolved_score=evolved_score,
        baseline_score=baseline_score,
        tolerance=tolerance,
    )
    details = json.dumps(
        {
            "delta": metrics["delta"],
            "tolerance_pp": metrics["tolerance_pp"],
            "evolved_score": metrics["evolved_score"],
            "baseline_score": metrics["baseline_score"],
        },
        sort_keys=True,
    )
    return ConstraintResult(
        passed=metrics["passed"],
        constraint_name="v1_baseline_gate",
        message=metrics["message"],
        details=details,
    )
```

From `evolution/tools/v1_baseline_gate.py:349-394`(类 API 模板):
```python
class V1BaselineGate:
    def __init__(self, tolerance: float = 0.02):
        self.tolerance = float(tolerance)
    def check(self, *, evolved_score: float, baseline: dict) -> dict:
        metrics = _compute_baseline_gate_metrics(
            evolved_score=evolved_score,
            baseline_score=float(baseline["v1_baseline_holdout"]),
            tolerance=self.tolerance,
        )
        return {**baseline, **metrics}
```

From `evolution/core/constraints.py:15-21`:
```python
@dataclass
class ConstraintResult:
    passed: bool
    constraint_name: str
    message: str
    details: Optional[str] = None  # 必须是 str — 不是 dict
```

From `evolution/tools/v1_baseline_gate.py:184`(dspy.context 模式):
```python
ctx = dspy.context(lm=lm) if lm is not None else _NullCtx()
with ctx:
    for ex in holdout_examples:
        try:
            pred = module(task_description=task)
        except Exception:
            continue
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 evolution/tools/think_metrics.py — ThinkABGate 双 API + sample_latency_tokens helper + 4 个模块级常量</name>
  <files>evolution/tools/think_metrics.py</files>
  <read_first>
    - evolution/tools/v1_baseline_gate.py:1-200(双 API 模板 + ConstraintResult 写入 + dspy.context 模式)
    - evolution/core/constraints.py:1-30(ConstraintResult dataclass)
    - .planning/phases/15-think-augmented-tool-selection/15-RESEARCH.md §4 + §5.1(ThinkABGate 设计契约 + 完整代码契约)
    - .planning/phases/15-think-augmented-tool-selection/15-PATTERNS.md §2(完整 Deviations 列表)
    - .planning/phases/15-think-augmented-tool-selection/15-CONTEXT.md(D-14..D-17)
    - tests/tools/test_think_metrics.py(由 Plan 01 创建 — 15+ RED 测试是本任务的 GREEN 目标)
  </read_first>
  <behavior>
    模块导出的 public API:
    - `DEFAULT_FULL_REGRESSION_TOLERANCE_PP = 2.0`(D-15)
    - `DEFAULT_AMBIGUOUS_IMPROVEMENT_PP = 3.0`(D-15)
    - `DEFAULT_LATENCY_P95_BUDGET_SEC = 5.0`(D-15)
    - `AMBIGUOUS_SMALL_SAMPLE_THRESHOLD = 5`(D-16)
    - `_compute_think_ab_metrics(**kwargs) -> dict` 内部:返回含 keys (passed, full_regression_delta, ambiguous_delta, ambiguous_sample_size, ambiguous_gate_skipped, latency_p95_seconds, gates: {full_regression_gate_passed, ambiguous_gate_passed, latency_gate_passed}, tolerances: {...}, evolved_scores: {...}, message) 的 dict
    - `check_think_ab_gate(**kwargs) -> ConstraintResult`:details=json.dumps(metrics, sort_keys=True);constraint_name="think_ab_gate"
    - `class ThinkABGate`:`__init__(*, full_regression_tolerance_pp=DEFAULT_..., ambiguous_improvement_pp=DEFAULT_..., latency_p95_budget_sec=DEFAULT_...)`;`.check(*, think_on_holdout_score, think_off_holdout_score, ambiguous_think_on_score, ambiguous_think_off_score, ambiguous_sample_size, latency_p95_seconds) -> dict`
    - `sample_latency_tokens(module, examples, lm) -> dict`:在 dspy.context(lm=lm) 内逐例 time.perf_counter 计时;捕获 ex.task_description;调 `module(task_description=...)`;读 pred.reasoning_tokens(默认 0);失败 try/except skip;返回 {"latency_seconds": [...], "reasoning_tokens": [...], "stats": {latency_p50, latency_p95, latency_mean, reasoning_token_p50, reasoning_token_p95, reasoning_token_mean}};**不**新增 GEPA metric

    三重门逻辑(D-14):
    - full_regression: think_on - think_off >= -(tolerance_pp/100)  → passed
    - ambiguous: 若 sample_size < AMBIGUOUS_SMALL_SAMPLE_THRESHOLD → skipped=True, passed=True;否则 (ambig_on - ambig_off) >= (improvement_pp/100) → passed
    - latency: latency_p95_seconds <= latency_p95_budget_sec → passed
    - 总 passed = full AND ambiguous AND latency
  </behavior>
  <action>
新建文件 `evolution/tools/think_metrics.py`。完整实现:

```python
"""Phase 15 think-augmented A/B gate + latency/token sampler (D-08, D-14..D-17).

Three-AND gate validating that enable_reasoning=True is a net win:

  1. full_regression: think-on holdout_score ≥ think-off - 2pp tolerance (D-14.1)
  2. ambiguous:       think-on subset_score - think-off subset_score ≥ +3pp (D-14.2)
                      skipped when ambiguous sample size < 5 (D-16)
  3. latency:         think-on p95 latency ≤ 5.0 sec budget (D-14.3)

Mirrors evolution/tools/v1_baseline_gate.py dual-API pattern:
  - check_think_ab_gate(...)  →  ConstraintResult (constraint-chain compatible)
  - ThinkABGate.check(...)    →  full metrics dict (CLI consumes for metrics.json)

Default thresholds hardcoded as module-level constants per D-15 — EvolutionConfig
is NOT extended (Phase 15 stays config-clean). CLI flags override at call time.

Pitfall 12 guard: this module MUST NOT export any 5-param GEPA metric. Gate runs
POST-compile, never inside GEPA's reflection loop. RESEARCH §1.5 / §10.2 守门
test test_no_gepa_metric_added 会扫描本模块并断言无 5-param signature 函数。
"""

from __future__ import annotations

import json
import statistics
import time
from typing import Any, Optional

from evolution.core.constraints import ConstraintResult


# ── Module-level defaults (D-15) ────────────────────────────────────────

DEFAULT_FULL_REGRESSION_TOLERANCE_PP: float = 2.0
DEFAULT_AMBIGUOUS_IMPROVEMENT_PP: float = 3.0
DEFAULT_LATENCY_P95_BUDGET_SEC: float = 5.0
AMBIGUOUS_SMALL_SAMPLE_THRESHOLD: int = 5  # D-16


# ── Internal: three-gate metric computation ─────────────────────────────


def _compute_think_ab_metrics(
    *,
    think_on_holdout_score: float,
    think_off_holdout_score: float,
    ambiguous_think_on_score: float,
    ambiguous_think_off_score: float,
    ambiguous_sample_size: int,
    latency_p95_seconds: float,
    full_regression_tolerance_pp: float = DEFAULT_FULL_REGRESSION_TOLERANCE_PP,
    ambiguous_improvement_pp: float = DEFAULT_AMBIGUOUS_IMPROVEMENT_PP,
    latency_p95_budget_sec: float = DEFAULT_LATENCY_P95_BUDGET_SEC,
) -> dict:
    """Compute the three-AND gate metrics.

    Returns a dict containing per-gate pass/fail + deltas + tolerances + an
    overall `passed` boolean + a human-readable message. Designed to be the
    single source of truth consumed by both check_think_ab_gate (function API)
    and ThinkABGate.check (class API).
    """
    # ── Gate 1: full regression ──
    full_delta = round(
        float(think_on_holdout_score) - float(think_off_holdout_score), 10
    )
    full_threshold = -(float(full_regression_tolerance_pp) / 100.0)
    full_passed = full_delta >= full_threshold

    # ── Gate 2: ambiguous improvement (with small-sample skip, D-16) ──
    ambiguous_delta = round(
        float(ambiguous_think_on_score) - float(ambiguous_think_off_score), 10
    )
    ambiguous_threshold = float(ambiguous_improvement_pp) / 100.0
    ambiguous_skipped = int(ambiguous_sample_size) < AMBIGUOUS_SMALL_SAMPLE_THRESHOLD
    if ambiguous_skipped:
        # D-16: small sample → skip gate, treat as PASS.
        ambiguous_passed = True
    else:
        ambiguous_passed = ambiguous_delta >= ambiguous_threshold

    # ── Gate 3: latency p95 budget ──
    latency_passed = float(latency_p95_seconds) <= float(latency_p95_budget_sec)

    # ── Three-AND total ──
    all_passed = bool(full_passed and ambiguous_passed and latency_passed)

    # Human-readable message
    msg_parts = [
        f"full_regression={'OK' if full_passed else 'FAIL'} "
        f"(delta={full_delta:+.4f}, tolerance={full_regression_tolerance_pp}pp)",
    ]
    if ambiguous_skipped:
        msg_parts.append(
            f"ambiguous=SKIPPED (sample_size={ambiguous_sample_size} "
            f"< {AMBIGUOUS_SMALL_SAMPLE_THRESHOLD})"
        )
    else:
        msg_parts.append(
            f"ambiguous={'OK' if ambiguous_passed else 'FAIL'} "
            f"(delta={ambiguous_delta:+.4f}, "
            f"required≥{ambiguous_improvement_pp}pp, "
            f"n={ambiguous_sample_size})"
        )
    msg_parts.append(
        f"latency={'OK' if latency_passed else 'FAIL'} "
        f"(p95={latency_p95_seconds:.2f}s, budget={latency_p95_budget_sec}s)"
    )

    status = "PASS" if all_passed else "FAIL"
    message = f"think_ab_gate {status} | " + " | ".join(msg_parts)

    return {
        "passed": all_passed,
        "full_regression_delta": full_delta,
        "ambiguous_delta": ambiguous_delta,
        "ambiguous_sample_size": int(ambiguous_sample_size),
        "ambiguous_gate_skipped": ambiguous_skipped,
        "latency_p95_seconds": float(latency_p95_seconds),
        "tolerances": {
            "full_regression_tolerance_pp": float(full_regression_tolerance_pp),
            "ambiguous_improvement_pp": float(ambiguous_improvement_pp),
            "latency_p95_budget_sec": float(latency_p95_budget_sec),
        },
        "evolved_scores": {
            "think_on_holdout_score": float(think_on_holdout_score),
            "think_off_holdout_score": float(think_off_holdout_score),
            "ambiguous_think_on_score": float(ambiguous_think_on_score),
            "ambiguous_think_off_score": float(ambiguous_think_off_score),
        },
        "gates": {
            "full_regression_gate_passed": bool(full_passed),
            "ambiguous_gate_passed": bool(ambiguous_passed),
            "latency_gate_passed": bool(latency_passed),
        },
        "message": message,
    }


# ── Function API: returns ConstraintResult ──────────────────────────────


def check_think_ab_gate(
    *,
    think_on_holdout_score: float,
    think_off_holdout_score: float,
    ambiguous_think_on_score: float,
    ambiguous_think_off_score: float,
    ambiguous_sample_size: int,
    latency_p95_seconds: float,
    full_regression_tolerance_pp: float = DEFAULT_FULL_REGRESSION_TOLERANCE_PP,
    ambiguous_improvement_pp: float = DEFAULT_AMBIGUOUS_IMPROVEMENT_PP,
    latency_p95_budget_sec: float = DEFAULT_LATENCY_P95_BUDGET_SEC,
) -> ConstraintResult:
    """Three-AND gate facade returning ConstraintResult (constraint-chain compatible).

    Mirrors evolution/tools/v1_baseline_gate.check_v1_baseline_gate API shape.
    The full metrics dict is serialized into the .details field as sort_keys
    JSON for CLI to echo into metrics.json.
    """
    metrics = _compute_think_ab_metrics(
        think_on_holdout_score=think_on_holdout_score,
        think_off_holdout_score=think_off_holdout_score,
        ambiguous_think_on_score=ambiguous_think_on_score,
        ambiguous_think_off_score=ambiguous_think_off_score,
        ambiguous_sample_size=ambiguous_sample_size,
        latency_p95_seconds=latency_p95_seconds,
        full_regression_tolerance_pp=full_regression_tolerance_pp,
        ambiguous_improvement_pp=ambiguous_improvement_pp,
        latency_p95_budget_sec=latency_p95_budget_sec,
    )
    details = json.dumps(metrics, sort_keys=True, ensure_ascii=False)
    return ConstraintResult(
        passed=metrics["passed"],
        constraint_name="think_ab_gate",
        message=metrics["message"],
        details=details,
    )


# ── Class API: returns full metrics dict (for CLI metrics.json) ─────────


class ThinkABGate:
    """Three-AND gate class API for Phase 15 think-augmented selection.

    Mirrors evolution.tools.v1_baseline_gate.V1BaselineGate construction:
    instantiate once with tolerances, call .check() with run-time scores.
    Returns the full metrics dict so the CLI can splat into metrics.json.
    """

    def __init__(
        self,
        *,
        full_regression_tolerance_pp: float = DEFAULT_FULL_REGRESSION_TOLERANCE_PP,
        ambiguous_improvement_pp: float = DEFAULT_AMBIGUOUS_IMPROVEMENT_PP,
        latency_p95_budget_sec: float = DEFAULT_LATENCY_P95_BUDGET_SEC,
    ):
        self.full_regression_tolerance_pp = float(full_regression_tolerance_pp)
        self.ambiguous_improvement_pp = float(ambiguous_improvement_pp)
        self.latency_p95_budget_sec = float(latency_p95_budget_sec)

    def check(
        self,
        *,
        think_on_holdout_score: float,
        think_off_holdout_score: float,
        ambiguous_think_on_score: float,
        ambiguous_think_off_score: float,
        ambiguous_sample_size: int,
        latency_p95_seconds: float,
    ) -> dict:
        """Run the three-AND gate. Returns the full metrics dict."""
        return _compute_think_ab_metrics(
            think_on_holdout_score=think_on_holdout_score,
            think_off_holdout_score=think_off_holdout_score,
            ambiguous_think_on_score=ambiguous_think_on_score,
            ambiguous_think_off_score=ambiguous_think_off_score,
            ambiguous_sample_size=ambiguous_sample_size,
            latency_p95_seconds=latency_p95_seconds,
            full_regression_tolerance_pp=self.full_regression_tolerance_pp,
            ambiguous_improvement_pp=self.ambiguous_improvement_pp,
            latency_p95_budget_sec=self.latency_p95_budget_sec,
        )


# ── Helper: per-example latency + reasoning-token sampler (D-17) ───────


def _percentile(values: list[float], p: float) -> float:
    """Compute p-th percentile (0..100) of values; 0.0 on empty."""
    if not values:
        return 0.0
    sorted_vals = sorted(values)
    n = len(sorted_vals)
    if n == 1:
        return float(sorted_vals[0])
    rank = (p / 100.0) * (n - 1)
    lo = int(rank)
    hi = min(lo + 1, n - 1)
    frac = rank - lo
    return float(sorted_vals[lo] * (1 - frac) + sorted_vals[hi] * frac)


def sample_latency_tokens(
    module: Any,
    examples: list,
    lm: Any,
) -> dict:
    """Per-example latency + reasoning-token sampler for think-on holdout (D-17).

    Wraps the module call with time.perf_counter and reads
    `pred.reasoning_tokens` (set by ToolModule.forward when enable_reasoning=True;
    defaults to 0 for think-off modules so callers can still pass a baseline
    module without crashes).

    Failed example calls (raised exception) are skipped — keep going so a
    single transient LM error does not tear down the whole sampling pass.
    Mirrors evolution/tools/v1_baseline_gate.py:184 _score_module_on_holdout
    error-handling style.

    Returns dict with:
        latency_seconds: list[float]
        reasoning_tokens: list[int]
        stats:
            latency_p50, latency_p95, latency_mean
            reasoning_token_p50, reasoning_token_p95, reasoning_token_mean
    """
    import dspy  # local import — keeps module importable in tests that skip dspy

    latencies: list[float] = []
    rtokens: list[int] = []

    ctx_mgr = dspy.context(lm=lm) if lm is not None else _NullCtx()
    with ctx_mgr:
        for ex in examples:
            # Defensive task extraction — examples may be dspy.Example or MagicMock
            try:
                task = ex.task_description
            except AttributeError:
                task = getattr(ex, "task_description", "")

            t0 = time.perf_counter()
            try:
                pred = module(task_description=task)
            except Exception:
                # Mirror _score_module_on_holdout: skip the example on LM error.
                continue
            t1 = time.perf_counter()

            latencies.append(t1 - t0)
            # reasoning_tokens may be missing on think-off Predictions — default 0.
            tokens = int(getattr(pred, "reasoning_tokens", 0) or 0)
            rtokens.append(tokens)

    stats = {
        "latency_p50": _percentile(latencies, 50.0),
        "latency_p95": _percentile(latencies, 95.0),
        "latency_mean": (statistics.fmean(latencies) if latencies else 0.0),
        "reasoning_token_p50": _percentile([float(t) for t in rtokens], 50.0),
        "reasoning_token_p95": _percentile([float(t) for t in rtokens], 95.0),
        "reasoning_token_mean": (statistics.fmean(rtokens) if rtokens else 0.0),
    }

    return {
        "latency_seconds": latencies,
        "reasoning_tokens": rtokens,
        "stats": stats,
    }


class _NullCtx:
    """No-op context manager when sample_latency_tokens called without LM."""

    def __enter__(self):
        return None

    def __exit__(self, *args):
        return False
```

⚠️ 注意事项:
- 不导入 `evolution.tools.tool_metric.joint_tool_param_metric` 或任何 GEPA-bound metric(Pitfall 12 守门 — `test_no_gepa_metric_added` 会扫描本模块)
- 不扩 EvolutionConfig(D-15)
- `_NullCtx` 类放文件末尾(避免循环引用)
- 所有公开 API 必须有 docstring(供 IDE 与 reviewer 阅读)
- `_percentile` 不依赖 numpy(避免新依赖)
  </action>
  <verify>
    <automated>/Users/slj/项目/hermes-agent-self-evolution/.venv/bin/python -m pytest /Users/slj/项目/hermes-agent-self-evolution/tests/tools/test_think_metrics.py -v --tb=short 2>&1 | tee /tmp/15-03-test.log | tail -60 && echo "--- summary ---" && grep -E "(passed|failed|error|PASSED|FAILED|ERROR)" /tmp/15-03-test.log | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `evolution/tools/think_metrics.py` 存在
    - `grep -q "DEFAULT_FULL_REGRESSION_TOLERANCE_PP = 2.0" evolution/tools/think_metrics.py` 命中(D-15)
    - `grep -q "DEFAULT_AMBIGUOUS_IMPROVEMENT_PP = 3.0" evolution/tools/think_metrics.py` 命中(D-15)
    - `grep -q "DEFAULT_LATENCY_P95_BUDGET_SEC = 5.0" evolution/tools/think_metrics.py` 命中(D-15)
    - `grep -q "AMBIGUOUS_SMALL_SAMPLE_THRESHOLD = 5" evolution/tools/think_metrics.py` 命中(D-16)
    - `grep -q "^class ThinkABGate" evolution/tools/think_metrics.py` 命中
    - `grep -q "^def check_think_ab_gate" evolution/tools/think_metrics.py` 命中
    - `grep -q "^def sample_latency_tokens" evolution/tools/think_metrics.py` 命中
    - `grep -q "from evolution.core.constraints import ConstraintResult" evolution/tools/think_metrics.py` 命中
    - `grep -q "constraint_name=\"think_ab_gate\"" evolution/tools/think_metrics.py` 命中
    - `grep -q "ambiguous_gate_skipped" evolution/tools/think_metrics.py` 命中(D-16)
    - `grep -q "time.perf_counter" evolution/tools/think_metrics.py` 命中(D-17)
    - `grep -q "dspy.context" evolution/tools/think_metrics.py` 命中(D-17)
    - `grep -v '^#' evolution/tools/think_metrics.py | grep -c 'joint_tool_param_metric'` == 0(Pitfall 12 守门 — 不引入 GEPA metric)
    - `.venv/bin/python -m pytest tests/tools/test_think_metrics.py -v` 全部 PASSED(15+ 测试 GREEN)
    - `.venv/bin/python -m pytest tests/tools/test_think_metrics.py::test_no_gepa_metric_added -x` PASSED(守门测试)
  </acceptance_criteria>
  <done>
    think_metrics.py 模块就位,包含 4 个模块级常量 + _compute_think_ab_metrics 内部函数 + check_think_ab_gate 函数 API + ThinkABGate 类 API + sample_latency_tokens helper + _percentile / _NullCtx 内部辅助。`pytest tests/tools/test_think_metrics.py -v` 15+ 测试全部 GREEN。Plan 04 CLI 现在可以 `from evolution.tools.think_metrics import ThinkABGate, sample_latency_tokens, DEFAULT_FULL_REGRESSION_TOLERANCE_PP, ...`。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| think_metrics 入参 → gate 判决 | 调用方传入的 score 浮点 / sample_size 整数若被恶意篡改可能误判 PASS;但 think_metrics 不是攻击面入口(CLI 层先做基本输入校验) |
| sample_latency_tokens 内调用 module(task_description=) | module 是 CLI 构造的 ToolModule;若调用方传错类型可能在 forward 中 raise |
| ConstraintResult.details (json str) → metrics.json | json.dumps(sort_keys=True) 保证可重现序列化,避免 dict iteration order 差异 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering | _compute_think_ab_metrics 浮点比较 | mitigate | round(..., 10) 与 v1_baseline_gate 一致;避免 1e-15 误判;parametrize 真值表测试覆盖 8 行 |
| T-15-03-02 | Info Disclosure | ConstraintResult.details json | accept | metrics 字段是数值与布尔,不含 PII;sort_keys=True 保证可 diff |
| T-15-03-03 | Elevation of Privilege | think_metrics 被错放进 GEPA 反思 metric 链 | mitigate | Pitfall 12 守门 — test_no_gepa_metric_added 扫描模块拒绝 5-param signature |
| T-15-03-04 | DoS | sample_latency_tokens 在 100k example 上跑 | accept | Phase 15 holdout 上限约 100 例(Phase 4 数据集设计);非生产路径;成本由 CostTracker 在 CLI 层守门 |
</threat_model>

<verification>
**Plan-level checks:**

1. **think_metrics.py 完整 API:** `python -c "from evolution.tools.think_metrics import ThinkABGate, check_think_ab_gate, sample_latency_tokens, DEFAULT_FULL_REGRESSION_TOLERANCE_PP, DEFAULT_AMBIGUOUS_IMPROVEMENT_PP, DEFAULT_LATENCY_P95_BUDGET_SEC, AMBIGUOUS_SMALL_SAMPLE_THRESHOLD; gate = ThinkABGate(); print('Wave 2 import: OK')"` 退出 0

2. **15+ 测试全 GREEN:** `pytest tests/tools/test_think_metrics.py -v` 输出 15+ PASSED,0 FAILED

3. **Pitfall 12 守门 GREEN:** `pytest tests/tools/test_think_metrics.py::test_no_gepa_metric_added -v` PASSED

4. **现有测试套件不退化:** `pytest tests/ -x --tb=short --ignore=tests/tools/test_evolve_tool_reasoning.py` 退出 0(Plan 03 完成后 evolve_tool_reasoning 仍待 Plan 04 实现,故跳过)

5. **D-14..D-17 覆盖确认:**
   - D-14 三重 AND → TestThreeGate.test_three_and_logic(parametrize 8 行)
   - D-15 默认阈值与常量 → grep 4 个 DEFAULT_* / AMBIGUOUS_SMALL_SAMPLE_THRESHOLD 命中
   - D-16 small-sample skip → TestThreeGate.test_small_sample_skip PASSED
   - D-17 latency / token sampler → TestSampler.test_emits_p50_p95_mean PASSED

6. **不扩 EvolutionConfig 守门(D-15):** `grep -c "EvolutionConfig" evolution/tools/think_metrics.py` ≤ 0(零引用)
</verification>

<success_criteria>
1. `evolution/tools/think_metrics.py` 创建,~150-200 LoC,包含完整双 API 实现
2. 4 个模块级常量(D-15):DEFAULT_FULL_REGRESSION_TOLERANCE_PP=2.0、DEFAULT_AMBIGUOUS_IMPROVEMENT_PP=3.0、DEFAULT_LATENCY_P95_BUDGET_SEC=5.0、AMBIGUOUS_SMALL_SAMPLE_THRESHOLD=5
3. 三重门 AND 逻辑(D-14)正确:full_regression / ambiguous_improvement / latency_p95 任一 fail → 总 passed=False
4. 小样本保护(D-16):ambiguous_sample_size < 5 → ambiguous_gate_skipped=True,该门视为 PASS
5. 函数式 API check_think_ab_gate 返回 ConstraintResult(constraint_name="think_ab_gate", details=sort_keys json)
6. 类式 API ThinkABGate.check 返回完整 metrics dict(含 gates 子字典)
7. sample_latency_tokens(module, examples, lm)(D-17):time.perf_counter 计时 + dspy.context(lm=) 包裹 + try/except skip + p50/p95/mean stats
8. Pitfall 12 守门 GREEN — test_no_gepa_metric_added PASSED
9. `pytest tests/tools/test_think_metrics.py -v` 全部 PASSED
10. EvolutionConfig 不扩(D-15)— `grep "EvolutionConfig" evolution/tools/think_metrics.py` 零命中
</success_criteria>

<output>
After completion, create `.planning/phases/15-think-augmented-tool-selection/15-03-SUMMARY.md` recording:
- think_metrics.py 最终 LoC
- pytest tests/tools/test_think_metrics.py -v 通过测试数与失败数
- Pitfall 12 守门测试 test_no_gepa_metric_added 结果
- 实际 stats 浮点计算的精度(避免 1e-15 误差导致 D-14 误判 — 已用 round(..., 10) 锁定)
- D-14..D-17 覆盖审计表
</output>
