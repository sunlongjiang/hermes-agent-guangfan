---
phase: 15
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - evolution/tools/evolve_tool_reasoning.py
autonomous: true
requirements:
  - TOOL-V2-03
must_haves:
  truths:
    - "D-06: A/B 双边固定为 think-off (no reasoning) baseline vs think-on evolved — 对照必须是不加 reasoning 的原 selector,这样才能归因于 reasoning 本身是否有贡献"
    - "新 CLI 入口 `python -m evolution.tools.evolve_tool_reasoning` 可运行(D-09)"
    - "CLI 同时构造 think-off baseline 和 think-on evolved 两个 ToolModule(D-07);GEPA 只 compile think-on(evolved)实例"
    - "holdout 上跑两次评估:think-off / think-on 各拿 full 与 ambiguous 子集分数(D-13 / D-14)"
    - "并行跑两道门:V1BaselineGate 跑 2 次(vs think-off + vs think-on);ThinkABGate 跑 1 次(think-on vs think-off)(D-10)"
    - "任一门 FAILED → `output/tools_reasoning/FAILED_<ts>/` + exit 1;CostBudgetExceeded → `output/tools_reasoning/ABORTED_<ts>/` + exit 2(D-10 / D-11)"
    - "成功路径输出 `output/tools_reasoning/<ts>/metrics.json` + `reasoning_prompt.txt` + `diff.txt` + `ab_comparison.json`(D-11)"
    - "metrics.json schema 完整(RESEARCH §6.2):think_on_score / think_off_score / ambiguous_* / reasoning_token_stats / latency_stats / think_ab_gate / v1_gate_passed / iterations / eval_model / cost_usd_spent"
    - "ab_comparison.json 是 JSON array,逐例含 task_id / selected_off / selected_on / is_ambiguous / reasoning_text_on / latency_seconds_off/on / reasoning_tokens_on(RESEARCH §6.3)"
    - "输出目录物理隔离:所有写盘 `output/tools_reasoning/`,绝不触达 `output/tools/`(D-11)"
    - "dry-run 早返 echo 完整 schema(D-09 / RESEARCH §6.4)"
    - "CLI 4 个新 flag:--reasoning-tokens-cap / --ab-tolerance-pp(别名 --full-regression-tolerance-pp)/ --ambiguous-improvement-pp / --latency-budget-sec / --ambiguous-only;复用 Phase 13 flag(D-12)"
  artifacts:
    - path: "evolution/tools/evolve_tool_reasoning.py"
      provides: "Click CLI + _evolve_impl + 16 步流水线 + 双 ToolModule + 双门跑动 + 4 个输出文件写盘 + FAILED_/ABORTED_/SUCCESS 三路径"
      contains: "@click.command"
  key_links:
    - from: "evolve_tool_reasoning.py CLI"
      to: "ToolModule(enable_reasoning=True)"
      via: "evolved_module = ToolModule(all_tools, enable_reasoning=True, eval_model=config.eval_model, lm_kwargs=config.get_lm_kwargs())"
      pattern: "ToolModule\\(.*enable_reasoning=True"
    - from: "evolve_tool_reasoning.py CLI"
      to: "ThinkABGate"
      via: "from evolution.tools.think_metrics import ThinkABGate, sample_latency_tokens; gate = ThinkABGate(...); gate.check(...)"
      pattern: "ThinkABGate"
    - from: "evolve_tool_reasoning.py CLI"
      to: "V1BaselineGate"
      via: "from evolution.tools.v1_baseline_gate import V1BaselineGate, compute_v1_baseline; v1_gate.check(...) × 2"
      pattern: "V1BaselineGate"
    - from: "evolve_tool_reasoning.py CLI"
      to: "output/tools_reasoning/"
      via: "Path('output') / 'tools_reasoning' / ts.mkdir(parents=True)"
      pattern: "output.*tools_reasoning"
---

<objective>
实现 Phase 15 端到端 CLI `python -m evolution.tools.evolve_tool_reasoning`:复用 Phase 13 `evolve_tool_params.py` 16 步流水线骨架,替换 step 3 / 11-14 以支持 think-off baseline + think-on evolved 双 ToolModule、双门(V1BaselineGate ×2 + ThinkABGate)并行跑动、4 个新输出文件(metrics.json / reasoning_prompt.txt / diff.txt / ab_comparison.json)。输出物理隔离到 `output/tools_reasoning/<ts>/`,不触达 `output/tools/`(D-11)。

Purpose:
- TOOL-V2-03 SC-3:A/B 比较显示 ambiguous 任务上的改进通过 ThinkABGate 三重门验证
- D-08 / D-10 / D-11 / D-12 落地:gate 隔离不污染 fitness/metric;双门并跑;输出独立目录;新增 4 flag
- 复用 Phase 13 全部基础设施(CostTracker / V1BaselineGate / GEPA / dspy.configure):不重写

Output:
- 新建 `evolution/tools/evolve_tool_reasoning.py`(~800-1000 LoC,镜像 evolve_tool_params.py)
- Plan 01 的 `tests/tools/test_evolve_tool_reasoning.py` 10 个集成测试中 9 个 GREEN(test_e2e_mock_pipeline 留给 Plan 05)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-think-augmented-tool-selection/15-CONTEXT.md
@.planning/phases/15-think-augmented-tool-selection/15-RESEARCH.md
@.planning/phases/15-think-augmented-tool-selection/15-PATTERNS.md
@.planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md
@.planning/phases/15-think-augmented-tool-selection/15-01-PLAN.md
@.planning/phases/15-think-augmented-tool-selection/15-02-PLAN.md
@.planning/phases/15-think-augmented-tool-selection/15-03-PLAN.md
@.planning/research/PITFALLS.md
@evolution/tools/evolve_tool_params.py
@evolution/tools/v1_baseline_gate.py
@evolution/tools/think_metrics.py
@evolution/tools/tool_module.py
</context>

<interfaces>
<!-- 复用的关键 API — 全部已在 Phase 13/14/15 Wave 1-2 落地 -->

From `evolution/tools/evolve_tool_params.py:741-751`(dry-run 早返模板):
```python
if dry_run:
    click.echo(f"param_predictors_discovered={num_predictors}")
    click.echo(f"tools_in_scope={len(all_tools)}")
    click.echo(f"iterations_planned={iterations}")
    click.echo(f"eval_source={eval_source}")
    click.echo(f"max_cost_usd_cap={config.max_cost_usd}")
    budget_estimate = iterations * max(50, 3 * num_predictors)
    click.echo(f"max_metric_calls_estimate={budget_estimate}")
    console.print("[bold green]DRY RUN — setup validated.[/bold green]")
    return 0
```

From `evolution/tools/evolve_tool_params.py:763-765`(LM configure):
```python
lm = dspy.LM(config.eval_model, **config.get_lm_kwargs())
dspy.configure(lm=lm, track_usage=True)
```

From `evolution/tools/evolve_tool_params.py:785-815`(CostTracker + GEPA pattern):
```python
tracker = CostTracker(max_usd=config.max_cost_usd)
final_spent_usd = 0.0
try:
    with tracker:
        stopper = _CostStopper(tracker)
        gepa_kwargs: dict[str, Any] = {"stop_callbacks": [stopper]}
        reflection_lm = dspy.LM(reflection_model_name, **config.get_lm_kwargs())
        optimizer_init_kwargs = {
            "metric": joint_tool_param_metric_with_feedback,
            "reflection_lm": reflection_lm,
            "component_selector": component_selector,
            "track_stats": True,
            "seed": 0,
            "gepa_kwargs": gepa_kwargs,
        }
        # ... max_metric_calls / auto
        optimizer = dspy.GEPA(**optimizer_init_kwargs)
        optimized_module = optimizer.compile(baseline_module, trainset=trainset, valset=valset)
        final_spent_usd = float(tracker.poll())
except CostBudgetExceeded:
    # → ABORTED_
```

From `evolution/tools/think_metrics.py`(Plan 03 创建):
```python
from evolution.tools.think_metrics import (
    ThinkABGate,
    check_think_ab_gate,
    sample_latency_tokens,
    DEFAULT_FULL_REGRESSION_TOLERANCE_PP,  # 2.0
    DEFAULT_AMBIGUOUS_IMPROVEMENT_PP,       # 3.0
    DEFAULT_LATENCY_P95_BUDGET_SEC,         # 5.0
    AMBIGUOUS_SMALL_SAMPLE_THRESHOLD,       # 5
)
```

From `evolution/tools/v1_baseline_gate.py`:
```python
from evolution.tools.v1_baseline_gate import V1BaselineGate, compute_v1_baseline
# v1_gate.check(evolved_score=, baseline=v1_info_dict) -> dict
```

From `evolution/tools/tool_module.py`(Plan 02 修改):
```python
from evolution.tools.tool_module import ToolModule
# baseline_module = ToolModule(all_tools, enable_reasoning=False)
# evolved_module = ToolModule(all_tools, enable_reasoning=True, eval_model=..., lm_kwargs=...)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 evolution/tools/evolve_tool_reasoning.py — Click CLI + flags(D-12)+ dry-run + 16 步流水线 + 双 ToolModule + 双门跑动 + 4 输出文件</name>
  <files>evolution/tools/evolve_tool_reasoning.py</files>
  <read_first>
    - evolution/tools/evolve_tool_params.py(1-1133 全文 — Phase 13 CLI 模板)
    - evolution/tools/v1_baseline_gate.py(V1BaselineGate + compute_v1_baseline + _score_module_on_holdout)
    - evolution/tools/think_metrics.py(Plan 03 产物)
    - evolution/tools/tool_module.py(Plan 02 enable_reasoning kwarg)
    - .planning/phases/15-think-augmented-tool-selection/15-RESEARCH.md §6 + §7 Wave 3(完整 schema 与 step 替换列表)
    - .planning/phases/15-think-augmented-tool-selection/15-PATTERNS.md §3(完整 Deviations 列表)
    - tests/tools/test_evolve_tool_reasoning.py(Plan 01 创建 — 10 个 RED 集成测试是本任务的 GREEN 目标)
  </read_first>
  <behavior>
    CLI 命令:`python -m evolution.tools.evolve_tool_reasoning [FLAGS]`,Click 入口名 `evolve` 或 `main`(测试 patches by name)。

    Flags(D-12,RESEARCH §6.4):
    - **新增 4 个 Phase 15-only:**
      - `--reasoning-tokens-cap INT` 默认 200(警告:目前 reasoner LM-level max_tokens 在 ToolModule.__init__ 内硬编码 200,该 flag 仅用于显示与未来扩展;不动态改 reasoner LM 配置 — 见 Plan 02)
      - `--ab-tolerance-pp FLOAT` 默认 2.0(full regression tolerance)
      - `--ambiguous-improvement-pp FLOAT` 默认 3.0
      - `--latency-budget-sec FLOAT` 默认 5.0
      - `--ambiguous-only / --no-ambiguous-only` 默认 False(仅评估 ambiguous 子集,跳过全集)
    - **复用 Phase 13:** `--eval-source` / `--tools` / `--dry-run` / `--max-cost-usd` / `--baseline-run` / `--reflection-model` / `--iterations` / `--auto` / `--allow-miprov2-fallback` / `--component-selector` / `--hermes-repo` / `--session-source`(若 Phase 14 提供)

    16 步流水线(复刻 evolve_tool_params.py,替换 step 3 / 11-14):
    1. Load config / resolve hermes_agent_path
    2. Discover + extract + filter tools
    3. **NEW**: Build **2 个** ToolModule:`baseline_module = ToolModule(all_tools, enable_reasoning=False)`、`evolved_module = ToolModule(all_tools, enable_reasoning=True, eval_model=config.eval_model, lm_kwargs=config.get_lm_kwargs())`
    4. Dry-run early return(echo Phase 15 schema 全字段)
    5. Load dataset(reuse `_load_dataset` from Phase 13;但 import 失败时 raise — 见 Implementation note)
    6. Configure dspy.LM(eval_model, track_usage=True)
    7. Resolve reflection_model
    8. GEPA setup(reflection_lm, max_metric_calls=iterations*max(50, 3*num_predictors), `metric=joint_tool_param_metric_with_feedback`)
    9. CostTracker 包裹 GEPA `optimizer.compile(evolved_module, trainset=, valset=)`(只 compile think-on)
    10. CostBudgetExceeded → ABORTED_<ts>/(D-10)
    11. **REPLACED**: Holdout 评估**两次**:
        - `th_off_full = _score_module_on_holdout(baseline_module, holdout, lm)`(think-off)
        - `th_on_full = _score_module_on_holdout(optimized_module, holdout, lm)`(think-on,使用 GEPA 编译产物)
        - Ambiguous 子集 = `[ex for ex in holdout if len(ex.confuser_tools or []) >= 2]`
        - `th_off_ambig = _score_module_on_holdout(baseline_module, ambiguous_subset, lm)`
        - `th_on_ambig = _score_module_on_holdout(optimized_module, ambiguous_subset, lm)`
        - `sample_latency_tokens(optimized_module, holdout, lm)` → 拿 latency_stats + reasoning_token_stats
    12. **REPLACED**: 双门跑动:
        - `v1_info = compute_v1_baseline(baseline_run, baseline_module=baseline_module, holdout=holdout, lm=lm)`
        - `v1_gate = V1BaselineGate(tolerance=ab_tolerance_pp/100.0)`
        - `v1_off_metrics = v1_gate.check(evolved_score=th_off_full, baseline=v1_info)` → 期望 PASS
        - `v1_on_metrics = v1_gate.check(evolved_score=th_on_full, baseline=v1_info)` → 期望 PASS
        - `think_gate = ThinkABGate(full_regression_tolerance_pp=ab_tolerance_pp, ambiguous_improvement_pp=ambiguous_improvement_pp, latency_p95_budget_sec=latency_budget_sec)`
        - `think_ab_metrics = think_gate.check(think_on_holdout_score=th_on_full, think_off_holdout_score=th_off_full, ambiguous_think_on_score=th_on_ambig, ambiguous_think_off_score=th_off_ambig, ambiguous_sample_size=len(ambiguous_subset), latency_p95_seconds=latency_stats["latency_p95"])`
    13. **REPLACED**: 任一门 FAILED → FAILED_<ts>/:status="V1_OFF_FAILED" / "V1_ON_FAILED" / "THINK_AB_FAILED"
    14. **REPLACED**: SUCCESS 路径写盘 4 文件到 `output/tools_reasoning/<ts>/`:
        - `metrics.json`(完整 schema — RESEARCH §6.2)
        - `reasoning_prompt.txt`(evolved_module.reasoner.signature.instructions plain text)
        - `diff.txt`(unified diff baseline → evolved instructions,使用 `difflib.unified_diff`)
        - `ab_comparison.json`(JSON array per-example — RESEARCH §6.3)
    15. Rich Table / Panel 输出 PASS/FAIL summary
    16. Return 0(SUCCESS)/ 1(FAILED)/ 2(ABORTED)

    Implementation notes:
    - 复用 Phase 13 的 helper 函数:
      - `_load_tool_descriptions` / `_filter_tools` / `_load_dataset` — 直接从 `evolution.tools.evolve_tool_params` import(避免 dup;但若 Phase 13 helpers 是私有 `_` 前缀,需在新 CLI 重新声明本地 wrapper)
      - 推荐方案:**在新 CLI 顶部 `from evolution.tools.evolve_tool_params import _load_tool_descriptions, _filter_tools, _load_dataset, _CostStopper, _write_aborted_dir`**(以下划线开头但仍可 import;若 Phase 13 重构这些 helper 为 public,会更稳但本 phase 不做)
      - 或在新 CLI 本地写极简版 wrapper:`def _load_tool_descriptions(repo): from evolution.tools.tool_loader import extract_tool_descriptions; return extract_tool_descriptions(repo)`
    - `_score_module_on_holdout`:可从 `evolution.tools.v1_baseline_gate` import(私有 `_` 但 Plan 03 test_sampler 已先用同款模式 — 见 evolve_tool_params._evaluate_holdout 也可 import)
    - `_evolve_impl` 命名沿用 Phase 13(测试 patch 的 module path 已对齐)
    - 测试 patch 路径(Plan 01 的 test_evolve_tool_reasoning.py 用):`evolution.tools.evolve_tool_reasoning._load_tool_descriptions`、`._load_dataset`、`.dspy.GEPA`、`.dspy.LM`、`.V1BaselineGate`、`.ThinkABGate`、`.ToolModule` — 这些**必须**在新模块顶层是直接可见的(`from X import Y` 而非 `import X`,patch 才能命中)

    输出文件细节:

    **metrics.json schema(RESEARCH §6.2,完整):**
    ```json
    {
      "timestamp": "YYYYMMDD_HHMMSS",
      "started_at": "ISO 8601 with timezone",
      "status": "SUCCESS" | "V1_OFF_FAILED" | "V1_ON_FAILED" | "THINK_AB_FAILED" | "ABORTED",
      "iterations": int,
      "eval_model": str,
      "optimizer_used": "gepa" | "miprov2",
      "reflection_model": str,
      "cost_usd_spent": float,
      "cost_usd_cap": float,
      "tool_count": int,
      "param_predictors_discovered": int,
      "train_examples": int,
      "val_examples": int,
      "holdout_examples": int,
      "ambiguous_subset_size": int,
      "ambiguous_gate_skipped": bool,
      "elapsed_seconds": float,
      "think_off_score": float,
      "think_on_score": float,
      "v1_score": float,
      "ambiguous_think_on": float,
      "ambiguous_think_off": float,
      "reasoning_token_stats": {"p50": ..., "p95": ..., "mean": ...},
      "latency_stats": {"p50": ..., "p95": ..., "mean": ...},
      "v1_baseline_holdout": float,
      "v1_baseline_source": str,
      "v1_gate_delta_think_off": float,
      "v1_gate_delta_think_on": float,
      "v1_gate_passed": bool,
      "think_ab_gate": {
        "passed": bool,
        "full_regression_delta": float,
        "ambiguous_delta": float,
        "latency_p95_seconds": float,
        "ambiguous_sample_size": int,
        "ambiguous_gate_skipped": bool,
        "tolerances": {...},
        "gates": {...},
        "message": str
      }
    }
    ```

    **ab_comparison.json schema(RESEARCH §6.3,JSON array):**
    每条:`{task_id (int), task_description (str), correct_tool (str), selected_off (str), selected_on (str), is_correct_off (bool), is_correct_on (bool), is_ambiguous (bool), confuser_tools (list[str]), reasoning_text_on (str), reasoning_tokens_on (int), latency_seconds_off (float), latency_seconds_on (float)}`

    **reasoning_prompt.txt:** `evolved_module.reasoner.signature.instructions` 原 plain text(可能含 \n);若 reasoner None(理论不可能,enable_reasoning=True 才走到此),写 "(no reasoner constructed)"。

    **diff.txt:** `difflib.unified_diff(baseline_instructions.splitlines(keepends=True), evolved_instructions.splitlines(keepends=True), fromfile='baseline_reasoning_prompt.txt', tofile='evolved_reasoning_prompt.txt')`。baseline_instructions 是 `ToolReasoningSignature.__doc__`(原始 docstring);evolved_instructions 是 `evolved_module.reasoner.signature.instructions`(GEPA 编译后)。
  </behavior>
  <action>
新建文件 `evolution/tools/evolve_tool_reasoning.py`。完整代码骨架(实现 16 步流水线):

```python
"""Phase 15 think-augmented tool selection CLI (D-09..D-12).

Entry point: python -m evolution.tools.evolve_tool_reasoning

Architecture (RESEARCH §7 Wave 3):
- Reuses evolve_tool_params.py's 16-step pipeline skeleton, replacing
  steps 3 / 11-14 to:
    * Construct TWO ToolModules (baseline enable_reasoning=False + evolved=True).
    * Compile only the evolved (think-on) module via GEPA.
    * Score holdout TWICE (think-off + think-on), plus an ambiguous subset cut
      (D-13: confuser_tools length >= 2).
    * Sample per-call latency + reasoning tokens on think-on holdout (D-17).
    * Run TWO gates: V1BaselineGate (think-off vs v1 + think-on vs v1) AND
      ThinkABGate (three-AND on full regression + ambiguous improvement + latency).
    * Write 4 output files to output/tools_reasoning/<ts>/ (NEVER output/tools/).

Outputs:
    output/tools_reasoning/<ts>/                  # SUCCESS
        metrics.json
        reasoning_prompt.txt
        diff.txt
        ab_comparison.json
    output/tools_reasoning/FAILED_<ts>/           # constraint / regression / gate failed
    output/tools_reasoning/ABORTED_<ts>/          # cost cap exceeded
"""

from __future__ import annotations

import difflib
import json
import sys
import time
from datetime import datetime, timezone
from pathlib import Path
from typing import Any, Optional

import click
import dspy
from rich.console import Console
from rich.panel import Panel
from rich.table import Table

from evolution.core.config import EvolutionConfig, get_hermes_agent_path
from evolution.core.cost_tracker import CostBudgetExceeded, CostTracker
from evolution.tools.tool_metric import joint_tool_param_metric_with_feedback
from evolution.tools.tool_module import ToolModule, ToolReasoningSignature
from evolution.tools.v1_baseline_gate import (
    V1BaselineGate,
    compute_v1_baseline,
    _score_module_on_holdout,
)
from evolution.tools.think_metrics import (
    AMBIGUOUS_SMALL_SAMPLE_THRESHOLD,
    DEFAULT_AMBIGUOUS_IMPROVEMENT_PP,
    DEFAULT_FULL_REGRESSION_TOLERANCE_PP,
    DEFAULT_LATENCY_P95_BUDGET_SEC,
    ThinkABGate,
    sample_latency_tokens,
)

# Phase 13 helper reuse — these are underscore-private but stable.
from evolution.tools.evolve_tool_params import (
    _load_tool_descriptions,
    _filter_tools,
    _load_dataset,
    _CostStopper,
)

console = Console()


OUTPUT_ROOT = Path("output") / "tools_reasoning"  # D-11: physical isolation


# ── CLI ────────────────────────────────────────────────────────────────


@click.command(name="evolve")
@click.option("--iterations", type=int, default=10, show_default=True,
              help="GEPA iterations (max_metric_calls = iterations * max(50, 3*num_predictors))")
@click.option("--eval-source",
              type=click.Choice(["load", "synthetic", "external", "sessiondb"]),
              default="load", show_default=True,
              help="Dataset source (Phase 4/14 patterns)")
@click.option("--tools", type=str, default=None,
              help="Comma-separated tool filter; empty = all discovered tools")
@click.option("--hermes-repo", type=click.Path(file_okay=False, dir_okay=True), default=None,
              help="Override HERMES_AGENT_REPO env var")
@click.option("--dry-run", is_flag=True, default=False,
              help="Echo setup schema and exit 0 without running optimization")
@click.option("--max-cost-usd", type=float, default=None,
              help="Hard cost cap (USD); abort if exceeded (D-10)")
@click.option("--baseline-run", type=click.Path(exists=False, file_okay=False, dir_okay=True),
              default=None,
              help="Path to a Phase 5 / Phase 13 output dir; reads v1_baseline_holdout")
@click.option("--reflection-model", type=str, default=None,
              help="Override config.reflection_model for GEPA reflection LM")
@click.option("--auto", type=click.Choice(["light", "medium", "heavy"]), default=None,
              help="GEPA auto budget mode (mutually exclusive with --iterations)")
@click.option("--allow-miprov2-fallback", is_flag=True, default=False,
              help="Fall back to MIPROv2 if GEPA fails (loud by default — opt-in here)")
@click.option("--component-selector",
              type=click.Choice(["round_robin", "all", "random"]),
              default="round_robin", show_default=True)
@click.option("--session-source", type=click.Path(exists=False), default=None,
              help="Phase 14 sessiondb path (optional)")
# Phase 15 new flags (D-12)
@click.option("--reasoning-tokens-cap", type=int, default=200, show_default=True,
              help="Reasoning output token cap (display-only; ToolModule enforces internally at LM level)")
@click.option("--ab-tolerance-pp", "--full-regression-tolerance-pp", "ab_tolerance_pp",
              type=float, default=DEFAULT_FULL_REGRESSION_TOLERANCE_PP, show_default=True,
              help="Full regression tolerance (pp); think-on full ≥ think-off - tolerance")
@click.option("--ambiguous-improvement-pp", type=float,
              default=DEFAULT_AMBIGUOUS_IMPROVEMENT_PP, show_default=True,
              help="Required ambiguous-subset improvement (pp)")
@click.option("--latency-budget-sec", type=float,
              default=DEFAULT_LATENCY_P95_BUDGET_SEC, show_default=True,
              help="Latency p95 budget (sec); think-on p95 ≤ budget")
@click.option("--ambiguous-only", is_flag=True, default=False,
              help="Evaluate only on ambiguous subset (skip full holdout)")
def evolve(
    iterations: int,
    eval_source: str,
    tools: Optional[str],
    hermes_repo: Optional[str],
    dry_run: bool,
    max_cost_usd: Optional[float],
    baseline_run: Optional[str],
    reflection_model: Optional[str],
    auto: Optional[str],
    allow_miprov2_fallback: bool,
    component_selector: str,
    session_source: Optional[str],
    reasoning_tokens_cap: int,
    ab_tolerance_pp: float,
    ambiguous_improvement_pp: float,
    latency_budget_sec: float,
    ambiguous_only: bool,
) -> int:
    """Phase 15: think-augmented tool selection — A/B optimization + dual gates."""
    return _evolve_impl(
        iterations=iterations,
        eval_source=eval_source,
        tools_filter=tools,
        hermes_repo=hermes_repo,
        dry_run=dry_run,
        max_cost_usd=max_cost_usd,
        baseline_run=baseline_run,
        reflection_model=reflection_model,
        auto=auto,
        allow_miprov2_fallback=allow_miprov2_fallback,
        component_selector=component_selector,
        session_source=session_source,
        reasoning_tokens_cap=reasoning_tokens_cap,
        ab_tolerance_pp=ab_tolerance_pp,
        ambiguous_improvement_pp=ambiguous_improvement_pp,
        latency_budget_sec=latency_budget_sec,
        ambiguous_only=ambiguous_only,
    )


def _evolve_impl(
    *,
    iterations: int,
    eval_source: str,
    tools_filter: Optional[str],
    hermes_repo: Optional[str],
    dry_run: bool,
    max_cost_usd: Optional[float],
    baseline_run: Optional[str],
    reflection_model: Optional[str],
    auto: Optional[str],
    allow_miprov2_fallback: bool,
    component_selector: str,
    session_source: Optional[str],
    reasoning_tokens_cap: int,
    ab_tolerance_pp: float,
    ambiguous_improvement_pp: float,
    latency_budget_sec: float,
    ambiguous_only: bool,
) -> int:
    started_at = datetime.now(timezone.utc).isoformat()
    t_start = time.perf_counter()

    # ── 1. Config ──────────────────────────────────────────────────────
    config = EvolutionConfig(
        hermes_agent_path=Path(hermes_repo) if hermes_repo else get_hermes_agent_path(),
        max_cost_usd=max_cost_usd if max_cost_usd is not None else 20.0,
        reflection_model=reflection_model,
    )

    # ── 2. Discover ─────────────────────────────────────────────────────
    try:
        all_tools = _load_tool_descriptions(config.hermes_agent_path)
    except FileNotFoundError as e:
        console.print(f"[red]{e}[/red]")
        return 1
    if not all_tools:
        click.echo("param_predictors_discovered=0 (no tools available)", err=True)
        return 1
    all_tools = _filter_tools(all_tools, tools_filter)

    # ── 3. Build TWO ToolModules (Phase 15 specific) ───────────────────
    original_tools = list(all_tools)
    baseline_module = ToolModule(all_tools, enable_reasoning=False)
    evolved_module = ToolModule(
        all_tools,
        enable_reasoning=True,
        eval_model=config.eval_model,
        lm_kwargs=config.get_lm_kwargs(),
    )
    num_predictors = len(list(evolved_module.named_predictors()))

    # ── 4. Load dataset (early — need ambiguous subset size for dry-run) ──
    if not dry_run:
        trainset, valset, holdout = _load_dataset(
            eval_source, config, all_tools, session_source=session_source,
        )
    else:
        # Dry-run: still try loading so we can echo ambiguous_subset_size
        try:
            trainset, valset, holdout = _load_dataset(
                eval_source, config, all_tools, session_source=session_source,
            )
        except Exception:
            trainset, valset, holdout = [], [], []

    ambiguous_subset = [
        ex for ex in holdout
        if len(getattr(ex, "confuser_tools", []) or []) >= 2
    ]
    ambiguous_skipped = len(ambiguous_subset) < AMBIGUOUS_SMALL_SAMPLE_THRESHOLD

    # ── 5. Dry-run early return (D-09 / RESEARCH §6.4) ──────────────────
    if dry_run:
        click.echo(f"param_predictors_discovered={num_predictors}")
        click.echo(f"tools_in_scope={len(all_tools)}")
        click.echo(f"holdout_size={len(holdout)}")
        click.echo(f"ambiguous_subset_size={len(ambiguous_subset)}")
        click.echo(f"ambiguous_gate_skipped={str(ambiguous_skipped).lower()}")
        click.echo(f"reasoning_tokens_cap={reasoning_tokens_cap}")
        click.echo(f"latency_p95_budget_sec={latency_budget_sec}")
        click.echo(f"ab_tolerance_pp={ab_tolerance_pp}")
        click.echo(f"ambiguous_improvement_pp={ambiguous_improvement_pp}")
        click.echo(f"full_regression_tolerance_pp={ab_tolerance_pp}")
        click.echo(f"iterations_planned={iterations}")
        click.echo(f"eval_source={eval_source}")
        click.echo(f"max_cost_usd_cap={config.max_cost_usd}")
        click.echo(f"max_metric_calls_estimate={iterations * max(50, 3 * num_predictors)}")
        console.print("[bold green]DRY RUN — setup validated.[/bold green]")
        return 0

    if not trainset and not valset and not holdout:
        console.print("[red]Empty dataset — cannot run optimization.[/red]")
        return 1

    # ── 6. Configure LM ─────────────────────────────────────────────────
    lm = dspy.LM(config.eval_model, **config.get_lm_kwargs())
    dspy.configure(lm=lm, track_usage=True)

    # ── 7. Resolve reflection model ─────────────────────────────────────
    reflection_model_name = config.reflection_model or config.optimizer_model

    # ── 8/9. GEPA + CostTracker (compile only EVOLVED module) ──────────
    tracker = CostTracker(max_usd=config.max_cost_usd)
    optimizer_used = "gepa"
    final_spent_usd = 0.0
    optimized_module: Any = None

    try:
        with tracker:
            stopper = _CostStopper(tracker)
            reflection_lm = dspy.LM(reflection_model_name, **config.get_lm_kwargs())
            gepa_kwargs: dict[str, Any] = {"stop_callbacks": [stopper]}
            optimizer_init: dict[str, Any] = {
                "metric": joint_tool_param_metric_with_feedback,
                "reflection_lm": reflection_lm,
                "component_selector": component_selector,
                "track_stats": True,
                "seed": 0,
                "gepa_kwargs": gepa_kwargs,
            }
            if auto is not None:
                optimizer_init["auto"] = auto
            else:
                optimizer_init["max_metric_calls"] = max(
                    iterations * 50, 3 * num_predictors
                )
            optimizer = dspy.GEPA(**optimizer_init)
            optimized_module = optimizer.compile(
                evolved_module, trainset=trainset, valset=valset,
            )
            final_spent_usd = float(tracker.poll())
    except CostBudgetExceeded as cap_exc:
        abort_dir = _write_aborted_dir(
            tracker=tracker, evolved_module=evolved_module, cap_exc=cap_exc,
            reflection_model_name=reflection_model_name, started_at=started_at,
        )
        console.print(f"[red]Cost budget exceeded — aborted.[/red] Wrote {abort_dir}/")
        return 2
    except Exception as gepa_err:
        if not allow_miprov2_fallback:
            console.print(f"[red]GEPA compile failed (loud-by-default).[/red] {gepa_err}")
            failed_dir = _write_failed_dir(
                status="GEPA_FAILED", baseline_module=baseline_module,
                evolved_module=evolved_module, optimized_module=None,
                started_at=started_at, holdout=holdout, ambiguous_subset=ambiguous_subset,
                lm=lm, ab_tolerance_pp=ab_tolerance_pp,
                ambiguous_improvement_pp=ambiguous_improvement_pp,
                latency_budget_sec=latency_budget_sec,
                config=config, iterations=iterations,
                num_predictors=num_predictors, optimizer_used="gepa",
                error_msg=str(gepa_err), trainset=trainset, valset=valset,
            )
            return 1
        else:
            # MIPROv2 fallback path (Phase 13 pattern — out of scope here)
            optimizer_used = "miprov2"
            optimized_module = evolved_module  # naive fallback: no opt
            console.print("[yellow]Falling back to baseline (no optimization).[/yellow]")

    if optimized_module is None:
        optimized_module = evolved_module

    # ── 10. Holdout evaluation × 2 (think-off + think-on) ──────────────
    eval_holdout = ambiguous_subset if ambiguous_only else holdout
    th_off_full = float(_score_module_on_holdout(baseline_module, eval_holdout, lm))
    th_on_full = float(_score_module_on_holdout(optimized_module, eval_holdout, lm))
    th_off_ambig = float(_score_module_on_holdout(baseline_module, ambiguous_subset, lm))
    th_on_ambig = float(_score_module_on_holdout(optimized_module, ambiguous_subset, lm))

    # ── 11. Sample latency + reasoning tokens on think-on holdout ──────
    sampling = sample_latency_tokens(optimized_module, eval_holdout, lm)
    latency_stats = sampling["stats"]

    # ── 12. V1 baseline gate (D-10: ×2 — think-off + think-on) ─────────
    v1_info = compute_v1_baseline(
        baseline_run=baseline_run, baseline_module=baseline_module,
        holdout=holdout, lm=lm,
    )
    v1_gate = V1BaselineGate(tolerance=ab_tolerance_pp / 100.0)
    v1_off_metrics = v1_gate.check(evolved_score=th_off_full, baseline=v1_info)
    v1_on_metrics = v1_gate.check(evolved_score=th_on_full, baseline=v1_info)

    # ── 13. ThinkABGate (D-14) ─────────────────────────────────────────
    think_gate = ThinkABGate(
        full_regression_tolerance_pp=ab_tolerance_pp,
        ambiguous_improvement_pp=ambiguous_improvement_pp,
        latency_p95_budget_sec=latency_budget_sec,
    )
    think_ab_metrics = think_gate.check(
        think_on_holdout_score=th_on_full,
        think_off_holdout_score=th_off_full,
        ambiguous_think_on_score=th_on_ambig,
        ambiguous_think_off_score=th_off_ambig,
        ambiguous_sample_size=len(ambiguous_subset),
        latency_p95_seconds=float(latency_stats.get("latency_p95", 0.0)),
    )

    # ── 14. Determine final status ─────────────────────────────────────
    failed_reason: Optional[str] = None
    if not v1_off_metrics.get("passed", True):
        failed_reason = "V1_OFF_FAILED"
    elif not v1_on_metrics.get("passed", True):
        failed_reason = "V1_ON_FAILED"
    elif not think_ab_metrics.get("passed", True):
        failed_reason = "THINK_AB_FAILED"

    # ── 15. Assemble metrics.json ──────────────────────────────────────
    elapsed = time.perf_counter() - t_start
    ts = datetime.now().strftime("%Y%m%d_%H%M%S")
    metrics = {
        "timestamp": ts,
        "started_at": started_at,
        "status": failed_reason or "SUCCESS",
        "iterations": iterations,
        "eval_model": config.eval_model,
        "optimizer_used": optimizer_used,
        "reflection_model": reflection_model_name,
        "cost_usd_spent": final_spent_usd,
        "cost_usd_cap": float(config.max_cost_usd or 0.0),
        "tool_count": len(all_tools),
        "param_predictors_discovered": num_predictors,
        "train_examples": len(trainset),
        "val_examples": len(valset),
        "holdout_examples": len(holdout),
        "ambiguous_subset_size": len(ambiguous_subset),
        "ambiguous_gate_skipped": ambiguous_skipped,
        "elapsed_seconds": round(elapsed, 3),
        "think_off_score": th_off_full,
        "think_on_score": th_on_full,
        "v1_score": float(v1_info.get("v1_baseline_holdout", 0.0)),
        "ambiguous_think_on": th_on_ambig,
        "ambiguous_think_off": th_off_ambig,
        "reasoning_token_stats": {
            "p50": latency_stats.get("reasoning_token_p50", 0.0),
            "p95": latency_stats.get("reasoning_token_p95", 0.0),
            "mean": latency_stats.get("reasoning_token_mean", 0.0),
        },
        "latency_stats": {
            "p50": latency_stats.get("latency_p50", 0.0),
            "p95": latency_stats.get("latency_p95", 0.0),
            "mean": latency_stats.get("latency_mean", 0.0),
        },
        "v1_baseline_holdout": float(v1_info.get("v1_baseline_holdout", 0.0)),
        "v1_baseline_source": str(v1_info.get("v1_baseline_source", "unknown")),
        "v1_gate_delta_think_off": float(v1_off_metrics.get("delta", 0.0)),
        "v1_gate_delta_think_on": float(v1_on_metrics.get("delta", 0.0)),
        "v1_gate_passed": (
            bool(v1_off_metrics.get("passed", False))
            and bool(v1_on_metrics.get("passed", False))
        ),
        "think_ab_gate": think_ab_metrics,
    }

    # ── 16. Build ab_comparison.json (per-example) ─────────────────────
    ab_comparison = _build_ab_comparison(
        holdout=eval_holdout,
        baseline_module=baseline_module,
        optimized_module=optimized_module,
        lm=lm,
        sampling=sampling,
    )

    # ── 17. Write output dir ───────────────────────────────────────────
    if failed_reason:
        out_dir = OUTPUT_ROOT / f"FAILED_{ts}"
    else:
        out_dir = OUTPUT_ROOT / ts
    out_dir.mkdir(parents=True, exist_ok=True)

    _write_metrics(out_dir, metrics)
    _write_reasoning_prompt(out_dir, optimized_module)
    _write_diff(out_dir, baseline_instructions=ToolReasoningSignature.__doc__ or "",
                evolved_module=optimized_module)
    _write_ab_comparison(out_dir, ab_comparison)

    # ── 18. Final console summary ──────────────────────────────────────
    if failed_reason:
        console.print(Panel(
            f"[red]{failed_reason}[/red] — see {out_dir}/metrics.json",
            title="Phase 15 Run", border_style="red",
        ))
        return 1

    table = Table(title="Phase 15 — Think-Augmented A/B Result")
    table.add_column("Metric", style="bold")
    table.add_column("Value")
    table.add_row("think_off_score (full)", f"{th_off_full:.4f}")
    table.add_row("think_on_score  (full)", f"{th_on_full:.4f}")
    table.add_row("ambiguous_off", f"{th_off_ambig:.4f} (n={len(ambiguous_subset)})")
    table.add_row("ambiguous_on", f"{th_on_ambig:.4f}")
    table.add_row("latency_p95", f"{latency_stats.get('latency_p95', 0):.3f}s")
    table.add_row("ThinkABGate", "PASS" if think_ab_metrics.get("passed") else "FAIL")
    table.add_row("V1BaselineGate", "PASS" if metrics["v1_gate_passed"] else "FAIL")
    console.print(table)
    console.print(f"[bold green]Wrote {out_dir}/[/bold green]")
    return 0


# ── Output helpers ─────────────────────────────────────────────────────


def _write_metrics(out_dir: Path, metrics: dict) -> None:
    (out_dir / "metrics.json").write_text(
        json.dumps(metrics, indent=2, ensure_ascii=False, sort_keys=True),
        encoding="utf-8",
    )


def _write_reasoning_prompt(out_dir: Path, evolved_module: Any) -> None:
    """Write evolved reasoner.signature.instructions to plain text file (D-11)."""
    if evolved_module is None or getattr(evolved_module, "reasoner", None) is None:
        text = "(no reasoner constructed)"
    else:
        text = str(evolved_module.reasoner.signature.instructions or "")
    (out_dir / "reasoning_prompt.txt").write_text(text, encoding="utf-8")


def _write_diff(out_dir: Path, baseline_instructions: str, evolved_module: Any) -> None:
    """Unified diff between baseline (original docstring) and evolved instructions."""
    evolved_instructions = ""
    if evolved_module is not None and getattr(evolved_module, "reasoner", None) is not None:
        evolved_instructions = str(evolved_module.reasoner.signature.instructions or "")
    diff_lines = difflib.unified_diff(
        (baseline_instructions or "").splitlines(keepends=True),
        evolved_instructions.splitlines(keepends=True),
        fromfile="baseline_reasoning_prompt.txt",
        tofile="evolved_reasoning_prompt.txt",
        lineterm="",
    )
    diff_text = "".join(diff_lines) or "(no diff — instructions unchanged)\n"
    (out_dir / "diff.txt").write_text(diff_text, encoding="utf-8")


def _write_ab_comparison(out_dir: Path, ab_data: list[dict]) -> None:
    (out_dir / "ab_comparison.json").write_text(
        json.dumps(ab_data, indent=2, ensure_ascii=False),
        encoding="utf-8",
    )


def _build_ab_comparison(
    *,
    holdout: list,
    baseline_module: Any,
    optimized_module: Any,
    lm: Any,
    sampling: dict,
) -> list[dict]:
    """Per-example A/B comparison (D-11, RESEARCH §6.3)."""
    rows: list[dict] = []
    latencies_on = sampling.get("latency_seconds", [])
    reasoning_tokens = sampling.get("reasoning_tokens", [])
    for i, ex in enumerate(holdout):
        task = getattr(ex, "task_description", "")
        correct = str(getattr(ex, "correct_tool", ""))
        confuser_tools = list(getattr(ex, "confuser_tools", []) or [])
        is_ambig = len(confuser_tools) >= 2

        t0 = time.perf_counter()
        try:
            with dspy.context(lm=lm):
                pred_off = baseline_module(task_description=task)
        except Exception:
            pred_off = None
        t_off = time.perf_counter() - t0

        try:
            with dspy.context(lm=lm):
                pred_on = optimized_module(task_description=task)
        except Exception:
            pred_on = None

        sel_off = str(getattr(pred_off, "selected_tool", "") or "")
        sel_on = str(getattr(pred_on, "selected_tool", "") or "")
        reasoning_text = str(getattr(pred_on, "reasoning", "") or "")
        reasoning_tokens_on = int(getattr(pred_on, "reasoning_tokens", 0) or 0)

        rows.append({
            "task_id": i,
            "task_description": str(task),
            "correct_tool": correct,
            "selected_off": sel_off,
            "selected_on": sel_on,
            "is_correct_off": sel_off == correct,
            "is_correct_on": sel_on == correct,
            "is_ambiguous": is_ambig,
            "confuser_tools": confuser_tools,
            "reasoning_text_on": reasoning_text,
            "reasoning_tokens_on": reasoning_tokens_on,
            "latency_seconds_off": round(t_off, 4),
            "latency_seconds_on": round(latencies_on[i], 4) if i < len(latencies_on) else 0.0,
        })
    return rows


def _write_failed_dir(
    *, status: str, baseline_module: Any, evolved_module: Any,
    optimized_module: Optional[Any], started_at: str, holdout: list,
    ambiguous_subset: list, lm: Any, ab_tolerance_pp: float,
    ambiguous_improvement_pp: float, latency_budget_sec: float,
    config: EvolutionConfig, iterations: int, num_predictors: int,
    optimizer_used: str, error_msg: str, trainset: list, valset: list,
) -> Path:
    """Write FAILED_<ts>/ with minimal metrics.json + reasoning_prompt.txt placeholder."""
    ts = datetime.now().strftime("%Y%m%d_%H%M%S")
    out_dir = OUTPUT_ROOT / f"FAILED_{ts}"
    out_dir.mkdir(parents=True, exist_ok=True)
    metrics = {
        "timestamp": ts,
        "started_at": started_at,
        "status": status,
        "iterations": iterations,
        "eval_model": config.eval_model,
        "optimizer_used": optimizer_used,
        "tool_count": 0,
        "param_predictors_discovered": num_predictors,
        "holdout_examples": len(holdout),
        "ambiguous_subset_size": len(ambiguous_subset),
        "error_message": error_msg,
        "ambiguous_gate_skipped": len(ambiguous_subset) < AMBIGUOUS_SMALL_SAMPLE_THRESHOLD,
    }
    (out_dir / "metrics.json").write_text(
        json.dumps(metrics, indent=2, ensure_ascii=False, sort_keys=True),
        encoding="utf-8",
    )
    _write_reasoning_prompt(out_dir, optimized_module or evolved_module)
    _write_diff(out_dir, ToolReasoningSignature.__doc__ or "",
                optimized_module or evolved_module)
    (out_dir / "ab_comparison.json").write_text("[]", encoding="utf-8")
    return out_dir


def _write_aborted_dir(
    *, tracker: CostTracker, evolved_module: Any,
    cap_exc: CostBudgetExceeded, reflection_model_name: str, started_at: str,
) -> Path:
    """Write ABORTED_<ts>/aborted.json (cost cap exceeded)."""
    ts = datetime.now().strftime("%Y%m%d_%H%M%S")
    out_dir = OUTPUT_ROOT / f"ABORTED_{ts}"
    out_dir.mkdir(parents=True, exist_ok=True)
    aborted = {
        "timestamp": ts,
        "started_at": started_at,
        "status": "ABORTED",
        "error_class": "CostBudgetExceeded",
        "error_message": str(cap_exc),
        "reflection_model": reflection_model_name,
        "spent_usd": float(getattr(tracker, "poll", lambda: 0.0)() or 0.0),
    }
    (out_dir / "aborted.json").write_text(
        json.dumps(aborted, indent=2, ensure_ascii=False, sort_keys=True),
        encoding="utf-8",
    )
    return out_dir


# ── Module entry ───────────────────────────────────────────────────────


main = evolve  # alias for test discovery


if __name__ == "__main__":
    sys.exit(evolve(standalone_mode=True))
```

⚠️ Critical implementation notes:
1. **patch 路径精确性:** 测试 patches `evolution.tools.evolve_tool_reasoning.dspy.GEPA`、`.dspy.LM`、`.V1BaselineGate`、`.ThinkABGate`、`.ToolModule`、`._load_tool_descriptions`、`._load_dataset`。模块顶层 `from X import Y` 而非 `import X`,使得这些 name 在 evolve_tool_reasoning 命名空间可被 patch。
2. **dry-run echo schema:** 与 RESEARCH §6.4 字段完全一致(test_dry_run_emits_setup 断言;包括 `ambiguous_gate_skipped`、`reasoning_tokens_cap`、`latency_p95_budget_sec`、`ab_tolerance_pp`、`ambiguous_improvement_pp`、`full_regression_tolerance_pp`、`max_metric_calls_estimate`)
3. **OUTPUT_ROOT 物理隔离:** 全文件硬编码 `Path("output") / "tools_reasoning"`;不导入 `from evolution.tools.evolve_tool_params import OUTPUT_ROOT`(若 Phase 13 有同名常量)
4. **dry-run 路径 dataset 容错:** `_load_dataset` 在 dry-run 下若失败 → trainset/valset/holdout = []。`ambiguous_subset_size=0`、`ambiguous_gate_skipped=true` echo 仍输出
5. **`_load_dataset` 已 `from evolution.tools.evolve_tool_params import _load_dataset`:** 测试 `patch("evolution.tools.evolve_tool_reasoning._load_dataset", return_value=(...))` 命中 — Python 把 import 后的 name 注册在当前模块命名空间
6. **`compute_v1_baseline` 行为:** Phase 13 实现支持 historical(从 baseline_run 路径读 metrics.json)与 inline(用 baseline_module 在 holdout 上跑 joint metric);若调用方未传 baseline_run,inline 路径需要 baseline_module 可调用(测试场景:测试中 patch mock_v1_cls 时不实际跑 inline)
7. **测试 6 (test_metrics_json_schema) 路径:** 测试期望非 FAILED dir;由于 mock 的 V1BaselineGate.check 未设 return_value → 默认 MagicMock 返回 truthy → `.get("passed", True)` 返回 MagicMock(truthy)→ `not truthy` = False → not failed → SUCCESS 路径。完美。
  </action>
  <verify>
    <automated>/Users/slj/项目/hermes-agent-self-evolution/.venv/bin/python -m pytest /Users/slj/项目/hermes-agent-self-evolution/tests/tools/test_evolve_tool_reasoning.py -v --tb=short -k "not test_e2e_mock_pipeline" 2>&1 | tee /tmp/15-04-test.log | tail -100 && echo "--- summary ---" && grep -cE "PASSED|FAILED|ERROR" /tmp/15-04-test.log</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `evolution/tools/evolve_tool_reasoning.py` 存在
    - `python -c "import ast; ast.parse(open('evolution/tools/evolve_tool_reasoning.py').read())"` 退出 0(语法合法)
    - `grep -q '@click.command' evolution/tools/evolve_tool_reasoning.py` 命中
    - `grep -q 'enable_reasoning=False' evolution/tools/evolve_tool_reasoning.py` 命中(baseline_module)
    - `grep -q 'enable_reasoning=True' evolution/tools/evolve_tool_reasoning.py` 命中(evolved_module)
    - `grep -q 'V1BaselineGate' evolution/tools/evolve_tool_reasoning.py` 命中
    - `grep -q 'ThinkABGate' evolution/tools/evolve_tool_reasoning.py` 命中
    - `grep -q 'sample_latency_tokens' evolution/tools/evolve_tool_reasoning.py` 命中
    - `grep -q 'output.*tools_reasoning' evolution/tools/evolve_tool_reasoning.py` 命中(D-11 物理隔离)
    - `grep -v '^#' evolution/tools/evolve_tool_reasoning.py | grep -c 'output.*"tools"[^_]'` == 0(不触达 output/tools/ — 排除 tools_reasoning;允许字符串字面量 "tools_reasoning")
    - `grep -q 'reasoning_prompt.txt' evolution/tools/evolve_tool_reasoning.py` 命中
    - `grep -q 'ab_comparison.json' evolution/tools/evolve_tool_reasoning.py` 命中
    - `grep -q 'diff.txt' evolution/tools/evolve_tool_reasoning.py` 命中
    - `grep -q '--reasoning-tokens-cap' evolution/tools/evolve_tool_reasoning.py` 命中(D-12)
    - `grep -q '--ambiguous-improvement-pp' evolution/tools/evolve_tool_reasoning.py` 命中(D-12)
    - `grep -q '--latency-budget-sec' evolution/tools/evolve_tool_reasoning.py` 命中(D-12)
    - `grep -q '--ambiguous-only' evolution/tools/evolve_tool_reasoning.py` 命中(D-12)
    - `grep -q 'THINK_AB_FAILED' evolution/tools/evolve_tool_reasoning.py` 命中(状态码)
    - `grep -q 'ABORTED' evolution/tools/evolve_tool_reasoning.py` 命中
    - `grep -q 'CostBudgetExceeded' evolution/tools/evolve_tool_reasoning.py` 命中
    - `.venv/bin/python -m evolution.tools.evolve_tool_reasoning --help 2>&1 | grep -q 'Phase 15\|think-augmented\|evolve'`(帮助文本可见)
    - `.venv/bin/python -m pytest tests/tools/test_evolve_tool_reasoning.py -v -k "not test_e2e_mock_pipeline" --tb=short` 至少 8/9 PASSED(test_e2e_mock_pipeline 由 Plan 05 处理)
    - `.venv/bin/python -m pytest tests/tools/ -x --tb=short -k "not test_e2e_mock_pipeline"` 退出 0(Wave 1-3 全集 GREEN)
  </acceptance_criteria>
  <done>
    evolve_tool_reasoning.py CLI 模块就位,Click 入口 + 16 步流水线 + 双 ToolModule + 双门 + 4 输出文件 + FAILED_/ABORTED_/SUCCESS 三路径 + 物理隔离到 `output/tools_reasoning/`。`python -m evolution.tools.evolve_tool_reasoning --help` 可显示完整 flag。Plan 01 的 9/10 集成测试 GREEN(test_e2e_mock_pipeline 留待 Plan 05)。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags → _evolve_impl | Click 已做类型校验;但 --max-cost-usd / --latency-budget-sec 负数未拦截 |
| 第三方 LM API → GEPA / forward | OpenAI/OpenRouter LM 错误已由 CostTracker / _score_module_on_holdout try/except 兜底 |
| disk write → output/tools_reasoning | CLI 在 cwd 下写;若 cwd 不可写则 raise PermissionError(测试用 tmp_path / monkeypatch.chdir) |
| baseline_run path → compute_v1_baseline | Path traversal 风险(读外部 metrics.json);Phase 13 V1BaselineGate 已在 compute_v1_baseline 做基础校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | metrics.json 内 cost/score 字段 | mitigate | json.dumps(sort_keys=True) 保证可重现;cost_usd_spent 从 CostTracker.poll 取值(BL-01 fix Phase 13 已 close) |
| T-15-04-02 | Repudiation | ABORTED_/FAILED_ 目录不写则 user 误以为 SUCCESS | mitigate | mkdir(parents=True, exist_ok=True) + 错误路径全 _write_failed_dir / _write_aborted_dir 兜底;test_think_ab_gate_failure_writes_failed 守门 |
| T-15-04-03 | Info Disclosure | reasoning_prompt.txt 含 GEPA 演化产物 | accept | 这是 phase 输出物;不含 PII(reasoner 没看过 sessiondb 原文,只看过 task_description) |
| T-15-04-04 | DoS | ambiguous_only=True + holdout 大 → 跑两次 forward 仍可控 | accept | Phase 4/14 holdout 上限 ~100 例;cost cap 兜底 |
| T-15-04-05 | Elevation of Privilege | reasoner instructions 注入 selector 行为 | mitigate | Plan 02 已锁:selector 仍接收完整 tools 列表,reasoning 仅是 InputField,Pitfall 4 §3 守门 |
| T-15-04-06 | Tampering | output/tools/ 被误写(违反 D-11) | mitigate | grep-verify acceptance criterion 守门 — 实现期间不允许 "output/tools/" 出现在源代码中(允许 "output/tools_reasoning/") |
</threat_model>

<verification>
**Plan-level checks:**

1. **模块 import 路径完整:** `python -c "from evolution.tools.evolve_tool_reasoning import evolve, _evolve_impl, OUTPUT_ROOT; print(OUTPUT_ROOT)"` 输出 `output/tools_reasoning`

2. **CLI --help 可见:** `python -m evolution.tools.evolve_tool_reasoning --help` 退出 0,输出含所有 Phase 15 新 flag(--reasoning-tokens-cap / --ambiguous-improvement-pp / --latency-budget-sec / --ambiguous-only)

3. **9/10 集成测试 GREEN(Plan 01):**
   - test_dry_run_emits_setup ✓
   - test_baseline_module_off_evolved_on_constructed ✓
   - test_dual_v1_baseline_calls ✓
   - test_think_ab_gate_failure_writes_failed ✓
   - test_v1_failed_think_on_writes_failed_dir ✓
   - test_metrics_json_schema ✓
   - test_reasoning_prompt_files ✓
   - test_ab_comparison_schema ✓
   - test_output_isolated_directory ✓
   - test_cost_cap_aborts ✓
   - test_e2e_mock_pipeline(skipped — Plan 05 处理)

4. **D-11 物理隔离守门:** `grep -v "^#" evolution/tools/evolve_tool_reasoning.py | grep -E '"output"\s*/\s*"tools"[^_]'` 零命中(只允许 "tools_reasoning")

5. **D-12 4 个新 flag 命中:** grep --reasoning-tokens-cap / --ab-tolerance-pp / --ambiguous-improvement-pp / --latency-budget-sec / --ambiguous-only

6. **现有测试不退化:** `pytest tests/ -x --tb=short -k "not test_e2e_mock_pipeline"` 全 GREEN(包括 Phase 13/14 baseline + Wave 0-3 新测试)
</verification>

<success_criteria>
1. `evolution/tools/evolve_tool_reasoning.py` ~800-1000 LoC 创建,包含 Click CLI 命令 `evolve` + `_evolve_impl` 16 步流水线
2. 复用 Phase 13 全部基础设施(CostTracker / V1BaselineGate / GEPA / dspy.configure / Phase 13 helper),不重写
3. 替换 step 3 / 11-14 实现:
   - 双 ToolModule 构造(baseline enable_reasoning=False + evolved enable_reasoning=True)
   - GEPA 只 compile evolved_module
   - holdout 双跑(think-off / think-on),ambiguous 子集筛选(D-13)
   - sample_latency_tokens 采样 latency + reasoning tokens(D-17)
   - V1BaselineGate ×2(think-off vs v1 + think-on vs v1)+ ThinkABGate ×1(think-on vs think-off 三重门)
4. 输出物理隔离 `output/tools_reasoning/<ts>/`(D-11);FAILED_<ts>/ 与 ABORTED_<ts>/ 错误路径
5. 4 个输出文件(metrics.json / reasoning_prompt.txt / diff.txt / ab_comparison.json),schema 与 RESEARCH §6.2-6.3 一致
6. dry-run 早返 echo 完整 Phase 15 schema(D-09)
7. 4 个新 CLI flag(--reasoning-tokens-cap / --ab-tolerance-pp / --ambiguous-improvement-pp / --latency-budget-sec / --ambiguous-only)+ 复用 Phase 13 flag(D-12)
8. CostBudgetExceeded → ABORTED_<ts>/(exit 2);任一 gate FAILED → FAILED_<ts>/(exit 1);SUCCESS → SUCCESS 目录(exit 0)
9. 9/10 集成测试(Plan 01 创建)GREEN(test_e2e_mock_pipeline 给 Plan 05)
10. 现有 Phase 13/14 测试不退化 — `pytest tests/ -k "not test_e2e_mock_pipeline" -x` 退出 0
</success_criteria>

<output>
After completion, create `.planning/phases/15-think-augmented-tool-selection/15-04-SUMMARY.md` recording:
- evolve_tool_reasoning.py 最终 LoC + 16 步流水线步骤 ↔ Phase 13 对应步骤映射表
- Plan 01 的 10 个集成测试:9 GREEN + 1 skipped 的具体结果
- `python -m evolution.tools.evolve_tool_reasoning --help` 完整 stdout 截取(便于审计 D-12 flag)
- D-09 / D-10 / D-11 / D-12 实现覆盖 trace table
- 现有测试套件回归检查(385+Wave 0-3 全集仍 GREEN)
</output>
