---
phase: 15
plan: 05
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - tests/tools/test_evolve_tool_reasoning.py
  - evolution/tools/__init__.py
autonomous: false
requirements:
  - TOOL-V2-03
user_setup: []
must_haves:
  truths:
    - "End-to-end smoke test(mock LM)跑完整 pipeline 断言 metrics.json schema 完整(test_e2e_mock_pipeline)"
    - "'python -m evolution.tools.evolve_tool_reasoning --iterations 1 --dry-run' 在真实 hermes-agent 仓库上退出 0 + echo 完整 schema(含实测 ambiguous_subset_size)"
    - "evolution/tools/__init__.py 若已存在则追加 Phase 15 public symbol 导出(ThinkABGate / check_think_ab_gate / sample_latency_tokens);若不存在则 skip(与 Phase 13 惯例一致)"
    - "真实数据集 ambiguous 子集大小最终回声:'.venv/bin/python -m pytest tests/tools/test_dataset_ambiguous_size.py -v -s' 输出含 'ambiguous_subset_size=N',N 与 metrics.json 在 dry-run 下 echo 值一致"
    - "Phase gate sign-off:'nyquist_compliant: true' 设入 VALIDATION.md frontmatter"
  artifacts:
    - path: "tests/tools/test_evolve_tool_reasoning.py"
      provides: "test_e2e_mock_pipeline — mock LM 跑完整 pipeline 的 GREEN smoke"
    - path: "evolution/tools/__init__.py"
      provides: "(optional) Phase 15 public symbol re-export;若 Phase 13 已有 __init__.py 则追加"
    - path: ".planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md"
      provides: "frontmatter 更新:nyquist_compliant: true + wave_0_complete: true + status: approved"
  key_links:
    - from: "test_e2e_mock_pipeline"
      to: "整个 Phase 15 pipeline"
      via: "mock dspy.LM / dspy.GEPA + ToolModule.forward → 真实走 V1BaselineGate / ThinkABGate / 写盘"
      pattern: "test_e2e_mock_pipeline"
    - from: "'python -m evolution.tools.evolve_tool_reasoning --dry-run'"
      to: "真实 hermes-agent 仓库"
      via: "HERMES_AGENT_REPO env var 或 --hermes-repo flag"
      pattern: "HERMES_AGENT_REPO"
---

<objective>
Phase 15 收尾:
1. **Integration smoke**:GREEN 化 Plan 01 留的 `test_e2e_mock_pipeline` — mock LM + 10 example 跑完整 16 步流水线,断言 metrics.json schema 完整、4 个输出文件齐备、ambiguous_subset_size=5(确定性)
2. **真实 dry-run 验收**(checkpoint:human-verify):操作者在实际 hermes-agent 仓库上跑 `python -m evolution.tools.evolve_tool_reasoning --iterations 1 --dry-run`,确认 dry-run echo 的 ambiguous_subset_size 与 Plan 01 Task 4 数据集观察结果一致,且退出 0
3. **可选 public export**:若 `evolution/tools/__init__.py` 已存在,追加 Phase 15 的 public symbol 导出;不存在则不新建(与 Phase 13/14 惯例一致)
4. **VALIDATION.md sign-off**:更新 frontmatter 为 `nyquist_compliant: true` / `wave_0_complete: true` / `status: approved`

Purpose:
- 真实路径冒烟:mock 路径测试已覆盖分支,真实 dry-run 验证配置与 I/O 路径无硬编码 typo
- 为 /gsd-verify-phase 15 提供确定性绿色基线
- Phase gate sign-off — Phase 15 的 nyquist 契约闭环

Output:
- test_e2e_mock_pipeline GREEN
- Manual dry-run checkpoint 通过
- VALIDATION.md frontmatter 已更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-think-augmented-tool-selection/15-CONTEXT.md
@.planning/phases/15-think-augmented-tool-selection/15-RESEARCH.md
@.planning/phases/15-think-augmented-tool-selection/15-PATTERNS.md
@.planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md
@.planning/phases/15-think-augmented-tool-selection/15-01-PLAN.md
@.planning/phases/15-think-augmented-tool-selection/15-02-PLAN.md
@.planning/phases/15-think-augmented-tool-selection/15-03-PLAN.md
@.planning/phases/15-think-augmented-tool-selection/15-04-PLAN.md
@evolution/tools/evolve_tool_reasoning.py
@tests/tools/test_evolve_tool_reasoning.py
</context>

<interfaces>
<!-- 需在 Wave 4 收尾使用的关键 API -->

From `tests/tools/test_evolve_tool_reasoning.py`(Plan 01 创建,Plan 04 使 9/10 GREEN):
```python
@pytest.mark.skip(reason="Wave 4 smoke test placeholder — fills in 15-05-PLAN.md")
def test_e2e_mock_pipeline():
    """End-to-end mock LM pipeline — Wave 4 fills in."""
    pass
```

From `evolution/tools/evolve_tool_reasoning.py`(Plan 04 产物):
```python
def evolve(...) -> int
def _evolve_impl(*, ...) -> int
main = evolve
OUTPUT_ROOT = Path("output") / "tools_reasoning"
```

From `evolution/tools/__init__.py`:若文件存在,通常含若干 `from .module import Symbol`;若不存在或 Phase 13/14 未建立该模式,则遵循惯例不新建。
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN 化 test_e2e_mock_pipeline — mock LM 跑完整 Phase 15 pipeline,断 metrics.json 完整 schema + 4 输出文件齐备 + exit code 合理</name>
  <files>tests/tools/test_evolve_tool_reasoning.py</files>
  <read_first>
    - tests/tools/test_evolve_tool_reasoning.py(Plan 01 创建,Plan 04 已使 9/10 GREEN;末尾含 test_e2e_mock_pipeline stub)
    - evolution/tools/evolve_tool_reasoning.py(Plan 04 产物 — CLI 与 _evolve_impl)
    - .planning/phases/15-think-augmented-tool-selection/15-RESEARCH.md §6.2(metrics.json 完整 schema)
    - .planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md §10.3(Phase gate sampling rate)
  </read_first>
  <behavior>
    - 移除 `@pytest.mark.skip(...)` 装饰
    - 新 test_e2e_mock_pipeline 接受 tmp_path + monkeypatch fixture;monkeypatch.chdir(tmp_path)
    - 构造 10 个 mock example:前 5 个 confuser_tools=["read_file", "ls"](ambiguous, len >= 2),后 5 个 confuser_tools=["ls"](non-ambiguous)
    - 构造 3 个 fake ToolDescription(search / read_file / ls)
    - patch 外部 seams:
      - `evolution.tools.evolve_tool_reasoning._load_tool_descriptions` → 返回 3 fake tools
      - `evolution.tools.evolve_tool_reasoning._load_dataset` → 返回 (train=20, val=10, holdout=10)
      - `evolution.tools.evolve_tool_reasoning.dspy.LM` → MagicMock()
      - `evolution.tools.evolve_tool_reasoning.dspy.GEPA` → compile 返回 input module(no-op optimization)
      - `evolution.tools.tool_module.ToolModule.forward` → 返回 canned dspy.Prediction(含 reasoning_tokens=14 当 enable_reasoning 时)
    - **不** patch:V1BaselineGate / ThinkABGate / sample_latency_tokens / _score_module_on_holdout / _build_ab_comparison / _write_* —— 走真实代码路径
    - 断言:
      - result.exit_code ∈ {0, 1}(SUCCESS 或 gate FAILED 都 OK — smoke 验管道)
      - `output/tools_reasoning/` 有 ≥ 1 子目录
      - 该目录存在 metrics.json / reasoning_prompt.txt / diff.txt / ab_comparison.json 4 文件
      - metrics.json 含所有 RESEARCH §6.2 关键字段
      - metrics["ambiguous_subset_size"] == 5(前 5 example 的 confuser 长度确定)
      - ab_comparison.json 是 JSON array,长度 >= 1,首条含 required keys
      - `output/tools/` 不存在或为空(物理隔离守门)
  </behavior>
  <action>
定位 `test_evolve_tool_reasoning.py` 文件末尾的 `test_e2e_mock_pipeline` stub(由 Plan 01 创建,带 `@pytest.mark.skip`)。替换为完整实现:

```python
def test_e2e_mock_pipeline(tmp_path, monkeypatch):
    """End-to-end mock LM pipeline smoke (Plan 05 Wave 4).

    Mocks LM + GEPA + ToolModule.forward externally but runs REAL
    V1BaselineGate / ThinkABGate / sample_latency_tokens / _build_ab_comparison /
    _write_* to verify the full 16-step pipeline wires end-to-end without
    crashes, writes all 4 output files, and produces metrics.json with the
    complete Phase 15 schema.
    """
    monkeypatch.chdir(tmp_path)

    # Build 10 mock holdout examples — 5 ambiguous, 5 non-ambiguous
    def _mock_example(i: int):
        ex = MagicMock()
        ex.task_description = f"task_{i}: find or read a file"
        ex.correct_tool = "search" if i % 2 == 0 else "read_file"
        ex.correct_params = '{"q": "foo"}'
        ex.confuser_tools = ["read_file", "ls"] if i < 5 else ["ls"]
        return ex

    fake_holdout = [_mock_example(i) for i in range(10)]
    fake_train = [_mock_example(i + 100) for i in range(20)]
    fake_val = [_mock_example(i + 200) for i in range(10)]

    # 3 fake tools (search / read_file / ls)
    fake_tools = [
        ToolDescription(
            name=n,
            file_path=Path(f"/fake/{n}.py"),
            description=f"Tool {n} for fake use",
            params=[ToolParam(name="q", type="string", required=True, description="query")],
        )
        for n in ("search", "read_file", "ls")
    ]

    # ToolModule.forward canned predictions — reasoning_tokens > 0 when reasoner set
    def _fwd_patched(self, task_description):
        has_reasoner = getattr(self, "reasoner", None) is not None
        return dspy.Prediction(
            selected_tool="search",
            selected_params='{"q": "x"}',
            reasoning=(
                "search fits better than read_file for query-shaped tasks"
                if has_reasoner else ""
            ),
            reasoning_tokens=(14 if has_reasoner else 0),
        )

    # GEPA.compile returns the student module as-is (no-op optimization)
    def _gepa_compile(student, trainset=None, valset=None):
        return student

    with patch("evolution.tools.evolve_tool_reasoning._load_tool_descriptions",
               return_value=fake_tools), \
         patch("evolution.tools.evolve_tool_reasoning._load_dataset",
               return_value=(fake_train, fake_val, fake_holdout)), \
         patch("evolution.tools.evolve_tool_reasoning.dspy.LM",
               return_value=MagicMock()), \
         patch("evolution.tools.evolve_tool_reasoning.dspy.GEPA") as mock_gepa, \
         patch("evolution.tools.tool_module.ToolModule.forward", _fwd_patched):
        mock_gepa.return_value.compile.side_effect = _gepa_compile

        runner = CliRunner()
        result = runner.invoke(
            evolve_mod.evolve if hasattr(evolve_mod, "evolve") else evolve_mod.main,
            ["--iterations", "1", "--max-cost-usd", "5.0", "--eval-source", "load"],
            catch_exceptions=False,
        )

    # Pipeline did not crash — SUCCESS (0) or gate FAILED (1) both acceptable
    assert result.exit_code in (0, 1), (
        f"unexpected exit code {result.exit_code}\n"
        f"Output:\n{result.output}"
    )

    # Output dir structure
    tr_root = tmp_path / "output" / "tools_reasoning"
    assert tr_root.exists()
    all_dirs = [d for d in tr_root.iterdir() if d.is_dir()]
    assert len(all_dirs) >= 1, f"expected ≥1 output dir; got {all_dirs}"
    out_dir = all_dirs[0]

    # 4 output files present
    for fname in ("metrics.json", "reasoning_prompt.txt", "diff.txt", "ab_comparison.json"):
        assert (out_dir / fname).exists(), f"missing {fname} in {out_dir}"

    # metrics.json schema
    metrics = json.loads((out_dir / "metrics.json").read_text())
    for key in (
        "think_on_score", "think_off_score",
        "ambiguous_think_on", "ambiguous_think_off",
        "reasoning_token_stats", "latency_stats",
        "think_ab_gate", "v1_gate_passed",
        "ambiguous_subset_size", "ambiguous_gate_skipped",
        "iterations", "eval_model", "status", "timestamp",
    ):
        assert key in metrics, (
            f"metrics.json missing key: {key}\nAvailable: {sorted(metrics.keys())}"
        )

    # Deterministic: 5 ambiguous examples in fake_holdout
    assert metrics["ambiguous_subset_size"] == 5, (
        f"ambiguous_subset_size mismatch: expected 5, got {metrics['ambiguous_subset_size']}"
    )

    # ab_comparison.json is JSON array with required keys
    ab_data = json.loads((out_dir / "ab_comparison.json").read_text())
    assert isinstance(ab_data, list)
    assert len(ab_data) >= 1
    required_keys = {
        "task_id", "task_description", "correct_tool",
        "selected_off", "selected_on",
        "is_correct_off", "is_correct_on",
        "is_ambiguous", "confuser_tools",
        "reasoning_text_on", "reasoning_tokens_on",
        "latency_seconds_off", "latency_seconds_on",
    }
    missing = required_keys - set(ab_data[0].keys())
    assert not missing, f"ab_comparison[0] missing keys: {missing}"

    # Physical isolation: output/tools/ absent or empty (D-11)
    tools_dir = tmp_path / "output" / "tools"
    if tools_dir.exists():
        assert not any(tools_dir.iterdir()), (
            f"Phase 15 must NOT write to output/tools/; found: {list(tools_dir.iterdir())}"
        )
```

⚠️ 注意:测试顶部已有的 import 列表(Plan 01 创建)应包含 `json`、`pathlib.Path`、`pytest`、`unittest.mock.MagicMock, patch`、`click.testing.CliRunner`、`dspy`、`ToolDescription`、`ToolParam`。如缺失请追加。`evolve_mod = pytest.importorskip("evolution.tools.evolve_tool_reasoning")` 需放在文件顶部或函数开头一致。
  </action>
  <verify>
    <automated>/Users/slj/项目/hermes-agent-self-evolution/.venv/bin/python -m pytest /Users/slj/项目/hermes-agent-self-evolution/tests/tools/test_evolve_tool_reasoning.py::test_e2e_mock_pipeline -v --tb=long 2>&1 | tee /tmp/15-05-e2e.log | tail -80 && echo "--- summary ---" && grep -cE "PASSED" /tmp/15-05-e2e.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "^#" tests/tools/test_evolve_tool_reasoning.py | grep -c "@pytest.mark.skip"` == 0(skip 装饰已移除)
    - `grep -q "def test_e2e_mock_pipeline(tmp_path, monkeypatch)" tests/tools/test_evolve_tool_reasoning.py` 命中
    - `grep -q 'metrics\["ambiguous_subset_size"\] == 5' tests/tools/test_evolve_tool_reasoning.py` 命中(精确确定性断言)
    - `grep -q 'reasoning_prompt.txt' tests/tools/test_evolve_tool_reasoning.py` 命中
    - `grep -q 'ab_comparison.json' tests/tools/test_evolve_tool_reasoning.py` 命中
    - `grep -q 'diff.txt' tests/tools/test_evolve_tool_reasoning.py` 命中
    - `.venv/bin/python -m pytest tests/tools/test_evolve_tool_reasoning.py::test_e2e_mock_pipeline -v` 退出 0(GREEN,1 PASSED)
    - `.venv/bin/python -m pytest tests/tools/test_evolve_tool_reasoning.py -v` 全部 10/10 PASSED
    - `.venv/bin/python -m pytest tests/ -x --tb=short` 全部 GREEN(baseline + Wave 0-4 新)
  </acceptance_criteria>
  <done>
    test_e2e_mock_pipeline GREEN。Plan 01 创建的 10 个集成测试现在全部 PASSED。Phase 13/14 baseline 不退化。mock LM 端到端走通 Phase 15 所有分支。
  </done>
</task>

<task type="auto">
  <name>Task 2: 可选更新 evolution/tools/__init__.py — 追加 Phase 15 public symbol 导出(若文件已存在且有 Phase 13 模式);否则 skip</name>
  <files>evolution/tools/__init__.py</files>
  <read_first>
    - evolution/tools/__init__.py(若存在)
    - evolution/tools/(顶级目录 ls 确认 __init__.py 是否存在)
  </read_first>
  <action>
**先跑 ls 检查:** `ls -la evolution/tools/__init__.py 2>&1`。

**条件分支决策:**

**情形 A — 文件存在且已有 `from X import Y` 模式(Phase 13 已建立导出惯例):**
   在文件末尾(保留现有 import 顺序)追加 Phase 15 symbols:
   ```python
   # Phase 15 public surface
   from evolution.tools.think_metrics import (
       AMBIGUOUS_SMALL_SAMPLE_THRESHOLD,
       DEFAULT_AMBIGUOUS_IMPROVEMENT_PP,
       DEFAULT_FULL_REGRESSION_TOLERANCE_PP,
       DEFAULT_LATENCY_P95_BUDGET_SEC,
       ThinkABGate,
       check_think_ab_gate,
       sample_latency_tokens,
   )
   ```
   **不** re-export `evolve_tool_reasoning.evolve`(Phase 13 的 evolve_tool_params 也未 re-export — 保持一致)。

**情形 B — 文件不存在或为空(Phase 13 未建立该惯例):**
   - **Skip**:不新建,与 Phase 13/14 保持一致
   - 在 15-05-SUMMARY.md 记录决定理由

**情形 C — 文件存在但只有 `__all__ = [...]` 或仅 docstring 而无 import:**
   - **Skip**:不破坏现有简洁性
   - Phase 15 的 public surface 通过完整模块路径访问(例:`from evolution.tools.think_metrics import ThinkABGate`)

选择分支后在 Summary 中说明决定。本任务**非 blocking** — 即使 skip,Phase 15 phase gate 仍可通过(用户代码通过完整模块路径访问 API)。
  </action>
  <verify>
    <automated>ls -la /Users/slj/项目/hermes-agent-self-evolution/evolution/tools/__init__.py 2>&1 | head -5 && if [ -f /Users/slj/项目/hermes-agent-self-evolution/evolution/tools/__init__.py ]; then /Users/slj/项目/hermes-agent-self-evolution/.venv/bin/python -c "import ast; ast.parse(open('/Users/slj/项目/hermes-agent-self-evolution/evolution/tools/__init__.py').read())" && echo "__init__.py syntax OK" && /Users/slj/项目/hermes-agent-self-evolution/.venv/bin/python -c "from evolution.tools import think_metrics; print('think_metrics importable via full path: OK')"; else echo "__init__.py absent — Phase 15 skip per惯例"; fi</automated>
  </verify>
  <acceptance_criteria>
    - 若文件存在:`python -c "import ast; ast.parse(open('evolution/tools/__init__.py').read())"` 退出 0
    - 若文件存在且做了追加:`grep -q "from evolution.tools.think_metrics import" evolution/tools/__init__.py` 命中
    - 若文件不存在:accept — 不新建
    - Summary 文档中记录本任务所做决定(情形 A/B/C 哪一个)
    - `python -c "from evolution.tools.think_metrics import ThinkABGate"` 永远可工作(完整模块路径 import 不依赖 __init__.py 改动)
  </acceptance_criteria>
  <done>
    根据 evolution/tools/__init__.py 现状选择情形 A / B / C 之一并执行;不引入测试退化。ThinkABGate 等 Phase 15 symbol 通过完整模块路径始终可 import。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — 人工执行真实 dry-run 验证</name>
  <files>(no source change — manual verification on real hermes-agent repo)</files>
  <action>
**What has been built (context for verifier):**
Phase 15 端到端 pipeline 已就位:
- evolution/tools/think_metrics.py (Plan 03)
- evolution/tools/tool_module.py 改造完毕 (Plan 02)
- evolution/tools/evolve_tool_reasoning.py 新 CLI (Plan 04)
- 4 输出文件 + 双门 + 物理隔离到 output/tools_reasoning/
- mock LM 端到端 smoke GREEN (Task 1)

**How to verify (operator runs these commands):**
1. **真实 dry-run:**
   ```bash
   cd /Users/slj/项目/hermes-agent-self-evolution
   .venv/bin/python -m evolution.tools.evolve_tool_reasoning --iterations 1 --dry-run 2>&1 | tee /tmp/phase15-dryrun.log
   echo "exit=$?"
   ```

2. **断言 stdout 内容:**
   - 退出码 == 0
   - stdout 含 `param_predictors_discovered=<N>`(N 是真实 tool 数)
   - stdout 含 `tools_in_scope=<M>`(M == hermes-agent 真实 tool 数)
   - stdout 含 `holdout_size=<H>`(若数据集已构建则 H>0;若未构建 H=0 是 OK)
   - stdout 含 `ambiguous_subset_size=<A>`(A 与 `pytest tests/tools/test_dataset_ambiguous_size.py -s` 的 echo 值一致)
   - stdout 含 `ambiguous_gate_skipped=false` 若 A>=5,`=true` 若 A<5
   - stdout 含 `reasoning_tokens_cap=200`
   - stdout 含 `latency_p95_budget_sec=5.0`
   - stdout 含 `ab_tolerance_pp=2.0`
   - stdout 含 `ambiguous_improvement_pp=3.0`
   - stdout 含 `full_regression_tolerance_pp=2.0`
   - stdout 含 `max_metric_calls_estimate=<B>`(B > 0)
   - stdout 最后一行 "DRY RUN — setup validated."

3. **数据集观察一致性(与 Plan 01 Task 4 交叉验证):**
   ```bash
   .venv/bin/python -m pytest tests/tools/test_dataset_ambiguous_size.py -v -s 2>&1 | grep ambiguous_subset_size
   ```
   对比 dry-run stdout 的 ambiguous_subset_size — 两者应相等。

4. **全集回归:**
   ```bash
   .venv/bin/python -m pytest tests/ -x --tb=short 2>&1 | tail -10
   ```
   应输出 "N passed, 0 failed"(N = 385 + Wave 0-4 新测试数约 30+)。

5. **VALIDATION.md frontmatter 更新(本任务后由操作者手动或由 Task 4 自动更新):**
   ```bash
   grep -E "nyquist_compliant|wave_0_complete|status" .planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md
   ```
   应变为:`nyquist_compliant: true`、`wave_0_complete: true`、`status: approved`

**如果 dry-run 失败:**
- `ImportError: HERMES_AGENT_REPO not configured` → `export HERMES_AGENT_REPO=/path/to/hermes-agent` 或 `--hermes-repo <path>`
- `datasets/tools/holdout.jsonl not found` → 运行 `evolve_tool_params --eval-source synthetic --tools search` 先生成数据集;dry-run 下 holdout_size=0 也 OK(ambiguous 子集观察仍会 echo)
- 任何 TypeError / AttributeError / ImportError → 提交 issue 或 /gsd-quick 修复;checkpoint 不通过
  </action>
  <verify>
    <automated>echo "Checkpoint verify automation: operator must run the dry-run and respond with approved/issues:<detail>"</automated>
  </verify>
  <done>
    操作者回复 "approved" — dry-run 退出 0,ambiguous_subset_size 与 test_dataset_ambiguous_size echo 一致,pytest tests/ -x 全 GREEN,则继续 Task 4 更新 VALIDATION.md。操作者回复 "issues: <具体问题>" 则 Claude 读取 issue 发起 /gsd-quick 修复后重新进入此 checkpoint。
  </done>
</task>

<task type="auto">
  <name>Task 4: 更新 VALIDATION.md frontmatter 为 approved,记录 Phase 15 nyquist sign-off</name>
  <files>.planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md</files>
  <read_first>
    - .planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md(当前 frontmatter:status: draft / nyquist_compliant: false / wave_0_complete: false)
  </read_first>
  <action>
用 Edit 工具把 `.planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md` frontmatter 从:

```yaml
  ---
phase: 15
slug: think-augmented-tool-selection
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-09
  ---
```

替换为:

```yaml
  ---
phase: 15
slug: think-augmented-tool-selection
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-09
signed_off: 2026-05-11
  ---
```

并在文件末尾(在 "**Approval:** pending" 之后或替换该行)追加:

```markdown
**Approval:** approved (2026-05-11)

Phase 15 nyquist sign-off:
- [x] All Wave 0-4 tests green (Plan 01-05)
- [x] `python -m evolution.tools.evolve_tool_reasoning --dry-run` 真实仓库上退出 0(Plan 05 Task 3 checkpoint)
- [x] ambiguous_subset_size 在 dry-run 与 test_dataset_ambiguous_size 之间一致
- [x] 385 + Wave 0-4 新测试 pytest tests/ -x 全 GREEN
- [x] Pitfall 12 守门测试 test_no_gepa_metric_added GREEN
- [x] 物理隔离守门(output/tools/ 未被 Phase 15 触达)
```
  </action>
  <verify>
    <automated>grep -E "^(status|nyquist_compliant|wave_0_complete|signed_off):" /Users/slj/项目/hermes-agent-self-evolution/.planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^status: approved" .planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md` 命中
    - `grep -q "^nyquist_compliant: true" .planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md` 命中
    - `grep -q "^wave_0_complete: true" .planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md` 命中
    - `grep -q "signed_off:" .planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md` 命中
    - `grep -q "Phase 15 nyquist sign-off" .planning/phases/15-think-augmented-tool-selection/15-VALIDATION.md` 命中
  </acceptance_criteria>
  <done>
    VALIDATION.md frontmatter 已更新为 approved 状态,记录 nyquist sign-off checklist 5 项全部 ✓。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 真实 dry-run → disk | `output/tools_reasoning/` 被创建;若 cwd 不可写则 raise |
| `--hermes-repo` flag → 仓库读取 | Path traversal;由 get_hermes_agent_path 做基本路径 validation |
| test_e2e_mock_pipeline patch 边界 | tmp_path + monkeypatch.chdir 隔离,不污染工作区 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Tampering | test_e2e_mock_pipeline 的 mock 路径 | mitigate | monkeypatch.chdir(tmp_path) 限定写入 tmp_path;pytest tmp_path 自动清理 |
| T-15-05-02 | Info Disclosure | 真实 dry-run echo 的 tool 数 / predictor 数 | accept | 非敏感数据;Phase 13/14 dry-run 已走过相同路径 |
| T-15-05-03 | Denial of Service | test_e2e_mock_pipeline 在 CI 上占用时间 | accept | mock LM 零 API 调用;单测 < 5s 完成 |
</threat_model>

<verification>
**Plan-level checks:**

1. **test_e2e_mock_pipeline GREEN:** `pytest tests/tools/test_evolve_tool_reasoning.py::test_e2e_mock_pipeline -v` 1/1 PASSED

2. **Phase 15 测试套件 10/10 GREEN:** `pytest tests/tools/test_evolve_tool_reasoning.py -v` 10/10 PASSED

3. **全集测试 GREEN:** `pytest tests/ -x --tb=short` 退出 0,0 failed,0 error

4. **真实 dry-run checkpoint:** 操作者确认退出 0 + stdout 包含所有 D-12 schema 字段

5. **数据集一致性:** dry-run 的 ambiguous_subset_size == test_dataset_ambiguous_size 的 echo 值

6. **VALIDATION.md sign-off:** frontmatter 含 status: approved / nyquist_compliant: true / wave_0_complete: true

7. **物理隔离最终守门:** `find /Users/slj/项目/hermes-agent-self-evolution/output/tools/ -type f -newer .planning/phases/15-think-augmented-tool-selection/15-CONTEXT.md 2>/dev/null | wc -l` == 0(Phase 15 执行期间 output/tools/ 无新文件)
</verification>

<success_criteria>
1. test_e2e_mock_pipeline GREEN — mock LM 端到端跑完整 Phase 15 流水线,metrics.json schema 完整,4 个输出文件齐备,ambiguous_subset_size=5(确定性)
2. Plan 01 的 10 个集成测试全部 PASSED
3. `python -m evolution.tools.evolve_tool_reasoning --iterations 1 --dry-run` 在真实 hermes-agent 上退出 0,stdout 含所有 D-12 schema 字段
4. ambiguous_subset_size 在两处观察(test_dataset_ambiguous_size + dry-run stdout)一致
5. 全集 pytest 退出 0,包括 Phase 13/14 baseline(385)+ Wave 0-4 Phase 15 新增(~30+)
6. VALIDATION.md frontmatter 更新为 approved / nyquist_compliant: true / wave_0_complete: true
7. `output/tools/` 未被 Phase 15 任何测试或真实 dry-run 触达(D-11 物理隔离终极守门)
8. 可选:evolution/tools/__init__.py 按情形 A/B/C 之一处理(Summary 记录决定)
</success_criteria>

<output>
After completion, create `.planning/phases/15-think-augmented-tool-selection/15-05-SUMMARY.md` recording:
- test_e2e_mock_pipeline GREEN 结果
- 真实 dry-run checkpoint 操作者反馈
- 真实数据集 ambiguous_subset_size 终值
- evolution/tools/__init__.py 处理决定(A/B/C 之一及理由)
- Phase 15 全部测试数(Wave 0-4 新增) + 总计 pytest 退出 0
- Phase 15 phase gate nyquist sign-off checklist
- (Optional)Phase 15 Executive Summary:17 条 D-XX 决策映射到最终 artifact 的追溯表
</output>
