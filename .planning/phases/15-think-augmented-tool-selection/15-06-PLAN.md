---
phase: 15
plan: 06
type: execute
wave: 5
gap_closure: true
depends_on:
  - "05"
files_modified:
  - evolution/tools/tool_dataset.py
  - tests/tools/test_tool_dataset.py
autonomous: true
requirements:
  - TOOL-V2-03
must_haves:
  truths:
    - "to_dspy_examples() 输出 dspy.Example 含 task_description (input) + correct_tool + correct_params + confuser_tools 四个字段"
    - "task_description 仍是唯一 input(.with_inputs('task_description'));其余字段是 label/metadata"
    - "新增/修改 test_to_dspy_examples 测试 — 断言 confuser_tools 与 correct_params 通过 to_dspy_examples 传递,正确支持 D-13 ambiguous filter `len(ex.confuser_tools) >= 2`"
    - "evolve_tool_reasoning.py --dry-run on real hermes-agent: ambiguous_subset_size=75(与 test_dataset_ambiguous_size 观察一致),与 Plan 15-05 SUMMARY 中记录的修复后值一致"
    - "Phase 13 evolve_tool_params + Phase 4 旧管线无回归 — 465+ 测试套件保持 GREEN"
  artifacts:
    - path: "evolution/tools/tool_dataset.py"
      provides: "to_dspy_examples() 修正版本,carries confuser_tools + correct_params"
      contains: "confuser_tools=ex.confuser_tools"
    - path: "tests/tools/test_tool_dataset.py"
      provides: "test_to_dspy_examples 强化:断言 confuser_tools / correct_params 字段存在;新增 test_to_dspy_examples_supports_ambiguous_filter (D-13 兼容)"
  key_links:
    - from: "evolution/tools/tool_dataset.py:to_dspy_examples"
      to: "evolution.tools.evolve_tool_reasoning._evaluate_holdout / _build_ab_comparison"
      via: "dspy.Example(... confuser_tools=...).with_inputs('task_description')"
      pattern: "confuser_tools=ex.confuser_tools"
    - from: "tests/tools/test_tool_dataset.py:test_to_dspy_examples"
      to: "evolution.tools.tool_dataset.ToolSelectionDataset.to_dspy_examples"
      via: "ds.to_dspy_examples('train')[0].confuser_tools"
      pattern: "to_dspy_examples"
---

<objective>
修复 `evolution/tools/tool_dataset.py:to_dspy_examples()` 丢失 `confuser_tools` 和 `correct_params` 的 bug。该 bug 在 Plan 15-05 执行期间被发现并 in-plan 修复 (`621e1c1`),但因超出 15-05 `files_modified` 声明被用户拒绝并 revert (`b78e70b`)。本 plan 作为独立 gap-closure 处理。

**核心问题:**
- 当前 `to_dspy_examples()` 只构造 `dspy.Example(task_description=..., correct_tool=...)`
- 缺 `correct_params` → Phase 13 `joint_tool_param_metric()` 评分 param_match 对比空 `{}`,虚高
- 缺 `confuser_tools` → Phase 15 D-13 ambiguous 过滤 `[ex for ex in holdout if len(ex.confuser_tools) >= 2]` 永远空,`ambiguous_subset_size=0`

**修复后:** dry-run 报 `ambiguous_subset_size=75`(与 `test_dataset_ambiguous_size.py` 直读 JSONL 的观察一致),Phase 15 的核心 ambiguous-improvement gate 才能被正确度量。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-think-augmented-tool-selection/15-CONTEXT.md
@.planning/phases/15-think-augmented-tool-selection/15-RESEARCH.md
@.planning/phases/15-think-augmented-tool-selection/15-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 to_dspy_examples() — 补 confuser_tools + correct_params 字段</name>
  <files>evolution/tools/tool_dataset.py</files>
  <read_first>
    - evolution/tools/tool_dataset.py:135-153(当前 to_dspy_examples 实现)
    - evolution/tools/tool_dataset.py:33-58(ToolSelectionExample 字段)
    - evolution/tools/evolve_tool_reasoning.py(grep `confuser_tools` 看下游消费点)
    - evolution/tools/evolve_tool_params.py(grep `correct_params` 看 Phase 13 消费点)
  </read_first>
  <action>
修改 `evolution/tools/tool_dataset.py:to_dspy_examples()`,在 `dspy.Example(...)` 构造中追加两个字段:

```python
def to_dspy_examples(self, split: str = "train") -> list[dspy.Example]:
    """Convert a split to DSPy Example objects.

    Only task_description is marked as input; correct_tool, correct_params,
    and confuser_tools are labels/metadata consumed by downstream metrics
    (joint_tool_param_metric in Phase 13) and filters (D-13 ambiguous subset
    in Phase 15).

    Args:
        split: Which split to convert ('train', 'val', or 'holdout').

    Returns:
        List of dspy.Example instances.
    """
    data = getattr(self, split)
    return [
        dspy.Example(
            task_description=ex.task_description,
            correct_tool=ex.correct_tool,
            correct_params=ex.correct_params,
            confuser_tools=ex.confuser_tools,
        ).with_inputs("task_description")
        for ex in data
    ]
```

保持唯一 input 仍是 `task_description`,只增加两个 label/metadata 字段。
  </action>
  <verify>
    <automated>grep -q 'confuser_tools=ex.confuser_tools' evolution/tools/tool_dataset.py && grep -q 'correct_params=ex.correct_params' evolution/tools/tool_dataset.py && .venv/bin/python -c "from evolution.tools.tool_dataset import ToolSelectionDataset, ToolSelectionExample; ds=ToolSelectionDataset(train=[ToolSelectionExample(task_description='t', correct_tool='a', correct_params={'x':1}, confuser_tools=['b','c'])]); ex=ds.to_dspy_examples('train')[0]; assert ex.confuser_tools == ['b','c'] and ex.correct_params == {'x':1} and 'task_description' in ex.inputs(); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'confuser_tools=ex.confuser_tools' evolution/tools/tool_dataset.py` ≥ 1
    - `grep -c 'correct_params=ex.correct_params' evolution/tools/tool_dataset.py` ≥ 1
    - `to_dspy_examples` docstring 提及 confuser_tools 和 correct_params 作为 label/metadata
    - `.with_inputs("task_description")` 保留(仅 task_description 是 input)
    - 上面 inline verify 命令打印 `ok`
  </acceptance_criteria>
  <done>
    `to_dspy_examples()` 输出 dspy.Example 含全 4 字段(task_description input + correct_tool/correct_params/confuser_tools label)。下游 Phase 13 param_match scoring 与 Phase 15 D-13 ambiguous filter 都能正确消费这些字段。
  </done>
</task>

<task type="auto">
  <name>Task 2: 补 test_to_dspy_examples 测试 — 覆盖 confuser_tools / correct_params 字段</name>
  <files>tests/tools/test_tool_dataset.py</files>
  <read_first>
    - tests/tools/test_tool_dataset.py:125-140(现有 test_to_dspy_examples)
    - tests/tools/test_tool_dataset.py:60-100(找 _make_examples 辅助函数确认 confuser_tools/correct_params 默认值)
  </read_first>
  <action>
强化现有 `test_to_dspy_examples`,并新增 `test_to_dspy_examples_supports_ambiguous_filter` (D-13 兼容性回归测试)。

修改/追加内容(具体增改取决于 _make_examples 当前签名,executor 自行决断):

```python
def test_to_dspy_examples(self):
    """to_dspy_examples returns dspy.Example with correct inputs + labels."""
    ds = ToolSelectionDataset(
        train=self._make_examples(3),
        val=[],
        holdout=[],
    )
    dspy_examples = ds.to_dspy_examples("train")
    assert len(dspy_examples) == 3
    ex = dspy_examples[0]
    # input
    assert hasattr(ex, "task_description")
    assert "task_description" in ex.inputs()
    # labels / metadata
    assert hasattr(ex, "correct_tool")
    assert hasattr(ex, "correct_params"), "Phase 13 joint metric needs correct_params"
    assert hasattr(ex, "confuser_tools"), "Phase 15 D-13 ambiguous filter needs confuser_tools"

def test_to_dspy_examples_supports_ambiguous_filter(self):
    """D-13 regression: ambiguous subset filter `len(ex.confuser_tools) >= 2`
    must work on dspy.Example output of to_dspy_examples (not just on raw
    ToolSelectionExample JSONL)."""
    ds = ToolSelectionDataset(
        train=[],
        val=[],
        holdout=[
            ToolSelectionExample(
                task_description="t0",
                correct_tool="a",
                confuser_tools=[],
            ),
            ToolSelectionExample(
                task_description="t1",
                correct_tool="a",
                confuser_tools=["b"],
            ),
            ToolSelectionExample(
                task_description="t2",
                correct_tool="a",
                confuser_tools=["b", "c"],
            ),
            ToolSelectionExample(
                task_description="t3",
                correct_tool="a",
                confuser_tools=["b", "c", "d"],
            ),
        ],
    )
    holdout = ds.to_dspy_examples("holdout")
    ambiguous = [ex for ex in holdout if len(ex.confuser_tools) >= 2]
    assert len(ambiguous) == 2  # only t2 + t3
    assert ambiguous[0].task_description == "t2"
    assert ambiguous[1].task_description == "t3"
```
  </action>
  <verify>
    <automated>grep -q 'test_to_dspy_examples_supports_ambiguous_filter' tests/tools/test_tool_dataset.py && .venv/bin/python -m pytest tests/tools/test_tool_dataset.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/tools/test_tool_dataset.py` 含 `test_to_dspy_examples_supports_ambiguous_filter`
    - 强化的 `test_to_dspy_examples` 断言 `correct_params` / `confuser_tools` 字段存在
    - `.venv/bin/python -m pytest tests/tools/test_tool_dataset.py -v` 全 GREEN
    - 全套测试无回归(`.venv/bin/python -m pytest tests/ -q` exit 0)
  </acceptance_criteria>
  <done>
    新增 D-13 兼容性回归测试,锁定 confuser_tools/correct_params 通过 to_dspy_examples 传递的契约。
  </done>
</task>

<task type="auto">
  <name>Task 3: 重跑 dry-run 验证 ambiguous_subset_size 一致性</name>
  <files></files>
  <read_first>
    - 无 — 这是验证步骤,不改文件
  </read_first>
  <action>
在 hermes-agent 真实仓库上重跑 dry-run + 观察脚本,断两处 ambiguous_subset_size 一致:

```bash
# 1. Run dry-run
HERMES_AGENT_REPO=/Users/slj/.hermes/hermes-agent \
  .venv/bin/python -m evolution.tools.evolve_tool_reasoning --iterations 1 --dry-run 2>&1 \
  | tee /tmp/15-06-dryrun.log

# 2. Extract ambiguous_subset_size from dry-run output
DRY_RUN_VALUE=$(grep -oE 'ambiguous_subset_size=[0-9]+' /tmp/15-06-dryrun.log | head -1 | cut -d= -f2)
echo "dry-run ambiguous_subset_size=$DRY_RUN_VALUE"

# 3. Run dataset observation
.venv/bin/python -m pytest tests/tools/test_dataset_ambiguous_size.py -v -s 2>&1 \
  | tee /tmp/15-06-observe.log

OBSERVE_VALUE=$(grep -oE 'ambiguous_subset_size=[0-9]+' /tmp/15-06-observe.log | head -1 | cut -d= -f2)
echo "observe ambiguous_subset_size=$OBSERVE_VALUE"

# 4. Assert consistency
if [ "$DRY_RUN_VALUE" = "$OBSERVE_VALUE" ] && [ -n "$DRY_RUN_VALUE" ]; then
  echo "PASS: dry-run and observation agree on ambiguous_subset_size=$DRY_RUN_VALUE"
else
  echo "FAIL: dry-run=$DRY_RUN_VALUE observe=$OBSERVE_VALUE"; exit 1
fi
```
  </action>
  <verify>
    <automated>HERMES_AGENT_REPO=/Users/slj/.hermes/hermes-agent .venv/bin/python -m evolution.tools.evolve_tool_reasoning --iterations 1 --dry-run 2>&1 | grep -E 'ambiguous_subset_size=[0-9]+' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - dry-run exit=0
    - dry-run 输出包含 `ambiguous_subset_size=N` (N > 0)
    - test_dataset_ambiguous_size.py 报告同样的 N
    - 两处一致(N 相等)
  </acceptance_criteria>
  <done>
    Phase 15 dry-run 与数据集观察脚本对 ambiguous 子集大小给出一致结果。Plan 15-05 success criterion "N 与 metrics.json 在 dry-run 下 echo 值一致" 解锁。
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-15-06-01 | Tampering | tool_dataset.py | accept | 修改是纯字段补充,不引入外部输入解析或反序列化路径 |
| T-15-06-02 | Info Disclosure | confuser_tools 暴露 | accept | confuser_tools 是 dataset 内部 metadata,已通过 JSONL 持久化,不引入新信息泄漏面 |
</threat_model>

<verification>
1. `to_dspy_examples()` 输出含 4 字段(task_description input + 3 labels)
2. `test_to_dspy_examples` + `test_to_dspy_examples_supports_ambiguous_filter` 全 GREEN
3. dry-run 与 test_dataset_ambiguous_size 对 ambiguous_subset_size 报告一致
4. Phase 13 (`tests/tools/test_evolve_tool_params*.py`) 全套不退化(joint metric param_match 现在拿到真实 correct_params 而非空 dict — 任何分数变化属于 bug fix 的预期效应)
5. 整体 `tests/` 套件 ≥465 GREEN
</verification>

<success_criteria>
1. `evolution/tools/tool_dataset.py:to_dspy_examples()` 含 `correct_params` 与 `confuser_tools` 字段
2. `tests/tools/test_tool_dataset.py` 新增 `test_to_dspy_examples_supports_ambiguous_filter`,GREEN
3. dry-run on `~/.hermes/hermes-agent`: exit=0 且 `ambiguous_subset_size > 0`
4. dry-run 的 ambiguous_subset_size 与 `test_dataset_ambiguous_size.py` 一致
5. 全套测试 ≥465 GREEN,零回归
</success_criteria>

<output>
After completion, create `.planning/phases/15-think-augmented-tool-selection/15-06-SUMMARY.md` recording:
- 修改前后 `to_dspy_examples()` 的对比 + 修复理由
- dry-run 测得的 ambiguous_subset_size 实际值
- 全套测试结果(passed/skipped/failed 计数)
- 与 Plan 15-05 sign-off 衔接的说明:本 plan 完成后,15-05 的 Task 4 (VALIDATION.md sign-off) 可由继任 agent 安全推进
</output>
