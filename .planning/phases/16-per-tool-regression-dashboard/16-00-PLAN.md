---
plan: 16-00-foundation
phase: 16
type: execute
wave: 0
depends_on: []
files_modified:
  - path: evolution/tools/tool_metric.py
    role: modified
  - path: evolution/tools/tool_dataset.py
    role: modified
  - path: evolution/tools/evolve_tool_params.py
    role: modified
  - path: evolution/tools/evolve_tool_descriptions.py
    role: modified
  - path: evolution/tools/evolve_tool_reasoning.py
    role: modified
  - path: tests/tools/test_persist_raw_predictions.py
    role: new
  - path: tests/tools/test_tool_dataset.py
    role: modified
  - path: tests/tools/test_evolve_tool_params_cli.py
    role: modified
  - path: tests/tools/test_evolve_tool_descriptions.py
    role: modified
  - path: tests/tools/test_evolve_tool_reasoning.py
    role: modified
requirements: [TOOL-V2-04]
autonomous: true
estimated_minutes: 110

must_haves:
  truths:
    - "tool_metric.persist_raw_predictions exists with signature (metrics: dict, raw_predictions: list[dict]) -> dict"
    - "persist_raw_predictions does not mutate its inputs (shallow copy + per-record cleaning)"
    - "persist_raw_predictions emits stdout yellow warning when len(raw_predictions) > 2000"
    - "tool_dataset.to_dspy_examples includes difficulty in returned dspy.Example"
    - "evolve_tool_params metrics.json includes raw_predictions list (per_tool_*_rates already present from Phase 13)"
    - "evolve_tool_descriptions metrics.json includes per_tool_baseline_rates / per_tool_evolved_rates / raw_predictions (newly wired) (D-12 amend)"
    - "evolve_tool_reasoning metrics.json includes per_tool_baseline_rates / per_tool_evolved_rates / raw_predictions (newly wired) (D-12 amend)"
    - "raw_predictions schema 字段 = correct_tool / selected_tool / difficulty / num_available_tools(D-11 segment 切片源数据;num_available_tools = len(confuser_tools) + 1)"
    - "三 CLI metrics.json 都含 per_tool_*_rates + raw_predictions(D-12;Wave 0 数据基础;支持 D-08 fallback 路径)"
  artifacts:
    - path: evolution/tools/tool_metric.py
      provides: "persist_raw_predictions helper (D-12)"
      contains: "def persist_raw_predictions"
    - path: tests/tools/test_persist_raw_predictions.py
      provides: "Helper unit tests (immutability, empty/large list, field coercion, missing key fallback)"
    - path: tests/fixtures/dashboard_runs/
      provides: "Wave 1+ dashboard test fixture root (5 scenario subdirs created in Wave 0 stub but populated in Wave 1)"
  key_links:
    - from: evolution/tools/evolve_tool_params.py
      to: evolution/tools/tool_metric.py:persist_raw_predictions
      via: import + call after persist_per_tool_rates at line 1017
      pattern: "persist_raw_predictions\\(metrics,"
    - from: evolution/tools/evolve_tool_descriptions.py
      to: evolution/tools/tool_metric.py:persist_per_tool_rates+persist_raw_predictions
      via: new import + two calls before metrics.json write
      pattern: "from evolution.tools.tool_metric import.*persist_per_tool_rates.*persist_raw_predictions"
    - from: evolution/tools/evolve_tool_reasoning.py
      to: evolution/tools/tool_metric.py:persist_per_tool_rates+persist_raw_predictions
      via: new _score_with_predictions helper + two calls before metrics.json write
      pattern: "_score_with_predictions"
---

<objective>
Wave 0 — 数据基础。本 wave 解锁 Wave 1-4 dashboard 的数据来源,核心是把 `raw_predictions` schema 落到三 CLI 的 metrics.json 里,并把 `persist_per_tool_rates` 接线扩到所有三个 evolve_tool_* CLI(D-12 + Open Question 1 用户已决策)。

Purpose: D-08 fallback「缺 per_tool_*_rates → 整 run dropped」叠加现实「desc/reasoning 两 CLI 完全没接 helper」会让 dashboard 启动时 90% run 落入 dropped_runs[]——本 wave 关闭这个数据缺口,使 Phase 16 dashboard 上线后能看到完整 per-tool 数据。

**Scope boundary (W4 explicit)**: Wave 0 接线只影响**未来**新跑的 run。CONTEXT.md `## Out-of-scope` 第 6 条仍 hold —— 老的 `FAILED_*/` run 不被回填,继续走 `dropped_runs` fallback。这与 D-12 amend 不冲突:D-12 amend 针对未来 run 的字段集落地,Out-of-scope 第 6 条针对历史 run 的不回填策略。两者并存:历史 run 仍 dropped,未来 run 完整数据可见。

Output:
- `evolution/tools/tool_metric.py` 新增 `persist_raw_predictions` helper(镜像 `persist_per_tool_rates` 不可变模式)
- `evolution/tools/tool_dataset.py:135-158` 扩 `to_dspy_examples` 让 dspy.Example 携带 `difficulty`
- 三个 CLI(`evolve_tool_params.py` / `evolve_tool_descriptions.py` / `evolve_tool_reasoning.py`)在 holdout 评估后统一接 `persist_per_tool_rates(...)` + `persist_raw_predictions(...)` 两行
- `tests/tools/test_persist_raw_predictions.py` 新建,4 个测试覆盖 helper
- `tests/tools/test_tool_dataset.py` / `test_evolve_tool_*.py` 三个 CLI 测试加新断言
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md
@.planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md
@.planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md
@.planning/phases/16-per-tool-regression-dashboard/16-VALIDATION.md
@.planning/phases/13-per-parameter-description-optimization/13-CONTEXT.md

<interfaces>
<!-- Key existing contracts the executor needs verbatim. Extracted from current codebase. -->

From evolution/tools/tool_metric.py:439-477 (the analog template — Wave 0 helper mirrors this):
```python
def persist_per_tool_rates(
    metrics: dict,
    baseline_rates: dict[str, float],
    evolved_rates: dict[str, float],
) -> dict:
    out = dict(metrics)  # shallow copy — callers may reuse `metrics` afterwards
    out["per_tool_baseline_rates"] = {
        k: float(v) for k, v in sorted((baseline_rates or {}).items())
    }
    out["per_tool_evolved_rates"] = {
        k: float(v) for k, v in sorted((evolved_rates or {}).items())
    }
    return out
```

From evolution/tools/tool_dataset.py:135-158 (current to_dspy_examples — needs difficulty added):
```python
def to_dspy_examples(self, split: str = "train") -> list[dspy.Example]:
    data = getattr(self, split)
    return [
        dspy.Example(
            task_description=ex.task_description,
            correct_tool=ex.correct_tool,
            correct_params=ex.correct_params,
            confuser_tools=ex.confuser_tools,
        ).with_inputs("task_description")
        for ex in data
    ]
```

From evolution/tools/tool_dataset.py ToolSelectionExample (existing fields):
```python
# task_description: str, correct_tool: str, correct_params: dict,
# difficulty: str = "medium" (∈ {"easy","medium","hard"}),
# confuser_tools: list[str], reason: str, source: str, misselection_signals: list[str]
# NOTE: there is NO `available_tools` field — D-11 num_available_tools = len(confuser_tools) + 1
```

From evolution/tools/evolve_tool_params.py:1012-1017 (existing wire-point):
```python
regression_checker = CrossToolRegressionChecker()
baseline_rates = regression_checker.compute_per_tool_rates(baseline_tool_pairs)
evolved_rates = regression_checker.compute_per_tool_rates(evolved_tool_pairs)
regression_result = regression_checker.check_regression(baseline_rates, evolved_rates)
metrics = persist_per_tool_rates(metrics, baseline_rates, evolved_rates)
# ↑ Phase 16 inserts persist_raw_predictions(metrics, raw_preds) right after this line
```

From evolution/tools/evolve_tool_descriptions.py:349-369 (REAL control-flow — critical for BLOCKER 1 fix):
```python
# line 349-354 — happy-path setup before regression check
regression_checker = CrossToolRegressionChecker()
baseline_rates = regression_checker.compute_per_tool_rates(baseline_preds)
evolved_rates = regression_checker.compute_per_tool_rates(evolved_preds)
regression_result = regression_checker.check_regression(baseline_rates, evolved_rates)

# line 356-369 — FAILED branch (returns early, do NOT inject persist_per_tool_rates here)
if not regression_result.passed:
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    output_dir = Path("output") / "tools" / f"FAILED_{timestamp}"
    # ... writes FAILED metrics.json ...
    return  # ← happy-path NEVER reaches lines below if this branch fires

# line 371+ — happy-path continues (this is where Phase 16 metrics_extra goes)
# line 405-422 — metrics dict literal in happy-path
```

From evolution/tools/evolve_tool_reasoning.py:615-633 (current _safe_score — needs sibling _score_with_predictions):
```python
def _safe_score(module: Any, examples: list, lm: Any) -> float:
    if not examples:
        return 0.0
    try:
        return float(_score_module_on_holdout(module, examples, lm))
    except Exception:
        return 0.0
```

From evolution/tools/evolve_tool_reasoning.py:465-468 (the 4 _safe_score call-sites — only first 2 get replaced):
```python
# line 465-466 — full holdout scoring (REPLACE with _score_with_predictions)
th_off_full = _safe_score(baseline_module, eval_holdout, lm)
th_on_full = _safe_score(optimized_module, eval_holdout, lm)
# line 467-468 — ambiguous subset scoring (KEEP _safe_score; only mean needed)
ambiguous_off = _safe_score(baseline_module, ambiguous_subset, lm)
ambiguous_on = _safe_score(optimized_module, ambiguous_subset, lm)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 16-00-1: 新建 persist_raw_predictions helper + 单元测试</name>
  <files>evolution/tools/tool_metric.py, tests/tools/test_persist_raw_predictions.py</files>
  <read_first>
    - evolution/tools/tool_metric.py (整个文件,以理解现有 imports 与 persist_per_tool_rates 在 line 442-477 的位置)
    - tests/tools/test_cross_tool_regression.py (Phase 13 helper 测试模板 — 整个文件,80 LoC)
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Excerpt A — persist_raw_predictions 镜像模板" 与 §"Test Pattern Map" 行
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"persist_raw_predictions 函数签名草稿"
    - .planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md §D-12
  </read_first>
  <behavior>
    - test_immutability_and_shape: 调 helper 后入参 metrics dict 与入参 list 都未被修改;返回值含原 metrics 全部 keys + 新 key `raw_predictions`;返回值是新 dict 对象(`out is not metrics`);每条 record 的 keys 集合精确等于 `{"correct_tool","selected_tool","difficulty","num_available_tools"}`
    - test_field_coercion: 入参 record 含额外 keys(如 `extra_field`)时,这些 keys 不出现在返回值;`correct_tool=None` 被强转为 `""`;`num_available_tools=None` 被强转为 `0`;`difficulty` 缺失时默认 `"medium"`
    - test_empty_and_none: `persist_raw_predictions(metrics, [])` 返回 `out["raw_predictions"] == []`;`persist_raw_predictions(metrics, None)` 同样返回 `out["raw_predictions"] == []`
    - test_large_list_warning: 传 2001 条 record 时,helper 触发 stdout(用 capsys / Console capture)含 `"raw_predictions large"` 字符串,但仍返回完整列表(不截断)
  </behavior>
  <action>
在 `evolution/tools/tool_metric.py` 文件 line 477 之后(即紧贴 `persist_per_tool_rates` 函数定义之下)追加新函数 `persist_raw_predictions`,签名与实现完全镜像 RESEARCH §"persist_raw_predictions 函数签名草稿"。具体要求:

1. **签名**: `def persist_raw_predictions(metrics: dict, raw_predictions: list[dict]) -> dict:`
2. **docstring**: 第一段「Merge raw_predictions list into a metrics dict (Phase 16: D-12 dashboard).」 第二段「Pattern matches persist_per_tool_rates: shallow copy of metrics, None tolerance, stable schema. Differs from persist_per_tool_rates: does NOT sort (raw_predictions is order-sensitive time-series); does NOT all-float-coerce (per-field types are str/str/str/int); emits stdout yellow warning when len > 2000 (Pitfall 10 retention placeholder).」第三段 Args 列出 `metrics` 与 `raw_predictions`;Returns 段说明返回新 dict 含原 metrics keys 加 `raw_predictions: list[dict]`。
3. **实现**:
   ```python
   out = dict(metrics)  # shallow copy
   cleaned: list[dict] = []
   for rec in (raw_predictions or []):
       cleaned.append({
           "correct_tool": str(rec.get("correct_tool", "") or ""),
           "selected_tool": str(rec.get("selected_tool", "") or ""),
           "difficulty": str(rec.get("difficulty", "medium") or "medium"),
           "num_available_tools": int(rec.get("num_available_tools", 0) or 0),
       })
   out["raw_predictions"] = cleaned
   if len(cleaned) > 2000:
       console.print(
           f"[yellow]raw_predictions large ({len(cleaned)} records); "
           f"metrics.json may exceed 1MB[/yellow]"
       )
   return out
   ```
4. **不**新增 `import` — `console` 已在文件顶部 module-level 声明(检查 line 1-30 现有 imports);若发现没有 module-level `console = Console()`,则用 `from rich.console import Console; Console().print(...)` inline 风格(实测 tool_metric.py 现有 console 引用,直接复用)。

新建 `tests/tools/test_persist_raw_predictions.py` 文件,文件头部:
```python
"""Unit tests for persist_raw_predictions (Phase 16: D-12 dashboard helper)."""
import pytest

pytest.importorskip("dspy")  # match tests/tools/ defensive convention

from evolution.tools.tool_metric import persist_raw_predictions
```

包含 4 个测试函数,严格按 <behavior> 行为编写:
- `def test_immutability_and_shape():` 构 `metrics = {"timestamp": "abc", "iterations": 5}`、`recs = [{"correct_tool": "a", "selected_tool": "b", "difficulty": "easy", "num_available_tools": 3}]`,调 `out = persist_raw_predictions(metrics, recs)`,断言 `metrics == {"timestamp": "abc", "iterations": 5}`(未被 mutate)、`recs[0]` 仍然原值、`out is not metrics`、`out["timestamp"] == "abc"`、`out["raw_predictions"][0].keys() == {"correct_tool","selected_tool","difficulty","num_available_tools"}`、`len(out["raw_predictions"]) == 1`。
- `def test_field_coercion():` 构 `recs = [{"correct_tool": "a", "selected_tool": None, "difficulty": "medium", "num_available_tools": None, "extra_field": "leak"}]`,调用后断言 `out["raw_predictions"][0]["selected_tool"] == ""`、`out["raw_predictions"][0]["num_available_tools"] == 0`、`"extra_field" not in out["raw_predictions"][0]`。再构一条缺 `difficulty` 的 `{"correct_tool": "x", "selected_tool": "y", "num_available_tools": 1}`,断言 `out["raw_predictions"][0]["difficulty"] == "medium"`。
- `def test_empty_and_none():` `persist_raw_predictions({}, [])` → `out["raw_predictions"] == []`;`persist_raw_predictions({}, None)` → `out["raw_predictions"] == []`(None 容错)。
- `def test_large_list_warning(capsys):` 构 2001 条 record,调 helper,断言 `out["raw_predictions"]` 长度仍是 2001(不截断);`captured = capsys.readouterr(); assert "raw_predictions large" in captured.out`。注意 Rich Console 默认写到 stdout,capsys 应能捕获;如果失败,改用 `from rich.console import Console; from io import StringIO; console_test = Console(file=StringIO())` 注入风格(参考 RESEARCH §"Rich 渲染异常" 中提到的 `Console(record=True)` 模式)。

不修改其他文件。运行 `.venv/bin/pytest tests/tools/test_persist_raw_predictions.py -x` 必须 4 测试全绿。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_persist_raw_predictions.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def persist_raw_predictions' evolution/tools/tool_metric.py` 输出 1 行
    - `grep -n 'def persist_raw_predictions(metrics: dict, raw_predictions: list\[dict\]) -> dict:' evolution/tools/tool_metric.py` 输出 1 行(签名精确)
    - `grep -n 'raw_predictions large' evolution/tools/tool_metric.py` 输出 1 行(size warning 字符串存在)
    - `grep -c '^def test_' tests/tools/test_persist_raw_predictions.py` 输出 `4`(精确 4 个测试函数)
    - `.venv/bin/pytest tests/tools/test_persist_raw_predictions.py -x` 退出码 0
    - `python -c "from evolution.tools.tool_metric import persist_raw_predictions; m={'a':1}; out=persist_raw_predictions(m,[]); assert out is not m and out['raw_predictions']==[] and m=={'a':1}"` 退出码 0
  </acceptance_criteria>
  <done>
    persist_raw_predictions 镜像 persist_per_tool_rates 不可变模式实现完成,4 单元测试全绿。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-00-2: 扩 to_dspy_examples 加 difficulty 字段</name>
  <files>evolution/tools/tool_dataset.py, tests/tools/test_tool_dataset.py</files>
  <read_first>
    - evolution/tools/tool_dataset.py (整个文件,~165 LoC,理解 ToolSelectionExample 字段集与 to_dspy_examples 现状)
    - tests/tools/test_tool_dataset.py (现有 TestToolSelectionExample 类风格)
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Excerpt B — to_dspy_examples 加 difficulty 字段"
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"raw_predictions 字段精确 schema" — difficulty 是 raw_predictions schema 第三个字段
  </read_first>
  <behavior>
    - test_dspy_example_has_difficulty: 用 `ToolSelectionExample(task_description="t", correct_tool="x", correct_params={}, difficulty="hard", confuser_tools=["y"])` 构样本,放到 dataset.holdout(或 train),调 `to_dspy_examples("holdout")` 返回的 dspy.Example 上 `getattr(ex, "difficulty", None) == "hard"`(non-None,值精确等于设置值)
    - test_dspy_example_difficulty_default_medium: 不传 difficulty 构 ToolSelectionExample(走 dataclass default),返回的 dspy.Example 上 `getattr(ex, "difficulty", None) == "medium"`
    - test_dspy_example_inputs_unchanged: 调 to_dspy_examples 返回的 ex 上 `ex.inputs() == {"task_description": "t"}`(只 task_description 是 input,difficulty 仍是 metadata 不是 input)
  </behavior>
  <action>
修改 `evolution/tools/tool_dataset.py:135-158` 的 `to_dspy_examples` 方法:

1. 在 `confuser_tools=ex.confuser_tools,` 之后(line 155)、`).with_inputs("task_description")` 之前(line 156)追加一行 `difficulty=ex.difficulty,`。最终 dspy.Example 构造块为:
   ```python
   dspy.Example(
       task_description=ex.task_description,
       correct_tool=ex.correct_tool,
       correct_params=ex.correct_params,
       confuser_tools=ex.confuser_tools,
       difficulty=ex.difficulty,
   ).with_inputs("task_description")
   ```
2. 同步更新 docstring 第二段(line 138-141),把 `(joint_tool_param_metric in Phase 13) and filters (D-13 ambiguous subset in Phase 15).` 改为 `(joint_tool_param_metric in Phase 13), filters (D-13 ambiguous subset in Phase 15), and segmentation (D-11 in Phase 16).` 即追加「and segmentation (D-11 in Phase 16).」短语。
3. **不**改 `.with_inputs("task_description")` — difficulty 是 metadata 不是 input。
4. **不**改 ToolSelectionExample 数据类(其本身已有 `difficulty: str = "medium"` 字段)。

在 `tests/tools/test_tool_dataset.py` 文件末尾追加 3 个测试函数(找到现有最后一个 test class 定义,在其 outdent 后追加):

```python
class TestToDspyExamplesDifficulty:
    """Phase 16 Wave 0: to_dspy_examples must include difficulty (D-11 segmentation)."""

    def test_dspy_example_has_difficulty(self):
        from evolution.tools.tool_dataset import ToolSelectionDataset, ToolSelectionExample
        ex = ToolSelectionExample(
            task_description="t", correct_tool="x", correct_params={},
            difficulty="hard", confuser_tools=["y"],
        )
        ds = ToolSelectionDataset(holdout=[ex])
        out = ds.to_dspy_examples("holdout")
        assert len(out) == 1
        assert getattr(out[0], "difficulty", None) == "hard"

    def test_dspy_example_difficulty_default_medium(self):
        from evolution.tools.tool_dataset import ToolSelectionDataset, ToolSelectionExample
        ex = ToolSelectionExample(
            task_description="t", correct_tool="x", correct_params={},
            confuser_tools=["y"],
        )
        ds = ToolSelectionDataset(holdout=[ex])
        out = ds.to_dspy_examples("holdout")
        assert getattr(out[0], "difficulty", None) == "medium"

    def test_dspy_example_inputs_unchanged(self):
        from evolution.tools.tool_dataset import ToolSelectionDataset, ToolSelectionExample
        ex = ToolSelectionExample(
            task_description="t", correct_tool="x", correct_params={},
            difficulty="hard", confuser_tools=["y"],
        )
        ds = ToolSelectionDataset(holdout=[ex])
        out = ds.to_dspy_examples("holdout")
        assert set(out[0].inputs().keys()) == {"task_description"}
```

如果 `ToolSelectionDataset` 构造器签名不匹配 `holdout=[ex]`(即可能需要先 `ds = ToolSelectionDataset(); ds.holdout = [ex]`),按现有文件实测的构造方式调整。同样 import 语句可放文件顶部 import block。

不修改其他文件。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_tool_dataset.py::TestToDspyExamplesDifficulty -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'difficulty=ex.difficulty' evolution/tools/tool_dataset.py` 输出至少 1 行
    - `grep -n 'segmentation (D-11 in Phase 16)' evolution/tools/tool_dataset.py` 输出 1 行
    - `grep -n 'class TestToDspyExamplesDifficulty' tests/tools/test_tool_dataset.py` 输出 1 行
    - `grep -c 'def test_' tests/tools/test_tool_dataset.py | awk '{print $1}'` 至少为 3 加上现有计数(即比之前多 3 个 test)
    - `.venv/bin/pytest tests/tools/test_tool_dataset.py::TestToDspyExamplesDifficulty -x` 退出码 0
    - `python -c "from evolution.tools.tool_dataset import ToolSelectionDataset, ToolSelectionExample; ex=ToolSelectionExample(task_description='t', correct_tool='x', correct_params={}, difficulty='hard', confuser_tools=['y']); ds=ToolSelectionDataset(holdout=[ex]); out=ds.to_dspy_examples('holdout'); assert getattr(out[0],'difficulty',None)=='hard'"` 退出码 0
  </acceptance_criteria>
  <done>
    to_dspy_examples 现在为 dspy.Example 携带 difficulty,3 测试全绿,Wave 1+ 的 dashboard 分桶切片可正常读取此字段。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-00-3: evolve_tool_params CLI 接 persist_raw_predictions</name>
  <files>evolution/tools/evolve_tool_params.py, tests/tools/test_evolve_tool_params_cli.py</files>
  <read_first>
    - evolution/tools/evolve_tool_params.py (line 60-90 imports 块 + line 343-432 _evaluate_holdout + line 1000-1030 helper 接线点)
    - evolution/tools/tool_metric.py (line 442-477 persist_per_tool_rates 模板 — task 1 已新建 persist_raw_predictions 紧贴其下)
    - tests/tools/test_evolve_tool_params_cli.py (整个文件 — CliRunner+patch 模式)
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Excerpt C — Phase 13 唯一已接 helper 接线点"
    - .planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md §D-12 (locked-decision: persist_raw_predictions 接线在三 CLI)
  </read_first>
  <behavior>
    - test_metrics_includes_raw_predictions: mock holdout 跑通 → 读 `metrics.json` → 断言 `"raw_predictions" in metrics` 且 `metrics["raw_predictions"]` 是 list,每条 record 含精确 4 keys `{"correct_tool","selected_tool","difficulty","num_available_tools"}`,值类型 str/str/str/int
    - test_metrics_includes_raw_predictions_preserves_per_tool_rates: 同上,额外断言 `"per_tool_baseline_rates" in metrics` 与 `"per_tool_evolved_rates" in metrics`(Phase 13 既有字段未被破坏)
  </behavior>
  <action>
修改 `evolution/tools/evolve_tool_params.py`:

1. **Import 扩展**: 找到现有 import 块(line 71-76),把 `persist_per_tool_rates` 那一项改为 `persist_per_tool_rates,\n    persist_raw_predictions,`(增加 `persist_raw_predictions` 到 multi-line import tuple)。最终 import 块形如:
   ```python
   from evolution.tools.tool_metric import (
       joint_tool_param_metric,
       joint_tool_param_metric_with_feedback,
       CrossToolRegressionChecker,
       persist_per_tool_rates,
       persist_raw_predictions,
   )
   ```

2. **接线**: 在 line 1017 `metrics = persist_per_tool_rates(metrics, baseline_rates, evolved_rates)` 之后、line 1019 `if not regression_result.passed:` 之前,插入新 block:
   ```python
   # ── 13b. Persist raw_predictions for Phase 16 dashboard distribution ──
   raw_preds: list[dict] = []
   for ex, (correct, selected) in zip(holdout, evolved_tool_pairs):
       raw_preds.append({
           "correct_tool": correct,
           "selected_tool": selected,
           "difficulty": getattr(ex, "difficulty", "medium") or "medium",
           "num_available_tools": len(getattr(ex, "confuser_tools", []) or []) + 1,
       })
   metrics = persist_raw_predictions(metrics, raw_preds)
   ```
   注意:`holdout` 在该作用域内是 `_evaluate_holdout` 的 holdout 参数(检查 line 998-1003 的调用上下文,确认 `holdout` 变量在此可见;若不可见,从 `_evaluate_holdout` 上游传入处取——典型情况下在同一函数体内 holdout 是已经定义的本地变量)。`evolved_tool_pairs` 是 line 1002-1004 已经返回的 tuple list。

3. **不**改 `_evaluate_holdout` 函数签名(Open Question 3 已决:保留签名,在外部 zip 出 raw_predictions)。

修改 `tests/tools/test_evolve_tool_params_cli.py`:

在文件末尾追加 1 个测试函数:
```python
def test_metrics_includes_raw_predictions(tmp_path, monkeypatch):
    """Phase 16 Wave 0 (D-12): metrics.json must include raw_predictions list with 4-key schema."""
    # 复用文件内现有的 GEPA mock 模式(参照同文件 line 64-99 的 test_loud_gepa_failure_and_opt_in
    # 或类似名为 *_cli 的测试 — 找到能跑通 happy-path 到写盘的最简 mock)
    # 关键断言部分:
    runner = CliRunner()
    # ...(沿用现有 fixture / patch 配置,让 holdout 走通)
    # result = runner.invoke(main, [...], catch_exceptions=True)
    # assert result.exit_code == 0
    # metrics = json.loads((tmp_path / "<output_dir>" / "metrics.json").read_text())
    assert "raw_predictions" in metrics
    assert isinstance(metrics["raw_predictions"], list)
    assert "per_tool_baseline_rates" in metrics  # Phase 13 既有字段保留
    assert "per_tool_evolved_rates" in metrics
    if metrics["raw_predictions"]:
        first = metrics["raw_predictions"][0]
        assert set(first.keys()) == {"correct_tool", "selected_tool", "difficulty", "num_available_tools"}
        assert isinstance(first["correct_tool"], str)
        assert isinstance(first["selected_tool"], str)
        assert isinstance(first["difficulty"], str)
        assert isinstance(first["num_available_tools"], int)
```

具体 patch / fixture 配置照搬同文件现有的 happy-path 测试(如 `test_loud_gepa_failure_and_opt_in` 或文件中其他已经能跑通 holdout 写盘的测试);如果现有所有测试都是错误路径(FAILED_*),则 mock 让 GEPA 返回原 module、让 V1BaselineGate 返回 passed=True、让 CrossToolRegressionChecker.check_regression 返回 ConstraintResult(passed=True)。

**Fallback 策略(BLOCKER 4 fix — 严格约束)**:如果具体 CLI happy-path mock 复杂度高于 30 LoC 而你必须 fallback,**fallback 必须经过真实 holdout 评估代码;helper 纯单测不构成 acceptance pass**。可接受的 fallback 形态二选一:

- **形态 A(优先)**:import `_evaluate_holdout` + 构 holdout fixture(2-3 examples)直接调真实函数,绕过 GEPA optimizer 但走完真实 raw_preds 收集块
- **形态 B**:`monkeypatch.setattr("dspy.GEPA", lambda *a, **kw: <fake optimizer returning fixture optimized_module>)` 并保留真实 holdout evaluation 代码路径 — 即 `_evaluate_holdout` 与 line 1018-1027 `raw_preds = [...]; persist_raw_predictions(...)` 收集块必须由测试触发执行

**拒绝形态**:在测试里复制粘贴 5 行 `raw_preds = [...]` 收集逻辑做断言 + 直接调 `persist_raw_predictions` helper —— 这绕过 CLI 接线代码,不算 acceptance 通过。这种纯单测在 `test_persist_raw_predictions.py` 已覆盖,不补价值。如果实在必须采用,test 名改为 `test_persist_raw_predictions_helper_extra` 并在 task 16-00-3 之外开一个新 ticket 跟踪 CLI 集成测试缺失。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_evolve_tool_params_cli.py::test_metrics_includes_raw_predictions -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'persist_raw_predictions' evolution/tools/evolve_tool_params.py` 输出至少 2 行(import + 调用)
    - `grep -n 'raw_preds: list\[dict\] = \[\]' evolution/tools/evolve_tool_params.py` 输出 1 行(初始化)
    - `grep -n 'metrics = persist_raw_predictions(metrics, raw_preds)' evolution/tools/evolve_tool_params.py` 输出 1 行
    - `grep -n 'len(getattr(ex, "confuser_tools", \[\]) or \[\]) + 1' evolution/tools/evolve_tool_params.py` 输出 1 行(num_available_tools 公式)
    - `grep -n 'def test_metrics_includes_raw_predictions' tests/tools/test_evolve_tool_params_cli.py` 输出 1 行
    - `.venv/bin/pytest tests/tools/test_evolve_tool_params_cli.py::test_metrics_includes_raw_predictions -x` 退出码 0
    - `.venv/bin/pytest tests/tools/test_evolve_tool_params_cli.py -x` 整文件全绿(回归确认未破坏 Phase 13 测试)
    - **BLOCKER 4 强制**: 测试函数体内必须出现 `_evaluate_holdout` 直接调用,或 `monkeypatch.setattr("dspy.GEPA", ...)` patch + invoke main 完整跑通 — `grep -E '_evaluate_holdout\(|monkeypatch\.setattr.*dspy\.GEPA|monkeypatch\.setattr.*GEPA' tests/tools/test_evolve_tool_params_cli.py` 输出至少 1 行(证明经过真实接线代码,非 helper 纯单测)
  </acceptance_criteria>
  <done>
    evolve_tool_params holdout 写 metrics 时同时含 per_tool_*_rates(Phase 13 已有)+ raw_predictions(Phase 16 新增),回归测试无破坏,acceptance 测试经过真实 CLI/holdout 评估代码路径。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-00-4: evolve_tool_descriptions CLI 接两个 helper(per_tool_rates + raw_predictions)</name>
  <files>evolution/tools/evolve_tool_descriptions.py, tests/tools/test_evolve_tool_descriptions.py</files>
  <read_first>
    - evolution/tools/evolve_tool_descriptions.py (整个文件,理解 imports / line 326-422 holdout block / **关键: line 356-369 `if not regression_result.passed:` 块的 early return** / metrics dict literal 结构)
    - evolution/tools/evolve_tool_params.py (line 71-76 import 模板 + line 1012-1027 接线模板 — task 16-00-3 刚完成的版本)
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Excerpt E — evolve_tool_descriptions 当前 holdout 接线" 段
    - .planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md §D-12 (D-12 amend: 三 CLI 都接两 helper) §<deferred> 第 6 条 amend 「不回填老 run」
  </read_first>
  <behavior>
    - test_metrics_includes_per_tool_and_raw: mock holdout 跑通 → 读 `metrics.json` → 断言三个新字段都在: `"per_tool_baseline_rates" in metrics` ∧ `"per_tool_evolved_rates" in metrics` ∧ `"raw_predictions" in metrics`
    - test_metrics_raw_predictions_schema: 每条 record 含精确 4 keys + 类型正确(同 task 16-00-3 schema)
  </behavior>
  <action>
修改 `evolution/tools/evolve_tool_descriptions.py`:

1. **Import 扩展**: 找到 import `CrossToolRegressionChecker` 的那一行(实测在 line 1-50 imports block 里),改为:
   ```python
   from evolution.tools.tool_metric import (
       CrossToolRegressionChecker,
       persist_per_tool_rates,
       persist_raw_predictions,
   )
   ```
   如果当前是单行 import,改为多行 tuple import 风格(与 evolve_tool_params.py:71-76 一致)。

2. **Holdout 循环改造**: 找到 line 326-339 的 holdout 评估循环,**保留** `baseline_preds: list[tuple[str, str]] = []` 和 `evolved_preds: list[tuple[str, str]] = []` 现有结构,**新增** 在循环开始前 `raw_preds: list[dict] = []`,在循环体内每次 `evolved_preds.append(...)` 之后追加:
   ```python
   raw_preds.append({
       "correct_tool": ex.correct_tool,
       "selected_tool": getattr(ep, "selected_tool", "") or "",
       "difficulty": getattr(ex, "difficulty", "medium") or "medium",
       "num_available_tools": len(getattr(ex, "confuser_tools", []) or []) + 1,
   })
   ```

3. **接两个 helper(BLOCKER 1 fix — 严格插入位置)**:

   **目标位置**:在 `if not regression_result.passed:` block(line 356-369)**完全闭合后**(即在该 block 的 `return` 之后)、line 405-422 的 metrics dict 字面量 `metrics = { ... }` **之前**插入新 block。
   
   具体说明:line 354 `regression_result = regression_checker.check_regression(...)` 之后是 line 356 起的 FAILED 分支(line 356-369)。该分支有 early `return`(line 369)——它写了一份 `FAILED_*/metrics.json` 后直接退出函数。**Phase 16 不在 FAILED 分支注入 metrics_extra**(老 FAILED run 不回填,见 W4 / Out-of-scope §6)。
   
   插入位置必须满足:
   - 在 line 369 `return`(FAILED 分支 close)之**后**
   - 在 line 371 注释 `# ── 10. Report results ───────────` 或之后任意位置,但必须在 line 407 `metrics = {` happy-path 字面量之**前**
   
   推荐插入到 line 391 `console.print(result_table)` 之后、line 392-393 timestamp 计算之前(语义上「holdout 评估完成,准备写盘」自然位置)。
   
   插入内容:
   ```python
   # ── Phase 16 Wave 0: persist per-tool rates + raw_predictions for dashboard ──
   metrics_extra = persist_per_tool_rates({}, baseline_rates, evolved_rates)
   metrics_extra = persist_raw_predictions(metrics_extra, raw_preds)
   ```

4. **合并到 metrics**: 在 line 422(metrics dict literal 结尾的 `}`)之后、写盘代码 `(out_dir / "metrics.json").write_text(...)` 之前,插入:
   ```python
   metrics = {**metrics, **metrics_extra}
   ```
   (这样 Phase 13 字段 + Phase 16 字段全部进入最终 metrics)。

5. **不**改 holdout 评估的核心逻辑(`baseline_module(...)` / `optimized_module(...)` 调用方式不动)。
6. **不**触碰已有的 size 约束 / 写回路径。
7. **不**在 line 356-369 FAILED 分支注入任何 metrics_extra 代码 — 老 FAILED run 不回填(W4 / Out-of-scope §6)。

修改 `tests/tools/test_evolve_tool_descriptions.py`:

在文件末尾追加 1 个测试函数 `test_metrics_includes_per_tool_and_raw`,沿用同文件现有 happy-path mock 模式(找到现有能让 holdout 跑通到写 metrics.json 的测试,复制其 fixture / patch 块,在最后断言新字段)。具体断言:
```python
def test_metrics_includes_per_tool_and_raw(tmp_path, monkeypatch):
    """Phase 16 Wave 0 (D-12): evolve_tool_descriptions metrics.json gains
    per_tool_*_rates + raw_predictions (newly wired in Wave 0)."""
    # ... mock GEPA + holdout to happy-path completion ...
    metrics = json.loads((tmp_path / "<run_dir>" / "metrics.json").read_text())
    assert "per_tool_baseline_rates" in metrics
    assert "per_tool_evolved_rates" in metrics
    assert "raw_predictions" in metrics
    assert isinstance(metrics["per_tool_baseline_rates"], dict)
    assert isinstance(metrics["raw_predictions"], list)
    if metrics["raw_predictions"]:
        first = metrics["raw_predictions"][0]
        assert set(first.keys()) == {"correct_tool", "selected_tool", "difficulty", "num_available_tools"}
```

**Fallback 策略(BLOCKER 4 fix)**: 与 task 16-00-3 同样规则。fallback **必须经过真实 holdout 评估代码**(直接调 `evolve_descriptions` 内部函数 + holdout fixture,或 monkeypatch GEPA 返回 fixture optimized_module 但保留真实 holdout 路径);helper 纯单测(直接调 `persist_per_tool_rates` / `persist_raw_predictions`)**不构成 acceptance pass**。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_evolve_tool_descriptions.py::test_metrics_includes_per_tool_and_raw -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'persist_per_tool_rates' evolution/tools/evolve_tool_descriptions.py` 输出至少 2 行(import + 调用)
    - `grep -n 'persist_raw_predictions' evolution/tools/evolve_tool_descriptions.py` 输出至少 2 行(import + 调用)
    - `grep -n 'raw_preds: list\[dict\] = \[\]' evolution/tools/evolve_tool_descriptions.py` 输出 1 行
    - `grep -n 'metrics = {\*\*metrics, \*\*metrics_extra}' evolution/tools/evolve_tool_descriptions.py` 输出 1 行
    - `grep -n 'def test_metrics_includes_per_tool_and_raw' tests/tools/test_evolve_tool_descriptions.py` 输出 1 行
    - **BLOCKER 1 强制 — 行号顺序断言**: 跑 `grep -nE 'if not regression_result\.passed:|metrics_extra = persist_per_tool_rates|metrics_extra = persist_raw_predictions' evolution/tools/evolve_tool_descriptions.py`,输出格式 `<lineno>:<text>`。要求三行的 `<lineno>` 严格满足: `lineno("if not regression_result.passed:") < lineno("metrics_extra = persist_per_tool_rates") < lineno("metrics_extra = persist_raw_predictions")`(即 metrics_extra 两行都在 if 块**之后**,确认 BLOCKER 1 fix 落地)
    - `.venv/bin/pytest tests/tools/test_evolve_tool_descriptions.py -x` 退出码 0(整文件回归绿)
    - **BLOCKER 4 强制**: 测试函数体内必须出现 `_evaluate_holdout` / `evolve_descriptions(` 直接调用,或 `monkeypatch.setattr("dspy.GEPA", ...)` / `monkeypatch.setattr.*MIPROv2` patch + invoke main 完整跑通 — `grep -E 'evolve_descriptions\(|monkeypatch\.setattr.*GEPA|monkeypatch\.setattr.*MIPROv2|holdout_examples' tests/tools/test_evolve_tool_descriptions.py` 输出至少 1 行
  </acceptance_criteria>
  <done>
    evolve_tool_descriptions metrics.json 现在含 per_tool_*_rates(新接) + raw_predictions(新接);Phase 16 dashboard 启动时能扫到 desc CLI 完整数据。FAILED 分支不被回填(W4 / Out-of-scope §6)。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-00-5: evolve_tool_reasoning CLI 加 _score_with_predictions + 接两个 helper</name>
  <files>evolution/tools/evolve_tool_reasoning.py, tests/tools/test_evolve_tool_reasoning.py</files>
  <read_first>
    - evolution/tools/evolve_tool_reasoning.py (整个文件 ~770 LoC;特别注意 line 463-468 的 4 次 _safe_score 调用、line 514-555 metrics dict assembly、line 615-633 _safe_score 实现、line 697-764 _build_ab_comparison)
    - evolution/tools/evolve_tool_params.py:343-432 _evaluate_holdout 实现(per-prediction 收集模板,task 16-00-3 已熟悉)
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Excerpt F — evolve_tool_reasoning._safe_score 重构点"
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"evolve_tool_reasoning.py — Phase 15 CLI"
  </read_first>
  <behavior>
    - test_metrics_includes_per_tool_and_raw: 用 conftest.py 的 `mock_reasoning_module` fixture mock 出双 ToolModule,跑完 think-off / think-on 评估 → 读 metrics.json → 断言 `"per_tool_baseline_rates" in metrics` ∧ `"per_tool_evolved_rates" in metrics` ∧ `"raw_predictions" in metrics`
    - test_metrics_raw_predictions_uses_think_on: raw_predictions 来自 think-on 评估路径(因为 think-on 是「evolved」分支),每条 record 含 4 keys schema
  </behavior>
  <action>
修改 `evolution/tools/evolve_tool_reasoning.py`:

1. **Import 扩展**: 找到现有 import `CrossToolRegressionChecker` 的位置,改为:
   ```python
   from evolution.tools.tool_metric import (
       CrossToolRegressionChecker,
       persist_per_tool_rates,
       persist_raw_predictions,
   )
   ```

2. **新增 helper `_score_with_predictions`**: 在 line 615 `_safe_score` 函数定义之前(或就近),新增同级函数:
   ```python
   def _score_with_predictions(
       module: Any,
       examples: list,
       lm: Any,
   ) -> tuple[float, list[tuple[str, str]], list[dict]]:
       """Score module on examples + return per-prediction tool_pairs + raw_preds.

       Replaces _safe_score for paths where Phase 16 dashboard needs raw data.
       Returns (mean_score, tool_pairs, raw_preds). On any error returns
       (0.0, [], []).
       """
       if not examples:
           return 0.0, [], []
       tool_pairs: list[tuple[str, str]] = []
       raw_preds: list[dict] = []
       total = 0.0
       n = 0
       try:
           with dspy.context(lm=lm):
               for ex in examples:
                   try:
                       pred = module(task_description=ex.task_description)
                   except Exception:
                       continue
                   correct = getattr(ex, "correct_tool", "") or ""
                   selected = getattr(pred, "selected_tool", "") or ""
                   tool_pairs.append((correct, selected))
                   raw_preds.append({
                       "correct_tool": correct,
                       "selected_tool": selected,
                       "difficulty": getattr(ex, "difficulty", "medium") or "medium",
                       "num_available_tools": len(getattr(ex, "confuser_tools", []) or []) + 1,
                   })
                   total += 1.0 if correct == selected else 0.0
                   n += 1
           score = total / n if n else 0.0
           return float(score), tool_pairs, raw_preds
       except Exception:
           return 0.0, tool_pairs, raw_preds
   ```
   (注:具体 score 计算逻辑应与 `_safe_score` 调用的底层 `_score_module_on_holdout` 行为一致——若 `_score_module_on_holdout` 用的是更复杂的 metric 而非纯 exact match,则在 `_score_with_predictions` 内同样调它,只是 zip 上 raw_preds 收集;参见 `_evaluate_holdout` 在 evolve_tool_params.py 的等效模式)。

3. **改造 line 465-466 的 full holdout 评分调用(W1 fix — 仅替换 2 处,**保留** ambiguous 评分)**: 

   将 line 465-466 两行
   ```python
   th_off_full = _safe_score(baseline_module, eval_holdout, lm)
   th_on_full = _safe_score(optimized_module, eval_holdout, lm)
   ```
   改为(在原位置):
   ```python
   th_off_full, tool_pairs_off, _ = _score_with_predictions(baseline_module, eval_holdout, lm)
   th_on_full, tool_pairs_on, raw_preds_on = _score_with_predictions(optimized_module, eval_holdout, lm)
   ```
   保留 `th_off_full` / `th_on_full` 浮点数变量名供后续逻辑(line 470+)使用。

   **关键: line 467-468 的 ambiguous subset 评分调用必须**原样保留**,不替换为 `_score_with_predictions`**:
   ```python
   ambiguous_off = _safe_score(baseline_module, ambiguous_subset, lm)  # ← KEEP unchanged
   ambiguous_on = _safe_score(optimized_module, ambiguous_subset, lm)  # ← KEEP unchanged
   ```
   理由:ambiguous 评估只需 mean score,不需要 raw_predictions(D-13 ambiguous gate 仅用均值;dashboard raw_predictions 来自 full holdout 即可)。

4. **接两个 helper**: 在 line 514-555 的 metrics dict literal 结尾(找到 `metrics = {...}` 闭合后)、写盘前,插入:
   ```python
   # ── Phase 16 Wave 0: persist per-tool rates + raw_predictions for dashboard ──
   regression_checker = CrossToolRegressionChecker()
   baseline_rates = regression_checker.compute_per_tool_rates(tool_pairs_off)
   evolved_rates = regression_checker.compute_per_tool_rates(tool_pairs_on)
   metrics = persist_per_tool_rates(metrics, baseline_rates, evolved_rates)
   metrics = persist_raw_predictions(metrics, raw_preds_on)
   ```
   注意 reasoning CLI 的「baseline」是 think-off,「evolved」是 think-on(D-07 source legend 对齐),所以 `tool_pairs_off` → `baseline_rates`,`tool_pairs_on` → `evolved_rates`,`raw_preds_on` 喂给 raw_predictions(think-on 是评估的主信号)。

5. **不**改 `_build_ab_comparison`(line 697-764) — 它独立服务 ABStudy,与 raw_predictions 数据流并行。
6. **不**删除 `_safe_score` — 它仍由 line 467-468 的 ambiguous subset 评分使用(W1 fix:这两行**不**替换)。`_safe_score` 函数定义保留;**只有 line 465-466 两处 full holdout 评分**被替换为 `_score_with_predictions`。grep 验证:替换后文件里 `_safe_score` 调用应**精确为 2 处**(原 4 处,line 465-466 替换掉 2 处,line 467-468 保留 2 处)。

修改 `tests/tools/test_evolve_tool_reasoning.py`:

在文件末尾追加 1 个测试函数 `test_metrics_includes_per_tool_and_raw`。沿用同文件 / `tests/tools/conftest.py` 的 `mock_reasoning_module` fixture 跑通 think-off/think-on 路径,断言:
```python
def test_metrics_includes_per_tool_and_raw(tmp_path, mock_reasoning_module, monkeypatch):
    """Phase 16 Wave 0 (D-12): evolve_tool_reasoning metrics.json gains
    per_tool_*_rates + raw_predictions; raw_predictions populated from think-on path."""
    # ... mock GEPA + holdout to happy-path ...
    metrics = json.loads((tmp_path / "<run_dir>" / "metrics.json").read_text())
    assert "per_tool_baseline_rates" in metrics
    assert "per_tool_evolved_rates" in metrics
    assert "raw_predictions" in metrics
    assert "think_ab_gate" in metrics  # Phase 15 既有字段未被破坏
    if metrics["raw_predictions"]:
        first = metrics["raw_predictions"][0]
        assert set(first.keys()) == {"correct_tool", "selected_tool", "difficulty", "num_available_tools"}
```

**Fallback 策略(BLOCKER 4 fix)**: 同 task 16-00-3/16-00-4 严格规则。Fallback **必须经过真实 holdout 评估代码** —— 推荐形态:直接调 `_score_with_predictions(mock_module, [ex1, ex2], lm)` 并断言第三元素 `raw_preds` schema 正确(这是 task 新增的核心 helper,经过它 = 经过新接线)。**helper 纯单测(直接调 `persist_raw_predictions`)不构成 acceptance pass**。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_evolve_tool_reasoning.py::test_metrics_includes_per_tool_and_raw -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def _score_with_predictions' evolution/tools/evolve_tool_reasoning.py` 输出 1 行
    - `grep -n 'tool_pairs_off, _ = _score_with_predictions' evolution/tools/evolve_tool_reasoning.py` 输出 1 行
    - `grep -n 'tool_pairs_on, raw_preds_on = _score_with_predictions' evolution/tools/evolve_tool_reasoning.py` 输出 1 行
    - `grep -n 'metrics = persist_per_tool_rates(metrics, baseline_rates, evolved_rates)' evolution/tools/evolve_tool_reasoning.py` 输出 1 行
    - `grep -n 'metrics = persist_raw_predictions(metrics, raw_preds_on)' evolution/tools/evolve_tool_reasoning.py` 输出 1 行
    - `grep -n 'def test_metrics_includes_per_tool_and_raw' tests/tools/test_evolve_tool_reasoning.py` 输出 1 行
    - **W1 强制 — _safe_score 保留 ambiguous 调用**: `grep -v '^[[:space:]]*#' evolution/tools/evolve_tool_reasoning.py | grep -c '_safe_score(.*ambiguous_subset'` 输出 `2`(line 467-468 两处 ambiguous 调用保留;`grep -v '^#'` 过滤注释行避免 self-invalidating gate)
    - **W1 强制 — _safe_score 总调用数为 2**: `grep -v '^[[:space:]]*#' evolution/tools/evolve_tool_reasoning.py | grep -c '_safe_score(' | tr -d ' '` 输出 `2`(原 4 处,line 465-466 替换为 _score_with_predictions 后剩 line 467-468 两处 ambiguous)
    - `.venv/bin/pytest tests/tools/test_evolve_tool_reasoning.py -x` 退出码 0(整文件回归绿)
    - `.venv/bin/pytest tests/tools/ -x` 退出码 0(所有 tools 测试整体回归绿,确认 Phase 13/14/15 测试无破坏)
    - **BLOCKER 4 强制**: 测试函数体内必须出现 `_score_with_predictions(` 直接调用,或 `mock_reasoning_module` fixture + monkeypatch GEPA + invoke 完整跑通 — `grep -E '_score_with_predictions\(|mock_reasoning_module|monkeypatch\.setattr.*GEPA' tests/tools/test_evolve_tool_reasoning.py` 输出至少 1 行
  </acceptance_criteria>
  <done>
    evolve_tool_reasoning 通过 _score_with_predictions 收集 per-prediction 数据,metrics.json 含完整三字段;Phase 13/14/15 现有测试无破坏。`_safe_score` 在 ambiguous subset 评估中保留(W1 fix)。Wave 0 数据基础完成,Wave 1 dashboard 可读取真实数据。
  </done>
</task>

</tasks>

<verification>
**Wave 0 完成标志**:
- `.venv/bin/pytest tests/tools/test_persist_raw_predictions.py tests/tools/test_tool_dataset.py::TestToDspyExamplesDifficulty -x` 退出码 0
- `.venv/bin/pytest tests/tools/ -x` 整 tools 测试目录回归绿
- `python -c "from evolution.tools.tool_metric import persist_raw_predictions, persist_per_tool_rates; print('OK')"` 退出码 0
- 三个 CLI 文件的 grep 检查全部命中(详见每 task acceptance_criteria)
</verification>

<success_criteria>
- persist_raw_predictions helper 实现完成,4 单元测试全绿
- to_dspy_examples 携带 difficulty,3 单元测试全绿
- 三个 CLI(params/desc/reasoning)的 metrics.json 均含 per_tool_*_rates + raw_predictions(共 3+1=4 个 D-12 字段)
- 现有 Phase 13/14/15 测试零破坏(`pytest tests/tools/ -x` 退出码 0)
- Wave 1 起可在 dashboard 中读取真实 per-tool 数据,不再 90% dropped
</success_criteria>

<output>
After completion, create `.planning/phases/16-per-tool-regression-dashboard/16-00-SUMMARY.md` with:
- Files modified summary (5 source + 4 test files)
- D-12 schema 决定:`raw_predictions = [{correct_tool, selected_tool, difficulty, num_available_tools}]`
- Open Question 1/2/3 resolved confirmation:OQ-1 三 CLI 同时接两 helper(用户已决);OQ-2 num_available_tools = len(confuser_tools) + 1;OQ-3 不改 _evaluate_holdout 签名,在外部 zip
- 任何遇到的真实 mock 复杂度问题与解决方案
</output>
</content>
</invoke>