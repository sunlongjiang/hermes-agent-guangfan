---
plan: 16-01-dashboard-latest
phase: 16
type: execute
wave: 1
depends_on: [16-00-foundation]
files_modified:
  - path: evolution/tools/regression_dashboard.py
    role: new
  - path: tests/tools/test_regression_dashboard.py
    role: new
  - path: tests/fixtures/dashboard_runs/desc_old/metrics.json
    role: new
  - path: tests/fixtures/dashboard_runs/params_complete/metrics.json
    role: new
  - path: tests/fixtures/dashboard_runs/params_no_raw/metrics.json
    role: new
  - path: tests/fixtures/dashboard_runs/reasoning_complete/metrics.json
    role: new
  - path: tests/fixtures/dashboard_runs/reasoning_complete/ab_comparison.json
    role: new
  - path: tests/fixtures/dashboard_runs/reasoning_old/metrics.json
    role: new
requirements: [TOOL-V2-04]
autonomous: true
estimated_minutes: 130

must_haves:
  truths:
    - "python -m evolution.tools.regression_dashboard 是一个可调用的 standalone Click CLI(D-01:不动 evolve_tool_* 入口,只读分析工具)"
    - "CLI 默认扫描 output/tools/ + output/tools_reasoning/ 两个根目录,--runs 多次累加"
    - "stdout 输出 Rich Table 三区(LATEST 在 Wave 1 落地,DIFF/TREND/ABStudy 在 Wave 2-3),并在 Wave 4 同时落 dashboard.json 双产物(D-03)"
    - "LATEST 区从 mtime 最新 run 渲染 Rich Table,含 12 列 (source/tool/baseline_rate/evolved_rate/delta_pp/sample_count/min/p25/median/p75/max/status)"
    - "status 颜色编码遵循 D-09: OK 默认 / WARN 黄 (delta ≤ -2pp) / FAIL 红 (delta ≤ -5pp) / GAIN 绿 (delta ≥ +5pp)"
    - "LATEST 表底单独 Panel 渲染频次柱图,top-12 + 'others (N tools)' 聚合行(D-16:Phase 14 工具频次不均衡可视化合入)"
    - "per-segment sample_count < 3 时 distribution 列(min/p25/median/p75/max)显示 n/a 而非误导平均值(D-10 语义 A 单 run 内分桶 + D-11 segment difficulty/pool_size 切片)"
    - "tests/fixtures/dashboard_runs/ 含 6 个文件,覆盖 5 fixture run 场景"
  artifacts:
    - path: evolution/tools/regression_dashboard.py
      provides: "Dashboard CLI 骨架 + LATEST 区 + 频次柱图"
      contains: "def main"
      min_lines: 200
    - path: tests/tools/test_regression_dashboard.py
      provides: "Wave 1 4 个 functional 测试 + Wave 2-4 占位"
    - path: tests/fixtures/dashboard_runs/params_complete/metrics.json
      provides: "完整 params run fixture(LATEST 主路径)"
      contains: "per_tool_baseline_rates"
  key_links:
    - from: evolution/tools/regression_dashboard.py
      to: tests/fixtures/dashboard_runs/
      via: _scan_runs glob + CliRunner patch
      pattern: "patch\\(\"evolution.tools.regression_dashboard._scan_runs\""
    - from: tests/tools/test_regression_dashboard.py
      to: evolution/tools/regression_dashboard.py:main
      via: CliRunner.invoke
      pattern: "runner.invoke\\(main"
---

<objective>
Wave 1 — Dashboard CLI 骨架 + LATEST 区。建立 `evolution/tools/regression_dashboard.py` 可执行入口,实现:
1. Click + Rich CLI 框架(9 个 flag,沿用 mine_tool_sessions 风格)
2. `_scan_runs` / `_load_run` / `_detect_source` / `_status_style` / `_segment_distribution` / `_render_latest` / `_render_frequency_bars` 7 个 helper
3. LATEST 区渲染:Rich Table 12 列 + status 颜色编码 + 频次柱图 Panel(D-09 / D-16)
4. per-segment sample_count <3 → 渲染 `n/a`(Pitfall 10 收口 / D-11 退化)

Purpose: 解锁 dashboard 主路径——给定一个完整 params/reasoning run,能输出可读 Rich Table + 颜色编码状态;为 Wave 2 (DIFF/TREND) / Wave 3 (ABStudy) / Wave 4 (E2E) 提供基础渲染原语。

Output:
- 新建 `evolution/tools/regression_dashboard.py` (~250 LoC)
- 新建 `tests/tools/test_regression_dashboard.py` (Wave 1 部分,4 个测试 + Wave 2-4 占位 stubs 用 `@pytest.mark.skip(reason="Wave N")`)
- 新建 `tests/fixtures/dashboard_runs/` 6 个 fixture 文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md
@.planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md
@.planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md
@.planning/phases/16-per-tool-regression-dashboard/16-VALIDATION.md
@.planning/phases/16-per-tool-regression-dashboard/16-00-SUMMARY.md
@evolution/tools/regression_dashboard.py
@evolution/tools/mine_tool_sessions.py
@evolution/tools/tool_metric.py

<interfaces>
<!-- Wave 0 outputs available to Wave 1: -->
<!-- - tool_metric.persist_raw_predictions: list[dict] in metrics.json `raw_predictions` -->
<!-- - raw_predictions schema: [{correct_tool: str, selected_tool: str, difficulty: str, num_available_tools: int}] -->
<!-- - tool_dataset.to_dspy_examples now carries `difficulty` (not directly used by dashboard but documented) -->

From mine_tool_sessions.py:1-40 (CLI module-header skeleton template):
```python
"""SessionDB tool-misselection mining CLI — Phase 14 (TOOL-V2-01).
...
Usage: python -m evolution.tools.mine_tool_sessions ...
"""
import json
import sys
from datetime import datetime
from pathlib import Path
from typing import Optional
import click
from rich.console import Console
from rich.panel import Panel
from rich.table import Table
```

Phase 15 metrics.json field set (real shape):
```
ambiguous_gate_skipped, ambiguous_subset_size, ambiguous_think_off, ambiguous_think_on,
cost_usd_cap, cost_usd_spent, elapsed_seconds, eval_model, holdout_examples, iterations,
latency_stats, optimizer_used, param_predictors_discovered, reasoning_token_stats,
reflection_model, started_at, status, think_ab_gate, think_off_score, think_on_score,
timestamp, tool_count, train_examples, v1_baseline_holdout, v1_baseline_source,
v1_gate_delta_*, v1_gate_passed, v1_score, val_examples
# After Wave 0: + per_tool_baseline_rates, per_tool_evolved_rates, raw_predictions
```

D-09 LATEST columns (12):
source / tool / baseline_rate / evolved_rate / delta_pp / sample_count /
min / p25 / median / p75 / max / status

D-09 status thresholds:
delta_pp ≤ -5.0 → FAIL ❌ (style: "bold red")
delta_pp ≤ -warning_threshold_pp (default 2.0) → WARN ⚠️ (style: "yellow")
delta_pp ≥ +5.0 → GAIN ✨ (style: "bold green")
otherwise → OK ✅ (style: "")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 16-01-1: 创建 dashboard CLI 骨架 + 6 fixture 文件 + scan/load/detect_source helper</name>
  <files>evolution/tools/regression_dashboard.py, tests/fixtures/dashboard_runs/desc_old/metrics.json, tests/fixtures/dashboard_runs/params_complete/metrics.json, tests/fixtures/dashboard_runs/params_no_raw/metrics.json, tests/fixtures/dashboard_runs/reasoning_complete/metrics.json, tests/fixtures/dashboard_runs/reasoning_complete/ab_comparison.json, tests/fixtures/dashboard_runs/reasoning_old/metrics.json</files>
  <read_first>
    - evolution/tools/mine_tool_sessions.py (line 1-40 模块头模板 + line 329-413 Click block + 末尾 `if __name__`)
    - evolution/tools/evolve_tool_reasoning.py (line 75 `OUTPUT_ROOT = Path("output") / "tools_reasoning"` 风格)
    - evolution/tools/evolve_skill.py (line 317-323 Click @click.option 风格)
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Excerpt G/H/I/J — Click+Rich CLI/multiple=True/glob+sort/source 启发判定"
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"Implementation Recipes Wave 1" + §"D-07 source 启发判定决策树" + §"failure modes"
    - .planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md §D-01..D-08
    - tests/fixtures/sessions/ 任一文件(确认 fixture 文件目录约定)
  </read_first>
  <action>
**步骤 1**: 创建 `evolution/tools/regression_dashboard.py`,完整文件结构如下:

**WARNING 5 fix — Rich Console 实例化约束**: module-level `console = Console()` 必须使用 default 行为,**不传 `force_terminal=True` 也不传 `no_color=False` 也不传 `file=...`**。这样 CliRunner 在测试时能正确捕获 stdout(CliRunner 把 stdout 捕获到 in-memory buffer,Rich 默认 isatty 检测会自动降级为 plain text 输出,从而保证 `result.output` 包含可断言的 ASCII 字符串)。如果传 `force_terminal=True`,Rich 会强制启用 ANSI 转义码,使得 `assert "FAIL" in result.output` 仍可通过(因为 "FAIL" 是普通 ASCII),但任何与列宽/换行相关的断言会变脆弱。统一用 default `Console()`。

```python
"""Per-tool regression dashboard for evolve_tool_* pipelines — Phase 16 (TOOL-V2-04).

Standalone read-only CLI: aggregates per-tool selection rates from
output/tools/<ts>/metrics.json and output/tools_reasoning/<ts>/metrics.json
(plus ab_comparison.json for reasoning runs), renders a Rich console
dashboard (LATEST + DIFF + TREND + ABStudy regions), and emits a JSON
report to ./dashboard_<ts>.json.

Usage:
    python -m evolution.tools.regression_dashboard
    python -m evolution.tools.regression_dashboard --runs path/to/run1 --runs path/to/run2
    python -m evolution.tools.regression_dashboard --baseline-run <path> --evolved-run <path>
    python -m evolution.tools.regression_dashboard --trend-window 5 --segment difficulty

Failure paths:
    exit 2 — usage error (default roots empty + no --runs; mutex --trend-window/--trend-days)

READ-ONLY guarantee: this CLI never writes to hermes-agent, never calls LLMs,
never imports dspy. It only reads metrics.json / ab_comparison.json and emits
stdout + dashboard.json.
"""

import json
import statistics
import sys
from collections import Counter
from datetime import datetime
from pathlib import Path
from typing import Optional

import click
from rich.console import Console
from rich.panel import Panel
from rich.table import Table

from evolution.core.external_importers import _contains_secret


# WARNING 5: keep default Console() — no force_terminal, no_color, file overrides.
# This ensures CliRunner can capture stdout cleanly.
console = Console()

DEFAULT_ROOTS: tuple[Path, ...] = (
    Path("output") / "tools",
    Path("output") / "tools_reasoning",
)
_SPARK_CHARS = "▁▂▃▄▅▆▇█"
_MIN_SEGMENT_SAMPLE = 3  # below this, render distribution column as n/a (D-11 / Pitfall 10)


# ── Run discovery & loading ────────────────────────────────────────────────


def _scan_runs(roots: tuple[Path, ...]) -> list[Path]:
    """Glob <root>/*/metrics.json, sorted by mtime ascending (oldest first)."""
    found: list[Path] = []
    for root in roots:
        if root.exists():
            found.extend(root.glob("*/metrics.json"))
    return sorted(found, key=lambda p: p.stat().st_mtime)


def _load_run(metrics_path: Path) -> Optional[dict]:
    """Return {path, source, metrics, ab_comparison?} or None on parse error.

    Also returns None for runs that are unrecoverable (status=FAILED, missing
    per_tool_*_rates) — those appear in dropped_runs[] via the caller.
    """
    try:
        metrics = json.loads(metrics_path.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError) as e:
        return {"path": str(metrics_path), "metrics": None, "source": None, "_drop_reason": f"json parse error: {e}"}
    run_dir = metrics_path.parent
    source = _detect_source(metrics, metrics_path)
    record: dict = {"path": str(run_dir), "metrics": metrics, "source": source, "ab_comparison": None}
    # Optional ab_comparison.json (reasoning source only)
    ab_path = run_dir / "ab_comparison.json"
    if ab_path.exists():
        try:
            record["ab_comparison"] = json.loads(ab_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            record["ab_comparison"] = None
    return record


def _detect_source(metrics: dict, run_path: Path) -> Optional[str]:
    """D-07 启发判定 — 字段集 + 目录名 fallback.

    Order matters:
    1. think_ab_gate present → reasoning (Phase 15 unique)
    2. param_predictors_discovered present → params (Phase 13 unique;
       NOT used for reasoning even though reasoning shares — step 1 catches first)
    3. parent dir name == 'tools_reasoning' → reasoning (defensive fallback)
    4. parent dir name == 'tools' AND has baseline_score → desc
    5. otherwise → None (run goes to dropped_runs)
    """
    if metrics is None:
        return None
    if "think_ab_gate" in metrics:
        return "reasoning"
    if "param_predictors_discovered" in metrics:
        return "params"
    parent_root = run_path.parent.parent.name
    if parent_root == "tools_reasoning":
        return "reasoning"
    if parent_root == "tools" and "baseline_score" in metrics:
        return "desc"
    return None


# ── Status colour coding (D-09) ────────────────────────────────────────────


def _status_style(delta_pp: float, warning_threshold_pp: float = 2.0) -> tuple[str, str]:
    """Return (status_label, rich_style) per D-09."""
    if delta_pp <= -5.0:
        return ("FAIL ❌", "bold red")
    if delta_pp <= -warning_threshold_pp:
        return ("WARN ⚠️", "yellow")
    if delta_pp >= 5.0:
        return ("GAIN ✨", "bold green")
    return ("OK ✅", "")


# ── Distribution segmentation (D-11) ───────────────────────────────────────


def _quintiles(values: list[float]) -> dict[str, float]:
    """Return {min, p25, median, p75, max}; uses statistics.quantiles for p25/p75."""
    if not values:
        return {"min": 0.0, "p25": 0.0, "median": 0.0, "p75": 0.0, "max": 0.0}
    sorted_v = sorted(values)
    if len(sorted_v) == 1:
        v = float(sorted_v[0])
        return {"min": v, "p25": v, "median": v, "p75": v, "max": v}
    qs = statistics.quantiles(sorted_v, n=4)
    return {
        "min": float(min(sorted_v)),
        "p25": float(qs[0]),
        "median": float(statistics.median(sorted_v)),
        "p75": float(qs[2]),
        "max": float(max(sorted_v)),
    }


def _segment_distribution(
    raw_preds: list[dict],
    segment: str,
) -> dict[str, dict]:
    """Per-tool, per-segment evolved_rate quintiles.

    Returns {tool_name: {min,p25,median,p75,max, sample_count, n_segments}}.
    If sample_count for a tool is < _MIN_SEGMENT_SAMPLE, returns
    {tool_name: {"_n_a": True, "sample_count": int}} so caller can render
    n/a in distribution columns (D-11 / Pitfall 10).
    """
    if segment == "none" or not raw_preds:
        return {}
    out: dict[str, dict] = {}
    by_tool: dict[str, list[dict]] = {}
    for rec in raw_preds:
        by_tool.setdefault(rec["correct_tool"], []).append(rec)
    for tool, recs in by_tool.items():
        # Group recs by segment key
        by_seg: dict[str, list[dict]] = {}
        for rec in recs:
            if segment == "difficulty":
                key = rec.get("difficulty", "medium") or "medium"
            elif segment == "pool_size":
                n = int(rec.get("num_available_tools", 0) or 0)
                key = "1-3" if n <= 3 else ("4-7" if n <= 7 else "8+")
            else:
                key = "all"
            by_seg.setdefault(key, []).append(rec)
        # Per-segment evolved_rate
        seg_rates: list[float] = []
        for seg_key, seg_recs in by_seg.items():
            if len(seg_recs) < _MIN_SEGMENT_SAMPLE:
                continue
            n_correct = sum(1 for r in seg_recs if r["correct_tool"] == r["selected_tool"])
            seg_rates.append(n_correct / len(seg_recs))
        if not seg_rates:
            out[tool] = {"_n_a": True, "sample_count": len(recs)}
        else:
            out[tool] = {**_quintiles(seg_rates), "sample_count": len(recs), "n_segments": len(seg_rates)}
    return out


# ── LATEST region rendering (D-09 + D-16) ──────────────────────────────────


def _render_latest(
    run: dict,
    segment: str,
    warning_threshold_pp: float,
) -> dict:
    """Render LATEST Rich Table + frequency bar Panel; return latest dict for JSON.

    LATEST table columns (D-09): source / tool / baseline_rate / evolved_rate /
    delta_pp / sample_count / min / p25 / median / p75 / max / status.
    """
    metrics = run["metrics"]
    source = run["source"]
    baseline_rates = metrics.get("per_tool_baseline_rates", {}) or {}
    evolved_rates = metrics.get("per_tool_evolved_rates", {}) or {}
    raw_preds = metrics.get("raw_predictions", []) or []
    distribution = _segment_distribution(raw_preds, segment)

    # Per-tool sample counts from raw_predictions (correct_tool occurrences)
    sample_counts: Counter = Counter(rec.get("correct_tool", "") for rec in raw_preds)

    table = Table(title=f"LATEST [{source}] @ {run['path']}", show_lines=False)
    table.add_column("source", justify="left")
    table.add_column("tool", justify="left", style="cyan")
    table.add_column("baseline", justify="right")
    table.add_column("evolved", justify="right")
    table.add_column("delta_pp", justify="right")
    table.add_column("sample", justify="right")
    table.add_column("min", justify="right")
    table.add_column("p25", justify="right")
    table.add_column("median", justify="right")
    table.add_column("p75", justify="right")
    table.add_column("max", justify="right")
    table.add_column("status", justify="left")

    per_tool_records: list[dict] = []
    all_tools = sorted(set(baseline_rates.keys()) | set(evolved_rates.keys()))

    for tool in all_tools:
        b = float(baseline_rates.get(tool, 0.0))
        e = float(evolved_rates.get(tool, 0.0))
        delta = (e - b) * 100.0
        label, style = _status_style(delta, warning_threshold_pp)
        sc = int(sample_counts.get(tool, 0))
        dist = distribution.get(tool, {})
        if dist.get("_n_a") or not dist:
            cells_dist = ("n/a", "n/a", "n/a", "n/a", "n/a")
            dist_record: Optional[dict] = None
        else:
            cells_dist = (
                f"{dist['min']:.2f}", f"{dist['p25']:.2f}", f"{dist['median']:.2f}",
                f"{dist['p75']:.2f}", f"{dist['max']:.2f}",
            )
            dist_record = {k: dist[k] for k in ("min", "p25", "median", "p75", "max")}
        table.add_row(
            source or "?", tool, f"{b:.3f}", f"{e:.3f}",
            f"[{style}]{delta:+.2f}[/{style}]" if style else f"{delta:+.2f}",
            str(sc), *cells_dist,
            f"[{style}]{label}[/{style}]" if style else label,
        )
        per_tool_records.append({
            "tool": tool,
            "baseline_rate": b,
            "evolved_rate": e,
            "delta_pp": delta,
            "sample_count": sc,
            "distribution": dist_record,
            "status": label.split()[0],  # OK/WARN/FAIL/GAIN without emoji
        })

    console.print(Panel(
        f"[dim]baseline meaning: desc/params=v1 frozen; reasoning=think-off (D-07 legend)[/dim]",
        title="source legend",
    ))
    console.print(table)

    # D-16 frequency bars (top-12 + others aggregate)
    _render_frequency_bars(sample_counts)

    return {
        "run_path": run["path"],
        "source": source,
        "per_tool": per_tool_records,
    }


def _render_frequency_bars(sample_counts: Counter) -> None:
    """D-16: top-12 + 'others (N tools)' aggregate; rendered as text rows in a Panel."""
    if not sample_counts:
        return
    sorted_tools = sample_counts.most_common()
    top = sorted_tools[:12]
    rest = sorted_tools[12:]
    max_count = top[0][1] if top else 1
    bar_width = 30
    lines: list[str] = []
    for name, count in top:
        bar_len = int((count / max_count) * bar_width) if max_count else 0
        lines.append(f"{name:<25} {'█' * bar_len} {count}")
    if rest:
        rest_total = sum(c for _, c in rest)
        bar_len = int((rest_total / max_count) * bar_width) if max_count else 0
        lines.append(f"{'others (' + str(len(rest)) + ' tools)':<25} {'█' * bar_len} {rest_total}")
    console.print(Panel("\n".join(lines), title="Sample frequency"))


# ── Click CLI ──────────────────────────────────────────────────────────────


@click.command()
@click.option("--runs", "runs", multiple=True, type=click.Path(),
              help="Run directory (repeatable; appends to default roots)")
@click.option("--baseline-run", default=None, type=click.Path(),
              help="DIFF baseline run (must be paired with --evolved-run)")
@click.option("--evolved-run", default=None, type=click.Path(),
              help="DIFF evolved run")
@click.option("--trend-window", default=None, type=int,
              help="TREND: most recent N runs (default 10; mutex with --trend-days)")
@click.option("--trend-days", default=None, type=int,
              help="TREND: runs from past D days (mutex with --trend-window)")
@click.option("--segment", default="difficulty",
              type=click.Choice(["difficulty", "pool_size", "none"]),
              help="Distribution segment dimension (D-11)")
@click.option("--warning-threshold-pp", default=2.0, type=float,
              help="Per-tool delta threshold for yellow warning (D-13)")
@click.option("--output", default=None, type=click.Path(),
              help="dashboard.json path (default: ./dashboard_<ts>.json)")
def main(runs, baseline_run, evolved_run, trend_window, trend_days, segment,
         warning_threshold_pp, output):
    """Per-tool regression dashboard for evolve_tool_* pipelines."""
    # D-06 mutex check
    if trend_window is not None and trend_days is not None:
        raise click.UsageError("--trend-window and --trend-days are mutually exclusive")

    # Build root list: defaults + --runs additions
    roots = list(DEFAULT_ROOTS)
    for r in runs:
        roots.append(Path(r))
    runs_paths = _scan_runs(tuple(roots))

    # D-02: empty default roots + no --runs → exit 2
    if not runs_paths:
        console.print("[red]No runs found in default roots or --runs paths[/red]")
        raise click.UsageError("no runs found; pass --runs <path> or populate output/tools[_reasoning]/")

    # Wave 1: render LATEST only (Wave 2/3/4 add DIFF/TREND/ABStudy/JSON)
    latest_run = None
    dropped: list[dict] = []
    for p in reversed(runs_paths):  # newest first
        loaded = _load_run(p)
        if loaded is None or loaded.get("metrics") is None:
            dropped.append({"path": str(p), "reason": loaded.get("_drop_reason", "load failed") if loaded else "load failed"})
            continue
        m = loaded["metrics"]
        if loaded["source"] is None:
            dropped.append({"path": str(p), "reason": "unknown source"})
            continue
        if "per_tool_baseline_rates" not in m or "per_tool_evolved_rates" not in m:
            dropped.append({"path": str(p), "reason": "missing per_tool_*_rates"})
            continue
        if m.get("status") == "FAILED":
            dropped.append({"path": str(p), "reason": "run status=FAILED"})
            continue
        latest_run = loaded
        break

    if latest_run is None:
        console.print("[yellow]No usable runs for LATEST region[/yellow]")
        for d in dropped:
            console.print(f"[yellow]dropped: {d['path']} — {d['reason']}[/yellow]")
        # Wave 4 will write dashboard.json even when empty; Wave 1 returns 0.
        return 0

    _render_latest(latest_run, segment, warning_threshold_pp)
    if dropped:
        console.print(f"\n[yellow]Dropped {len(dropped)} run(s); see dashboard.json (Wave 4)[/yellow]")
    return 0


if __name__ == "__main__":
    main()
```

**步骤 2**: 创建 6 个 fixture 文件,目录 `tests/fixtures/dashboard_runs/<scenario>/`,每个 metrics.json 用 `json.dumps(..., indent=2)` 风格写入:

`tests/fixtures/dashboard_runs/desc_old/metrics.json`(缺 per_tool_*_rates → dropped):
```json
{
  "timestamp": "20260422_201215",
  "status": "FAILED",
  "constraints_passed": false,
  "baseline_score": 0.5
}
```

`tests/fixtures/dashboard_runs/params_complete/metrics.json`(完整,LATEST 主路径,含 raw_predictions):
```json
{
  "timestamp": "20260512_103000",
  "iterations": 5,
  "param_predictors_discovered": 12,
  "optimizer_used": "gepa",
  "baseline_score": 0.75,
  "evolved_score": 0.82,
  "improvement": 0.07,
  "per_tool_baseline_rates": {
    "search_files": 0.80,
    "read_file": 0.85,
    "browser_navigate": 0.60,
    "edit_file": 0.70,
    "list_files": 0.90
  },
  "per_tool_evolved_rates": {
    "search_files": 0.84,
    "read_file": 0.87,
    "browser_navigate": 0.55,
    "edit_file": 0.78,
    "list_files": 0.92
  },
  "raw_predictions": [
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "easy", "num_available_tools": 4},
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "easy", "num_available_tools": 4},
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "medium", "num_available_tools": 5},
    {"correct_tool": "search_files", "selected_tool": "read_file", "difficulty": "medium", "num_available_tools": 5},
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "hard", "num_available_tools": 6},
    {"correct_tool": "read_file", "selected_tool": "read_file", "difficulty": "easy", "num_available_tools": 3},
    {"correct_tool": "read_file", "selected_tool": "read_file", "difficulty": "medium", "num_available_tools": 4},
    {"correct_tool": "read_file", "selected_tool": "read_file", "difficulty": "medium", "num_available_tools": 5},
    {"correct_tool": "browser_navigate", "selected_tool": "browser_navigate", "difficulty": "easy", "num_available_tools": 3},
    {"correct_tool": "browser_navigate", "selected_tool": "search_files", "difficulty": "hard", "num_available_tools": 6},
    {"correct_tool": "edit_file", "selected_tool": "edit_file", "difficulty": "medium", "num_available_tools": 4},
    {"correct_tool": "list_files", "selected_tool": "list_files", "difficulty": "easy", "num_available_tools": 3}
  ]
}
```
(注:确保至少一个 tool 有每 difficulty bucket ≥3 sample 让 distribution 列能渲染数字,另一个 tool sample 极少触发 n/a 路径——上述 search_files 占 5 例, browser_navigate 占 2 例 → browser_navigate distribution 应是 n/a。)

`tests/fixtures/dashboard_runs/params_no_raw/metrics.json`(老 params,缺 raw_predictions):
```json
{
  "timestamp": "20260301_100000",
  "iterations": 3,
  "param_predictors_discovered": 8,
  "baseline_score": 0.70,
  "evolved_score": 0.72,
  "per_tool_baseline_rates": {"search_files": 0.70, "read_file": 0.80},
  "per_tool_evolved_rates": {"search_files": 0.72, "read_file": 0.81}
}
```

`tests/fixtures/dashboard_runs/reasoning_complete/metrics.json`(完整 reasoning,含 think_ab_gate):
```json
{
  "timestamp": "20260512_150748",
  "think_ab_gate": {"passed": true, "evolved_scores": {}, "gates": {}},
  "param_predictors_discovered": 10,
  "optimizer_used": "gepa",
  "think_off_score": 0.60,
  "think_on_score": 0.68,
  "per_tool_baseline_rates": {"search_files": 0.60, "browser_navigate": 0.55},
  "per_tool_evolved_rates": {"search_files": 0.68, "browser_navigate": 0.50},
  "raw_predictions": [
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "easy", "num_available_tools": 3},
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "medium", "num_available_tools": 4},
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "medium", "num_available_tools": 4},
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "hard", "num_available_tools": 5},
    {"correct_tool": "browser_navigate", "selected_tool": "browser_navigate", "difficulty": "easy", "num_available_tools": 3},
    {"correct_tool": "browser_navigate", "selected_tool": "search_files", "difficulty": "hard", "num_available_tools": 5}
  ]
}
```

`tests/fixtures/dashboard_runs/reasoning_complete/ab_comparison.json`(Wave 3 需,Wave 1 创建占位即可):
```json
[
  {"task_id": 0, "task_description": "find Python files in src/", "correct_tool": "search_files", "selected_off": "list_files", "selected_on": "search_files", "is_correct_off": false, "is_correct_on": true, "is_ambiguous": false, "confuser_tools": ["list_files"], "reasoning_text_on": "I should use search_files because it can pattern match", "reasoning_tokens_on": 12, "latency_seconds_off": 0.5, "latency_seconds_on": 1.8},
  {"task_id": 1, "task_description": "open https://example.com", "correct_tool": "browser_navigate", "selected_off": "browser_navigate", "selected_on": "search_files", "is_correct_off": true, "is_correct_on": false, "is_ambiguous": false, "confuser_tools": ["search_files"], "reasoning_text_on": "search_files seems related to URL", "reasoning_tokens_on": 8, "latency_seconds_off": 0.4, "latency_seconds_on": 1.5},
  {"task_id": 2, "task_description": "do something complex", "correct_tool": "edit_file", "selected_off": "list_files", "selected_on": "list_files", "is_correct_off": false, "is_correct_on": false, "is_ambiguous": true, "confuser_tools": ["list_files"], "reasoning_text_on": "ambiguous task", "reasoning_tokens_on": 5, "latency_seconds_off": 0.3, "latency_seconds_on": 1.2}
]
```

`tests/fixtures/dashboard_runs/reasoning_old/metrics.json`(Phase 15 现存 17 mock run 之一形态,缺 per_tool 与 raw):
```json
{
  "timestamp": "20260512_150748",
  "think_ab_gate": {"passed": true},
  "think_off_score": 0.50,
  "think_on_score": 0.55,
  "param_predictors_discovered": 6,
  "iterations": 1
}
```

(这个 fixture 在 Wave 3 fallback 测试中用——它有 think_ab_gate 但缺 per_tool_*_rates → dropped_runs[]。)

**步骤 3**: 必须保证 fixture 文件 mtime 顺序合理(创建顺序即时间序);若需精确控制 mtime,在测试 fixture 加载前 monkeypatch 不依赖 mtime 排序(改为按 timestamp 字段或 path 字符串排序)——本任务保持 mtime 默认即可,Wave 1 测试用 `_scan_runs` patch 注入。

**步骤 4**: 不在本 task 写 test 文件——下一 task 16-01-2 写 test_regression_dashboard.py 占位 + 4 测试。
  </action>
  <verify>
    <automated>python -c "from evolution.tools.regression_dashboard import main, _scan_runs, _detect_source, _status_style, _segment_distribution, _render_latest; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f evolution/tools/regression_dashboard.py` (文件存在)
    - `test -f tests/fixtures/dashboard_runs/desc_old/metrics.json && test -f tests/fixtures/dashboard_runs/params_complete/metrics.json && test -f tests/fixtures/dashboard_runs/params_no_raw/metrics.json && test -f tests/fixtures/dashboard_runs/reasoning_complete/metrics.json && test -f tests/fixtures/dashboard_runs/reasoning_complete/ab_comparison.json && test -f tests/fixtures/dashboard_runs/reasoning_old/metrics.json` (6 fixture 文件全部存在)
    - `grep -n '@click.command()' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n '@click.option("--runs"' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'multiple=True' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'def _scan_runs' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'def _detect_source' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'def _status_style' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'def _segment_distribution' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'def _render_latest' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'def _render_frequency_bars' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n '_SPARK_CHARS = "▁▂▃▄▅▆▇█"' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'if __name__ == "__main__":' evolution/tools/regression_dashboard.py` 输出 1 行
    - **W5 强制 — Console default**: `grep -nE 'force_terminal|no_color=False|file=' evolution/tools/regression_dashboard.py | grep -i 'console'` 输出为空(没有任何 Console() 关键字参数;过滤掉 docstring/comment 中的同名词靠 `grep -i 'console'` 限定到 console 行)
    - `grep -n '^console = Console()$' evolution/tools/regression_dashboard.py` 输出 1 行(精确匹配 default 实例化 — 无任何参数)
    - `python -c "from evolution.tools.regression_dashboard import main, _scan_runs, _detect_source, _status_style; print('OK')"` 退出码 0
    - `python -c "import json; json.loads(open('tests/fixtures/dashboard_runs/params_complete/metrics.json').read()); print('valid json')"` 退出码 0
    - `python -c "from evolution.tools.regression_dashboard import _detect_source; from pathlib import Path; assert _detect_source({'think_ab_gate': {}}, Path('a/b/metrics.json')) == 'reasoning'; assert _detect_source({'param_predictors_discovered': 5}, Path('a/b/metrics.json')) == 'params'; assert _detect_source({}, Path('a/b/metrics.json')) is None; print('OK')"` 退出码 0
  </acceptance_criteria>
  <done>
    Dashboard CLI 骨架建立完成,6 fixture 创建,核心 helper(_scan_runs / _load_run / _detect_source / _status_style / _quintiles / _segment_distribution / _render_latest / _render_frequency_bars)定义齐全,可被 import 与单元测试。Console() 保持 default 行为(W5 fix)。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-01-2: Wave 1 functional 测试 — LATEST 表 / status 颜色 / 频次柱图 / sample <3 退化 n/a</name>
  <files>tests/tools/test_regression_dashboard.py</files>
  <read_first>
    - evolution/tools/regression_dashboard.py (task 16-01-1 刚完成的版本,理解 _render_latest 输出与 status_style 阈值)
    - tests/tools/test_evolve_tool_params_cli.py (line 64-99 CliRunner+patch 模式 — 任意一个能跑通的测试)
    - tests/fixtures/dashboard_runs/params_complete/metrics.json (确认 fixture 内容:5 tools,sample 数从 5 到 1 不等)
    - .planning/phases/16-per-tool-regression-dashboard/16-VALIDATION.md §"Per-Task Verification Map" 16-01-01 ~ 16-01-04 行
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Test Pattern Map"
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"Implementation Recipes Wave 1" (B2 fix — read_first 现已包含 RESEARCH §)
    - .planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md §D-09 (status 阈值精确值) §D-11 (sample <3 退化)
  </read_first>
  <behavior>
    - test_latest_renders_per_tool_table: 用 patch 注入 fixture params_complete → invoke main → result.exit_code == 0 → result.output 含 "LATEST" 字符串 + 至少一个 tool name(如 "search_files") + "delta_pp" 列头(或简写 "delta")
    - test_status_color_coding (W2 fix — 双断言:unit + functional): unit 部分直接调 `_status_style` 验证 4 档阈值与 style;functional 部分 invoke main + fixture(browser_navigate delta=-7pp 触发 FAIL),断言 result.output 含 "FAIL" 字符串(证明 inline markup 在 _render_latest 内生成 stdout 真渲染了 status label,catch markup escape bug 如 `[{label}]{label}[/{label}]`)
    - test_frequency_bars_aggregate_long_tail: 构 patch 注入一个 raw_predictions 含 13 个不同 correct_tool 的 fixture(top-12 + 1 落到 others) → output 含 "others (1 tool" 字符串(注意单数复数,严格按实现写)
    - test_distribution_n_a_when_sample_low: 用 fixture params_complete(其中 browser_navigate 仅 2 例) → output 在该 tool 行含 "n/a" 字符串(distribution 5 列退化为 n/a)
  </behavior>
  <action>
创建 `tests/tools/test_regression_dashboard.py`,文件结构:

```python
"""Functional tests for regression_dashboard CLI — Phase 16 (TOOL-V2-04).

Wave 1: LATEST region + status color coding + frequency bars + sample-low n/a.
Wave 2-4 tests are stubbed with @pytest.mark.skip until those waves land.

WARNING 7 fix — 13 skip-stub names are LOCKED. Wave 2/3/4 unskip these exact
names; do not rename them in this file.
"""
import json
from pathlib import Path
from unittest.mock import patch

import pytest
from click.testing import CliRunner

from evolution.tools.regression_dashboard import main, _segment_distribution, _status_style


FIXTURES = Path(__file__).parent.parent / "fixtures" / "dashboard_runs"


# ── Wave 1 tests (active) ─────────────────────────────────────────────────


def test_latest_renders_per_tool_table():
    """16-01-01 — LATEST renders Rich Table with tool names, delta_pp, sample columns."""
    runner = CliRunner()
    with patch(
        "evolution.tools.regression_dashboard._scan_runs",
        return_value=[FIXTURES / "params_complete" / "metrics.json"],
    ):
        result = runner.invoke(main, ["--runs", str(FIXTURES / "params_complete")], catch_exceptions=False)
    assert result.exit_code == 0, f"stdout: {result.output}"
    assert "LATEST" in result.output
    assert "search_files" in result.output
    assert "read_file" in result.output
    # delta_pp column header (could be abbreviated to "delta_pp" or "delta")
    assert "delta" in result.output.lower()
    # sample column
    assert "sample" in result.output.lower()


def test_status_color_coding():
    """16-01-02 (W2 fix — dual unit + functional) — status thresholds AND inline-markup
    correctness in _render_latest stdout.

    Unit half: _status_style returns correct (label, style) for FAIL/WARN/GAIN/OK.
    Functional half: invoke main on params_complete fixture (browser_navigate delta=-7pp);
    assert "FAIL" appears in stdout. This catches Rich markup bugs like
    `f"[{label}]{label}[/{label}]"` where label gets used as a style — unit pass,
    stdout rendering broken (label stripped or rendered as raw markup).
    """
    # ── Unit half: _status_style direct ──
    assert _status_style(-7.0)[0].startswith("FAIL")
    assert _status_style(-7.0)[1] == "bold red"
    assert _status_style(-3.0)[0].startswith("WARN")
    assert _status_style(-3.0)[1] == "yellow"
    assert _status_style(6.0)[0].startswith("GAIN")
    assert _status_style(6.0)[1] == "bold green"
    assert _status_style(1.0)[0].startswith("OK")
    assert _status_style(1.0)[1] == ""
    # Threshold edge cases
    assert _status_style(-5.0)[0].startswith("FAIL")  # ≤ -5pp inclusive
    assert _status_style(-2.0)[0].startswith("WARN")  # ≤ -2pp inclusive
    assert _status_style(5.0)[0].startswith("GAIN")   # ≥ +5pp inclusive
    # Custom warning_threshold_pp
    assert _status_style(-1.5, warning_threshold_pp=1.0)[0].startswith("WARN")  # -1.5 ≤ -1.0
    assert _status_style(-1.5, warning_threshold_pp=3.0)[0].startswith("OK")    # -1.5 > -3.0

    # ── Functional half: invoke main with fixture that has FAIL-level delta ──
    # params_complete fixture: browser_navigate baseline=0.60, evolved=0.55 → delta=-5pp → FAIL ❌
    runner = CliRunner()
    fixture_path = FIXTURES / "params_complete" / "metrics.json"
    with patch(
        "evolution.tools.regression_dashboard._scan_runs",
        return_value=[fixture_path],
    ):
        result = runner.invoke(main, ["--runs", str(fixture_path.parent)], catch_exceptions=False)
    assert result.exit_code == 0, f"stdout: {result.output}"
    # The status label "FAIL" must appear in stdout — proves _render_latest inline markup
    # is rendering the label correctly (catches `f"[{label}]{label}[/{label}]"` bugs).
    assert "FAIL" in result.output, \
        f"expected 'FAIL' in stdout for browser_navigate row (delta=-5pp); got: {result.output[:1000]}"
    # browser_navigate row must be visible (proves table render reached this row)
    assert "browser_navigate" in result.output


def test_frequency_bars_aggregate_long_tail(tmp_path):
    """16-01-03 — frequency bars show top-12, aggregate the rest into 'others (N tool[s])'."""
    # Build a fixture run with 13 distinct correct_tools
    run_dir = tmp_path / "long_tail"
    run_dir.mkdir()
    raw_preds = [
        {"correct_tool": f"tool_{i:02d}", "selected_tool": f"tool_{i:02d}",
         "difficulty": "easy", "num_available_tools": 3}
        for i in range(13)
    ]
    metrics = {
        "timestamp": "20260512_999999",
        "param_predictors_discovered": 1,
        "baseline_score": 0.5,
        "per_tool_baseline_rates": {f"tool_{i:02d}": 0.5 for i in range(13)},
        "per_tool_evolved_rates": {f"tool_{i:02d}": 0.5 for i in range(13)},
        "raw_predictions": raw_preds,
    }
    metrics_path = run_dir / "metrics.json"
    metrics_path.write_text(json.dumps(metrics))

    runner = CliRunner()
    with patch(
        "evolution.tools.regression_dashboard._scan_runs",
        return_value=[metrics_path],
    ):
        result = runner.invoke(main, ["--runs", str(run_dir)], catch_exceptions=False)
    assert result.exit_code == 0, f"stdout: {result.output}"
    assert "Sample frequency" in result.output
    # 13 tools, top 12 shown, 1 in others
    assert "others (1 tool" in result.output  # matches "1 tool" or "1 tools"


def test_distribution_n_a_when_sample_low():
    """16-01-04 — per-tool sample_count < 3 across segments → distribution columns render n/a."""
    runner = CliRunner()
    fixture_metrics_path = FIXTURES / "params_complete" / "metrics.json"
    assert fixture_metrics_path.exists(), f"fixture missing: {fixture_metrics_path}"
    with patch(
        "evolution.tools.regression_dashboard._scan_runs",
        return_value=[fixture_metrics_path],
    ):
        result = runner.invoke(main, ["--runs", str(fixture_metrics_path.parent), "--segment", "difficulty"], catch_exceptions=False)
    assert result.exit_code == 0, f"stdout: {result.output}"
    # browser_navigate has only 2 raw_predictions in fixture → must show n/a
    output = result.output
    # Find the browser_navigate row and assert n/a
    # Rich tables wrap so we look for "n/a" within close proximity to "browser_navigate"
    assert "browser_navigate" in output
    assert "n/a" in output


# ── Wave 2-4 stubs (skipped until those waves) ────────────────────────────
# WARNING 7 fix: these 13 names are LOCKED contracts.
# Wave 2 unskips: test_diff_requires_both_runs / test_trend_window_days_mutex / test_trend_sparkline
# Wave 3 unskips: test_ab_study_categories / test_ab_study_secret_redaction / test_source_detection /
#                 test_fallback_dropped_run / test_fallback_no_raw_predictions /
#                 test_fallback_no_ab_comparison / test_warning_threshold_no_exit
# Wave 4 unskips: test_e2e_dashboard_json_schema / test_dashboard_json_output_path / test_no_runs_exits_2
# Renaming any of these stubs breaks the wave-2/3/4 grep gates.


@pytest.mark.skip(reason="Wave 2 — DIFF region")
def test_diff_requires_both_runs():
    pass


@pytest.mark.skip(reason="Wave 2 — TREND mutex")
def test_trend_window_days_mutex():
    pass


@pytest.mark.skip(reason="Wave 2 — TREND sparkline")
def test_trend_sparkline():
    pass


@pytest.mark.skip(reason="Wave 3 — ABStudy categories")
def test_ab_study_categories():
    pass


@pytest.mark.skip(reason="Wave 3 — ABStudy secret redaction")
def test_ab_study_secret_redaction():
    pass


@pytest.mark.skip(reason="Wave 3 — source detection")
def test_source_detection():
    pass


@pytest.mark.skip(reason="Wave 3 — fallback dropped run")
def test_fallback_dropped_run():
    pass


@pytest.mark.skip(reason="Wave 3 — fallback no raw_predictions")
def test_fallback_no_raw_predictions():
    pass


@pytest.mark.skip(reason="Wave 3 — fallback no ab_comparison")
def test_fallback_no_ab_comparison():
    pass


@pytest.mark.skip(reason="Wave 3 — warning threshold no exit")
def test_warning_threshold_no_exit():
    pass


@pytest.mark.skip(reason="Wave 4 — E2E dashboard.json schema")
def test_e2e_dashboard_json_schema():
    pass


@pytest.mark.skip(reason="Wave 4 — dashboard.json output path")
def test_dashboard_json_output_path():
    pass


@pytest.mark.skip(reason="Wave 4 — no runs exits 2")
def test_no_runs_exits_2():
    pass
```

跑测试,4 active 测试必须全绿(13 个 skip 不算失败)。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_regression_dashboard.py::test_latest_renders_per_tool_table tests/tools/test_regression_dashboard.py::test_status_color_coding tests/tools/test_regression_dashboard.py::test_frequency_bars_aggregate_long_tail tests/tools/test_regression_dashboard.py::test_distribution_n_a_when_sample_low -x</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/tools/test_regression_dashboard.py` 文件存在
    - `grep -c '^def test_' tests/tools/test_regression_dashboard.py` 输出 17(4 active + 13 skip stubs)
    - `grep -c 'pytest.mark.skip' tests/tools/test_regression_dashboard.py` 输出 13
    - **W7 强制 — 13 skip-stub 名字精确锁定**: 跑 `for n in test_diff_requires_both_runs test_trend_window_days_mutex test_trend_sparkline test_ab_study_categories test_ab_study_secret_redaction test_source_detection test_fallback_dropped_run test_fallback_no_raw_predictions test_fallback_no_ab_comparison test_warning_threshold_no_exit test_e2e_dashboard_json_schema test_dashboard_json_output_path test_no_runs_exits_2; do grep -c "^def $n" tests/tools/test_regression_dashboard.py | grep -q '^1$' || { echo "MISSING: $n"; exit 1; }; done`,要求 13 个名字全部精确出现 1 次。这 13 名字是 Wave 2/3/4 grep gate 的固定契约,不可重命名。
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py -x` 退出码 0(passed: 4, skipped: 13)
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py::test_latest_renders_per_tool_table -x` 退出码 0
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py::test_status_color_coding -x` 退出码 0
    - **W2 强制 — test_status_color_coding functional 半段存在**: `grep -nE 'CliRunner\(\)|runner\.invoke\(main' tests/tools/test_regression_dashboard.py` 输出至少 4 行(test_status_color_coding 内部必须有 CliRunner + invoke main 调用,证明已加 functional 半段)
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py::test_frequency_bars_aggregate_long_tail -x` 退出码 0
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py::test_distribution_n_a_when_sample_low -x` 退出码 0
    - 手动 smoke: `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/params_complete` 退出码 0,stdout 含 "LATEST" 与 "search_files"
  </acceptance_criteria>
  <done>
    Wave 1 4 个 functional 测试全绿,LATEST 区可在 stdout 渲染 12 列 Rich Table + 频次柱图,status 颜色编码与 sample<3 退化 n/a 都被验证。test_status_color_coding 现在 unit + functional 双断言守门 inline markup 渲染正确性(W2 fix)。Wave 2/3/4 测试占位已生成且名字精确锁定(W7 fix),后续 wave 直接 unskip 填实。
  </done>
</task>

</tasks>

<verification>
**Wave 1 完成标志**:
- `.venv/bin/pytest tests/tools/test_regression_dashboard.py -x` 退出码 0(4 passed, 13 skipped)
- `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/params_complete` 退出码 0,stdout 含 "LATEST" 与 fixture 中的 tool name
- `python -c "from evolution.tools.regression_dashboard import main, _scan_runs, _detect_source, _status_style, _segment_distribution, _quintiles, _render_latest, _render_frequency_bars; print('OK')"` 退出码 0
- `find tests/fixtures/dashboard_runs -name "*.json" | wc -l | tr -d ' '` 输出 6
</verification>

<success_criteria>
- regression_dashboard.py 创建完成(~250 LoC),含 9 个 Click flag + 7 个 helper + main 入口
- LATEST 区渲染 Rich Table 12 列 + 频次柱图 Panel + source legend Panel
- status 颜色编码 4 档(OK/WARN/FAIL/GAIN)按 D-09 阈值精确实现 + functional 测试守门 stdout 渲染(W2 fix)
- per-segment sample <3 → distribution 列 n/a(D-11 / Pitfall 10 收口)
- 6 fixture 文件就位,Wave 2-4 可直接复用
- Wave 1 4 个 functional 测试全绿,Wave 2-4 占位测试结构已建,13 名字精确锁定(W7 fix)
- Console() 默认实例化,无 force_terminal 关键字参数(W5 fix)
</success_criteria>

<output>
After completion, create `.planning/phases/16-per-tool-regression-dashboard/16-01-SUMMARY.md` with:
- 文件清单(1 source + 1 test + 6 fixture)
- 关键 helper 签名(_scan_runs / _detect_source / _status_style / _segment_distribution)
- D-09 status 阈值实测确认
- D-11 sample<3 退化 n/a 的具体阈值常量(_MIN_SEGMENT_SAMPLE = 3)
- 任何 Rich rendering 中遇到的真实 stdout 捕获问题与解决
</output>
</content>
</invoke>
