---
plan: 16-02-diff-trend
phase: 16
type: execute
wave: 2
depends_on: [16-01-dashboard-latest]
files_modified:
  - path: evolution/tools/regression_dashboard.py
    role: modified
  - path: tests/tools/test_regression_dashboard.py
    role: modified
  - path: tests/fixtures/dashboard_runs/params_complete_v2/metrics.json
    role: new
requirements: [TOOL-V2-04]
autonomous: true
estimated_minutes: 80

must_haves:
  truths:
    - "DIFF 区仅在 --baseline-run 与 --evolved-run 都传时启用,缺一报 click.UsageError 或 stdout 黄色 hint(D-05)"
    - "--trend-window 与 --trend-days 同时传 → click.UsageError → exit code 2(D-06)"
    - "TREND 区按窗口取最近 N run 或近 D 天 run,默认 trend-window=10"
    - "TREND 区每个 tool 渲染 sparkline 字符串(▁▂▃▄▅▆▇█ 中的字符)+ 跨 run min/p25/median/p75/max 摘要列(D-10 语义 B:跨 run 同工具 evolved_rate 时间序列)"
    - "DIFF 区复用 LATEST 表格渲染原语,展示两 run per-tool 对比"
  artifacts:
    - path: evolution/tools/regression_dashboard.py
      provides: "DIFF 与 TREND 区渲染逻辑 + sparkline helper"
      contains: "def _render_diff"
    - path: tests/fixtures/dashboard_runs/params_complete_v2/metrics.json
      provides: "用于 DIFF/TREND 的第二个 params run fixture(与 params_complete 形成对比)"
  key_links:
    - from: evolution/tools/regression_dashboard.py:_render_trend
      to: evolution/tools/regression_dashboard.py:_sparkline
      via: 函数调用
      pattern: "_sparkline\\("
    - from: tests/tools/test_regression_dashboard.py
      to: evolution/tools/regression_dashboard.py:main
      via: CliRunner.invoke with --baseline-run/--evolved-run/--trend-*
      pattern: "--trend-window"
---

<objective>
Wave 2 — DIFF 区 + TREND 区 + sparkline。在 Wave 1 LATEST 骨架之上,扩展 dashboard 处理两区:
1. DIFF: `--baseline-run <path>` + `--evolved-run <path>` 双传时启用,显示两 run per-tool 双对比表格(D-05)
2. TREND: `--trend-window N`(默认 10) 或 `--trend-days D`(互斥),展示跨 run 同工具 evolved_rate 序列 sparkline + min/p25/median/p75/max 摘要列(D-06 / D-10 语义 B)
3. 互斥校验:同时传 `--trend-window` 与 `--trend-days` → click.UsageError → exit 2

Purpose: LATEST 仅展示最新单 run。DIFF 让用户对比任意两 run(回归归因 / cherry-pick 对比);TREND 让用户看跨 N 历史 run 同工具的演变趋势(覆盖 ROADMAP §Phase 16 成功标准 2「Rich console dashboard 显示 per-tool 选择率变化」的时间维度)。

Output:
- 在 `evolution/tools/regression_dashboard.py` 新增 `_render_diff` / `_render_trend` / `_sparkline` 三个 helper + main 中的 orchestration 调用
- 在 `tests/tools/test_regression_dashboard.py` unskip 并实现 3 个 Wave 2 测试(test_diff_requires_both_runs / test_trend_window_days_mutex / test_trend_sparkline)
- 新建 `tests/fixtures/dashboard_runs/params_complete_v2/metrics.json` 作为 DIFF / TREND 的第二个 params run fixture
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md
@.planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md
@.planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md
@.planning/phases/16-per-tool-regression-dashboard/16-VALIDATION.md
@.planning/phases/16-per-tool-regression-dashboard/16-00-SUMMARY.md
@.planning/phases/16-per-tool-regression-dashboard/16-01-SUMMARY.md
@evolution/tools/regression_dashboard.py
@tests/tools/test_regression_dashboard.py

<interfaces>
<!-- Wave 1 already provided (in regression_dashboard.py): -->
<!-- - _scan_runs(roots) -> list[Path]: glob + mtime sort -->
<!-- - _load_run(metrics_path) -> dict | None: {path, source, metrics, ab_comparison?} -->
<!-- - _detect_source(metrics, run_path) -> Optional[str]: D-07 启发判定 -->
<!-- - _status_style(delta_pp, warning_threshold_pp) -> tuple[str, str] -->
<!-- - _quintiles(values) -> dict: {min, p25, median, p75, max} -->
<!-- - _segment_distribution(raw_preds, segment) -> dict[tool, dict] -->
<!-- - _render_latest(run, segment, warning_threshold_pp) -> dict -->
<!-- - _render_frequency_bars(sample_counts) -> None -->

<!-- Existing main flag set (Wave 1): -->
<!-- @click.option("--runs", "runs", multiple=True, type=click.Path()) -->
<!-- @click.option("--baseline-run", default=None, type=click.Path()) -->
<!-- @click.option("--evolved-run", default=None, type=click.Path()) -->
<!-- @click.option("--trend-window", default=None, type=int) -->
<!-- @click.option("--trend-days", default=None, type=int) -->
<!-- @click.option("--segment", default="difficulty", type=click.Choice([...])) -->
<!-- @click.option("--warning-threshold-pp", default=2.0, type=float) -->
<!-- @click.option("--output", default=None, type=click.Path()) -->

<!-- D-05 DIFF: only render when both --baseline-run AND --evolved-run passed; if exactly one → -->
<!--          stdout yellow hint AND skip DIFF region (do NOT abort — continues with LATEST) -->
<!-- D-06 TREND mutex: trend_window AND trend_days both not-None → click.UsageError → exit 2 -->
<!-- D-10 语义 B (TREND): cross-run same-tool evolved_rate time-series → quintiles + sparkline -->

<!-- Sparkline algorithm (RESEARCH §Library API): -->
_SPARK_CHARS = "▁▂▃▄▅▆▇█"  # 8 chars, U+2581..U+2588

def _sparkline(values: list[float]) -> str:
    if not values:
        return ""
    lo, hi = min(values), max(values)
    span = (hi - lo) or 1.0
    return "".join(_SPARK_CHARS[min(7, int((v - lo) / span * 8))] for v in values)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 16-02-1: 在 dashboard 中新增 _sparkline / _render_diff / _render_trend + main 编排</name>
  <files>evolution/tools/regression_dashboard.py, tests/fixtures/dashboard_runs/params_complete_v2/metrics.json</files>
  <read_first>
    - evolution/tools/regression_dashboard.py (Wave 1 完整版本,理解 _render_latest 表格结构 + main 当前 orchestration)
    - tests/fixtures/dashboard_runs/params_complete/metrics.json (做 DIFF 的对比 run 1)
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Excerpt K — Rich Style 颜色编码 + sparkline"
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"Library API/Sparkline" + §"Implementation Recipes Wave 2"
    - .planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md §D-05 §D-06 §D-10
  </read_first>
  <action>
**步骤 1**: 创建 `tests/fixtures/dashboard_runs/params_complete_v2/metrics.json`(作为 DIFF 第二个 run + TREND 序列第二个点):

```json
{
  "timestamp": "20260520_100000",
  "iterations": 7,
  "param_predictors_discovered": 14,
  "optimizer_used": "gepa",
  "baseline_score": 0.78,
  "evolved_score": 0.85,
  "improvement": 0.07,
  "per_tool_baseline_rates": {
    "search_files": 0.82,
    "read_file": 0.86,
    "browser_navigate": 0.62,
    "edit_file": 0.72,
    "list_files": 0.91
  },
  "per_tool_evolved_rates": {
    "search_files": 0.88,
    "read_file": 0.89,
    "browser_navigate": 0.58,
    "edit_file": 0.81,
    "list_files": 0.94
  },
  "raw_predictions": [
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "easy", "num_available_tools": 4},
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "medium", "num_available_tools": 5},
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "medium", "num_available_tools": 5},
    {"correct_tool": "read_file", "selected_tool": "read_file", "difficulty": "easy", "num_available_tools": 3},
    {"correct_tool": "read_file", "selected_tool": "read_file", "difficulty": "medium", "num_available_tools": 4},
    {"correct_tool": "browser_navigate", "selected_tool": "browser_navigate", "difficulty": "easy", "num_available_tools": 3},
    {"correct_tool": "edit_file", "selected_tool": "edit_file", "difficulty": "medium", "num_available_tools": 4},
    {"correct_tool": "list_files", "selected_tool": "list_files", "difficulty": "easy", "num_available_tools": 3}
  ]
}
```

**步骤 2**: 在 `evolution/tools/regression_dashboard.py` 中追加 / 修改:

(a) **新增 `_sparkline` 函数**(在 `_render_frequency_bars` 之后,Click 装饰器之前):

```python
def _sparkline(values: list[float]) -> str:
    """8-char Unicode block sparkline (▁▂▃▄▅▆▇█).

    Maps each value linearly to one of 8 block characters based on (v-min)/(max-min) span.
    Empty list → "". Single value → "▄" (mid-band).
    """
    if not values:
        return ""
    lo, hi = min(values), max(values)
    span = (hi - lo) or 1.0
    return "".join(
        _SPARK_CHARS[min(7, int((v - lo) / span * 8))]
        for v in values
    )
```

(b) **新增 `_render_diff` 函数**(在 `_render_latest` 之后):

```python
def _render_diff(
    baseline_run: dict,
    evolved_run: dict,
    warning_threshold_pp: float,
) -> dict:
    """Render DIFF region: per-tool comparison between two specific runs (D-05).

    Returns dict for dashboard.json:
        {baseline_run, evolved_run, per_tool: [{tool, baseline_rate, evolved_rate, delta_pp, status}]}
    """
    b_metrics = baseline_run["metrics"]
    e_metrics = evolved_run["metrics"]
    b_rates = b_metrics.get("per_tool_evolved_rates", {}) or {}  # 「baseline」run 的 evolved 是新跑后 rate
    e_rates = e_metrics.get("per_tool_evolved_rates", {}) or {}
    all_tools = sorted(set(b_rates.keys()) | set(e_rates.keys()))

    table = Table(
        title=f"DIFF [{baseline_run['source']} vs {evolved_run['source']}]: "
              f"{baseline_run['path']} → {evolved_run['path']}",
        show_lines=False,
    )
    table.add_column("tool", justify="left", style="cyan")
    table.add_column("baseline_run rate", justify="right")
    table.add_column("evolved_run rate", justify="right")
    table.add_column("delta_pp", justify="right")
    table.add_column("status", justify="left")

    per_tool: list[dict] = []
    for tool in all_tools:
        b = float(b_rates.get(tool, 0.0))
        e = float(e_rates.get(tool, 0.0))
        delta = (e - b) * 100.0
        label, style = _status_style(delta, warning_threshold_pp)
        table.add_row(
            tool, f"{b:.3f}", f"{e:.3f}",
            f"[{style}]{delta:+.2f}[/{style}]" if style else f"{delta:+.2f}",
            f"[{style}]{label}[/{style}]" if style else label,
        )
        per_tool.append({
            "tool": tool,
            "baseline_rate": b,
            "evolved_rate": e,
            "delta_pp": delta,
            "status": label.split()[0],
        })

    console.print(table)
    return {
        "baseline_run": baseline_run["path"],
        "evolved_run": evolved_run["path"],
        "per_tool": per_tool,
    }
```

(c) **新增 `_render_trend` 函数**(在 `_render_diff` 之后):

```python
def _render_trend(
    runs: list[dict],
    window_kind: str,
    window_value: int,
) -> dict:
    """Render TREND region: cross-run sparkline + quintile summary per tool (D-10 语义 B).

    `runs` is the filtered list (already windowed by caller).
    For each tool, gather evolved_rate time-series across runs (oldest → newest).
    """
    if not runs:
        console.print("[dim]No runs for TREND region[/dim]")
        return {"window_kind": window_kind, "window_value": window_value, "tools": []}

    # Build {tool: [(run_path, evolved_rate)]} time-series, oldest first
    series: dict[str, list[tuple[str, float]]] = {}
    for run in runs:
        rates = run["metrics"].get("per_tool_evolved_rates", {}) or {}
        for tool, rate in rates.items():
            series.setdefault(tool, []).append((run["path"], float(rate)))

    table = Table(
        title=f"TREND ({window_kind}={window_value}, {len(runs)} runs)",
        show_lines=False,
    )
    table.add_column("tool", justify="left", style="cyan")
    table.add_column("sparkline", justify="left")
    table.add_column("min", justify="right")
    table.add_column("p25", justify="right")
    table.add_column("median", justify="right")
    table.add_column("p75", justify="right")
    table.add_column("max", justify="right")
    table.add_column("n_runs", justify="right")

    tools_records: list[dict] = []
    for tool in sorted(series.keys()):
        ts = series[tool]
        rates = [r for _, r in ts]
        spark = _sparkline(rates)
        q = _quintiles(rates)
        table.add_row(
            tool, spark,
            f"{q['min']:.2f}", f"{q['p25']:.2f}", f"{q['median']:.2f}",
            f"{q['p75']:.2f}", f"{q['max']:.2f}",
            str(len(rates)),
        )
        tools_records.append({
            "tool": tool,
            "sparkline": spark,
            "series": [{"run_path": p, "rate": r} for p, r in ts],
            "summary": q,
        })

    console.print(Panel(
        "[dim]Distribution semantics (D-10 B): min/p25/median/p75/max are computed across N runs' "
        "evolved_rate time-series for each tool (cross-run, not within-run segment).[/dim]",
        title="TREND legend",
    ))
    console.print(table)

    return {
        "window_kind": window_kind,
        "window_value": window_value,
        "tools": tools_records,
    }
```

(d) **修改 `main` 编排**:在 Wave 1 的 LATEST 渲染之后(在 `if dropped:` 行之前)插入 DIFF / TREND 编排:

```python
    # ── DIFF region (D-05) ────────────────────────────────────────────────
    if baseline_run is not None or evolved_run is not None:
        if baseline_run is None or evolved_run is None:
            console.print(
                "[yellow]DIFF region requires both --baseline-run AND --evolved-run; "
                "got only one — skipping DIFF[/yellow]"
            )
        else:
            b_loaded = _load_run(Path(baseline_run) / "metrics.json")
            e_loaded = _load_run(Path(evolved_run) / "metrics.json")
            if (b_loaded and b_loaded.get("metrics") and "per_tool_evolved_rates" in b_loaded["metrics"]
                and e_loaded and e_loaded.get("metrics") and "per_tool_evolved_rates" in e_loaded["metrics"]):
                _render_diff(b_loaded, e_loaded, warning_threshold_pp)
            else:
                console.print("[yellow]DIFF: one or both runs missing per_tool_evolved_rates — skipped[/yellow]")

    # ── TREND region (D-06) ──────────────────────────────────────────────
    valid_runs: list[dict] = []
    for p in runs_paths:
        loaded = _load_run(p)
        if (loaded and loaded.get("metrics") and loaded.get("source")
            and "per_tool_evolved_rates" in loaded["metrics"]
            and loaded["metrics"].get("status") != "FAILED"):
            valid_runs.append(loaded)

    # Determine TREND window
    if trend_days is not None:
        window_kind = "days"
        window_value = trend_days
        cutoff = datetime.now().timestamp() - (trend_days * 86400.0)
        windowed = [r for r in valid_runs if Path(r["path"], "metrics.json").stat().st_mtime >= cutoff]
    else:
        window_kind = "n"
        window_value = trend_window if trend_window is not None else 10
        windowed = valid_runs[-window_value:] if len(valid_runs) > window_value else valid_runs

    if windowed:
        _render_trend(windowed, window_kind, window_value)
```

(注:确保 `from datetime import datetime` 在 file 顶部已 import — Wave 1 已有,无需新增。)

**步骤 3**: 不动 Wave 1 的 LATEST 区代码;不删 `_render_latest` / `_render_frequency_bars`;不动 fixture 路径常量。
  </action>
  <verify>
    <automated>python -c "from evolution.tools.regression_dashboard import _sparkline, _render_diff, _render_trend; assert len(_sparkline([1.0, 2.0, 3.0])) == 3; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def _sparkline' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'def _render_diff' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'def _render_trend' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n '_SPARK_CHARS\[min(7' evolution/tools/regression_dashboard.py` 输出至少 1 行(sparkline 算法存在)
    - `grep -n 'DIFF region' evolution/tools/regression_dashboard.py` 输出至少 1 行(D-05 sentinel)
    - `grep -n 'TREND region' evolution/tools/regression_dashboard.py` 输出至少 1 行(D-06 sentinel)
    - `test -f tests/fixtures/dashboard_runs/params_complete_v2/metrics.json` 退出码 0
    - `python -c "from evolution.tools.regression_dashboard import _sparkline; assert _sparkline([]) == ''; assert len(_sparkline([0.1, 0.5, 0.9, 0.3])) == 4; assert all(c in '▁▂▃▄▅▆▇█' for c in _sparkline([0.1, 0.5, 0.9, 0.3]))" `退出码 0
    - `python -c "from evolution.tools.regression_dashboard import _render_diff, _render_trend; print('imports ok')"` 退出码 0
    - 手动 smoke: `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/params_complete --runs tests/fixtures/dashboard_runs/params_complete_v2 --trend-window 5` 退出码 0,stdout 含 "TREND" 与 sparkline 字符
  </acceptance_criteria>
  <done>
    DIFF / TREND / sparkline 三 helper 实现完成,main 中已 wire,可手动 smoke。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-02-2: unskip + 实现 3 个 Wave 2 测试(diff_requires_both / trend_mutex / trend_sparkline)</name>
  <files>tests/tools/test_regression_dashboard.py</files>
  <read_first>
    - evolution/tools/regression_dashboard.py (task 16-02-1 完成后版本 — 新增的 _sparkline / _render_diff / _render_trend 与 main 编排)
    - tests/tools/test_regression_dashboard.py (Wave 1 创建版本,3 个 Wave 2 测试当前是 skip stub)
    - tests/fixtures/dashboard_runs/params_complete/metrics.json + params_complete_v2/metrics.json
    - .planning/phases/16-per-tool-regression-dashboard/16-VALIDATION.md §16-02-01/16-02-02/16-02-03
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Test Pattern Map" (B2 fix — 现已包含 PATTERNS §)
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"Implementation Recipes Wave 2" (B2 fix — 现已包含 RESEARCH §)
    - .planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md §D-05/D-06/D-10
  </read_first>
  <behavior>
    - test_diff_requires_both_runs: invoke main 仅传 `--baseline-run` 时 stdout 含 "DIFF region requires both" 字符串(不应 exit 2,继续渲染 LATEST);两个都传时 stdout 含 "DIFF" + 对比表的两 run path
    - test_trend_window_days_mutex: invoke main 同时传 `--trend-window 5 --trend-days 7` → result.exit_code == 2(click.UsageError)且 result.output 含 "mutually exclusive"
    - test_trend_sparkline: invoke main 注入 2+ run fixture → stdout 含 "TREND" 区 + 至少一个 sparkline 字符(任一 ▁..█ 字符) + "min", "p25", "median", "p75", "max" 5 个列头
  </behavior>
  <action>
修改 `tests/tools/test_regression_dashboard.py`:

1. 删除以下 3 个 `@pytest.mark.skip` 装饰的 stub 的 `@pytest.mark.skip(reason="Wave 2 — ...")` 行:
   - `test_diff_requires_both_runs`
   - `test_trend_window_days_mutex`
   - `test_trend_sparkline`
   并替换函数体 `pass` 为真实实现:

```python
def test_diff_requires_both_runs():
    """16-02-01 (D-05) — DIFF region requires both --baseline-run AND --evolved-run.

    Behavior: passing only one → stdout yellow hint, no exit code change (still 0,
    LATEST still rendered). Passing both → DIFF region rendered.
    """
    runner = CliRunner()
    fixture_v1 = FIXTURES / "params_complete"
    fixture_v2 = FIXTURES / "params_complete_v2"

    # Case 1: only --baseline-run → DIFF skipped with hint, exit 0
    with patch(
        "evolution.tools.regression_dashboard._scan_runs",
        return_value=[fixture_v1 / "metrics.json"],
    ):
        result = runner.invoke(
            main,
            ["--runs", str(fixture_v1), "--baseline-run", str(fixture_v1)],
            catch_exceptions=False,
        )
    assert result.exit_code == 0, f"stdout: {result.output}"
    assert "DIFF region requires both" in result.output

    # Case 2: both → DIFF rendered
    with patch(
        "evolution.tools.regression_dashboard._scan_runs",
        return_value=[fixture_v1 / "metrics.json", fixture_v2 / "metrics.json"],
    ):
        result = runner.invoke(
            main,
            [
                "--runs", str(fixture_v1),
                "--runs", str(fixture_v2),
                "--baseline-run", str(fixture_v1),
                "--evolved-run", str(fixture_v2),
            ],
            catch_exceptions=False,
        )
    assert result.exit_code == 0, f"stdout: {result.output}"
    assert "DIFF" in result.output


def test_trend_window_days_mutex():
    """16-02-02 (D-06) — --trend-window and --trend-days are mutually exclusive."""
    runner = CliRunner()
    fixture_v1 = FIXTURES / "params_complete"
    with patch(
        "evolution.tools.regression_dashboard._scan_runs",
        return_value=[fixture_v1 / "metrics.json"],
    ):
        result = runner.invoke(
            main,
            ["--runs", str(fixture_v1), "--trend-window", "5", "--trend-days", "7"],
            catch_exceptions=False,
        )
    assert result.exit_code == 2, f"expected exit 2 (UsageError), got {result.exit_code}; stdout: {result.output}"
    assert "mutually exclusive" in result.output


def test_trend_sparkline():
    """16-02-03 (D-10 B) — TREND region renders sparkline (▁▂▃▄▅▆▇█) + quintile summary columns."""
    runner = CliRunner()
    fixture_v1 = FIXTURES / "params_complete"
    fixture_v2 = FIXTURES / "params_complete_v2"
    with patch(
        "evolution.tools.regression_dashboard._scan_runs",
        return_value=[fixture_v1 / "metrics.json", fixture_v2 / "metrics.json"],
    ):
        result = runner.invoke(
            main,
            ["--runs", str(fixture_v1), "--runs", str(fixture_v2), "--trend-window", "10"],
            catch_exceptions=False,
        )
    assert result.exit_code == 0, f"stdout: {result.output}"
    assert "TREND" in result.output
    # Sparkline characters present in stdout (any of the 8 block chars)
    spark_chars = "▁▂▃▄▅▆▇█"
    assert any(c in result.output for c in spark_chars), f"no sparkline chars in stdout: {result.output[:500]}"
    # Quintile column headers
    assert "min" in result.output.lower()
    assert "p25" in result.output
    assert "median" in result.output
    assert "p75" in result.output
    assert "max" in result.output.lower()
```

**关键 — 函数名不可改**(W7 / Wave 1 锁定):3 个测试函数名必须精确为 `test_diff_requires_both_runs` / `test_trend_window_days_mutex` / `test_trend_sparkline`。Wave 1 已建立这 13 名字契约。

2. 不动其他 active 测试(Wave 1 4 个测试)和 Wave 3-4 占位(应仍是 9 个 skip;不必动 Wave 3 中 `test_warning_threshold_no_exit` 的 stub,Wave 3 自己 unskip)。**注意:Wave 1 的 13 stub 中,将 `test_warning_threshold_no_exit` 列为 Wave 3 — 本 Wave 2 不动它。**

3. 跑全套测试(`.venv/bin/pytest tests/tools/test_regression_dashboard.py -x`):4(Wave 1) + 3(Wave 2)= 7 active passed,10 skipped。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_regression_dashboard.py::test_diff_requires_both_runs tests/tools/test_regression_dashboard.py::test_trend_window_days_mutex tests/tools/test_regression_dashboard.py::test_trend_sparkline -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@pytest.mark.skip' tests/tools/test_regression_dashboard.py` 输出 10(从 13 降到 10:Wave 2 unskip 3 个)
    - `grep -n 'def test_diff_requires_both_runs' tests/tools/test_regression_dashboard.py` 输出 1 行,且其前一行不是 `@pytest.mark.skip`
    - `grep -n 'def test_trend_window_days_mutex' tests/tools/test_regression_dashboard.py` 输出 1 行,且其前一行不是 `@pytest.mark.skip`
    - `grep -n 'def test_trend_sparkline' tests/tools/test_regression_dashboard.py` 输出 1 行,且其前一行不是 `@pytest.mark.skip`
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py::test_diff_requires_both_runs -x` 退出码 0
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py::test_trend_window_days_mutex -x` 退出码 0
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py::test_trend_sparkline -x` 退出码 0
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py -x` 退出码 0(7 passed + 10 skipped)
    - 手动 smoke: `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/params_complete --baseline-run tests/fixtures/dashboard_runs/params_complete --evolved-run tests/fixtures/dashboard_runs/params_complete_v2` 退出码 0,stdout 含 "DIFF" 与对比表
  </acceptance_criteria>
  <done>
    Wave 2 3 个 functional 测试全绿;DIFF 在双传时启用,trend mutex 触发 exit 2,TREND 跨 run 渲染 sparkline + 摘要列。
  </done>
</task>

</tasks>

<verification>
**Wave 2 完成标志**:
- `.venv/bin/pytest tests/tools/test_regression_dashboard.py -x` 退出码 0(7 passed + 10 skipped)
- `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/params_complete --runs tests/fixtures/dashboard_runs/params_complete_v2 --trend-window 5 --baseline-run tests/fixtures/dashboard_runs/params_complete --evolved-run tests/fixtures/dashboard_runs/params_complete_v2` 退出码 0,stdout 含 "LATEST" + "DIFF" + "TREND" 三区
- `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/params_complete --trend-window 5 --trend-days 7` 退出码 2,stderr/stdout 含 "mutually exclusive"
</verification>

<success_criteria>
- _sparkline / _render_diff / _render_trend 三 helper 实现并通过测试
- DIFF 双传约束 + 单传 yellow hint(不 abort)
- TREND mutex 触发 click.UsageError → exit 2
- TREND 跨 run sparkline + 5 列分位数摘要(语义 B)
- params_complete_v2 fixture 创建,与 params_complete 形成对比
</success_criteria>

<output>
After completion, create `.planning/phases/16-per-tool-regression-dashboard/16-02-SUMMARY.md` with:
- 文件清单(1 source modified + 1 test modified + 1 fixture new)
- DIFF 用 evolved_rate 而非 baseline_rate 作对比侧的依据(每 run 内部已经做了 baseline→evolved,跨 run 对比有意义的是「这 run 的最终 rate」即 evolved_rate)
- TREND window 实现:trend-days 用 mtime cutoff,trend-window 用 list slice;两者互斥校验在 main 入口
- sparkline 退化:空 list → ""、单点 → 任一字符(算法 `min(7, int((v-lo)/span * 8))` 保证不越界)
</output>
</content>
</invoke>
