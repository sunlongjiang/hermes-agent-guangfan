---
plan: 16-03-abstudy-source-fallback
phase: 16
type: execute
wave: 3
depends_on: [16-02-diff-trend]
files_modified:
  - path: evolution/tools/regression_dashboard.py
    role: modified
  - path: tests/tools/test_regression_dashboard.py
    role: modified
  - path: tests/fixtures/dashboard_runs/reasoning_with_secret/metrics.json
    role: new
  - path: tests/fixtures/dashboard_runs/reasoning_with_secret/ab_comparison.json
    role: new
  - path: tests/fixtures/dashboard_runs/json_corrupt/metrics.json
    role: new
  - path: tests/fixtures/dashboard_runs/params_multi_regress/metrics.json
    role: new
requirements: [TOOL-V2-04]
autonomous: true
estimated_minutes: 110

must_haves:
  truths:
    - "ABStudy 区仅在扫到 reasoning source 且该 run 同目录有 ab_comparison.json 时渲染"
    - "ABStudy 三类计数 think_on_saved (off=False, on=True) / think_on_regressed (off=True, on=False) / both_wrong (off=False, on=False) 全部从 ab_comparison.json 推导"
    - "ABStudy top-3 例子:saved 按 reasoning_text_on 长度倒序,regressed 按 task_description 长度倒序,both_wrong 任意序"
    - "ABStudy 渲染 task_description 与 reasoning_text_on 前必须过 _contains_secret 二次 redact(D-15 防御)"
    - "source 启发判定顺序: think_ab_gate 在 → reasoning, param_predictors_discovered 在 → params, parent dir 'tools' + baseline_score → desc, 其他 → None(D-07)"
    - "缺 per_tool_*_rates 整 run dropped + dropped_runs[].reason 含 'missing per_tool_*_rates' 字符串(D-08)"
    - "缺 raw_predictions 仅 distribution 列退化为 n/a + stdout yellow 一行 'distribution disabled for this run'(D-08)"
    - "缺 ab_comparison.json (reasoning source) → ABStudy 区不渲染该 run + stdout 不抱怨(D-08)"
    - "warning_threshold_pp 默认 -2pp 触发 stdout 黄色警告 'WARNING:' 且不影响 exit code(D-13)"
    - "严苛 warning_threshold_pp(0.1)在 params_multi_regress fixture 上触发 ≥3 条 WARNING:,exit code 仍为 0(B3 fix 双 case 守门)"
    - "D-14:不实现 p25-based hard gate(显式不在 Phase 16 范围内,deferred 到 Phase 19/22 holdout 扩量后再评估;dashboard 仅 warning 不返 exit code 即收口此决策)"
  artifacts:
    - path: evolution/tools/regression_dashboard.py
      provides: "ABStudy 渲染 + secret redact + dropped_runs 累计 + warnings 收集"
      contains: "def _render_ab_study"
    - path: tests/fixtures/dashboard_runs/reasoning_with_secret/ab_comparison.json
      provides: "含 secret-like 字符串的 ab_comparison 用于 redact 测试"
    - path: tests/fixtures/dashboard_runs/json_corrupt/metrics.json
      provides: "坏 JSON 导致 _load_run 返回 _drop_reason='json parse error'"
    - path: tests/fixtures/dashboard_runs/params_multi_regress/metrics.json
      provides: "params source + 3 个负向 delta(read_file -0.5pp, browser_navigate -5pp, edit_file -1.5pp),用于 B3 fix Case B 严苛阈值多条 WARNING: 守门"
      contains: "per_tool_baseline_rates"
  key_links:
    - from: evolution/tools/regression_dashboard.py
      to: evolution/core/external_importers._contains_secret
      via: import + _safe_truncate 调用
      pattern: "_contains_secret"
    - from: evolution/tools/regression_dashboard.py
      to: evolution/tools/regression_dashboard.py
      via: ABStudy 仅在 reasoning source wire
      pattern: "_render_ab_study"
---

<objective>
Wave 3 — ABStudy + 跨 CLI source 标注 + 老 run fallback。在 Wave 1+2 LATEST/DIFF/TREND 之上,扩展 dashboard 收尾覆盖:

1. **ABStudy 区**(D-15):仅在扫到 reasoning source 且该 run 同目录有 `ab_comparison.json` 时渲染。三类计数(think_on_saved / think_on_regressed / both_wrong) + 每类 top-3 例子,task_description 与 reasoning_text_on 渲染前过 `_contains_secret` 二次 redact
2. **D-07 source legend**(已实现 _detect_source,本 wave 渲染 legend Panel):desc/params source baseline=v1 frozen, reasoning source baseline=think-off
3. **三类 fallback**(D-08):
   - 缺 `per_tool_*_rates` → 整 run dropped + dropped_runs[].reason
   - 缺 `raw_predictions` → distribution 列退化 n/a + stdout 一行 yellow
   - 缺 `ab_comparison.json`(reasoning source 必须)→ ABStudy 区不渲染该 run, stdout 不抱怨
4. **Warning 门**(D-13):per-tool delta ≤ -warning_threshold_pp 累入 warnings list,stdout 黄色一行,**不影响 exit code**

Purpose: 覆盖 ROADMAP §Phase 16 成功标准 3「Regression 阈值可配」+ Phase 15 deferred 中的「错例分析归入 dashboard」。三类 fallback 是 Phase 16 上线后的真实主路径(因为 90% 现存 run 在 Wave 0 之前没接 helper)。

Output:
- 在 `evolution/tools/regression_dashboard.py` 新增 `_safe_truncate` / `_count_ab_categories` / `_render_ab_study` 三个 helper + main 中的 ABStudy + warning + dropped_runs 编排
- 在 `tests/tools/test_regression_dashboard.py` unskip 并实现 7 个 Wave 3 测试
- 新建 4 个 fixture(reasoning_with_secret/{metrics.json, ab_comparison.json} + json_corrupt/metrics.json + params_multi_regress/metrics.json)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md
@.planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md
@.planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md
@.planning/phases/16-per-tool-regression-dashboard/16-VALIDATION.md
@.planning/phases/16-per-tool-regression-dashboard/16-02-SUMMARY.md
@.planning/phases/15-think-augmented-tool-selection/15-CONTEXT.md
@evolution/tools/regression_dashboard.py
@evolution/core/external_importers.py
@tests/tools/test_regression_dashboard.py

<interfaces>
Existing helpers from Wave 1+2 (in regression_dashboard.py):
- _scan_runs / _load_run / _detect_source / _status_style / _quintiles
- _segment_distribution / _render_latest / _render_frequency_bars
- _sparkline / _render_diff / _render_trend
- main with 9 Click flags

ab_comparison.json schema (canonical source: 15-CONTEXT.md §D-11; runtime shape from
RESEARCH §Existing Code Survey, Phase 15 actual):
{
  "task_id": int, "task_description": str, "correct_tool": str,
  "selected_off": str, "selected_on": str,
  "is_correct_off": bool, "is_correct_on": bool, "is_ambiguous": bool,
  "confuser_tools": list[str], "reasoning_text_on": str,
  "reasoning_tokens_on": int, "latency_seconds_off": float, "latency_seconds_on": float
}

D-15 三类计数推导:
- think_on_saved = is_correct_off==False AND is_correct_on==True
- think_on_regressed = is_correct_off==True AND is_correct_on==False
- both_wrong = is_correct_off==False AND is_correct_on==False

D-15 top-3 排序键(CONTEXT specifics + research recommendation):
- saved: by reasoning_text_on length DESC
- regressed: by task_description length DESC
- both_wrong: 任意(不引导)

_contains_secret signature (existing in evolution/core/external_importers.py:108-121):
def _contains_secret(text: str) -> bool:
    # SECRET_PATTERNS regex match + Shannon entropy heuristic on 24+ char tokens

D-15 redact 规则:
def _safe_truncate(text: str, max_len: int = 80) -> str:
    if not text: return ""
    if _contains_secret(text): return "[REDACTED — secret-like content]"
    return text[:max_len] + "..." if len(text) > max_len else text

D-13 warning collection (D-13 不影响 exit code; main 末尾打印 yellow 一行 per warning):
warnings: list[dict] = [{"tool": str, "delta_pp": float, "run_path": str}]

B3 fix — Case B 严苛阈值守门 fixture (params_multi_regress):
- params source(param_predictors_discovered=5),5 个 tool
- per_tool deltas: search_files +4pp / read_file -0.5pp / browser_navigate -5pp / edit_file -1.5pp / list_files +2pp
- 3 个 tool 满足 -delta ≤ -0.1pp(read_file / browser_navigate / edit_file),Case B 阈值 0.1 → 3 条 WARNING:
- Case A 用 params_complete(只有 browser_navigate -5pp 越过 2.0pp)→ 1 条 WARNING:
- 双 case 都 exit_code=0(D-13)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 16-03-1: 新增 _safe_truncate / _count_ab_categories / _render_ab_study + main 编排 + 4 个新 fixture</name>
  <files>evolution/tools/regression_dashboard.py, tests/fixtures/dashboard_runs/reasoning_with_secret/metrics.json, tests/fixtures/dashboard_runs/reasoning_with_secret/ab_comparison.json, tests/fixtures/dashboard_runs/json_corrupt/metrics.json, tests/fixtures/dashboard_runs/params_multi_regress/metrics.json</files>
  <read_first>
    - evolution/tools/regression_dashboard.py (Wave 1+2 完整版本,理解 main 编排骨架与 _load_run 已经收 ab_comparison)
    - evolution/core/external_importers.py:108-121 (确认 _contains_secret 签名与 SECRET_PATTERNS 实现)
    - tests/fixtures/dashboard_runs/reasoning_complete/ab_comparison.json (Wave 1 创建,作为 happy-path 对比)
    - tests/fixtures/dashboard_runs/params_complete/metrics.json (Wave 1 fixture — Case A 来源,deltas 已固化为 search_files +4 / read_file +2 / browser_navigate -5 / edit_file +8 / list_files +2;不可改)
    - .planning/phases/15-think-augmented-tool-selection/15-CONTEXT.md §D-11 (B2 fix — ab_comparison.json schema canonical source — Wave 3 fixture 必须严格符合此 schema)
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"evolve_tool_reasoning ab_comparison 字段" (B2 fix — 字段运行时 shape 确认)
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Excerpt L — _contains_secret 复用模式"
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"Implementation Recipes Wave 3" + §"Failure Modes"
    - .planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md §D-08 §D-13 §D-15
  </read_first>
  <action>
**步骤 1**: 创建 `tests/fixtures/dashboard_runs/reasoning_with_secret/metrics.json`(reasoning source + 完整 per_tool 数据,触发 ABStudy 渲染):

写入 JSON 内容:
- timestamp: "20260513_120000"
- think_ab_gate: {"passed": true}
- param_predictors_discovered: 8
- think_off_score: 0.55
- think_on_score: 0.62
- per_tool_baseline_rates: {"search_files": 0.55, "browser_navigate": 0.50}
- per_tool_evolved_rates: {"search_files": 0.62, "browser_navigate": 0.45}
- raw_predictions: 4 条记录(search_files easy/medium/hard,browser_navigate hard,task_id 0-3)

**步骤 2**: 创建 `tests/fixtures/dashboard_runs/reasoning_with_secret/ab_comparison.json`(含 secret-like 字符串于 task_description **以及 reasoning_text_on**,用于 redact 验证 — W3 fix:secret 必须分布于两字段):

JSON 列表 3 条记录:

- **记录 0(saved 类:off 错 / on 对)**: 
  - `task_description` 含 sk-style API key:`"use API key sk-1234567890abcdefghijklmnopqrstuvwxyz0123456789ABCD to fetch"`(SECRET_PATTERNS 命中)
  - `reasoning_text_on` 含 AWS-style access key(W3 fix):`"Need to use search_files. Internally I noticed key AKIAIOSFODNN7EXAMPLE was set, indicating the user wants AWS S3 listing. So search_files matches."`(20-char `AKIA[A-Z0-9]{16}` AWS access key 模式 + 高熵 → SECRET_PATTERNS 与 entropy heuristic 双命中)
  - 其他字段:correct_tool="search_files", selected_off="list_files", selected_on="search_files", is_correct_off=false, is_correct_on=true, is_ambiguous=false, confuser_tools=["list_files"], reasoning_tokens_on=12, latency_seconds_off=0.5, latency_seconds_on=1.8

- **记录 1(regressed 类:off 对 / on 错)**:
  - `task_description="open https://example.com"`(无 secret)
  - `reasoning_text_on` 含 AWS key(W3 fix):`"search_files seems related to URLs. Backup credentials AKIAIOSFODNN7EXAMPLE may be relevant for the navigation."`
  - 其他字段:correct_tool="browser_navigate", selected_off="browser_navigate", selected_on="search_files", is_correct_off=true, is_correct_on=false, is_ambiguous=false, confuser_tools=["search_files"], reasoning_tokens_on=8, latency_seconds_off=0.4, latency_seconds_on=1.5

- **记录 2(both_wrong 类:都错)**:
  - `task_description="regular harmless task description for both wrong category test"`
  - `reasoning_text_on="ambiguous task — neither tool clearly fits"`
  - 其他字段:correct_tool="edit_file", selected_off="list_files", selected_on="list_files", is_correct_off=false, is_correct_on=false, is_ambiguous=true, confuser_tools=["list_files"], reasoning_tokens_on=5, latency_seconds_off=0.3, latency_seconds_on=1.2

W3 fix 解释:`_safe_truncate` 在 `_count_ab_categories._top` 内对 `task_description` 与 `reasoning_text_on` **两字段都调用**。如果 fixture 只在 task_description 加 secret,test 无法证明 reasoning_text_on 路径也走了 redact。把 AWS 风格 access key 注入记录 0/1 的 `reasoning_text_on`,使两字段都能被 test_ab_study_secret_redaction 断言覆盖。

**字段 schema 严格要求(B2 / 15-CONTEXT §D-11)**:每条记录必须含 13 个字段:`task_id`, `task_description`, `correct_tool`, `selected_off`, `selected_on`, `is_correct_off`, `is_correct_on`, `is_ambiguous`, `confuser_tools`, `reasoning_text_on`, `reasoning_tokens_on`, `latency_seconds_off`, `latency_seconds_on`。type 必须严格符合 15-CONTEXT.md §D-11 声明(int/str/bool/list[str]/float)。

**步骤 3**: 创建 `tests/fixtures/dashboard_runs/json_corrupt/metrics.json`(故意非法 JSON):

直接写入字面字符串 `{ this is not valid json at all, bro }`(不要 json.dumps 序列化)。这会让 `_load_run` 进入 json.JSONDecodeError 分支,返回带 `_drop_reason="json parse error: ..."` 的 dict。

**步骤 4 (B3 fix — 新增)**: 创建 `tests/fixtures/dashboard_runs/params_multi_regress/metrics.json`,为 test_warning_threshold_no_exit Case B 提供 ≥3 条负向 delta 的 params source run。这是 B3 BLOCKER 关闭点 — Wave 1 的 `params_complete` fixture 只有 1 个负向 delta(browser_navigate -5pp),无法在严苛阈值下产生多条 WARNING:。新建独立 fixture 让 Case B patch `_scan_runs` 指向它,避免破坏 `params_complete` 在 Wave 1 `test_status_color_coding` 等测试上的不变量。

`tests/fixtures/dashboard_runs/params_multi_regress/metrics.json`(用 `json.dumps(..., indent=2)` 风格写入):

```json
{
  "timestamp": "20260512_141500",
  "started_at": "2026-05-12T06:15:00.000000",
  "iterations": 50,
  "eval_model": "openai/gpt-4.1-mini",
  "param_predictors_discovered": 5,
  "tool_count": 5,
  "train_examples": 40,
  "val_examples": 20,
  "holdout_examples": 21,
  "elapsed_seconds": 70.3,
  "constraints_passed": true,
  "session_source": "fixture",
  "baseline_score": 0.78,
  "evolved_score": 0.74,
  "improvement": -0.04,
  "per_tool_baseline_rates": {
    "search_files": 0.80,
    "read_file": 0.85,
    "browser_navigate": 0.50,
    "edit_file": 0.92,
    "list_files": 0.90
  },
  "per_tool_evolved_rates": {
    "search_files": 0.84,
    "read_file": 0.845,
    "browser_navigate": 0.45,
    "edit_file": 0.905,
    "list_files": 0.92
  },
  "raw_predictions": [
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "easy", "num_available_tools": 4},
    {"correct_tool": "search_files", "selected_tool": "search_files", "difficulty": "medium", "num_available_tools": 5},
    {"correct_tool": "read_file", "selected_tool": "read_file", "difficulty": "easy", "num_available_tools": 4},
    {"correct_tool": "read_file", "selected_tool": "open_file", "difficulty": "hard", "num_available_tools": 6},
    {"correct_tool": "browser_navigate", "selected_tool": "browser_close", "difficulty": "medium", "num_available_tools": 5},
    {"correct_tool": "browser_navigate", "selected_tool": "browser_navigate", "difficulty": "easy", "num_available_tools": 4},
    {"correct_tool": "edit_file", "selected_tool": "create_file", "difficulty": "hard", "num_available_tools": 7},
    {"correct_tool": "edit_file", "selected_tool": "edit_file", "difficulty": "easy", "num_available_tools": 4},
    {"correct_tool": "list_files", "selected_tool": "list_files", "difficulty": "easy", "num_available_tools": 4}
  ]
}
```

**Per-tool delta 验证(确保 Case B 至少 3 条 WARNING: 触发,Case A 仍只在 params_complete 上 1 条 WARNING:)**:

| tool             | baseline | evolved | delta_pp | Case A (≤ -2pp on params_complete) | Case B (≤ -0.1pp on params_multi_regress) |
|------------------|----------|---------|----------|------------------------------------|-------------------------------------------|
| search_files     | 0.80     | 0.84    | +4.0pp   | n/a (Case A 用 params_complete)    | gain — 不触发                             |
| read_file        | 0.85     | 0.845   | -0.5pp   | n/a                                | -0.5 ≤ -0.1 ✓ → **WARNING:**              |
| browser_navigate | 0.50     | 0.45    | -5.0pp   | n/a                                | -5.0 ≤ -0.1 ✓ → **WARNING: + FAIL**       |
| edit_file        | 0.92     | 0.905   | -1.5pp   | n/a                                | -1.5 ≤ -0.1 ✓ → **WARNING:**              |
| list_files       | 0.90     | 0.92    | +2.0pp   | n/a                                | gain — 不触发                             |

Case B 在 params_multi_regress 上命中 **3 条 WARNING:**(read_file / browser_navigate / edit_file)。Case A 在 params_complete 上只命中 browser_navigate (-5pp ≤ -2pp) **1 条 WARNING:**。**两个 case 都触发但条数差异显著,exit code 都 = 0**(D-13)。

注意保持 `params_complete` fixture 不变,保留 Wave 1 `test_status_color_coding`(browser_navigate 是唯一 FAIL)和 `test_distribution_n_a_when_sample_low`(browser_navigate 仅 2 raw_predictions)的不变量。

**步骤 5**: 修改 `evolution/tools/regression_dashboard.py`:

(a) **顶部 import 检查**:确认 `from evolution.core.external_importers import _contains_secret` 在 import 块。Wave 1 的 task 16-01-1 已加,本 wave 仅复用。如未加,加上。

(b) **新增 `_safe_truncate` 函数**(在 `_render_frequency_bars` 之后,Click 装饰器之前):
```python
def _safe_truncate(text: str, max_len: int = 80) -> str:
    """D-15: redact-then-truncate ABStudy strings.

    Defensive: hermes-agent session importers already filter secrets upstream,
    but ab_comparison.json is downstream-rendered to stdout. Re-check before
    display to avoid second-order leak.
    """
    if not text:
        return ""
    if _contains_secret(text):
        return "[REDACTED — secret-like content]"
    return text[:max_len] + "..." if len(text) > max_len else text
```

(c) **新增 `_count_ab_categories` 函数**(D-15 三类计数 + top-3 排序):
```python
def _count_ab_categories(ab_records: list[dict]) -> dict:
    """D-15 三类计数 + top-3 examples per category."""
    saved = [r for r in ab_records if not r.get("is_correct_off") and r.get("is_correct_on")]
    regressed = [r for r in ab_records if r.get("is_correct_off") and not r.get("is_correct_on")]
    both_wrong = [r for r in ab_records if not r.get("is_correct_off") and not r.get("is_correct_on")]
    saved_sorted = sorted(saved, key=lambda r: len(r.get("reasoning_text_on", "") or ""), reverse=True)
    regressed_sorted = sorted(regressed, key=lambda r: len(r.get("task_description", "") or ""), reverse=True)
    def _top(recs: list[dict], k: int = 3) -> list[dict]:
        out = []
        for r in recs[:k]:
            out.append({
                "task_id": r.get("task_id"),
                "task_description": _safe_truncate(r.get("task_description", "") or ""),
                "correct_tool": r.get("correct_tool"),
                "selected_off": r.get("selected_off"),
                "selected_on": r.get("selected_on"),
                "reasoning_text_on": _safe_truncate(r.get("reasoning_text_on", "") or "", max_len=200),
            })
        return out
    return {
        "think_on_saved": {"count": len(saved), "top": _top(saved_sorted)},
        "think_on_regressed": {"count": len(regressed), "top": _top(regressed_sorted)},
        "both_wrong": {"count": len(both_wrong), "top": _top(both_wrong)},
    }
```

(d) **新增 `_render_ab_study` 函数**(在 `_render_trend` 之后):
```python
def _render_ab_study(reasoning_runs: list[dict]) -> dict:
    """D-15: ABStudy region — only renders for reasoning runs with ab_comparison.json present.

    Returns {detected_runs, by_run: [{run_path, think_on_saved, think_on_regressed, both_wrong, top_examples}]}
    """
    if not reasoning_runs:
        return {"detected_runs": 0, "by_run": []}
    by_run: list[dict] = []
    for run in reasoning_runs:
        ab = run.get("ab_comparison") or []
        cats = _count_ab_categories(ab)
        by_run.append({
            "run_path": run["path"],
            "think_on_saved": cats["think_on_saved"]["count"],
            "think_on_regressed": cats["think_on_regressed"]["count"],
            "both_wrong": cats["both_wrong"]["count"],
            "top_examples": {
                "saved": cats["think_on_saved"]["top"],
                "regressed": cats["think_on_regressed"]["top"],
                "both_wrong": cats["both_wrong"]["top"],
            },
        })
    table = Table(title="ABStudy (Phase 15 reasoning runs)", show_lines=False)
    table.add_column("run_path", justify="left", style="cyan")
    table.add_column("think_on_saved", justify="right", style="bold green")
    table.add_column("think_on_regressed", justify="right", style="bold red")
    table.add_column("both_wrong", justify="right", style="yellow")
    for entry in by_run:
        table.add_row(entry["run_path"], str(entry["think_on_saved"]),
                      str(entry["think_on_regressed"]), str(entry["both_wrong"]))
    console.print(table)
    for entry in by_run:
        top = entry["top_examples"]
        lines: list[str] = [f"[bold]Run:[/bold] {entry['run_path']}"]
        for cat_name, recs in [("saved", top["saved"]), ("regressed", top["regressed"]), ("both_wrong", top["both_wrong"])]:
            if recs:
                lines.append(f"\n[bold]{cat_name}[/bold]")
                for r in recs:
                    lines.append(
                        f"  task#{r['task_id']}: {r['task_description']}"
                        f" | correct={r['correct_tool']} | off={r['selected_off']} on={r['selected_on']}"
                    )
                    if r.get("reasoning_text_on"):
                        lines.append(f"    reasoning: {r['reasoning_text_on']}")
        console.print(Panel("\n".join(lines), title="ABStudy top-3"))
    return {"detected_runs": len(reasoning_runs), "by_run": by_run}
```

(e) **修改 `main` 编排**:在 Wave 2 的 TREND 渲染之后,新增 ABStudy + warnings + dropped_runs 收尾段落:

```python
    # ── ABStudy region (D-15) ────────────────────────────────────────────
    reasoning_with_ab = [r for r in valid_runs if r["source"] == "reasoning" and r.get("ab_comparison")]
    ab_study_data = _render_ab_study(reasoning_with_ab) if reasoning_with_ab else {"detected_runs": 0, "by_run": []}

    # ── D-13 warnings + D-08 fallback messages ───────────────────────────
    warnings_list: list[dict] = []
    if latest_run is not None:
        b_rates = latest_run["metrics"].get("per_tool_baseline_rates", {}) or {}
        e_rates = latest_run["metrics"].get("per_tool_evolved_rates", {}) or {}
        for tool in sorted(set(b_rates) | set(e_rates)):
            delta = (float(e_rates.get(tool, 0.0)) - float(b_rates.get(tool, 0.0))) * 100.0
            if delta <= -warning_threshold_pp:
                warnings_list.append({"tool": tool, "delta_pp": delta, "run_path": latest_run["path"]})
                console.print(
                    f"[yellow]WARNING: {tool} regressed by {delta:+.2f}pp in {latest_run['path']}"
                    f" (threshold: -{warning_threshold_pp:.1f}pp)[/yellow]"
                )
        if "raw_predictions" not in latest_run["metrics"]:
            console.print(
                f"[yellow]raw_predictions absent in {latest_run['path']}; "
                f"distribution disabled for this run[/yellow]"
            )

    if dropped:
        console.print(f"\n[yellow]Dropped {len(dropped)} run(s):[/yellow]")
        for d in dropped:
            console.print(f"[yellow]  - {d['path']}: {d['reason']}[/yellow]")

    # D-13: warnings 不影响 exit code; Wave 4 收口写 dashboard.json
    return 0
```

注意:Wave 1 的 main 已有 `dropped: list[dict]` 累计逻辑(在 LATEST run 选择循环里),Wave 2 引入 `valid_runs` 与 trend 编排。本 wave 在 Wave 2 编排末尾追加 ABStudy/warnings/dropped 打印段落,**不重新实现** dropped 收集。

(f) **确保 `_load_run` 处理 JSON 解析失败**:Wave 1 task 16-01-1 已实现 try/except + `_drop_reason` 字段;若实现遗漏,补:
```python
def _load_run(metrics_path: Path) -> Optional[dict]:
    try:
        metrics = json.loads(metrics_path.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError) as e:
        return {"path": str(metrics_path.parent), "metrics": None, "source": None,
                "_drop_reason": f"json parse error: {e}"}
    # ... rest as Wave 1
```
  </action>
  <verify>
    <automated>python -c "from evolution.tools.regression_dashboard import _safe_truncate, _count_ab_categories, _render_ab_study; cats = _count_ab_categories([{'is_correct_off': False, 'is_correct_on': True, 'task_description': 't', 'reasoning_text_on': 'r', 'task_id': 0, 'correct_tool': 'a', 'selected_off': 'b', 'selected_on': 'a'}]); assert cats['think_on_saved']['count'] == 1; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def _safe_truncate' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'def _count_ab_categories' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'def _render_ab_study' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'from evolution.core.external_importers import _contains_secret' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'REDACTED — secret-like' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'WARNING:' evolution/tools/regression_dashboard.py` 输出至少 1 行
    - `grep -n 'distribution disabled for this run' evolution/tools/regression_dashboard.py` 输出 1 行
    - `test -f tests/fixtures/dashboard_runs/reasoning_with_secret/metrics.json` 退出码 0
    - `test -f tests/fixtures/dashboard_runs/reasoning_with_secret/ab_comparison.json` 退出码 0
    - `test -f tests/fixtures/dashboard_runs/json_corrupt/metrics.json` 退出码 0
    - **B3 强制 — params_multi_regress fixture 存在**: `test -f tests/fixtures/dashboard_runs/params_multi_regress/metrics.json` 退出码 0
    - **B3 强制 — params_multi_regress 是合法 JSON**: `python -c "import json; json.load(open('tests/fixtures/dashboard_runs/params_multi_regress/metrics.json'))"` 退出码 0
    - **B3 强制 — params_multi_regress 有 ≥3 个负向 delta**: `python -c "import json; m=json.load(open('tests/fixtures/dashboard_runs/params_multi_regress/metrics.json')); negs=sum(1 for t in m['per_tool_baseline_rates'] if m['per_tool_evolved_rates'][t] - m['per_tool_baseline_rates'][t] < -0.001); assert negs >= 3, f'expected >=3 negative deltas, got {negs}'"` 退出码 0
    - **B3 强制 — params_multi_regress 是 params source**: `python -c "import json; m=json.load(open('tests/fixtures/dashboard_runs/params_multi_regress/metrics.json')); assert 'param_predictors_discovered' in m and 'think_ab_gate' not in m, 'must be params source not reasoning'"` 退出码 0
    - `python -c "import json; json.loads(open('tests/fixtures/dashboard_runs/reasoning_with_secret/metrics.json').read())"` 退出码 0(reasoning fixture 是有效 JSON)
    - `python -c "import json; json.loads(open('tests/fixtures/dashboard_runs/json_corrupt/metrics.json').read())" 2>/dev/null; test $? -ne 0` 退出码 0(corrupt fixture 故意非法,parse 失败)
    - `python -c "from evolution.tools.regression_dashboard import _safe_truncate; assert 'REDACTED' in _safe_truncate('sk-1234567890abcdefghijklmnopqrstuvwxyzABCDEFGHIJ'); assert _safe_truncate('hello') == 'hello'; assert _safe_truncate('x' * 100, max_len=10).startswith('xxxxxxxxxx...')"` 退出码 0
    - **W3 强制 — fixture 双字段 secret**: `grep -c 'AKIAIOSFODNN7EXAMPLE' tests/fixtures/dashboard_runs/reasoning_with_secret/ab_comparison.json` 输出至少 `2`(记录 0 和记录 1 的 reasoning_text_on 都有 AWS access key)
    - **W3 强制 — fixture 含 sk- 模式**: `grep -c 'sk-1234567890abcdefghijklmnop' tests/fixtures/dashboard_runs/reasoning_with_secret/ab_comparison.json` 输出至少 `1`(task_description 中 sk- API key)
  </acceptance_criteria>
  <done>
    ABStudy / source legend / fallback / warning 编排实现完成,4 个新 fixture 就位(reasoning_with_secret/{metrics.json, ab_comparison.json} + json_corrupt/metrics.json + params_multi_regress/metrics.json — B3 fix)。fixture 中 secret 分布于 task_description (sk-) 与 reasoning_text_on (AKIA...) 两字段(W3 fix),严格符合 15-CONTEXT §D-11 schema(B2 fix)。params_multi_regress 含 ≥3 条负向 delta(B3 fix),为 Case B 严苛阈值多 WARNING: 守门提供数据基础。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-03-2: unskip + 实现 7 个 Wave 3 测试(ab_categories / secret_redact / source_detection / fallback_dropped / fallback_no_raw / fallback_no_ab / warning_no_exit)</name>
  <files>tests/tools/test_regression_dashboard.py</files>
  <read_first>
    - evolution/tools/regression_dashboard.py (task 16-03-1 完成后版本)
    - tests/tools/test_regression_dashboard.py (Wave 2 完成版本,7 个 Wave 3 测试当前为 skip stub)
    - tests/fixtures/dashboard_runs/ 全部 10 个文件(reasoning_with_secret + json_corrupt + params_multi_regress 已新增)
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Test Pattern Map" (B2 fix — 现已包含 PATTERNS §)
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"Implementation Recipes Wave 3" (B2 fix — 现已包含 RESEARCH §)
    - .planning/phases/16-per-tool-regression-dashboard/16-VALIDATION.md §16-03-01 ~ 16-03-07
    - .planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md §D-07 §D-08 §D-13 §D-15
  </read_first>
  <behavior>
    - test_ab_study_categories: 用 reasoning_complete fixture(3 ab records) → invoke main → output 含 "ABStudy" + "think_on_saved" 列 + "think_on_regressed" + "both_wrong"
    - test_ab_study_secret_redaction (W3 fix — 双字段覆盖): 用 reasoning_with_secret fixture → output 含 "REDACTED" + 不含 "sk-1234567890abcdefghijklmnopqrstuvwxyz"(task_description 中的 sk- key) + **不含 "AKIAIOSFODNN7EXAMPLE"**(reasoning_text_on 中的 AWS access key)
    - test_source_detection: 直接调 _detect_source 单元测试 4 种 source case
    - test_fallback_dropped_run: 用 desc_old fixture(缺 per_tool_*_rates)→ output 含 "Dropped" 或 "missing per_tool" 字符串
    - test_fallback_no_raw_predictions: 用 params_no_raw fixture → output 含 "distribution disabled" + 仍渲染 LATEST + 含 "n/a"
    - test_fallback_no_ab_comparison: 用 reasoning_old fixture(缺 per_tool 与 ab) → output 不含 "ABStudy" 段
    - test_warning_threshold_no_exit (B3 fix — 双 fixture 双 case 守门): 
        - Case A:`params_complete` + `--warning-threshold-pp 2.0` → exit_code=0 + WARNING: 出现 1 次 + browser_navigate 出现
        - Case B:`params_multi_regress` + `--warning-threshold-pp 0.1` → exit_code 仍为 0 + WARNING: 出现次数 ≥ 3 + read_file/browser_navigate/edit_file 三 tool 全部出现
        - 双 case 守门防止「智能」逻辑如 `if len(warnings_list) > N: return 1` 在严苛阈值多 warning case 触发
  </behavior>
  <action>
修改 `tests/tools/test_regression_dashboard.py`,删除 7 个 Wave 3 stub 的 `@pytest.mark.skip` 装饰器并替换为真实实现。每个 stub 当前形如:

```python
@pytest.mark.skip(reason="Wave 3 — ABStudy categories")
def test_ab_study_categories():
    pass
```

替换为下列实现(7 个一并替换,保持原顺序;**函数名不可改,Wave 1 已锁定**):

**实现 1 — test_ab_study_categories**:
```python
def test_ab_study_categories():
    """16-03-01 (D-15) — ABStudy three categories with counts and top-3 examples."""
    runner = CliRunner()
    fixture_path = FIXTURES / "reasoning_complete" / "metrics.json"
    with patch("evolution.tools.regression_dashboard._scan_runs", return_value=[fixture_path]):
        result = runner.invoke(main, ["--runs", str(fixture_path.parent)], catch_exceptions=False)
    assert result.exit_code == 0, f"stdout: {result.output}"
    assert "ABStudy" in result.output
    assert "think_on_saved" in result.output
    assert "think_on_regressed" in result.output
    assert "both_wrong" in result.output
```

**实现 2 — test_ab_study_secret_redaction (W3 fix — 双字段覆盖)**:
```python
def test_ab_study_secret_redaction():
    """16-03-02 (D-15 / W3 fix) — secrets in BOTH task_description AND reasoning_text_on
    must be redacted before stdout render. Fixture has:
    - sk-style API key in task_description (record 0)
    - AKIAIOSFODNN7EXAMPLE AWS access key in reasoning_text_on (records 0 and 1)
    Both must be absent from stdout; "REDACTED" must appear (proving _safe_truncate fired
    on both fields)."""
    runner = CliRunner()
    fixture_dir = FIXTURES / "reasoning_with_secret"
    with patch("evolution.tools.regression_dashboard._scan_runs", return_value=[fixture_dir / "metrics.json"]):
        result = runner.invoke(main, ["--runs", str(fixture_dir)], catch_exceptions=False)
    assert result.exit_code == 0, f"stdout: {result.output}"
    # task_description sk- key (record 0)
    assert "sk-1234567890abcdefghijklmnopqrstuvwxyz" not in result.output, \
        f"task_description sk- key leaked: {result.output[:1000]}"
    # reasoning_text_on AWS key (records 0 and 1) — W3 fix
    assert "AKIA" not in result.output, \
        f"AWS access key prefix leaked from reasoning_text_on: {result.output[:1000]}"
    assert "AKIAIOSFODNN7EXAMPLE" not in result.output, \
        f"AWS access key full token leaked from reasoning_text_on: {result.output[:1000]}"
    # Positive: REDACTED marker must appear (otherwise the secret simply got truncated, not redacted)
    assert "REDACTED" in result.output
```

**实现 3 — test_source_detection**:
```python
def test_source_detection():
    """16-03-03 (D-07) — source heuristic: think_ab_gate→reasoning, param_predictors→params,
    parent dir + baseline_score → desc, otherwise None."""
    from pathlib import Path as P
    from evolution.tools.regression_dashboard import _detect_source
    assert _detect_source({"think_ab_gate": {}}, P("output/tools_reasoning/abc/metrics.json")) == "reasoning"
    assert _detect_source({"param_predictors_discovered": 5}, P("output/tools/abc/metrics.json")) == "params"
    # think_ab_gate AND param_predictors_discovered → reasoning wins (order matters)
    assert _detect_source({"think_ab_gate": {}, "param_predictors_discovered": 5}, P("output/tools_reasoning/abc/metrics.json")) == "reasoning"
    # No positive marker but parent==tools_reasoning → reasoning (defensive)
    assert _detect_source({"some_field": 1}, P("output/tools_reasoning/abc/metrics.json")) == "reasoning"
    # No positive marker, parent==tools, has baseline_score → desc
    assert _detect_source({"baseline_score": 0.5}, P("output/tools/abc/metrics.json")) == "desc"
    # No positive marker, parent==tools, no baseline_score → None
    assert _detect_source({}, P("output/tools/abc/metrics.json")) is None
    assert _detect_source({}, P("/tmp/abc/metrics.json")) is None
```

**实现 4 — test_fallback_dropped_run**:
```python
def test_fallback_dropped_run():
    """16-03-04 (D-08) — run missing per_tool_*_rates → dropped + dropped_runs[]."""
    runner = CliRunner()
    fixture_path = FIXTURES / "desc_old" / "metrics.json"
    with patch("evolution.tools.regression_dashboard._scan_runs", return_value=[fixture_path]):
        result = runner.invoke(main, ["--runs", str(fixture_path.parent)], catch_exceptions=False)
    assert result.exit_code == 0, f"stdout: {result.output}"
    output_lower = result.output.lower()
    # Drop signal: either "Dropped" header, or "missing per_tool", or status=FAILED
    assert ("dropped" in output_lower or "missing per_tool" in result.output or "failed" in output_lower)
```

**实现 5 — test_fallback_no_raw_predictions**:
```python
def test_fallback_no_raw_predictions():
    """16-03-05 (D-08) — run has per_tool_*_rates but no raw_predictions →
    LATEST renders, distribution columns degrade to n/a, stdout warns."""
    runner = CliRunner()
    fixture_path = FIXTURES / "params_no_raw" / "metrics.json"
    with patch("evolution.tools.regression_dashboard._scan_runs", return_value=[fixture_path]):
        result = runner.invoke(main, ["--runs", str(fixture_path.parent)], catch_exceptions=False)
    assert result.exit_code == 0, f"stdout: {result.output}"
    assert "LATEST" in result.output
    assert "distribution disabled" in result.output
    assert "n/a" in result.output
```

**实现 6 — test_fallback_no_ab_comparison**:
```python
def test_fallback_no_ab_comparison():
    """16-03-06 (D-08) — reasoning run missing both per_tool_*_rates AND ab_comparison.json →
    run dropped before reaching ABStudy filter; stdout does not complain about missing ab_comparison."""
    runner = CliRunner()
    fixture_path = FIXTURES / "reasoning_old" / "metrics.json"
    with patch("evolution.tools.regression_dashboard._scan_runs", return_value=[fixture_path]):
        result = runner.invoke(main, ["--runs", str(fixture_path.parent)], catch_exceptions=False)
    assert result.exit_code == 0, f"stdout: {result.output}"
    assert "ABStudy" not in result.output
    assert "missing ab_comparison" not in result.output.lower()
```

**实现 7 — test_warning_threshold_no_exit (BLOCKER 3 fix — 双 fixture 双 case 守门)**:

Case A 用 `params_complete` fixture(只有 browser_navigate -5pp 一个负向 delta,Wave 1 已固化的不变量,2.0pp 阈值下 1 条 WARNING:)。Case B 用新建的 `params_multi_regress` fixture(read_file -0.5pp / browser_navigate -5pp / edit_file -1.5pp 三个负向 delta,0.1pp 阈值下 ≥3 条 WARNING:)。两个 case 必须 exit_code=0,从而封死「智能」exit gating 路径。

```python
def test_warning_threshold_no_exit(monkeypatch, tmp_path):
    """16-03-07 (D-13 / B3 fix) — warnings NEVER affect exit code regardless of threshold severity.

    Two invocations on TWO distinct fixtures:
      Case A: params_complete + --warning-threshold-pp 2.0
              → only browser_navigate (-5pp) crosses → exactly 1 WARNING:; exit 0.
      Case B: params_multi_regress + --warning-threshold-pp 0.1
              → read_file (-0.5pp) + browser_navigate (-5pp) + edit_file (-1.5pp) cross
              → at least 3 WARNING: lines; exit 0 STILL.

    If the executor accidentally writes "smart" gating like
    `if len(warnings_list) > N: return 1`, Case A passes (1 warning < N) but Case B
    fails (3+ warnings ≥ N). Two-fixture coverage catches it.

    NOTE: We intentionally use a SEPARATE fixture for Case B (params_multi_regress) rather
    than reusing params_complete with a stricter threshold. params_complete only has 1 negative
    delta (browser_navigate); on a strict 0.1pp threshold it would still produce only 1
    WARNING: line, defeating the multi-warning guard. params_multi_regress fixture has 3
    negative-delta tools by construction, ensuring the strict-threshold case actually
    exercises the multi-warning code path.
    """
    runner = CliRunner()

    # ── Case A: default-ish 2.0pp threshold on params_complete ──
    # params_complete: browser_navigate baseline=0.60, evolved=0.55 → delta=-5pp (only negative).
    fixture_complete = FIXTURES / "params_complete"
    monkeypatch.chdir(tmp_path)
    with patch(
        "evolution.tools.regression_dashboard._scan_runs",
        return_value=[fixture_complete / "metrics.json"],
    ):
        result_a = runner.invoke(
            main,
            ["--runs", str(fixture_complete), "--warning-threshold-pp", "2.0"],
            catch_exceptions=False,
        )
    assert result_a.exit_code == 0, \
        f"Case A: expected exit 0 (D-13 no-gate), got {result_a.exit_code}; stdout: {result_a.output}"
    assert "WARNING:" in result_a.output, \
        f"Case A: expected 'WARNING:' in stdout for browser_navigate -5pp; got: {result_a.output[:1000]}"
    assert result_a.output.count("WARNING:") == 1, \
        f"Case A: expected exactly 1 WARNING: line on params_complete (only browser_navigate -5pp); " \
        f"got {result_a.output.count('WARNING:')}. stdout: {result_a.output[:1500]}"
    assert "browser_navigate" in result_a.output

    # ── Case B: strict 0.1pp threshold on params_multi_regress (B3 fix — many warnings, exit STILL 0) ──
    # params_multi_regress: 3 negative-delta tools by construction
    #   read_file:        0.85   → 0.845 = -0.5pp  (cross at 0.1pp threshold)
    #   browser_navigate: 0.50   → 0.45  = -5.0pp  (cross at 0.1pp threshold; also FAIL)
    #   edit_file:        0.92   → 0.905 = -1.5pp  (cross at 0.1pp threshold)
    fixture_multi = FIXTURES / "params_multi_regress"
    with patch(
        "evolution.tools.regression_dashboard._scan_runs",
        return_value=[fixture_multi / "metrics.json"],
    ):
        result_b = runner.invoke(
            main,
            ["--runs", str(fixture_multi), "--warning-threshold-pp", "0.1"],
            catch_exceptions=False,
        )
    assert result_b.exit_code == 0, \
        f"Case B (strict threshold): expected exit 0, got {result_b.exit_code}; " \
        f"this would catch any 'if len(warnings_list) > N: return 1' bug. stdout: {result_b.output}"
    warning_count_b = result_b.output.count("WARNING:")
    assert warning_count_b >= 3, \
        f"Case B: expected at least 3 WARNING: lines at strict 0.1 threshold on params_multi_regress " \
        f"(read_file/browser_navigate/edit_file all cross), got {warning_count_b}. " \
        f"stdout: {result_b.output[:1500]}"
    # All 3 negative-delta tools must appear in WARNING: output
    assert "read_file" in result_b.output, \
        f"Case B: read_file (-0.5pp) WARNING: missing. stdout: {result_b.output[:1500]}"
    assert "browser_navigate" in result_b.output, \
        f"Case B: browser_navigate (-5.0pp) WARNING: missing. stdout: {result_b.output[:1500]}"
    assert "edit_file" in result_b.output, \
        f"Case B: edit_file (-1.5pp) WARNING: missing. stdout: {result_b.output[:1500]}"
```

不动 Wave 1+2 已实现的 7 个 active 测试;不动 Wave 4 的 3 个 skip stub。运行后期望:Wave 1+2+3 = 14 active passed + 3 skipped(Wave 4 stubs)。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_regression_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@pytest.mark.skip' tests/tools/test_regression_dashboard.py` 输出 3(Wave 1+2+3 unskip 后只剩 Wave 4 的 3 个)
    - `grep -n 'def test_ab_study_categories' tests/tools/test_regression_dashboard.py` 输出 1 行,前一行不是 `@pytest.mark.skip`
    - `grep -n 'def test_ab_study_secret_redaction' tests/tools/test_regression_dashboard.py` 输出 1 行,前一行不是 `@pytest.mark.skip`
    - `grep -n 'def test_source_detection' tests/tools/test_regression_dashboard.py` 输出 1 行,前一行不是 `@pytest.mark.skip`
    - `grep -n 'def test_fallback_dropped_run' tests/tools/test_regression_dashboard.py` 输出 1 行,前一行不是 `@pytest.mark.skip`
    - `grep -n 'def test_fallback_no_raw_predictions' tests/tools/test_regression_dashboard.py` 输出 1 行,前一行不是 `@pytest.mark.skip`
    - `grep -n 'def test_fallback_no_ab_comparison' tests/tools/test_regression_dashboard.py` 输出 1 行,前一行不是 `@pytest.mark.skip`
    - `grep -n 'def test_warning_threshold_no_exit' tests/tools/test_regression_dashboard.py` 输出 1 行,前一行不是 `@pytest.mark.skip`
    - **W3 强制 — secret_redaction 测试断言 AKIA**: `grep -nE 'AKIA|AKIAIOSFODNN7EXAMPLE' tests/tools/test_regression_dashboard.py` 输出至少 2 行(test_ab_study_secret_redaction 内必须有 AKIA 与 AKIAIOSFODNN7EXAMPLE 双断言)
    - **B3 强制 — warning_threshold 双 case fixture**: `awk '/^def test_warning_threshold_no_exit/,/^def [a-z]/' tests/tools/test_regression_dashboard.py | grep -c 'params_multi_regress'` ≥ 1(Case B 引用新 fixture)
    - **B3 强制 — warning_threshold 双 case exit 0 断言**: `awk '/^def test_warning_threshold_no_exit/,/^def [a-z]/' tests/tools/test_regression_dashboard.py | grep -c 'exit_code == 0'` ≥ 2(Case A + Case B 各一条 exit 0 断言)
    - **B3 强制 — warning_threshold Case B 多条 WARNING: 断言**: `awk '/^def test_warning_threshold_no_exit/,/^def [a-z]/' tests/tools/test_regression_dashboard.py | grep -c '>= 3'` ≥ 1(Case B `warning_count_b >= 3` 断言)
    - **B3 强制 — warning_threshold 双 invoke 调用**: `awk '/^def test_warning_threshold_no_exit/,/^def [a-z]/' tests/tools/test_regression_dashboard.py | grep -c -E 'result_a|result_b'` ≥ 4(双 case 至少 result_a + result_b 各 2 次引用)
    - **B3 强制 — warning_threshold 双 fixture 双阈值 flag**: `awk '/^def test_warning_threshold_no_exit/,/^def [a-z]/' tests/tools/test_regression_dashboard.py | grep -cE '"--warning-threshold-pp", "0\.1"|"--warning-threshold-pp", "2\.0"'` ≥ 2(0.1 与 2.0 两个阈值都在测试体内)
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py -x` 退出码 0(14 passed + 3 skipped)
    - 手动 smoke: `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/reasoning_with_secret` 退出码 0,stdout 含 "ABStudy" + "REDACTED" 字符串,**不**含 "AKIAIOSFODNN7EXAMPLE" 与 "sk-1234567890..."
    - **B3 强制 — params_multi_regress smoke**: `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/params_multi_regress --warning-threshold-pp 0.1` 退出码 0,stdout `WARNING:` 至少 3 次出现
  </acceptance_criteria>
  <done>
    Wave 3 7 个 functional 测试全绿;ABStudy 渲染 + secret redact(双字段覆盖,W3 fix)+ 三类 fallback + warning 阈值不影响 exit code(双 fixture 双 case 守门,B3 fix)全部由测试守门。Case B 用 params_multi_regress fixture 真正触发 ≥3 条 WARNING:,封死 `if len(warnings_list) > N: return 1` 类智能 gate bug。
  </done>
</task>

</tasks>

<verification>
**Wave 3 完成标志**:
- `.venv/bin/pytest tests/tools/test_regression_dashboard.py -x` 退出码 0(14 passed + 3 skipped — Wave 4 stubs)
- 手动 smoke: `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/reasoning_with_secret` 退出码 0,stdout 含 ABStudy 三类计数 + REDACTED 字符串,不含任何 sk-/AKIA 前缀的 secret
- 手动 smoke: `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/desc_old` 退出码 0,stdout 含 dropped 段落
- 手动 smoke: `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/params_multi_regress --warning-threshold-pp 0.1` 退出码 0,stdout WARNING: 至少 3 行(read_file / browser_navigate / edit_file)— B3 fix 守门
</verification>

<success_criteria>
- _safe_truncate / _count_ab_categories / _render_ab_study 三 helper 实现并被测试守护
- ABStudy 仅在 reasoning + ab_comparison 双在时渲染
- secret redact 防御:含 sk-* 模式的 task_description **以及** AKIA-style 模式的 reasoning_text_on 都被 redact 为 "[REDACTED — secret-like content]"(W3 fix)
- D-07 source 启发判定四 case 单元测试全覆盖
- D-08 三类 fallback 全部由 functional 测试覆盖
- D-13 warning 不影响 exit code(双 fixture 双 case 守门;严苛阈值多 WARNING: 不退化为 exit 1 — B3 fix)
- 新 fixture params_multi_regress 提供 ≥3 条负向 delta 的 params source run,使 Case B 严苛阈值真正能触发多条 WARNING:(B3 fix)
</success_criteria>

<output>
After completion, create `.planning/phases/16-per-tool-regression-dashboard/16-03-SUMMARY.md` with:
- 文件清单(1 source modified + 1 test modified + 4 fixture new — 含 B3 fix 引入的 params_multi_regress)
- ABStudy top-3 排序键确认(saved by reasoning_text length, regressed by task_description length, both_wrong unsorted)
- secret redact pattern 实测命中(`sk-` 前缀 + 长 base64 token 触发 SECRET_PATTERNS;AKIA 20-char access key 触发 entropy heuristic)
- D-13 实测确认(B3 fix):warning 触发 stdout yellow 但 result.exit_code 仍为 0
  - Case A: params_complete + 2.0pp → 1 WARNING:,exit 0
  - Case B: params_multi_regress + 0.1pp → ≥3 WARNING:(read_file/browser_navigate/edit_file),exit 0
- params_multi_regress fixture 数学验证表(每 tool baseline / evolved / delta_pp / 是否触发)
</output>
