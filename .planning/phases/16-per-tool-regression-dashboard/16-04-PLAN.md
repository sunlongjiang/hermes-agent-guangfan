---
plan: 16-04-schema-e2e
phase: 16
type: execute
wave: 4
depends_on: [16-03-abstudy-source-fallback]
files_modified:
  - path: evolution/tools/regression_dashboard.py
    role: modified
  - path: tests/tools/test_regression_dashboard.py
    role: modified
  - path: .gitignore
    role: modified
requirements: [TOOL-V2-04]
autonomous: true
estimated_minutes: 70

must_haves:
  truths:
    - "main 在结尾把 latest/diff/trend/ab_study/source_legend/dropped_runs/summary/warnings 8 个顶层字段写入 dashboard.json(D-03 stdout Rich Table + dashboard.json 双产物收口)"
    - "dashboard.json 默认落 CWD 文件名 dashboard_<YYYYMMDD_HHMMSS>.json(D-04)"
    - "--output <path> 显式指定时覆盖默认路径"
    - "默认根 output/tools/ + output/tools_reasoning/ 都为空且无 --runs → click.UsageError → exit 2(D-02)"
    - "E2E 测试用 5 fixture run 跑通 main → dashboard.json 含全部 8 顶层字段(D-17 全包括分 5 Wave 切分,Wave 4 是收口 wave)"
    - "dashboard_*.json 已加入 .gitignore(CONCERNS §H4 防御)"
  artifacts:
    - path: evolution/tools/regression_dashboard.py
      provides: "完整 dashboard.json 序列化 + --output flag 支持"
      contains: "json.dumps"
    - path: .gitignore
      provides: "dashboard_*.json 被 git 忽略"
      contains: "dashboard_*.json"
  key_links:
    - from: evolution/tools/regression_dashboard.py
      to: dashboard.json (filesystem)
      via: json.dumps + write_text
      pattern: "dashboard_.*\\.json"
---

<objective>
Wave 4 — dashboard.json schema 收口 + E2E 集成测试 + .gitignore 卫生。本 wave 关闭 Phase 16 全部范围:
1. **dashboard.json 序列化**:把 Wave 1-3 收集的 latest_data / diff_data / trend_data / ab_study_data / dropped_runs / warnings_list / source_legend(D-07 文字)/ summary(配置块)8 个顶层字段写入 JSON,默认落 CWD,文件名 `dashboard_<YYYYMMDD_HHMMSS>.json`(D-04),`--output <path>` 覆盖
2. **D-02 空根 exit 2**:默认根 + --runs 都为空 → click.UsageError → exit 2(Wave 1 已部分实现,Wave 4 完整测试守门)
3. **E2E 集成测试**(D-04 + D-17):用 5 fixture run 跑完整 CLI → 断言 dashboard.json 含全部 8 顶层字段 + stdout 含 LATEST/TREND/Dropped 标记
4. **CONCERNS §H4 收口**:`.gitignore` 加 `dashboard_*.json`(防 dashboard 落 repo root 时被 git status 看到)

Purpose: 关闭 Phase 16 全部交付。E2E 测试 fixture 集合(2 个 dropped + 1 个 params 完整 + 1 个 reasoning 完整含 ABStudy + 1 个 distribution 退化)是本 phase 真实生产路径的最小集 — 一旦此测试绿,dashboard 已经能在真实 output/tools[_reasoning]/ 目录上跑通。

Output:
- 修改 `evolution/tools/regression_dashboard.py`:main 末尾追加 `_write_dashboard_json` 调用 + 完整 8 字段 dict 构建 + --output 处理
- 修改 `tests/tools/test_regression_dashboard.py`:unskip 并实现 3 个 Wave 4 测试(test_e2e_dashboard_json_schema / test_dashboard_json_output_path / test_no_runs_exits_2)
- 修改 `.gitignore`:加一行 `dashboard_*.json`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md
@.planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md
@.planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md
@.planning/phases/16-per-tool-regression-dashboard/16-VALIDATION.md
@.planning/phases/16-per-tool-regression-dashboard/16-03-SUMMARY.md
@.planning/codebase/CONCERNS.md
@evolution/tools/regression_dashboard.py
@tests/tools/test_regression_dashboard.py
@.gitignore

<interfaces>
Existing helpers from Wave 1+2+3 (in regression_dashboard.py):
- _scan_runs / _load_run / _detect_source / _status_style / _quintiles
- _segment_distribution / _render_latest / _render_frequency_bars
- _sparkline / _render_diff / _render_trend
- _safe_truncate / _count_ab_categories / _render_ab_study
- main with 9 Click flags

Wave 1-3 main 内已收集的局部变量(本 wave 整合到 dashboard.json):
- runs_paths: list[Path] — 全部扫到的 run
- dropped: list[dict] — {path, reason}
- latest_run: Optional[dict] — Wave 1 selection
- valid_runs: list[dict] — Wave 2 trend filtered
- ab_study_data: dict — Wave 3 _render_ab_study return
- warnings_list: list[dict] — Wave 3 D-13 warnings

dashboard.json 顶层 schema(D-04 / RESEARCH §"dashboard.json 顶层 schema 草案"):
{
  "generated_at": "ISO 8601 UTC",
  "scanned_runs": int,
  "dropped_runs": [{"path": str, "reason": str}],
  "source_legend": {
    "desc": "evolve_tool_descriptions, baseline=v1 frozen",
    "params": "evolve_tool_params, baseline=v1 frozen",
    "reasoning": "evolve_tool_reasoning, baseline=think-off"
  },
  "latest": {"run_path": str, "source": str, "per_tool": [...]} | null,
  "diff": {"baseline_run": str, "evolved_run": str, "per_tool": [...]} | null,
  "trend": {"window_kind": "n|days", "window_value": int, "tools": [...]},
  "ab_study": {"detected_runs": int, "by_run": [...]},
  "warnings": [{"tool": str, "delta_pp": float, "run_path": str}],
  "summary": {"warning_threshold_pp": float, "segment_kind": str}
}
8 顶层字段(不含 generated_at / scanned_runs):
latest, diff, trend, ab_study, source_legend, dropped_runs, summary, warnings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 16-04-1: 在 main 中收集 8 字段 dashboard.json + --output 支持 + .gitignore 加 dashboard_*.json</name>
  <files>evolution/tools/regression_dashboard.py, .gitignore</files>
  <read_first>
    - evolution/tools/regression_dashboard.py (Wave 3 完成版本,需要把 latest_data / diff_data / trend_data / ab_study_data 都从函数返回值收集起来)
    - .gitignore (line 1-30,确认 output/ 已被 ignore,Phase 12 完成)
    - .planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md §D-04
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"dashboard.json 顶层 schema 草案" §"output/ 与 dashboard.json 落点"
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Test Pattern Map" + Excerpt rows for `dashboard.json` schema (B2 fix — 现已包含 PATTERNS §,executor 阅读时可对照 schema 字段命名)
    - .planning/codebase/CONCERNS.md §H4
  </read_first>
  <action>
**步骤 1**: 修改 `.gitignore`,在文件末尾追加一行(若已存在则跳过):

```
# Phase 16 dashboard outputs (CONCERNS §H4)
dashboard_*.json
```

(注:确认 line 26-27 的 `output/` 仍在,Phase 12 已加。)

**步骤 2**: 修改 `evolution/tools/regression_dashboard.py`:

(a) **修改 helper 返回值收集**:Wave 1 的 `_render_latest` 已返回 dict;Wave 2 的 `_render_diff` / `_render_trend` 已返回 dict;Wave 3 的 `_render_ab_study` 已返回 dict。在 main 中:

- 把 Wave 1 的 `_render_latest(latest_run, segment, warning_threshold_pp)` 调用结果赋给 `latest_data` 变量
- Wave 2 的 DIFF 编排块里把 `_render_diff(b_loaded, e_loaded, warning_threshold_pp)` 结果赋给 `diff_data` 变量,初始 `diff_data: Optional[dict] = None`
- Wave 2 的 TREND 编排块里把 `_render_trend(windowed, window_kind, window_value)` 结果赋给 `trend_data` 变量,初始 `trend_data: dict = {"window_kind": "n", "window_value": trend_window or 10, "tools": []}`
- Wave 3 的 ABStudy 块里 `ab_study_data` 已经存在

(b) **新增 `_write_dashboard_json` helper**(在 _render_ab_study 之后,Click 装饰器之前):

```python
def _write_dashboard_json(
    output_path: Path,
    *,
    scanned_runs: int,
    dropped_runs: list[dict],
    latest_data: Optional[dict],
    diff_data: Optional[dict],
    trend_data: dict,
    ab_study_data: dict,
    warnings_list: list[dict],
    summary: dict,
) -> None:
    """Serialize 8 top-level fields to dashboard.json (D-04 schema)."""
    payload = {
        "generated_at": datetime.utcnow().isoformat() + "Z",
        "scanned_runs": scanned_runs,
        "dropped_runs": dropped_runs,
        "source_legend": {
            "desc": "evolve_tool_descriptions, baseline=v1 frozen",
            "params": "evolve_tool_params, baseline=v1 frozen",
            "reasoning": "evolve_tool_reasoning, baseline=think-off",
        },
        "latest": latest_data,
        "diff": diff_data,
        "trend": trend_data,
        "ab_study": ab_study_data,
        "warnings": warnings_list,
        "summary": summary,
    }
    output_path.write_text(json.dumps(payload, indent=2, sort_keys=True), encoding="utf-8")
    console.print(f"[green]Wrote dashboard.json: {output_path}[/green]")
```

(c) **在 main 末尾**(Wave 3 的 `return 0` 之前)追加 dashboard.json 写盘:

```python
    # ── Wave 4: emit dashboard.json (D-04) ────────────────────────────────
    if output is not None:
        out_path = Path(output)
    else:
        ts = datetime.now().strftime("%Y%m%d_%H%M%S")
        out_path = Path.cwd() / f"dashboard_{ts}.json"

    _write_dashboard_json(
        out_path,
        scanned_runs=len(runs_paths),
        dropped_runs=dropped,
        latest_data=latest_data if latest_run is not None else None,
        diff_data=diff_data,
        trend_data=trend_data,
        ab_study_data=ab_study_data,
        warnings_list=warnings_list,
        summary={"warning_threshold_pp": warning_threshold_pp, "segment_kind": segment},
    )
    return 0
```

(d) **D-02 空根 exit 2 加固**:Wave 1 task 16-01-1 main 中已有 `if not runs_paths: raise click.UsageError(...)`。本 wave 测试守门即可,无需新增逻辑。但确认 import `import sys` 已有(D-02 错误用 click.UsageError 抛出,Click 自动 exit 2,无需手动 sys.exit)。

(e) **变量初始化保证**:在 main 开头(参数解析之后),把 `latest_data / diff_data / trend_data / ab_study_data / warnings_list / dropped` 的初始值都先声明,避免某些 fixture 触发的执行路径下变量未定义:

```python
    # Initialize collectors for dashboard.json (Wave 4)
    latest_data: Optional[dict] = None
    diff_data: Optional[dict] = None
    trend_data: dict = {"window_kind": "n", "window_value": trend_window or 10, "tools": []}
    ab_study_data: dict = {"detected_runs": 0, "by_run": []}
    warnings_list: list[dict] = []
    dropped: list[dict] = []
```

确保所有 Wave 1-3 已经写过的 `latest_data = ...` / `diff_data = ...` / `trend_data = ...` / `ab_study_data = ...` / `dropped.append(...)` / `warnings_list.append(...)` 仍是同一个变量(不要在分支内 shadow)。

(f) **空 latest 时仍写 dashboard.json**:Wave 1 main 在 `latest_run is None` 分支直接 `return 0`,导致 dashboard.json 不写。本 wave 改为:`latest_run is None` 时把 `latest_data` 留 None,继续 fall-through 到 Wave 4 的 dashboard.json 写盘(让用户即使 0 usable run 也能看到 dropped_runs 列表)。

具体修改 Wave 1 的 main 块:把
```python
    if latest_run is None:
        console.print("[yellow]No usable runs for LATEST region[/yellow]")
        for d in dropped:
            console.print(f"[yellow]dropped: {d['path']} — {d['reason']}[/yellow]")
        return 0  # ← 删除这一行
```
改为(删 `return 0`,继续 fall-through 到 Wave 2-4 编排):
```python
    if latest_run is None:
        console.print("[yellow]No usable runs for LATEST region[/yellow]")
    else:
        latest_data = _render_latest(latest_run, segment, warning_threshold_pp)
```
其他 Wave 2/3/4 编排块都需 `if latest_run is not None:` / `if reasoning_with_ab:` 这类保护(Wave 3 已加)。
  </action>
  <verify>
    <automated>python -c "from evolution.tools.regression_dashboard import _write_dashboard_json; from pathlib import Path; import tempfile, json; tmp = Path(tempfile.mkstemp(suffix='.json')[1]); _write_dashboard_json(tmp, scanned_runs=0, dropped_runs=[], latest_data=None, diff_data=None, trend_data={'window_kind':'n','window_value':10,'tools':[]}, ab_study_data={'detected_runs':0,'by_run':[]}, warnings_list=[], summary={'warning_threshold_pp':2.0,'segment_kind':'difficulty'}); d = json.loads(tmp.read_text()); assert set(d.keys()) >= {'latest','diff','trend','ab_study','source_legend','dropped_runs','summary','warnings'}; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'dashboard_\*.json' .gitignore` 输出 1 行
    - `grep -n 'def _write_dashboard_json' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'json.dumps(payload, indent=2, sort_keys=True' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'dashboard_.*\.json' evolution/tools/regression_dashboard.py` 输出至少 1 行(默认文件名 pattern)
    - `grep -n '"source_legend":' evolution/tools/regression_dashboard.py` 输出 1 行(D-07 legend 写入 JSON)
    - `grep -n '"detected_runs"' evolution/tools/regression_dashboard.py` 输出至少 1 行(ab_study 默认结构)
    - `grep -n 'datetime.utcnow().isoformat()' evolution/tools/regression_dashboard.py` 输出至少 1 行(generated_at 时间戳)
    - `python -c "from evolution.tools.regression_dashboard import _write_dashboard_json; print('imports ok')"` 退出码 0
    - 手动 smoke(W6 跳过保留;只是 plan 文档级 acceptance,executor 跑时可适配本机绝对路径): `cd /tmp && python -m evolution.tools.regression_dashboard --runs $REPO_ROOT/tests/fixtures/dashboard_runs/params_complete --output /tmp/test_dashboard.json && python -c "import json; d=json.load(open('/tmp/test_dashboard.json')); assert all(k in d for k in ['latest','diff','trend','ab_study','source_legend','dropped_runs','summary','warnings'])"` 退出码 0(`$REPO_ROOT` 由 executor 替换为 git rev-parse --show-toplevel 输出;若不便替换,跳过此 manual smoke,Wave 4 task 16-04-2 自动测试已覆盖)
  </acceptance_criteria>
  <done>
    dashboard.json 8 字段 schema 序列化完成,--output 支持,.gitignore 加 dashboard_*.json 防御。main 完整 fall-through 到写盘,即使 0 usable run 也能 dump dropped_runs。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-04-2: unskip + 实现 3 个 Wave 4 测试(e2e_schema / output_path / no_runs_exits_2)</name>
  <files>tests/tools/test_regression_dashboard.py</files>
  <read_first>
    - evolution/tools/regression_dashboard.py (task 16-04-1 完成后版本)
    - tests/tools/test_regression_dashboard.py (Wave 3 完成版本,3 个 Wave 4 stub 当前为 skip)
    - tests/fixtures/dashboard_runs/(全部 9 个文件:desc_old / params_complete / params_complete_v2 / params_no_raw / reasoning_complete / reasoning_old / reasoning_with_secret / json_corrupt + ab_comparison.json fixtures)
    - .planning/phases/16-per-tool-regression-dashboard/16-PATTERNS.md §"Test Pattern Map" (B2 fix — 现已包含 PATTERNS §)
    - .planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md §"Validation Architecture" (B2 fix — 现已包含 RESEARCH §)
    - .planning/phases/16-per-tool-regression-dashboard/16-VALIDATION.md §16-04-01/16-04-02/16-04-03
    - .planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md §D-02 §D-04
  </read_first>
  <behavior>
    - test_e2e_dashboard_json_schema: 用 5 fixture run(desc_old + params_complete + reasoning_complete + reasoning_old + params_no_raw)→ invoke main → dashboard.json 含 8 顶层字段(latest/diff/trend/ab_study/source_legend/dropped_runs/summary/warnings)+ scanned_runs == 5 + dropped_runs 至少含 2 entries(desc_old 与 reasoning_old)
    - test_dashboard_json_output_path: 不传 --output 时落 CWD 文件名 `dashboard_<ts>.json`(用 tmp_path 作 CWD);传 `--output /custom/path.json` 时按指定路径写
    - test_no_runs_exits_2: patch _scan_runs 返回空 list 且不传 --runs(或传不存在路径)→ exit_code == 2
  </behavior>
  <action>
修改 `tests/tools/test_regression_dashboard.py`,删除最后 3 个 skip stub(test_e2e_dashboard_json_schema / test_dashboard_json_output_path / test_no_runs_exits_2)的 `@pytest.mark.skip` 装饰器并替换函数体:

**实现 1 — test_e2e_dashboard_json_schema**:
```python
def test_e2e_dashboard_json_schema(tmp_path):
    """16-04-01 (D-04 + D-17) — E2E: 5 fixture runs → dashboard.json with 8 top-level fields."""
    runner = CliRunner()
    fixture_paths = [
        FIXTURES / "desc_old" / "metrics.json",
        FIXTURES / "params_complete" / "metrics.json",
        FIXTURES / "params_no_raw" / "metrics.json",
        FIXTURES / "reasoning_complete" / "metrics.json",
        FIXTURES / "reasoning_old" / "metrics.json",
    ]
    out_json = tmp_path / "test_dashboard.json"
    with patch("evolution.tools.regression_dashboard._scan_runs", return_value=fixture_paths):
        result = runner.invoke(
            main,
            ["--runs", str(FIXTURES / "params_complete"), "--output", str(out_json),
             "--trend-window", "5"],
            catch_exceptions=False,
        )
    assert result.exit_code == 0, f"stdout: {result.output}"
    assert out_json.exists(), f"dashboard.json not written: {out_json}"
    data = json.loads(out_json.read_text())

    # 8 top-level fields (D-04)
    expected_keys = {"latest", "diff", "trend", "ab_study", "source_legend",
                     "dropped_runs", "summary", "warnings"}
    assert expected_keys.issubset(set(data.keys())), \
        f"missing top-level keys: {expected_keys - set(data.keys())}"

    # generated_at + scanned_runs auxiliary fields
    assert "generated_at" in data
    assert data["scanned_runs"] == 5

    # dropped_runs has at least desc_old + reasoning_old (both miss per_tool_*_rates)
    dropped_paths = [d["path"] for d in data["dropped_runs"]]
    assert any("desc_old" in p for p in dropped_paths)
    assert any("reasoning_old" in p for p in dropped_paths)

    # source_legend has 3 keys
    assert set(data["source_legend"].keys()) == {"desc", "params", "reasoning"}

    # latest + ab_study populated (params_complete + reasoning_complete are usable)
    assert data["latest"] is not None
    assert "per_tool" in data["latest"]
    # ab_study may be 0 if reasoning_complete fixture isn't selected as latest_run
    # but trend should have at least 1 tool aggregated
    assert "tools" in data["trend"]


def test_dashboard_json_output_path(tmp_path, monkeypatch):
    """16-04-02 (D-04) — dashboard.json default lands at CWD with `dashboard_<ts>.json` pattern;
    --output overrides path."""
    runner = CliRunner()
    fixture_path = FIXTURES / "params_complete" / "metrics.json"

    # Case 1: --output explicit path
    custom_out = tmp_path / "custom.json"
    with patch("evolution.tools.regression_dashboard._scan_runs", return_value=[fixture_path]):
        result = runner.invoke(
            main,
            ["--runs", str(fixture_path.parent), "--output", str(custom_out)],
            catch_exceptions=False,
        )
    assert result.exit_code == 0, f"stdout: {result.output}"
    assert custom_out.exists()

    # Case 2: no --output → default `dashboard_<ts>.json` in CWD
    monkeypatch.chdir(tmp_path)
    with patch("evolution.tools.regression_dashboard._scan_runs", return_value=[fixture_path]):
        result = runner.invoke(
            main,
            ["--runs", str(fixture_path.parent)],
            catch_exceptions=False,
        )
    assert result.exit_code == 0, f"stdout: {result.output}"
    # Find dashboard_*.json in tmp_path
    matches = list(tmp_path.glob("dashboard_*.json"))
    assert len(matches) >= 1, f"no dashboard_<ts>.json in CWD: {list(tmp_path.iterdir())}"
    # Filename pattern: dashboard_YYYYMMDD_HHMMSS.json
    import re
    assert any(re.match(r"^dashboard_\d{8}_\d{6}\.json$", m.name) for m in matches), \
        f"filename does not match pattern: {[m.name for m in matches]}"


def test_no_runs_exits_2():
    """16-04-03 (D-02) — empty default roots + no --runs → click.UsageError → exit 2."""
    runner = CliRunner()
    # Patch DEFAULT_ROOTS to empty AND _scan_runs to return [] AND don't pass --runs
    with patch("evolution.tools.regression_dashboard.DEFAULT_ROOTS", ()), \
         patch("evolution.tools.regression_dashboard._scan_runs", return_value=[]):
        result = runner.invoke(main, [], catch_exceptions=False)
    assert result.exit_code == 2, \
        f"expected exit 2 (UsageError), got {result.exit_code}; stdout: {result.output}"
    # UsageError prints message to stderr (Click captured into result.output)
    assert "no runs" in result.output.lower() or "no run" in result.output.lower()
```

不动其他 14 个 active 测试。运行后期望:全部 17 个测试 active passed,0 skipped。

**函数名不可改**(W7 / Wave 1 锁定):3 个测试函数名必须精确为 `test_e2e_dashboard_json_schema` / `test_dashboard_json_output_path` / `test_no_runs_exits_2`。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_regression_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@pytest.mark.skip' tests/tools/test_regression_dashboard.py` 输出 0(全部 unskip)
    - `grep -n 'def test_e2e_dashboard_json_schema' tests/tools/test_regression_dashboard.py` 输出 1 行,前一行不是 `@pytest.mark.skip`
    - `grep -n 'def test_dashboard_json_output_path' tests/tools/test_regression_dashboard.py` 输出 1 行,前一行不是 `@pytest.mark.skip`
    - `grep -n 'def test_no_runs_exits_2' tests/tools/test_regression_dashboard.py` 输出 1 行,前一行不是 `@pytest.mark.skip`
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py -x` 退出码 0(17 passed, 0 skipped)
    - `.venv/bin/pytest tests/tools/ -x` 退出码 0(整 tools 测试目录回归绿)
    - `.venv/bin/pytest tests/ -x` 退出码 0(整测试套件回归绿,Phase 13/14/15 测试无破坏)
    - 手动 smoke E2E: `cd /tmp && python -m evolution.tools.regression_dashboard --runs $REPO_ROOT/tests/fixtures/dashboard_runs/params_complete --runs $REPO_ROOT/tests/fixtures/dashboard_runs/reasoning_complete --trend-window 5 && ls dashboard_*.json | head -1` 输出含 dashboard_*.json 文件名
  </acceptance_criteria>
  <done>
    Wave 4 3 个 E2E 测试全绿,全部 17 个 dashboard 测试 active passed。dashboard.json 8 字段 schema 严格守门,--output 路径覆盖与默认 ts 文件名都被验证。Phase 16 全部交付完成。
  </done>
</task>

</tasks>

<verification>
**Wave 4(及整 Phase 16)完成标志**:
- `.venv/bin/pytest tests/tools/test_regression_dashboard.py -x` 退出码 0(17 passed, 0 skipped)
- `.venv/bin/pytest tests/tools/ -x` 退出码 0
- `.venv/bin/pytest tests/ -x` 退出码 0(整套件 — Phase 13/14/15 + Phase 16 全部回归绿)
- 手动 E2E: `python -m evolution.tools.regression_dashboard` 在仓库 root 运行(默认根 output/tools/ + output/tools_reasoning/ 可能含 17 reasoning runs,均缺 per_tool 全部入 dropped_runs)→ exit 0,dashboard.json 写出含 8 字段 + dropped_runs 列表
- `grep -c "dashboard_\*.json" .gitignore` 输出 ≥ 1
</verification>

<success_criteria>
- dashboard.json 含 8 顶层字段(latest/diff/trend/ab_study/source_legend/dropped_runs/summary/warnings)+ 2 辅助字段(generated_at/scanned_runs)
- D-04 默认文件名 `dashboard_<YYYYMMDD_HHMMSS>.json` 落 CWD,--output 覆盖
- D-02 空根 + 无 --runs → exit 2
- 17 测试全部 active passed,Phase 13/14/15 测试零破坏
- .gitignore 防御 dashboard_*.json 落 repo root
</success_criteria>

<output>
After completion, create `.planning/phases/16-per-tool-regression-dashboard/16-04-SUMMARY.md` 与 `.planning/phases/16-per-tool-regression-dashboard/16-SUMMARY.md`(phase 级总结):
- Phase 16 全部交付清单(5 wave / 5 PLAN.md / 17 test / ~280 LoC dashboard.py / 9 fixture file)
- ROADMAP §Phase 16 三条成功标准对照表(每条 → 具体哪个 wave 哪个 task 落地)
- D-01..D-17 决策落地对照表
- Pitfall 10「distribution 双层语义」收口确认(语义 A LATEST/DIFF / 语义 B TREND)
- VALIDATION.md 23 个 task verification 全部 ✅ green
- 已知遗留 / 后续优化(deferred items 不动)
- 实测 dashboard 在真实 output/tools_reasoning/ 17 runs 上的输出截图(stdout 前 50 行 + dashboard.json 大小)
</output>
</content>
</invoke>
