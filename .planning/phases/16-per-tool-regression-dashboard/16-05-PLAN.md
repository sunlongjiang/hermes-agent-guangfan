---
plan: 16-05-gap-closure
phase: 16
type: execute
wave: 5
depends_on: [16-04-schema-e2e]
gap_closure: true
files_modified:
  - path: evolution/tools/regression_dashboard.py
    role: modified
  - path: evolution/tools/evolve_tool_reasoning.py
    role: modified
  - path: evolution/tools/evolve_tool_descriptions.py
    role: modified
  - path: tests/tools/test_regression_dashboard.py
    role: modified
  - path: tests/tools/test_evolve_tool_reasoning.py
    role: modified
  - path: tests/tools/test_evolve_tool_descriptions.py
    role: modified
requirements: [TOOL-V2-04]
autonomous: true
estimated_minutes: 110

must_haves:
  truths:
    - "`--runs <dir>` 与 `--baseline-run <dir>` 语义统一:两者都把参数当作 run directory,各自读 `<dir>/metrics.json`(CR-01 + CR-02 关闭)"
    - "至少一个 dashboard 测试不 patch `_scan_runs`,经过真实 glob + explicit-path 解析(CR-01 测试遮蔽缺口关闭)"
    - "`evolve_tool_reasoning._score_with_predictions` 与 `_safe_score` 使用相同的 metric (`joint_tool_param_metric`),`ThinkABGate.check()` 接收同源指标(CR-03 关闭)"
    - "`_score_with_predictions` 在 per-example 异常时打印 yellow 日志,与 `evolve_tool_params._evaluate_holdout` 行为一致(WR-07 关闭)"
    - "`evolve_tool_descriptions` 在 `if not regression_result.passed:` 早期 return **之前**完成 `persist_per_tool_rates` + `persist_raw_predictions`;REGRESSION_FAILED metrics.json 含完整 per-tool 字段(WR-04 关闭;反转 16-00-PLAN 原决策,理由见 objective)"
    - "`regression_dashboard.py` 不再使用 deprecated `datetime.utcnow()`,改为 `datetime.now(timezone.utc)`;`--trend-days` 窗口与 `generated_at` 使用同一时钟基准(WR-01 + WR-02 关闭)"
    - "TOOL-V2-04 的 6 truths 全部 verified — `track individual tool selection rates across optimization runs` 真正达成(verifier 复检 score 6/6)"
  artifacts:
    - path: evolution/tools/regression_dashboard.py
      provides: "`_scan_runs(roots, explicit_runs=())` 双输入签名;`datetime.now(timezone.utc)` 替代 utcnow"
      contains: "explicit_runs"
    - path: evolution/tools/evolve_tool_reasoning.py
      provides: "`_score_with_predictions` 用 `joint_tool_param_metric` + yellow logging"
      contains: "joint_tool_param_metric"
    - path: evolution/tools/evolve_tool_descriptions.py
      provides: "FAILED 分支 metrics.json 含 per_tool_*_rates + raw_predictions"
      contains: "metrics_extra"
    - path: tests/tools/test_regression_dashboard.py
      provides: "`test_scan_runs_resolves_explicit_path` 不 patch `_scan_runs`,真实 glob/explicit path 覆盖;`test_json_corrupt_fixture_drops_with_reason` 覆盖 IN-02 fixture"
  key_links:
    - from: evolution/tools/regression_dashboard.py:main
      to: evolution/tools/regression_dashboard.py:_scan_runs
      via: "调用 `_scan_runs(DEFAULT_ROOTS, tuple(Path(r) for r in runs))` — `--runs` 走 explicit_runs 通道,默认根继续 glob"
      pattern: "_scan_runs\\(.*explicit_runs"
    - from: evolution/tools/evolve_tool_reasoning.py:_score_with_predictions
      to: evolution/tools/tool_metric.py:joint_tool_param_metric
      via: "import + 在 per-example 循环内调 `joint_tool_param_metric(ex, pred)` 作为 score 累加器"
      pattern: "joint_tool_param_metric\\(ex, pred\\)"
    - from: evolution/tools/evolve_tool_descriptions.py
      to: "REGRESSION_FAILED metrics.json (filesystem)"
      via: "在 `if not regression_result.passed:` block 内合并 metrics_extra 后再 write_text"
      pattern: "metrics_extra"
---

<objective>
**Phase 16 Gap Closure** — 关闭 16-VERIFICATION.md 报告的 2 个 BLOCKER 与 1 个 WARNING,让 `Track individual tool selection rates across optimization runs` 这一 phase 目标真正落地(verifier 复检 4/6 → 6/6)。同时清掉相邻 INFO 级别 hygiene(deprecated datetime / json_corrupt 未覆盖 / `_score_with_predictions` 静默吞异常)。

**关闭范围(权威来源:16-VERIFICATION.md + 16-REVIEW.md)**:

| Gap ID | 严重度 | 文件 | 关闭方式 |
|--------|--------|------|----------|
| CR-01 | 🛑 BLOCKER | `regression_dashboard.py` `_scan_runs` line 54-60 | 改双输入签名 `(roots, explicit_runs=())`,glob roots + 直读 explicit_runs |
| CR-02 | 🛑 BLOCKER | `regression_dashboard.py` line 624-627 vs 675-676 | `--runs` 走 explicit_runs 通道,与 `--baseline-run` 路径解析对齐 |
| CR-03 | 🛑 BLOCKER | `evolve_tool_reasoning.py` `_score_with_predictions` line 628-681 | 改为调 `joint_tool_param_metric(ex, pred)`,与 `_safe_score → _score_module_on_holdout` 同源 |
| WR-04 | ⚠ WARNING | `evolve_tool_descriptions.py` line 368-381 vs 405-406 | persist 调用前移到 FAILED 分支之**前**;FAILED 分支 metrics 注入 metrics_extra(反转 16-00-PLAN 决策,见下) |
| WR-01 | ⚠ WARNING | `regression_dashboard.py` line 563 | `datetime.utcnow()` → `datetime.now(timezone.utc)` |
| WR-02 | ⚠ WARNING | `regression_dashboard.py` line 703-707 vs 563 | 统一时钟基准(`generated_at` 与 `--trend-days cutoff` 都用 UTC) |
| WR-07 | ⚠ WARNING | `evolve_tool_reasoning.py` `_score_with_predictions` 异常路径 | 仿 `_evaluate_holdout` 打 yellow 日志 |
| IN-02 | ℹ INFO | `tests/fixtures/dashboard_runs/json_corrupt/` 未被引用 | 加测试覆盖,断言 dropped_runs 含 json parse error |
| IN-01 | ℹ INFO | `regression_dashboard.py` `_load_run` `loaded is None` 死分支 | 改 `_load_run` docstring 与实现:parse error 时 return None(代码语义与 docstring 对齐) |
| WR-05 | ℹ INFO(借机修) | `_render_diff` title `None vs None` | `baseline_run['source'] or '?'` 兜底 |
| WR-06 | ℹ INFO(借机修) | `_render_frequency_bars` 空 `correct_tool` 鬼柱 | counter 构造时过滤空字符串 |

**显式 deferred(本次不动)**:WR-03(`_load_run` 2-3× 调用 — 性能问题,影响小,留 todo);IN-03(测试注释 -5pp 误标);IN-04 / IN-05 / IN-06(其他 cosmetic / 跨 phase 重构)。

## 关键反转决策:WR-04(plan 文档级记录)

16-00-PLAN.md objective 段第 71 行原决策:「**Phase 16 不在 FAILED 分支注入 metrics_extra**(老 FAILED run 不回填,见 W4 / Out-of-scope §6)」。

verifier 在 16-VERIFICATION.md truth #1 / WR-04 指出:这个原决策直接抵消了 phase 目标。`dashboard._load_run` 会因为「missing per_tool_*_rates」把所有 REGRESSION_FAILED 的 desc run 全部 drop —— 而这些恰好就是 dashboard 存在的核心用例。`evolve_tool_params.py:1018-1029` 同时期已经**正确**地把 persist 放在 FAILED branch 之前(sibling inconsistency)。

**反转理由**:
- 16-00-PLAN 「Out-of-scope §6」是关于**历史**老 run 不回填,**不**关于未来 FAILED run 的 schema 完整性
- `evolve_tool_params` 的 sibling pattern 是正确做法,`evolve_tool_descriptions` 是离群者
- FAILED 分支的 per-tool 数据是 dashboard 的主要观察对象 — 把它隐藏起来与「Per-Tool Regression Dashboard」的 phase 标题直接冲突

本 plan 把 `persist_per_tool_rates` + `persist_raw_predictions` 调用移到 `if not regression_result.passed:` block 之**前**,并把 `metrics_extra` 合并到 FAILED 分支的 metrics dict literal 与 happy-path 两处。

**仍 hold 的 16-00 决策**:「**历史**老 FAILED run 不回填」继续 hold — 我们不去触碰 `output/tools/FAILED_<old_ts>/metrics.json` 等已存在文件;只让**未来**跑出来的 FAILED run 的 metrics.json 携带完整字段。

## Output

- `evolution/tools/regression_dashboard.py`:`_scan_runs` 双输入签名 + main 调用更新 + `datetime.now(timezone.utc)` 替换 utcnow + `--trend-days cutoff` 改为 UTC 基准 + `_render_diff` `source or '?'` 兜底 + `_render_frequency_bars` 过滤空 key + `_load_run` 改回 docstring 契约(parse error 返回 None)
- `evolution/tools/evolve_tool_reasoning.py`:`_score_with_predictions` 改用 `joint_tool_param_metric` + per-example yellow 日志
- `evolution/tools/evolve_tool_descriptions.py`:persist 调用前移 + FAILED 分支 metrics 注入 metrics_extra
- `tests/tools/test_regression_dashboard.py`:新增 `test_scan_runs_resolves_explicit_path`(不 patch `_scan_runs`)+ `test_json_corrupt_fixture_drops_with_reason`(IN-02 覆盖)
- `tests/tools/test_evolve_tool_reasoning.py`:新增 `test_score_with_predictions_uses_joint_metric`(metric 一致性单测)
- `tests/tools/test_evolve_tool_descriptions.py`:新增 `test_regression_failed_metrics_carry_per_tool_rates`(FAILED 分支 schema 完整性单测)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md
@.planning/phases/16-per-tool-regression-dashboard/16-RESEARCH.md
@.planning/phases/16-per-tool-regression-dashboard/16-VERIFICATION.md
@.planning/phases/16-per-tool-regression-dashboard/16-REVIEW.md
@.planning/phases/16-per-tool-regression-dashboard/16-00-PLAN.md
@.planning/phases/16-per-tool-regression-dashboard/16-04-PLAN.md
@evolution/tools/regression_dashboard.py
@evolution/tools/evolve_tool_reasoning.py
@evolution/tools/evolve_tool_descriptions.py
@evolution/tools/tool_metric.py
@evolution/tools/evolve_tool_params.py

<interfaces>
<!-- 执行 task 1/2 时所需的真实代码契约 — 全部从当前仓库提取,不需再 grep -->

From `evolution/tools/regression_dashboard.py:54-60` (current broken signature):
```python
def _scan_runs(roots: tuple[Path, ...]) -> list[Path]:
    """Glob <root>/*/metrics.json, sorted by mtime ascending (oldest first)."""
    found: list[Path] = []
    for root in roots:
        if root.exists():
            found.extend(root.glob("*/metrics.json"))
    return sorted(found, key=lambda p: p.stat().st_mtime)
```

From `evolution/tools/regression_dashboard.py:623-627` (current broken main wiring):
```python
# Build root list: defaults + --runs additions
roots = list(DEFAULT_ROOTS)
for r in runs:
    roots.append(Path(r))
runs_paths = _scan_runs(tuple(roots))
```

From `evolution/tools/regression_dashboard.py:675-676` (correct semantics — --baseline-run 已经是 run dir):
```python
b_loaded = _load_run(Path(baseline_run) / "metrics.json")
e_loaded = _load_run(Path(evolved_run) / "metrics.json")
```

From `evolution/tools/regression_dashboard.py:63-83` (`_load_run` — docstring says "None on parse error" but code returns dict):
```python
def _load_run(metrics_path: Path) -> Optional[dict]:
    """Return {path, source, metrics, ab_comparison?} or None on parse error.

    Also returns None for runs that are unrecoverable (status=FAILED, missing
    per_tool_*_rates) — those appear in dropped_runs[] via the caller.
    """
    try:
        metrics = json.loads(metrics_path.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError) as e:
        return {"path": str(metrics_path), "metrics": None, "source": None, "_drop_reason": f"json parse error: {e}"}
    # ... rest of body
```

From `evolution/tools/regression_dashboard.py:561-580` (current utcnow + json dump):
```python
payload = {
    "generated_at": datetime.utcnow().isoformat() + "Z",  # ← deprecated (WR-01)
    ...
}
```

From `evolution/tools/regression_dashboard.py:700-707` (current trend-days local-time cutoff — WR-02):
```python
if trend_days is not None:
    window_kind = "days"
    window_value = trend_days
    cutoff = datetime.now().timestamp() - (trend_days * 86400.0)  # ← local time (WR-02)
    windowed = [
        r for r in valid_runs
        if Path(r["path"]).stat().st_mtime >= cutoff
    ]
```

From `evolution/tools/regression_dashboard.py:329-333` (current DIFF title — WR-05):
```python
table = Table(
    title=(
        f"DIFF region [{baseline_run['source']} vs {evolved_run['source']}]: "  # ← None vs None possible
        f"{baseline_run['path']} → {evolved_run['path']}"
    ),
```

From `evolution/tools/regression_dashboard.py:209-210` (frequency-bar Counter — WR-06):
```python
# Per-tool sample counts from raw_predictions (correct_tool occurrences)
sample_counts: Counter = Counter(rec.get("correct_tool", "") for rec in raw_preds)
```

From `evolution/tools/evolve_tool_reasoning.py:628-681` (current `_score_with_predictions` — CR-03 + WR-07):
```python
def _score_with_predictions(
    module: Any,
    examples: list,
    lm: Any,
) -> tuple[float, list[tuple[str, str]], list[dict]]:
    if not examples:
        return 0.0, [], []
    tool_pairs: list[tuple[str, str]] = []
    raw_preds: list[dict] = []
    total = 0.0
    n = 0
    try:
        with dspy.context(lm=lm):
            for ex in examples:
                try:
                    pred = module(task_description=ex.task_description)
                except Exception:
                    continue  # ← WR-07: silent swallow, no logging
                correct = getattr(ex, "correct_tool", "") or ""
                selected = getattr(pred, "selected_tool", "") or ""
                tool_pairs.append((correct, selected))
                raw_preds.append({...})
                total += 1.0 if correct == selected else 0.0  # ← CR-03: raw case-sensitive
                n += 1
        score = total / n if n else 0.0
        return float(score), tool_pairs, raw_preds
    except Exception:
        return 0.0, tool_pairs, raw_preds  # ← WR-07: silent batch swallow
```

From `evolution/tools/evolve_tool_params.py:382-398` (mirror pattern — yellow logging on per-example error):
```python
try:
    pred = module(task_description=task)
except Exception as e:
    console.print(
        f"[yellow]holdout example skipped due to LM error: "
        f"{type(e).__name__}: {e}[/yellow]"
    )
    continue
```

From `evolution/tools/tool_metric.py:329-362` (current `joint_tool_param_metric` — the metric CR-03 will adopt):
```python
def joint_tool_param_metric(
    example: dspy.Example,
    prediction: dspy.Prediction,
    trace=None,
    pred_name=None,
    pred_trace=None,
) -> float:
    """0.5 tool_match + 0.5 param_match, both normalized via .strip().lower()."""
    selected_tool = (getattr(prediction, "selected_tool", "") or "").strip().lower()
    correct_tool = (getattr(example, "correct_tool", "") or "").strip().lower()
    tool_match = 1.0 if selected_tool == correct_tool else 0.0

    correct_params = getattr(example, "correct_params", {}) or {}
    raw = getattr(prediction, "selected_params", "")
    predicted = _parse_selected_params_json(raw)
    param_match = _param_match_score(predicted, correct_params)

    return 0.5 * tool_match + 0.5 * param_match
```

From `evolution/tools/evolve_tool_reasoning.py:1-50` (current imports — `joint_tool_param_metric` is NOT imported):
```python
# imports include CrossToolRegressionChecker, persist_per_tool_rates, persist_raw_predictions
# from tool_metric, but joint_tool_param_metric is NOT in the current import list.
# Task 2 must add it.
```

From `evolution/tools/evolve_tool_descriptions.py:336-381` (current control flow — WR-04 fix target):
```python
baseline_preds: list[tuple[str, str]] = []
evolved_preds: list[tuple[str, str]] = []
raw_preds: list[dict] = []

for ex in holdout_examples:
    with dspy.context(lm=lm):
        bp = baseline_module(task_description=ex.task_description)
        baseline_preds.append((ex.correct_tool, bp.selected_tool))
        ep = optimized_module(task_description=ex.task_description)
        evolved_preds.append((ex.correct_tool, ep.selected_tool))
        raw_preds.append({...})

baseline_correct = sum(...)
evolved_correct = sum(...)
n_holdout = max(1, len(holdout_examples))
baseline_score = baseline_correct / n_holdout
evolved_score = evolved_correct / n_holdout
improvement = evolved_score - baseline_score

regression_checker = CrossToolRegressionChecker()
baseline_rates = regression_checker.compute_per_tool_rates(baseline_preds)
evolved_rates = regression_checker.compute_per_tool_rates(evolved_preds)
regression_result = regression_checker.check_regression(baseline_rates, evolved_rates)

if not regression_result.passed:
    # ← WR-04: persist_per_tool_rates / persist_raw_predictions NOT called here
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    output_dir = Path("output") / "tools" / f"FAILED_{timestamp}"
    output_dir.mkdir(parents=True, exist_ok=True)
    (output_dir / "metrics.json").write_text(json.dumps({
        "timestamp": timestamp,
        "status": "REGRESSION_FAILED",
        "baseline_score": baseline_score,
        "evolved_score": evolved_score,
        "regressed_tools": regression_result.regressed_tools,
    }, indent=2))
    console.print("[red]Cross-tool regression detected -- not deploying[/red]")
    console.print(f"  Saved results to {output_dir}/")
    return  # ← early return; happy-path persist 永远跑不到

# ── 10. Report results ───────────────────────────────────────────────
# ... (happy-path)
# ── Phase 16 Wave 0: persist per-tool rates + raw_predictions for dashboard ──
metrics_extra = persist_per_tool_rates({}, baseline_rates, evolved_rates)
metrics_extra = persist_raw_predictions(metrics_extra, raw_preds)  # ← line 405-406, AFTER return
```

From `tests/fixtures/dashboard_runs/params_complete/metrics.json` (first lines — used by new tests):
```
{
  "timestamp": "20260512_103000",
  "param_predictors_discovered": 12,
  "per_tool_baseline_rates": {"search_files": 0.80, ...},
  "per_tool_evolved_rates": {"search_files": 0.84, ...},
  ...
}
```

From `tests/fixtures/dashboard_runs/json_corrupt/metrics.json` (full file, < 50 bytes):
```
{ this is not valid json at all, bro }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 16-05-1: dashboard CLI 语义统一 + datetime/timezone 卫生 + 边角修正</name>
  <files>evolution/tools/regression_dashboard.py, tests/tools/test_regression_dashboard.py</files>
  <read_first>
    - evolution/tools/regression_dashboard.py(整个文件 ~789 行,重点 line 54-60 / 63-83 / 209-210 / 329-333 / 561-580 / 623-627 / 675-676 / 700-707)
    - tests/tools/test_regression_dashboard.py(整文件 ~530 行,重点 line 25-44 现有 active 测试的 fixture 使用模式)
    - .planning/phases/16-per-tool-regression-dashboard/16-VERIFICATION.md(权威 gap 来源 — `--runs` 失败的 smoke test 证据在第 13-18 行 / 第 99 行)
    - .planning/phases/16-per-tool-regression-dashboard/16-REVIEW.md(CR-01 fix snippet line 89-101 / CR-02 line 106-120 / WR-01 line 155-168 / WR-02 line 170-183 / WR-05 line 217-229 / WR-06 line 231-243 / IN-01 line 265-274)
    - tests/fixtures/dashboard_runs/params_complete/metrics.json(目标 fixture — 测试 `--runs <dir>` 时会指向其父目录)
    - tests/fixtures/dashboard_runs/json_corrupt/metrics.json(IN-02 fixture — 内容是 `{ this is not valid json at all, bro }`,行为应是 dropped_runs[0].reason 含 "json parse error")
  </read_first>
  <behavior>
    - **test_scan_runs_resolves_explicit_path**:**不** patch `_scan_runs`。`CliRunner().invoke(main, ["--runs", str(FIXTURES / "params_complete"), "--output", str(out_json)])` 跑通,断言 `data["scanned_runs"] >= 1` 且 `data["latest"]["run_path"]` 字符串包含 `"params_complete"`(证明 `--runs <run-dir>` 真的把 fixture 的 metrics.json 纳入扫描 — CR-01/CR-02 闭合验证)。同时为了避免误触发默认根扫描(repo 中真实 `output/tools/` 可能有数据),用 `monkeypatch.setattr("evolution.tools.regression_dashboard.DEFAULT_ROOTS", ())` 把默认根清空。
    - **test_scan_runs_glob_and_explicit_combined**:**不** patch `_scan_runs`。在 `tmp_path` 下造一个伪 root 含 1 个 `<ts>/metrics.json`(复制 params_complete 的内容)+ 同时传 `--runs <explicit-fixture-dir>`,断言两边的 run 都出现在 `data["scanned_runs"]`(glob 根 + explicit 路径共存,不互斥)。
    - **test_json_corrupt_fixture_drops_with_reason**:`monkeypatch.setattr("evolution.tools.regression_dashboard.DEFAULT_ROOTS", ())` + `--runs <json_corrupt-dir>` + 至少一个 usable run(`--runs <params_complete-dir>`),断言 `data["scanned_runs"] >= 2`,`data["dropped_runs"]` 至少一项 `path` 含 `"json_corrupt"` 且 `reason` 含 `"json parse error"`(IN-02 fixture 覆盖)。
    - **test_dashboard_json_no_deprecation_warning**:运行 dashboard 并断言 `pytest.warns(None)` 或显式断言无 `DeprecationWarning` 来自 `datetime`(WR-01 验证)。

    *注意*:已有测试 `test_e2e_dashboard_json_schema` / `test_dashboard_json_output_path` / `test_no_runs_exits_2` 在 Wave 4 已 active 通过,继续保留;本 task **不** 修改任何已有测试。仅追加上述 4 个新测试。
  </behavior>
  <action>
**步骤 1**:修改 `evolution/tools/regression_dashboard.py`(顶部 import block,~line 23-29):

把
```python
from datetime import datetime
```
改为
```python
from datetime import datetime, timezone
```

**步骤 2**:修改 `_scan_runs` 函数(line 54-60),改为双输入签名:

```python
def _scan_runs(
    roots: tuple[Path, ...],
    explicit_runs: tuple[Path, ...] = (),
) -> list[Path]:
    """Discover metrics.json paths from default roots (globbed) and explicit run dirs.

    `roots` are parent directories whose `*/metrics.json` are globbed.
    `explicit_runs` are individual run directories whose `<dir>/metrics.json`
    is loaded directly — matches `--baseline-run` / `--evolved-run` semantics
    (CR-01 / CR-02 fix). The two channels are unioned and de-duplicated.

    Returns paths sorted by mtime ascending (oldest first).
    """
    found: list[Path] = []
    for root in roots:
        if root.exists():
            found.extend(root.glob("*/metrics.json"))
    for run_dir in explicit_runs:
        mp = Path(run_dir) / "metrics.json"
        if mp.exists():
            found.append(mp)
    # De-duplicate while preserving order
    seen: set[Path] = set()
    uniq: list[Path] = []
    for p in found:
        rp = p.resolve()
        if rp not in seen:
            seen.add(rp)
            uniq.append(p)
    return sorted(uniq, key=lambda p: p.stat().st_mtime)
```

**步骤 3**:修改 main 的 `_scan_runs` 调用点(line 623-627),改为:

```python
# Build root list: defaults only (--runs goes to explicit_runs channel below).
roots = tuple(DEFAULT_ROOTS)
explicit_runs = tuple(Path(r) for r in runs)
runs_paths = _scan_runs(roots, explicit_runs)
```

(**注意**:这与 16-04-PLAN.md 中 `roots = list(DEFAULT_ROOTS); for r in runs: roots.append(Path(r))` 的旧逻辑相比,把 `--runs` 从 `roots` 通道**移到** `explicit_runs` 通道 — 这是 CR-01/CR-02 fix 的核心改动。`--baseline-run` / `--evolved-run` 已经独立读 `<dir>/metrics.json`,不受此改动影响。)

**步骤 4**:修改 `_write_dashboard_json` 中的 `generated_at`(line 563):

把
```python
"generated_at": datetime.utcnow().isoformat() + "Z",
```
改为
```python
"generated_at": datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ"),
```

(用 `strftime` 是为了保留尾部 `Z` 而不是 `+00:00`,与 16-04 测试中的字符串模式兼容。)

**步骤 5**:修改 `--trend-days` cutoff(line 703),与 generated_at 用同一 UTC 基准:

把
```python
cutoff = datetime.now().timestamp() - (trend_days * 86400.0)
```
改为
```python
cutoff = datetime.now(timezone.utc).timestamp() - (trend_days * 86400.0)
```

(`stat().st_mtime` 本身就是 epoch UTC,所以 cutoff 也用 UTC 才一致。)

**步骤 6**:修复 `_load_run` 契约不一致(IN-01)。当前 docstring 说「return None on parse error」但代码返回 dict。改为代码对齐 docstring — parse error 时 return None,caller 已经在 line 642-647 处理了 None 分支(虽然按当前实现走不到,但改完后真正可达):

修改 `_load_run` line 69-72:

把
```python
try:
    metrics = json.loads(metrics_path.read_text(encoding="utf-8"))
except (json.JSONDecodeError, OSError) as e:
    return {"path": str(metrics_path), "metrics": None, "source": None, "_drop_reason": f"json parse error: {e}"}
```
改为
```python
try:
    metrics = json.loads(metrics_path.read_text(encoding="utf-8"))
except (json.JSONDecodeError, OSError) as e:
    return {
        "path": str(metrics_path.parent),
        "metrics": None,
        "source": None,
        "_drop_reason": f"json parse error: {e}",
    }
```

(**实际修改**:`str(metrics_path)` → `str(metrics_path.parent)`,让 dropped_runs[*].path 与 usable_runs[*].path 用同一约定 — run **directory**,而不是 `<dir>/metrics.json`。这让 `test_json_corrupt_fixture_drops_with_reason` 断言「path 含 'json_corrupt'」对仍成立(parent 仍含 dir 名),并与 line 645 `dropped.append({"path": str(p), ...})` 的 caller 路径风格一致。同时**保留** 返回 dict 而不是 None — IN-01 docstring 修改采用「与代码对齐 docstring」方向:把 docstring 改成「Return dict with metrics=None on parse error」,而不是改代码契约。把 line 64 docstring 改为:)

把 line 64-68 docstring:
```python
    """Return {path, source, metrics, ab_comparison?} or None on parse error.

    Also returns None for runs that are unrecoverable (status=FAILED, missing
    per_tool_*_rates) — those appear in dropped_runs[] via the caller.
    """
```
改为
```python
    """Return {path, source, metrics, ab_comparison?} record.

    On JSON/OS parse error returns a record with `metrics=None`,
    `source=None`, and a `_drop_reason` string explaining the failure;
    the caller surfaces these in `dropped_runs[]`. Never returns None
    (IN-01: docstring previously claimed None on parse error, code did
    not — this docstring matches the code).
    """
```

并把 line 63 签名 `Optional[dict]` 改为 `dict`:
```python
def _load_run(metrics_path: Path) -> dict:
```

并删除 caller 处(line 642)`if loaded is None or loaded.get("metrics") is None:` 中的 `loaded is None or` 部分,改为:
```python
if loaded.get("metrics") is None:
```
(line 645 的 `loaded.get(..., "load failed") if loaded else "load failed"` 也简化为 `loaded.get("_drop_reason", "load failed")`,因为 `loaded` 永远是 dict。)

**步骤 7**:修复 `_render_diff` 的 `None vs None` title(WR-05)。修改 line 329-333:

把
```python
table = Table(
    title=(
        f"DIFF region [{baseline_run['source']} vs {evolved_run['source']}]: "
        f"{baseline_run['path']} → {evolved_run['path']}"
    ),
    show_lines=False,
)
```
改为
```python
b_src = baseline_run["source"] or "?"
e_src = evolved_run["source"] or "?"
table = Table(
    title=(
        f"DIFF region [{b_src} vs {e_src}]: "
        f"{baseline_run['path']} → {evolved_run['path']}"
    ),
    show_lines=False,
)
```

**步骤 8**:修复 `_render_frequency_bars` / `_render_latest` 的空字符串鬼柱(WR-06)。修改 line 210:

把
```python
sample_counts: Counter = Counter(rec.get("correct_tool", "") for rec in raw_preds)
```
改为
```python
sample_counts: Counter = Counter(
    tool for tool in (rec.get("correct_tool", "") for rec in raw_preds) if tool
)
```

**步骤 9**:在 `tests/tools/test_regression_dashboard.py` 末尾追加 4 个新测试(不动现有 17 个测试一行):

```python
# ── Wave 5 (gap closure) tests ────────────────────────────────────────────


def test_scan_runs_resolves_explicit_path(tmp_path, monkeypatch):
    """16-05-G1 (CR-01/CR-02) — `--runs <run-dir>` resolves <dir>/metrics.json directly,
    matching `--baseline-run` semantics. Does NOT patch `_scan_runs` so the real
    glob/explicit-path resolution is exercised end-to-end.
    """
    monkeypatch.setattr(
        "evolution.tools.regression_dashboard.DEFAULT_ROOTS", ()
    )
    out_json = tmp_path / "dashboard.json"
    runner = CliRunner()
    result = runner.invoke(
        main,
        [
            "--runs", str(FIXTURES / "params_complete"),
            "--output", str(out_json),
            "--trend-window", "3",
        ],
        catch_exceptions=False,
    )
    assert result.exit_code == 0, f"stdout: {result.output}"
    assert out_json.exists()
    data = json.loads(out_json.read_text())
    assert data["scanned_runs"] >= 1, (
        f"--runs <fixture-dir> should resolve <dir>/metrics.json (CR-01); "
        f"got scanned_runs={data['scanned_runs']}"
    )
    assert data["latest"] is not None, (
        "fixture run not loaded as latest — CR-01 not fixed"
    )
    assert "params_complete" in data["latest"]["run_path"], (
        f"latest.run_path should reference the fixture dir; "
        f"got {data['latest']['run_path']}"
    )


def test_scan_runs_glob_and_explicit_combined(tmp_path, monkeypatch):
    """16-05-G2 (CR-01) — glob roots + explicit --runs both contribute; no double-count."""
    # Build a tmp glob root with one run subdir (copy of params_complete)
    glob_root = tmp_path / "glob_root"
    fake_run = glob_root / "20260101_000000"
    fake_run.mkdir(parents=True)
    import shutil
    shutil.copy(
        FIXTURES / "params_complete" / "metrics.json",
        fake_run / "metrics.json",
    )
    monkeypatch.setattr(
        "evolution.tools.regression_dashboard.DEFAULT_ROOTS", (glob_root,)
    )
    out_json = tmp_path / "dashboard.json"
    runner = CliRunner()
    result = runner.invoke(
        main,
        [
            "--runs", str(FIXTURES / "params_complete_v2"),
            "--output", str(out_json),
        ],
        catch_exceptions=False,
    )
    assert result.exit_code == 0, f"stdout: {result.output}"
    data = json.loads(out_json.read_text())
    # 1 from glob root + 1 from explicit --runs == 2
    assert data["scanned_runs"] == 2, (
        f"glob root + explicit --runs should contribute 2; "
        f"got {data['scanned_runs']}; dropped={data['dropped_runs']}"
    )


def test_json_corrupt_fixture_drops_with_reason(tmp_path, monkeypatch):
    """16-05-G3 (IN-02) — json_corrupt fixture surfaces in dropped_runs with json parse error."""
    monkeypatch.setattr(
        "evolution.tools.regression_dashboard.DEFAULT_ROOTS", ()
    )
    out_json = tmp_path / "dashboard.json"
    runner = CliRunner()
    result = runner.invoke(
        main,
        [
            "--runs", str(FIXTURES / "params_complete"),
            "--runs", str(FIXTURES / "json_corrupt"),
            "--output", str(out_json),
        ],
        catch_exceptions=False,
    )
    assert result.exit_code == 0, f"stdout: {result.output}"
    data = json.loads(out_json.read_text())
    corrupt_drop = [
        d for d in data["dropped_runs"] if "json_corrupt" in d["path"]
    ]
    assert len(corrupt_drop) == 1, (
        f"json_corrupt should appear in dropped_runs once; "
        f"got {[d['path'] for d in data['dropped_runs']]}"
    )
    assert "json parse error" in corrupt_drop[0]["reason"], (
        f"reason should mention 'json parse error'; got: {corrupt_drop[0]['reason']}"
    )


def test_dashboard_json_no_datetime_deprecation_warning(tmp_path, monkeypatch):
    """16-05-G4 (WR-01) — dashboard run emits no DeprecationWarning from datetime.utcnow()."""
    import warnings
    monkeypatch.setattr(
        "evolution.tools.regression_dashboard.DEFAULT_ROOTS", ()
    )
    out_json = tmp_path / "dashboard.json"
    runner = CliRunner()
    with warnings.catch_warnings(record=True) as caught:
        warnings.simplefilter("always")
        result = runner.invoke(
            main,
            [
                "--runs", str(FIXTURES / "params_complete"),
                "--output", str(out_json),
            ],
            catch_exceptions=False,
        )
    assert result.exit_code == 0, f"stdout: {result.output}"
    utc_now_warnings = [
        w for w in caught
        if issubclass(w.category, DeprecationWarning)
        and "utcnow" in str(w.message).lower()
    ]
    assert utc_now_warnings == [], (
        f"datetime.utcnow() should not appear; got: "
        f"{[str(w.message) for w in utc_now_warnings]}"
    )
```

**严格不动**:Wave 1-4 已经 active 的 17 个测试函数(line 25-531)。本 task 只 append。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_regression_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'from datetime import datetime, timezone' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -n 'def _scan_runs' evolution/tools/regression_dashboard.py` 输出 1 行,且其后 5 行内必有 `explicit_runs` 字样:`sed -n '/^def _scan_runs/,/^def /p' evolution/tools/regression_dashboard.py | grep -c 'explicit_runs'` ≥ 3
    - `grep -n 'explicit_runs = tuple(Path(r) for r in runs)' evolution/tools/regression_dashboard.py` 输出 1 行(main 调用点改造)
    - `grep -n 'runs_paths = _scan_runs(roots, explicit_runs)' evolution/tools/regression_dashboard.py` 输出 1 行
    - `grep -v '^[[:space:]]*#' evolution/tools/regression_dashboard.py | grep -c 'datetime\.utcnow()'` 输出 `0`(零残留;用 `grep -v '^#'` 过滤注释避免 self-invalidating gate)
    - `grep -n 'datetime.now(timezone.utc)' evolution/tools/regression_dashboard.py` 输出至少 2 行(generated_at + trend-days cutoff)
    - `grep -n 'b_src = baseline_run\["source"\] or "?"' evolution/tools/regression_dashboard.py` 输出 1 行(WR-05 fix)
    - `grep -n 'for tool in (rec.get("correct_tool", "") for rec in raw_preds) if tool' evolution/tools/regression_dashboard.py` 输出 1 行(WR-06 fix)
    - `grep -n 'def test_scan_runs_resolves_explicit_path' tests/tools/test_regression_dashboard.py` 输出 1 行
    - `grep -n 'def test_scan_runs_glob_and_explicit_combined' tests/tools/test_regression_dashboard.py` 输出 1 行
    - `grep -n 'def test_json_corrupt_fixture_drops_with_reason' tests/tools/test_regression_dashboard.py` 输出 1 行
    - `grep -n 'def test_dashboard_json_no_datetime_deprecation_warning' tests/tools/test_regression_dashboard.py` 输出 1 行
    - **CR-01 真测试覆盖断言**:`grep -B 2 'def test_scan_runs_resolves_explicit_path' tests/tools/test_regression_dashboard.py | grep -c 'patch.*_scan_runs'` 输出 `0`(该测试函数定义前不能有 `patch._scan_runs` 装饰器);并且 `sed -n '/def test_scan_runs_resolves_explicit_path/,/^def test_/p' tests/tools/test_regression_dashboard.py | grep -c 'patch.*_scan_runs'` 输出 `0`(函数体内也不 patch `_scan_runs`)
    - `.venv/bin/pytest tests/tools/test_regression_dashboard.py -x` 退出码 0(原 17 + 新 4 = 21 个测试全绿)
    - `.venv/bin/pytest tests/tools/ -x` 退出码 0(tools 测试目录整体回归绿,确认其他 phase 测试未破)
    - **手动 smoke**(CR-01/CR-02 真实流量验证):`cd /tmp && python -m evolution.tools.regression_dashboard --runs $REPO_ROOT/tests/fixtures/dashboard_runs/params_complete --output /tmp/smoke.json && python -c "import json; d=json.load(open('/tmp/smoke.json')); assert d['scanned_runs']>=1 and 'params_complete' in d['latest']['run_path'], d"` 退出码 0(`$REPO_ROOT` = `git rev-parse --show-toplevel`)
  </acceptance_criteria>
  <done>
    `--runs` 与 `--baseline-run` 语义统一;`datetime.utcnow()` 零残留 + `--trend-days` 用 UTC;DIFF 不再 `None vs None`;频次柱不再有空名鬼柱;`_load_run` docstring 与代码契约对齐;**至少 4 个新测试中至少 1 个不 patch `_scan_runs`**,真实 fixture 端到端覆盖 CR-01/CR-02;21 个 dashboard 测试全绿;原 Phase 13/14/15/16 Wave 0-4 测试零破坏。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-05-2: reasoning metric 一致性 + descriptions FAILED 分支 persist 顺序反转</name>
  <files>evolution/tools/evolve_tool_reasoning.py, evolution/tools/evolve_tool_descriptions.py, tests/tools/test_evolve_tool_reasoning.py, tests/tools/test_evolve_tool_descriptions.py</files>
  <read_first>
    - evolution/tools/evolve_tool_reasoning.py(整文件 ~720 行,重点 line 1-60 imports / line 465-505 holdout + ThinkABGate / line 628-681 `_score_with_predictions` / line 684-702 `_safe_score`)
    - evolution/tools/evolve_tool_descriptions.py(line 1-60 imports / line 336-381 holdout + REGRESSION_FAILED 早 return / line 404-440 happy-path metrics_extra + metrics dict literal)
    - evolution/tools/evolve_tool_params.py(line 382-432 `_evaluate_holdout` per-example logging 模板 — task 1 要镜像;line 1015-1029 sibling pattern — Phase 13 的 correct 接线方式,task 2 在 descriptions 端复制)
    - evolution/tools/tool_metric.py(line 329-362 `joint_tool_param_metric` — CR-03 要采用的 metric;line 442-477 `persist_per_tool_rates` + line 480-520 `persist_raw_predictions`)
    - tests/tools/test_evolve_tool_reasoning.py(整文件,理解现有 mock 模式)
    - tests/tools/test_evolve_tool_descriptions.py(整文件,理解现有 mock 模式)
    - .planning/phases/16-per-tool-regression-dashboard/16-VERIFICATION.md(权威 gap — truth #1 / #6 / WR-04 论证)
    - .planning/phases/16-per-tool-regression-dashboard/16-REVIEW.md(CR-03 line 122-152 / WR-04 line 201-215 / WR-07 line 246-261 fix snippets)
    - .planning/phases/16-per-tool-regression-dashboard/16-00-PLAN.md(原始决策「不在 FAILED 分支注入 metrics_extra」位于 objective 段第 71 行 — 本 task 显式反转)
  </read_first>
  <behavior>
    - **test_score_with_predictions_uses_joint_metric**:构造一个 fake module(返回 mock prediction 含 `selected_tool` 和 `selected_params`)与 2 个 dspy.Example(其中 1 个 `correct_tool` 大小写差异如 `'Read_File'` vs `'read_file'`)。调 `_score_with_predictions(module, examples, lm=None)`,断言:
      1. 返回的 `score` 是浮点,且**值与** `joint_tool_param_metric(ex, pred)` 直接对每条 ex 算的平均**精确相等**(证明 metric 一致 — CR-03 fix)
      2. **不**用 `correct == selected` 大小写敏感语义(用大小写差异样本断言:`'Read_File'` vs `'read_file'` 应当算正确,即 score == 1.0 而非旧的 0.0)
      3. 返回的 `raw_preds` 仍含 4-key schema(`correct_tool / selected_tool / difficulty / num_available_tools`),且 `correct_tool` 保留原大小写(不被 normalization 污染)
    - **test_score_with_predictions_logs_per_example_error**:构造一个 module,其 `__call__` 第一条 ex 抛 `RuntimeError("simulated LM failure")`,第二条 ex 正常返回。用 `capsys`(或 `monkeypatch` Rich console)捕获 stdout,断言:
      1. stdout 含 `"holdout example skipped"` 或类似 yellow 警告字串(WR-07 关闭 — 与 `_evaluate_holdout` line 388-398 风格一致)
      2. 返回的 `n` 有效预测数为 1(只有第二条),`raw_preds` 长度为 1
    - **test_regression_failed_metrics_carry_per_tool_rates**:构造 `evolve_tool_descriptions` happy-path mock 到 holdout 评估完成,但让 `CrossToolRegressionChecker.check_regression` 返回 `passed=False`。读 `output/tools/FAILED_<ts>/metrics.json` → 断言:
      1. `"per_tool_baseline_rates" in metrics` ∧ `"per_tool_evolved_rates" in metrics` ∧ `"raw_predictions" in metrics`(WR-04 关闭)
      2. `metrics["status"] == "REGRESSION_FAILED"`(状态不被覆盖)
      3. `metrics["regressed_tools"]` 仍存在(Phase 13 原字段保留)
      4. 每条 `raw_predictions` 含精确 4 keys
  </behavior>
  <action>
### 子步骤 A — `evolve_tool_reasoning.py` 改造(CR-03 + WR-07)

**A.1** 在 imports block(line 1-50)新增 `joint_tool_param_metric` 导入。找到现有
```python
from evolution.tools.tool_metric import (
    CrossToolRegressionChecker,
    persist_per_tool_rates,
    persist_raw_predictions,
)
```
改为
```python
from evolution.tools.tool_metric import (
    CrossToolRegressionChecker,
    joint_tool_param_metric,
    persist_per_tool_rates,
    persist_raw_predictions,
)
```

**A.2** 修改 `_score_with_predictions`(line 628-681)。把循环内
```python
correct = getattr(ex, "correct_tool", "") or ""
selected = getattr(pred, "selected_tool", "") or ""
tool_pairs.append((correct, selected))
raw_preds.append({
    "correct_tool": correct,
    "selected_tool": selected,
    "difficulty": getattr(ex, "difficulty", "medium") or "medium",
    "num_available_tools": len(getattr(ex, "confuser_tools", []) or []) + 1,
})
total += 1.0 if correct == selected else 0.0
n += 1
```
**整段**替换为
```python
correct = getattr(ex, "correct_tool", "") or ""
selected = getattr(pred, "selected_tool", "") or ""
tool_pairs.append((correct, selected))
raw_preds.append({
    "correct_tool": correct,
    "selected_tool": selected,
    "difficulty": getattr(ex, "difficulty", "medium") or "medium",
    "num_available_tools": len(getattr(ex, "confuser_tools", []) or []) + 1,
})
# CR-03 (Phase 16 gap closure): use joint_tool_param_metric — same as
# _safe_score → _score_module_on_holdout, so ThinkABGate.check() compares
# coherent (full vs ambiguous) score pairs. The previous raw
# `correct == selected` was case-sensitive tool-only and made the gate
# apples-vs-oranges. joint_tool_param_metric normalizes via
# .strip().lower() and combines 0.5 tool_match + 0.5 param_match.
try:
    sample_score = float(joint_tool_param_metric(ex, pred))
except Exception:
    sample_score = 0.0
total += sample_score
n += 1
```

**A.3** 同函数 per-example exception 路径(原 line 665-666):
```python
try:
    pred = module(task_description=ex.task_description)
except Exception:
    continue
```
改为(WR-07 — 与 `evolve_tool_params._evaluate_holdout` line 388-398 一致):
```python
try:
    pred = module(task_description=ex.task_description)
except Exception as e:
    console.print(
        f"[yellow]holdout example skipped due to LM error: "
        f"{type(e).__name__}: {e}[/yellow]"
    )
    continue
```

(`console` 在 evolve_tool_reasoning.py 中已经是 module-level 变量 — 确认 line 1-50 有 `console = Console()` 或类似引用。如无,则在 imports 后追加 `console = Console()`。)

**A.4** 同函数 batch-level except(line 680-681):
```python
except Exception:
    return 0.0, tool_pairs, raw_preds
```
改为
```python
except Exception as e:
    console.print(
        f"[yellow]_score_with_predictions batch-level failure: "
        f"{type(e).__name__}: {e}; returning partial result "
        f"(n={n}, raw_preds={len(raw_preds)})[/yellow]"
    )
    return float(total / n) if n else 0.0, tool_pairs, raw_preds
```

(改进:原 `return 0.0` 丢弃了已经积累的 score / tool_pairs / raw_preds;返回 partial result 与 verifier 期望的「distinguish crash from 0.0」对齐 — 至少 caller 看到 n=0 vs n>0 的差异。)

### 子步骤 B — `evolve_tool_descriptions.py` 改造(WR-04)

**B.1** 找到 line 336-381 holdout 评估块,在 `regression_result = regression_checker.check_regression(...)` 调用之后、`if not regression_result.passed:` 早 return 之**前**,**插入** persist 调用块:

具体修改 — 把当前 line 366-369(也就是 `regression_result = ...` 紧接 `if not regression_result.passed:` 之前)与 line 381 的 return 之间的逻辑改为:

把这一段:
```python
regression_checker = CrossToolRegressionChecker()
baseline_rates = regression_checker.compute_per_tool_rates(baseline_preds)
evolved_rates = regression_checker.compute_per_tool_rates(evolved_preds)
regression_result = regression_checker.check_regression(baseline_rates, evolved_rates)

if not regression_result.passed:
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    output_dir = Path("output") / "tools" / f"FAILED_{timestamp}"
    output_dir.mkdir(parents=True, exist_ok=True)
    (output_dir / "metrics.json").write_text(json.dumps({
        "timestamp": timestamp,
        "status": "REGRESSION_FAILED",
        "baseline_score": baseline_score,
        "evolved_score": evolved_score,
        "regressed_tools": regression_result.regressed_tools,
    }, indent=2))
    console.print("[red]Cross-tool regression detected -- not deploying[/red]")
    console.print(f"  Saved results to {output_dir}/")
    return
```
**整段**改为:
```python
regression_checker = CrossToolRegressionChecker()
baseline_rates = regression_checker.compute_per_tool_rates(baseline_preds)
evolved_rates = regression_checker.compute_per_tool_rates(evolved_preds)
regression_result = regression_checker.check_regression(baseline_rates, evolved_rates)

# WR-04 (Phase 16 gap closure, reverses 16-00-PLAN's "no metrics_extra
# in FAILED branch" decision): compute per_tool + raw_predictions BEFORE
# the regression-result branch so REGRESSION_FAILED metrics.json carries
# the per-tool data that the dashboard needs. Mirrors the sibling pattern
# in evolve_tool_params.py:1018-1029. Historical FAILED_<old_ts>/ runs
# are NOT backfilled (16-00-PLAN Out-of-scope §6 still holds for old data).
metrics_extra = persist_per_tool_rates({}, baseline_rates, evolved_rates)
metrics_extra = persist_raw_predictions(metrics_extra, raw_preds)

if not regression_result.passed:
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    output_dir = Path("output") / "tools" / f"FAILED_{timestamp}"
    output_dir.mkdir(parents=True, exist_ok=True)
    failed_metrics = {
        "timestamp": timestamp,
        "status": "REGRESSION_FAILED",
        "baseline_score": baseline_score,
        "evolved_score": evolved_score,
        "regressed_tools": regression_result.regressed_tools,
        **metrics_extra,
    }
    (output_dir / "metrics.json").write_text(json.dumps(failed_metrics, indent=2))
    console.print("[red]Cross-tool regression detected -- not deploying[/red]")
    console.print(f"  Saved results to {output_dir}/")
    return
```

**B.2** 删除当前 line 404-406 的重复 persist 块(因为已移到 step B.1):

把
```python
# ── Phase 16 Wave 0: persist per-tool rates + raw_predictions for dashboard ──
metrics_extra = persist_per_tool_rates({}, baseline_rates, evolved_rates)
metrics_extra = persist_raw_predictions(metrics_extra, raw_preds)
```
**整块**删除。`metrics_extra` 变量在 B.1 已经定义,happy-path 继续可见(同函数体内变量作用域)。

**B.3** happy-path 的 `metrics = {**metrics, **metrics_extra}`(line 438)保持不变 — `metrics_extra` 仍在作用域内,合并逻辑不变。

### 子步骤 C — `tests/tools/test_evolve_tool_reasoning.py` 末尾追加测试

```python
# ── Wave 5 (gap closure) tests ────────────────────────────────────────────


def test_score_with_predictions_uses_joint_metric():
    """16-05-G5 (CR-03) — _score_with_predictions uses joint_tool_param_metric,
    same as _safe_score path. Tool name comparison must be case-insensitive
    (.strip().lower()), proving the gate is now apples-to-apples.
    """
    import dspy
    from evolution.tools.evolve_tool_reasoning import _score_with_predictions
    from evolution.tools.tool_metric import joint_tool_param_metric

    # Build 2 examples; one has case-mismatched correct_tool to prove normalization
    ex1 = dspy.Example(
        task_description="read a file",
        correct_tool="Read_File",  # uppercase
        correct_params={},
        confuser_tools=["edit_file"],
        difficulty="easy",
    ).with_inputs("task_description")
    ex2 = dspy.Example(
        task_description="search code",
        correct_tool="search_files",
        correct_params={},
        confuser_tools=["read_file", "list_files"],
        difficulty="medium",
    ).with_inputs("task_description")

    class FakeModule:
        def __init__(self):
            self.calls = []
        def __call__(self, task_description):
            # Return prediction whose selected_tool matches correct_tool (lowercased)
            self.calls.append(task_description)
            if "read a file" in task_description:
                return dspy.Prediction(selected_tool="read_file", selected_params="{}")
            return dspy.Prediction(selected_tool="search_files", selected_params="{}")

    module = FakeModule()
    score, tool_pairs, raw_preds = _score_with_predictions(
        module, [ex1, ex2], lm=None
    )
    # Both should score 1.0 if metric is case-insensitive (joint_tool_param_metric)
    assert score == pytest.approx(1.0), (
        f"case-insensitive joint metric should score 1.0 on Read_File→read_file; "
        f"got {score} — CR-03 not fixed"
    )
    assert len(raw_preds) == 2
    # raw_preds preserves original case (not normalized)
    assert raw_preds[0]["correct_tool"] == "Read_File"


def test_score_with_predictions_logs_per_example_error(capsys):
    """16-05-G6 (WR-07) — per-example exception emits yellow log; partial result returned."""
    import dspy
    from evolution.tools.evolve_tool_reasoning import _score_with_predictions

    ex1 = dspy.Example(
        task_description="will fail",
        correct_tool="x",
        correct_params={},
        confuser_tools=[],
        difficulty="easy",
    ).with_inputs("task_description")
    ex2 = dspy.Example(
        task_description="will succeed",
        correct_tool="y",
        correct_params={},
        confuser_tools=[],
        difficulty="easy",
    ).with_inputs("task_description")

    class FlakyModule:
        def __call__(self, task_description):
            if "fail" in task_description:
                raise RuntimeError("simulated LM failure")
            return dspy.Prediction(selected_tool="y", selected_params="{}")

    score, tool_pairs, raw_preds = _score_with_predictions(
        FlakyModule(), [ex1, ex2], lm=None
    )
    # Only the second example contributes
    assert len(raw_preds) == 1
    assert raw_preds[0]["correct_tool"] == "y"
    # Yellow log captured
    captured = capsys.readouterr()
    assert "holdout example skipped" in captured.out, (
        f"expected per-example yellow log; got stdout: {captured.out!r}"
    )
```

### 子步骤 D — `tests/tools/test_evolve_tool_descriptions.py` 末尾追加测试

```python
# ── Wave 5 (gap closure) tests ────────────────────────────────────────────


def test_regression_failed_metrics_carry_per_tool_rates(tmp_path, monkeypatch):
    """16-05-G7 (WR-04, reverses 16-00-PLAN decision) — when CrossToolRegressionChecker
    rejects the run, the FAILED_<ts>/metrics.json must still carry per_tool_*_rates
    and raw_predictions so the dashboard can surface the regressed run.
    """
    # Use the sibling test's happy-path mock pattern to drive evolve() up to
    # holdout, then force regression_result.passed = False at the
    # CrossToolRegressionChecker boundary so the FAILED branch fires.
    # The simplest reliable path is to call evolve() with mocked baseline/optimized
    # modules whose predictions trigger a regression (e.g. evolved always wrong).
    # If the existing file does not already have a happy-path-to-FAILED mock,
    # copy the closest analog (look for tests that exercise the FAILED branch
    # in this file; if none exist, build one by:
    #   1. monkeypatch evolve_tool_descriptions.GEPA / MIPROv2 to no-op
    #   2. monkeypatch _evaluate_holdout-equivalent loop (or build small
    #      baseline_module/optimized_module fakes) so evolved_preds[i] ≠ correct
    #   3. ensure regression_result.passed becomes False
    # The acceptance below is on the resulting FAILED_<ts>/metrics.json schema.

    monkeypatch.chdir(tmp_path)
    # ... (executor: mirror the existing test_evolve_tool_descriptions.py
    #      pattern that drives evolve() through holdout. Build a minimal
    #      fake that returns deterministic wrong predictions for evolved
    #      so regression check fails.)

    # After evolve() returns, find the FAILED_*/metrics.json under
    # output/tools/ (relative to cwd, which is tmp_path):
    failed_dirs = list((tmp_path / "output" / "tools").glob("FAILED_*"))
    assert len(failed_dirs) == 1, (
        f"expected exactly one FAILED_<ts>/ dir; got {failed_dirs}"
    )
    metrics = json.loads((failed_dirs[0] / "metrics.json").read_text())
    assert metrics["status"] == "REGRESSION_FAILED"
    assert "regressed_tools" in metrics  # Phase 13 字段保留
    # WR-04 — Phase 16 gap closure:
    assert "per_tool_baseline_rates" in metrics, (
        f"FAILED metrics.json must carry per_tool_baseline_rates (WR-04); "
        f"keys: {sorted(metrics.keys())}"
    )
    assert "per_tool_evolved_rates" in metrics, (
        f"FAILED metrics.json must carry per_tool_evolved_rates (WR-04)"
    )
    assert "raw_predictions" in metrics, (
        f"FAILED metrics.json must carry raw_predictions (WR-04)"
    )
    assert isinstance(metrics["raw_predictions"], list)
    if metrics["raw_predictions"]:
        first = metrics["raw_predictions"][0]
        assert set(first.keys()) == {
            "correct_tool", "selected_tool", "difficulty", "num_available_tools"
        }
```

**Fallback 策略(BLOCKER-style 防 shallow execution)**: 测试函数体必须实际驱动 `evolve()` 跑到 holdout 阶段并触发 FAILED 分支 — **不**接受「直接调 `persist_per_tool_rates({}, ...)` + 写一份 mock metrics.json + 断言 schema」这种纯 helper 单测,因为那不证明 FAILED 分支真的被 evolve() 接到。验证手段:`grep -E 'evolve\(|evolve_tool_descriptions\.evolve|holdout_examples|baseline_module|optimized_module' tests/tools/test_evolve_tool_descriptions.py` 在新增测试函数体内必须命中至少 1 次。如果现有 test_evolve_tool_descriptions.py 没有可参考的「跑到 FAILED 分支」happy-path mock,executor 必须**新建**该 mock(可参考 `tests/tools/test_evolve_tool_params_cli.py` 中触发 FAILED_*/ 的现有测试模式)。
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_evolve_tool_reasoning.py tests/tools/test_evolve_tool_descriptions.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'joint_tool_param_metric' evolution/tools/evolve_tool_reasoning.py` 输出至少 2 行(import + 调用)
    - `grep -n 'sample_score = float(joint_tool_param_metric(ex, pred))' evolution/tools/evolve_tool_reasoning.py` 输出 1 行
    - `grep -v '^[[:space:]]*#' evolution/tools/evolve_tool_reasoning.py | grep -c 'total += 1.0 if correct == selected else 0.0'` 输出 `0`(旧 raw 比较被删除;过滤注释)
    - `grep -n 'holdout example skipped due to LM error' evolution/tools/evolve_tool_reasoning.py` 输出 1 行(WR-07 per-example log)
    - `grep -n '_score_with_predictions batch-level failure' evolution/tools/evolve_tool_reasoning.py` 输出 1 行(WR-07 batch-level log)
    - **WR-04 强制行号断言** — 验证 persist 调用前移:`grep -nE 'metrics_extra = persist_per_tool_rates|metrics_extra = persist_raw_predictions|if not regression_result.passed:' evolution/tools/evolve_tool_descriptions.py` 输出格式 `<lineno>:<text>`。要求满足:`lineno("metrics_extra = persist_per_tool_rates") < lineno("if not regression_result.passed:")` ∧ `lineno("metrics_extra = persist_raw_predictions") < lineno("if not regression_result.passed:")`(即 persist 调用都在 if block 之**前**,反转原顺序)
    - `grep -c 'metrics_extra = persist_per_tool_rates' evolution/tools/evolve_tool_descriptions.py` 输出 `1`(精确 1 次 — task B.2 删除了原 line 405 的重复)
    - `grep -c 'metrics_extra = persist_raw_predictions' evolution/tools/evolve_tool_descriptions.py` 输出 `1`(精确 1 次)
    - `grep -n '"regressed_tools": regression_result.regressed_tools,' evolution/tools/evolve_tool_descriptions.py` 输出 1 行(Phase 13 字段保留)
    - `grep -n '\*\*metrics_extra,' evolution/tools/evolve_tool_descriptions.py` 输出至少 1 行(FAILED 分支 dict 合并 metrics_extra)
    - `grep -n 'def test_score_with_predictions_uses_joint_metric' tests/tools/test_evolve_tool_reasoning.py` 输出 1 行
    - `grep -n 'def test_score_with_predictions_logs_per_example_error' tests/tools/test_evolve_tool_reasoning.py` 输出 1 行
    - `grep -n 'def test_regression_failed_metrics_carry_per_tool_rates' tests/tools/test_evolve_tool_descriptions.py` 输出 1 行
    - **shallow-execution 防御**:`sed -n '/def test_regression_failed_metrics_carry_per_tool_rates/,/^def /p' tests/tools/test_evolve_tool_descriptions.py | grep -cE 'evolve\(|evolve_tool_descriptions\.evolve|holdout|baseline_module|optimized_module'` 输出 ≥ 1(测试体内真的驱动 evolve() / holdout 流程,不是纯 helper 单测)
    - `.venv/bin/pytest tests/tools/test_evolve_tool_reasoning.py -x` 退出码 0
    - `.venv/bin/pytest tests/tools/test_evolve_tool_descriptions.py -x` 退出码 0
    - `.venv/bin/pytest tests/tools/ -x` 退出码 0(整 tools 目录回归绿)
    - `.venv/bin/pytest tests/ -x` 退出码 0(整套件回归绿 — Phase 13/14/15 测试无破坏)
    - **CR-03 概念断言** — 旧的「`th_off_full` 与 `th_off_ambig` 用不同 metric」状态被消除:确认 `_score_with_predictions` 内部 `total +=` 表达式不再是 `correct == selected`,而是 `joint_tool_param_metric(...)` 的结果(grep 已覆盖,行号一致性由 sed 段落确认)
  </acceptance_criteria>
  <done>
    `_score_with_predictions` 与 `_safe_score` 使用相同的 `joint_tool_param_metric`,`ThinkABGate.check()` 接收同源 (full vs ambiguous) 指标 — CR-03 关闭。`_score_with_predictions` per-example + batch 异常都打 yellow 日志 — WR-07 关闭。`evolve_tool_descriptions` REGRESSION_FAILED 分支的 metrics.json 携带 per_tool_*_rates + raw_predictions — WR-04 关闭(显式反转 16-00-PLAN 原决策,理由文档化)。所有 tools 测试 + 全套件回归绿。
  </done>
</task>

</tasks>

<verification>
**Phase 16 Gap Closure 完成标志**:

1. **测试套件全绿**:
   - `.venv/bin/pytest tests/tools/test_regression_dashboard.py -x` 退出码 0(原 17 + 新 4 = 21 个测试)
   - `.venv/bin/pytest tests/tools/test_evolve_tool_reasoning.py -x` 退出码 0
   - `.venv/bin/pytest tests/tools/test_evolve_tool_descriptions.py -x` 退出码 0
   - `.venv/bin/pytest tests/ -x` 退出码 0(全套件,Phase 13/14/15/16 Wave 0-4 测试零破坏)

2. **真实流量 smoke**(CR-01/CR-02 端到端):
   - `python -m evolution.tools.regression_dashboard --runs tests/fixtures/dashboard_runs/params_complete --output /tmp/gap.json && jq '.scanned_runs, .latest.run_path' /tmp/gap.json` 输出 scanned_runs ≥ 1 且 latest.run_path 含 `params_complete`(证明 `--runs <run-dir>` 真的解析了 fixture 的 `<dir>/metrics.json`)

3. **Deprecation 干净**:
   - `grep -v '^[[:space:]]*#' evolution/tools/regression_dashboard.py | grep -c 'datetime\.utcnow'` 输出 `0`
   - `python -W error::DeprecationWarning -c "from evolution.tools import regression_dashboard"` 退出码 0(import 时不触发任何 DeprecationWarning)

4. **CR-03 metric 一致性**:
   - `grep -v '^[[:space:]]*#' evolution/tools/evolve_tool_reasoning.py | grep -c 'total += 1.0 if correct == selected else 0.0'` 输出 `0`(旧 raw 比较绝迹)
   - `grep -c 'joint_tool_param_metric(ex, pred)' evolution/tools/evolve_tool_reasoning.py` 输出 `1`(新 metric 一处使用)

5. **WR-04 行号反转**:
   - persist 两行都在 `if not regression_result.passed:` 行号之**前**(见 task 16-05-2 acceptance_criteria 详细 grep)

6. **verifier 复检准备就绪**:
   - 6 个 truths 全部可由 grep / pytest 验证:
     - Truth 1(SC1 per-tool 记录):描述 CLI 在 FAILED + happy path 都写完整字段 ✓
     - Truth 2(SC2 Rich 控制台):Wave 1-3 已绿 ✓
     - Truth 3(SC3 threshold):Wave 4 已绿 ✓
     - Truth 4(standalone CLI + `--runs`):本 plan task 1 关闭 ✓
     - Truth 5(8 字段 dashboard.json):Wave 4 已绿 + 本 plan 未破坏 ✓
     - Truth 6(reasoning per-tool 一致 metric):本 plan task 2 关闭 ✓
</verification>

<success_criteria>
- 3 个 BLOCKER(CR-01 / CR-02 / CR-03)全部关闭,grep / pytest 可验证
- 1 个 WARNING(WR-04)关闭,显式反转 16-00-PLAN 原决策并文档化反转理由
- 4 个相邻 hygiene(WR-01 / WR-02 / WR-05 / WR-06 / WR-07 / IN-01 / IN-02)关闭
- 至少 1 个新测试**不** patch `_scan_runs`,真实 fixture 端到端覆盖 CR-01
- IN-02 `json_corrupt/` fixture 现在被新测试引用,parse error drop 路径有覆盖
- 原 17 个 dashboard 测试 + 4 个新测试 = 21 个测试 active passed,0 skipped
- 整套件 `.venv/bin/pytest tests/ -x` 退出码 0,Phase 13/14/15/16 Wave 0-4 测试零破坏
- `python -W error::DeprecationWarning -c "from evolution.tools import regression_dashboard"` 干净
- TOOL-V2-04 requirement 在 REQUIREMENTS.md 可以更新为 Complete(verifier 复检 6/6 truths 后)
</success_criteria>

<output>
After completion, create `.planning/phases/16-per-tool-regression-dashboard/16-05-SUMMARY.md` with:
- **Closed gaps**: CR-01, CR-02, CR-03, WR-04 (BLOCKER + 1 WARNING) + WR-01, WR-02, WR-05, WR-06, WR-07, IN-01, IN-02 (hygiene)
- **Deferred (explicit)**: WR-03(`_load_run` 2-3× 调用 — 性能,影响小,留 todo),IN-03(测试注释 -5pp 误标,纯文档),IN-04 / IN-05 / IN-06(其他)
- **Reversed decision**: 16-00-PLAN「不在 FAILED 分支注入 metrics_extra」原决策被本 plan 显式反转,反转理由(verifier 论证 + sibling `evolve_tool_params` 一致性)
- **Metric choice**: `_score_with_predictions` 采用 `joint_tool_param_metric`(理由:对齐 `_safe_score → _score_module_on_holdout` 现有语义;对齐 `evolve_tool_params._evaluate_holdout` sibling)
- **Test coverage delta**: 4 个新 dashboard 测试(1 个不 patch `_scan_runs` — CR-01 真测试覆盖)+ 2 个新 reasoning 测试 + 1 个新 descriptions 测试 = 7 个新测试
- **Files modified**: 3 source(regression_dashboard.py / evolve_tool_reasoning.py / evolve_tool_descriptions.py)+ 3 test(同名)
- **Phase 16 verifier 复检准备**:6 truths 全部 verified 预期 — 4/6 → 6/6
- **ROADMAP.md update**:把 Phase 16 plans count 改为 `7/6 plans complete`,追加 `**Wave 5** *(gap closure)* - [x] 16-05-PLAN.md — CR-01/CR-02/CR-03/WR-04 + hygiene` 行
- **REQUIREMENTS.md update prep**:TOOL-V2-04 可在 verifier 复检通过后改 `Complete`(本 plan 不直接改,留给 `/gsd-verify-phase 16` 重新跑后由 verifier 自动收尾)
</output>
</content>
