---
phase: 17-joint-section-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evolution/prompts/prompt_module.py
  - tests/prompts/test_prompt_module.py
autonomous: true
requirements: [PMPT-V2-01]
requirements_addressed: [PMPT-V2-01]
tags: [dspy, gepa, prompt-evolution, joint-optimization, state-machine]

must_haves:
  truths:
    # ROADMAP §Phase 17 Success Criterion 1
    - "PromptModule.set_joint_mode(True) 后,全部 N 个 section 的 dspy.Predict 都在 section_predictors 中,且 named_predictors() 全部可见"
    # Resolved decision 1 — Pitfall 1 fix (round-robin path included)
    - "forward() 在 _active_section 为单 sid 时,把 section_predictors[active].signature.instructions 拼入 frozen_context(修复 Pitfall 1),GEPA 反思后的 instructions 真实影响 selector 输出"
    # Resolved decision 1 — Joint mode three-state dispatch
    - "forward() 在 _active_section == '__JOINT__' 时,把全部 N 个 section 的 instructions(从 section_predictors[sid].signature.instructions 读)拼入 frozen_context,送入同一个 self.selector"
    # Resolved decision 1 — None state raises
    - "forward() 在 _active_section is None 时仍抛 RuntimeError(向后兼容,与现有 14 个测试用例一致)"
    # Resolved decision 2 — Selector freeze in joint mode
    - "joint mode 下 self.selector.predict 通过 _frozen_predictor_ids guard 不暴露给 GEPA — 只有 13 个 section_predictors 是 GEPA mutable"
    # Pitfall 3 fix
    - "set_active_section(sid) 在 _active_section == JOINT_SENTINEL 时自动退化(先 demote 全部 Predict 回 frozen,再走原逻辑),不抛 KeyError"
  artifacts:
    - path: "evolution/prompts/prompt_module.py"
      provides: "set_joint_mode() 方法 + JOINT_SENTINEL 常量 + forward 三态分发 + selector freeze"
      contains: "def set_joint_mode"
      contains_2: "JOINT_SENTINEL"
      contains_3: "__JOINT__"
    - path: "tests/prompts/test_prompt_module.py"
      provides: "新增 TestJointMode 类(精确 7 测试,W7 revision)+ 现有 14 测试零回归 = 总计 21 测试"
      contains: "class TestJointMode"
  key_links:
    - from: "evolution/prompts/prompt_module.py:set_joint_mode"
      to: "evolution/prompts/prompt_module.py:forward"
      via: "_active_section = JOINT_SENTINEL 标记 + section_predictors dict 全填"
      pattern: "self\\._active_section = JOINT_SENTINEL"
    - from: "evolution/prompts/prompt_module.py:forward (joint branch)"
      to: "evolution/prompts/prompt_module.py:self.selector"
      via: "frozen_context = concat 全部 section_predictors[sid].signature.instructions"
      pattern: "section_predictors\\[.*\\]\\.signature\\.instructions"
---

<objective>
扩展 `PromptModule` 状态机为三态:None / `__JOINT__` 哨兵 / 单 section sid。joint mode 下全部 N 个 section 的 instructions 升级为 `dspy.Predict`(通过 `section_predictors` dict 暴露给 GEPA `named_predictors()`),selector 自身保持冻结。Pitfall 1 一并修复:round-robin 单 active 路径下,active section 的 instructions 现在真正流入 `selector.frozen_context`,使 GEPA 反思的 mutation 实际影响 forward 输出。

Purpose: 解锁 Phase 17 后续两个 plan(CLI joint pipeline + 软门 A/B baseline)— 没有这个状态机,GEPA `component_selector="all"` 没有可优化的多 Predict 容器。

Output:
- `evolution/prompts/prompt_module.py` 扩展(`set_joint_mode()` + `JOINT_SENTINEL` 常量 + forward 三态 + Pitfall 1 修复 + Pitfall 3 自动退化)
- `tests/prompts/test_prompt_module.py` 新增 `TestJointMode` 测试类(覆盖 5+ 行为),现有 14 测试零回归(必要时小幅调整 mock 期望以匹配 Pitfall 1 修复后的语义,但绝不删除测试)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-joint-section-optimization/17-CONTEXT.md
@.planning/phases/17-joint-section-optimization/17-RESEARCH.md
@.planning/phases/17-joint-section-optimization/17-PATTERNS.md
@.planning/phases/17-joint-section-optimization/17-VALIDATION.md
@.planning/phases/08-prompt-module/08-CONTEXT.md

<interfaces>
<!-- 现有 PromptModule 关键 API(已实测),executor 直接据此扩展,无需重新探索代码库 -->

From evolution/prompts/prompt_module.py L33-99:

```python
class PromptSectionSignature(dspy.Signature):
    """Given a task and system prompt context, respond following the active section's guidance."""
    frozen_context: str = dspy.InputField(...)
    task_input: str = dspy.InputField(...)
    output: str = dspy.OutputField(...)

class PromptModule(dspy.Module):
    section_predictors: dict[str, dspy.Predict]   # 已是 dict,GEPA 自动暴露
    _frozen_instructions: dict[str, str]          # 普通字符串,不暴露
    _section_ids: list[str]
    _active_section: str | None
    _frozen_sections: dict[str, PromptSection]
    selector: dspy.ChainOfThought  # ChainOfThought(PromptSectionSignature)

    def set_active_section(self, section_id: str) -> None:
        # L80-99: pop frozen string -> build dspy.Signature("section_text -> confirmation", instructions=text)
        # -> wrap in dspy.Predict(sig) -> store in section_predictors[sid]
        # 切换时把 current active 的 Predict.signature.instructions 移回 _frozen_instructions

    def forward(self, task_input: str) -> dspy.Prediction:
        # L101-122: if _active_section is None: RuntimeError
        # else: frozen_context = self._build_frozen_context()
        #       result = self.selector(frozen_context=frozen_context, task_input=task_input)
        #       return dspy.Prediction(output=result.output)

    def _build_frozen_context(self) -> str:
        # L124-131: 现仅拼接非 active 的 _frozen_instructions[sid](Pitfall 1 根源:active 文本未流入)
        parts = []
        for sid in self._section_ids:
            if sid != self._active_section:
                text = self._frozen_instructions[sid]
                parts.append(f"[{sid}]: {text}")
        return "\n\n".join(parts)

    def get_evolved_sections(self) -> list[PromptSection]:
        # L133-155: 已支持二选一(sid in section_predictors 则读 Predict.signature.instructions,否则读 _frozen_instructions[sid])
        # joint mode 下所有 sid 都在 section_predictors,自然走 Predict 分支,零改动
```

From dspy 3.1.3(本地 venv 实测,RESEARCH §Pattern 4):
- `dspy.Module.named_predictors()` 自动遍历 `dict[str, Predict]` 字段下所有 entries,格式 `"section_predictors['<sid>']"`
- `dspy.ChainOfThought(sig).predict` 是底层 Predict,默认会被 GEPA 看见 — 本 plan 需要在 joint mode 下排除它(Resolved decision 2)
- `dspy.Signature("section_text -> confirmation", instructions=text)` + `dspy.Predict(sig)` 是 set_active_section 现有模式,joint mode 复刻
</interfaces>

<resolved_decisions>
<!-- 由 plan-phase 编排器锁定,与 CONTEXT.md 同等约束力 -->
1. **Pitfall 1 修复(forward 三态 + round-robin 一并修)** — joint 路径 concat 全部 sections;round-robin 单 active 路径也改 `_build_frozen_context()` 让 active section 的 instructions 从 `section_predictors[active].signature.instructions` 读取并拼入(不再仅拼 frozen)。
2. **Selector 冻结(joint mode)** — joint mode 下 `self.selector.predict` 不暴露给 GEPA。实现方式:在 `PromptModule.__init__` 添加 `_frozen_predictor_ids: set[str] = {"selector.predict"}` 注释 + 实际依靠 named_predictors() 在 joint mode 下不返回 selector — 本研究 §Pattern 4 实测显示 `selector.predict` 默认也会被列出。**实现路径:**在 `set_joint_mode(True)` 中调用 `self.selector.predict._compiled = True` 或 setattr 一个 freeze flag — 但 dspy 实际 API 是通过 **重写 named_predictors() 排除 selector**(详见 Action 步骤),保持其他部分不变。
</resolved_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 PromptModule — JOINT_SENTINEL 常量 + set_joint_mode() + forward 三态分发 + Pitfall 1 修复 + Pitfall 3 自动退化 + selector freeze</name>

  <read_first>
    - evolution/prompts/prompt_module.py(完整,156 行)— 现有 set_active_section L71-99 + forward L101-122 + _build_frozen_context L124-131 + get_evolved_sections L133-155 是镜像基础
    - .planning/phases/17-joint-section-optimization/17-RESEARCH.md §Architecture Patterns Pattern 2 (set_joint_mode 设计)+ Pattern 3 (forward 三态)+ Pitfall 1/3/8 + §Pattern 4 (named_predictors() 实测)
    - .planning/phases/17-joint-section-optimization/17-PATTERNS.md §Pattern A1/A2/A3/A4(state-switch helper / forward 三态 / frozen context / get_evolved 兼容性)
    - .planning/phases/17-joint-section-optimization/17-CONTEXT.md `<decisions>` D-RR-01(round-robin 共存)、Claude's Discretion 前两条
    - 该 plan 头部 `<resolved_decisions>` 块(Pitfall 1 修复 + selector freeze 两条 LOCKED 决策)
  </read_first>

  <files>evolution/prompts/prompt_module.py</files>

  <behavior>
    在 RED 单测执行后,以下行为为 GREEN 通过条件:
    - Test 1 (set_joint_mode_exposes_all_predictors): `module.set_joint_mode(True)` 后 `len(module.section_predictors) == N`(N = sections 数,fixture 用 3)、`len(module._frozen_instructions) == 0`、`module._active_section == JOINT_SENTINEL`("__JOINT__")
    - Test 2 (set_joint_mode_idempotent): 连调两次 `set_joint_mode(True)` 不抛、state 不变(`len(section_predictors) == N`)
    - Test 3 (set_joint_mode_false_demotes_all): `set_joint_mode(False)` 后 `len(section_predictors) == 0`、`len(_frozen_instructions) == N`、`_active_section is None`
    - Test 4 (joint_then_set_active_section_auto_demotes — Pitfall 3): `set_joint_mode(True)` 后调 `set_active_section("memory_guidance")`,断言:`_active_section == "memory_guidance"`、`section_predictors` 仅含 memory_guidance、其它两段回到 `_frozen_instructions` — 不抛 KeyError
    - Test 5 (named_predictors_in_joint_mode_excludes_selector): joint mode 下 `[n for n, _ in module.named_predictors()]` 含 N 个 `"section_predictors['<sid>']"` 但 **不含** 任何 `"selector"` 子串(selector frozen 决策)
    - Test 6 (forward_in_joint_mode_uses_all_section_texts): joint mode 下 mock `module.selector.forward` 返回 fake Prediction,调 `module.forward("test")`,断言 selector mock 被 called_once + `call_args.kwargs["frozen_context"]` 含全部 N 个 `[<sid>]:` 前缀 + 各自 instructions 文本
    - Test 7 (forward_in_round_robin_includes_active_text — Pitfall 1 修复): 单 active 模式下 `module.set_active_section("memory_guidance")` 然后 mock selector + forward("test"),`call_args.kwargs["frozen_context"]` **必须**含 `[memory_guidance]:` 前缀和 memory_guidance instructions(修复前不含)
    - Test 8 (forward_without_active_still_raises): `_active_section is None` 时 forward 仍抛 RuntimeError("No active section")— 与现有 `test_forward_without_active_raises` 同语义,新增不重复但确认 None 分支未被破坏
  </behavior>

  <action>
    在 `evolution/prompts/prompt_module.py` 做以下精确修改(保留所有现有 docstring 与命名风格):

    **1. 在 `import dspy` 后、`class PromptSectionSignature` 前加入常量(L11 后):**

    ```python
    # ── Constants ───────────────────────────────────────────────────────────────

    JOINT_SENTINEL = "__JOINT__"
    """Sentinel value for PromptModule._active_section in joint optimization mode.

    Indicates that all sections are simultaneously promoted to Predict instances
    in section_predictors, making them all discoverable by named_predictors()
    and mutable by GEPA with component_selector='all'.
    """
    ```

    **2. 修改 `PromptModule.__init__`(L49-69),在末尾加 `self._frozen_predictor_ids` 集合用于 selector freeze 判定(注释充分,不增其他逻辑):**

    ```python
    # In __init__, after self.selector = dspy.ChainOfThought(PromptSectionSignature) line:
    # IDs of internal predictors that must stay frozen (invisible to GEPA) in joint mode.
    # selector.predict is the underlying Predict of self.selector (ChainOfThought),
    # which exists for forward routing only — GEPA must not mutate its instructions.
    self._frozen_predictor_ids: set[str] = {"selector.predict"}
    ```

    **3. 新增方法 `set_joint_mode(active: bool = True) -> None`(放在 `set_active_section` L71-99 之后):**

    ```python
    def set_joint_mode(self, active: bool = True) -> None:
        """Activate or deactivate joint optimization mode (all sections simultaneously).

        In joint mode (active=True), every section's instruction text is promoted
        from a plain string in _frozen_instructions to a dspy.Predict instance in
        section_predictors. This makes all sections discoverable by
        named_predictors() and mutable by GEPA when called with
        component_selector='all'.

        Joint mode is idempotent — calling set_joint_mode(True) twice is a no-op.
        Calling set_active_section(sid) while in joint mode auto-demotes joint
        first (see set_active_section guard).

        Args:
            active: True to enter joint mode, False to demote all back to frozen.
        """
        if active:
            if self._active_section == JOINT_SENTINEL:
                return  # idempotent
            # If a single section is currently active, move it back to frozen first
            if self._active_section is not None:
                pred = self.section_predictors.pop(self._active_section)
                self._frozen_instructions[self._active_section] = (
                    pred.signature.instructions
                )
            # Promote ALL frozen sections to Predict instances
            for sid in list(self._frozen_instructions.keys()):
                text = self._frozen_instructions.pop(sid)
                sig = dspy.Signature(
                    "section_text -> confirmation",
                    instructions=text,
                )
                self.section_predictors[sid] = dspy.Predict(sig)
            self._active_section = JOINT_SENTINEL
        else:
            # Demote: move every Predict back to frozen string
            for sid in list(self.section_predictors.keys()):
                pred = self.section_predictors.pop(sid)
                self._frozen_instructions[sid] = pred.signature.instructions
            self._active_section = None
    ```

    **4. 在 `set_active_section` 入口加入 Pitfall 3 自动退化 guard。修改 L86-90 区块:**

    将现有:
    ```python
    # Move current active back to frozen (extract instructions from Predict)
    if self._active_section is not None:
        pred = self.section_predictors.pop(self._active_section)
        self._frozen_instructions[self._active_section] = (
            pred.signature.instructions
        )
    ```

    替换为:
    ```python
    # Pitfall 3 guard: if currently in joint mode, demote all first (auto-recover)
    if self._active_section == JOINT_SENTINEL:
        self.set_joint_mode(False)
    # Move current active back to frozen (extract instructions from Predict)
    elif self._active_section is not None:
        pred = self.section_predictors.pop(self._active_section)
        self._frozen_instructions[self._active_section] = (
            pred.signature.instructions
        )
    ```

    **5. 修改 `forward()`(L101-122)为三态分发,joint 分支用全段 concat:**

    替换 L101-122 整段为:
    ```python
    def forward(self, task_input: str) -> dspy.Prediction:
        """Respond to task using current optimization mode's section assembly.

        Three-state dispatch:
        - _active_section is None → RuntimeError (no section set; backward-compat)
        - _active_section == JOINT_SENTINEL → joint mode: concat ALL section
          instructions (from section_predictors[sid].signature.instructions) as
          frozen_context, feed selector once. GEPA mutations to any
          section_predictors[sid].signature.instructions thus actually affect
          forward output (precondition for component_selector='all' to work).
        - _active_section == <real_sid> → round-robin mode: build frozen_context
          including the active section's CURRENT Predict instructions
          (Pitfall 1 fix — previously the active text was not flowing into
          selector, making GEPA mutations no-ops at forward time).

        Args:
            task_input: The task or user message to respond to.

        Returns:
            dspy.Prediction with output attribute.

        Raises:
            RuntimeError: If no active section has been set.
        """
        if self._active_section is None:
            raise RuntimeError(
                "No active section set. Call set_active_section() "
                "or set_joint_mode() first."
            )
        frozen_context = self._build_frozen_context()
        result = self.selector(
            frozen_context=frozen_context,
            task_input=task_input,
        )
        return dspy.Prediction(output=result.output)
    ```

    **6. 修改 `_build_frozen_context()`(L124-131)— 两处:joint 路径读 Predict.instructions;round-robin 路径也读 Predict.instructions(Pitfall 1 修复):**

    替换 L124-131 整段为:
    ```python
    def _build_frozen_context(self) -> str:
        """Concatenate section texts as context string.

        Joint mode (_active_section == JOINT_SENTINEL): concat ALL N sections
        from section_predictors[sid].signature.instructions (the GEPA-mutable
        Predict instances).

        Round-robin mode (_active_section is a real sid): concat all N sections
        — non-active from _frozen_instructions[sid] (plain strings), active
        from section_predictors[active].signature.instructions (Pitfall 1 fix:
        the active section's current instructions must flow into selector for
        GEPA mutations to have any effect on forward output).
        """
        parts = []
        for sid in self._section_ids:
            if self._active_section == JOINT_SENTINEL:
                # Joint mode: ALL sections live as Predicts
                text = self.section_predictors[sid].signature.instructions
            elif sid == self._active_section:
                # Round-robin active: read from Predict (Pitfall 1 fix)
                text = self.section_predictors[sid].signature.instructions
            else:
                # Round-robin frozen: read from string
                text = self._frozen_instructions[sid]
            parts.append(f"[{sid}]: {text}")
        return "\n\n".join(parts)
    ```

    **7. 覆盖 `named_predictors()` 以在 joint mode 下排除 selector(Resolved decision 2 — selector freeze):**

    在 `get_evolved_sections()`(L133)前加入:
    ```python
    def named_predictors(self):
        """Override to exclude self.selector in joint mode (Phase 17 decision).

        In joint mode, only section_predictors entries should be visible to GEPA
        with component_selector='all'. The selector is a routing-only primitive
        whose instructions must remain stable across optimization runs.

        In round-robin mode, selector remains visible (existing behavior — see
        Phase 8 tests that expect selector.predict to be discoverable).
        """
        for name, pred in super().named_predictors():
            if self._active_section == JOINT_SENTINEL and name in self._frozen_predictor_ids:
                continue
            yield name, pred
    ```

    **不修改:** `get_evolved_sections()`(L133-155 已天然兼容:joint mode 下所有 sid 都在 section_predictors,自然走 Predict 分支)、`PromptSectionSignature`、`__init__` 的其他部分。

    Do NOT 在 `forward()` 的 round-robin 路径里循环调用全部 Predict — RESEARCH §Anti-Patterns 禁止该模式(N 倍 LM 成本 + GEPA 无法归因)。
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/prompts/test_prompt_module.py -v -x</automated>
  </verify>

  <done>
    - `grep -q "JOINT_SENTINEL = \"__JOINT__\"" evolution/prompts/prompt_module.py` 返回 0
    - `grep -q "def set_joint_mode" evolution/prompts/prompt_module.py` 返回 0
    - `grep -q "def named_predictors" evolution/prompts/prompt_module.py` 返回 0
    - `grep -q "Pitfall 3 guard" evolution/prompts/prompt_module.py` 返回 0
    - `grep -c "section_predictors\[.*sid.*\]\.signature\.instructions" evolution/prompts/prompt_module.py | head -1` 返回 ≥ 2(joint 与 round-robin 两个路径都引用)
    - Task 2 RED tests 编辑后,本任务实现使 `pytest tests/prompts/test_prompt_module.py -v -x` **精确 21** 测试 GREEN(14 现有 + 7 新 TestJointMode tests,W7 revision)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增 TestJointMode 测试类 + 调整 Pitfall 1 修复影响的现有测试期望(零删除)</name>

  <read_first>
    - tests/prompts/test_prompt_module.py(完整,224 行)— 全部 14 现有测试为基线,`_make_prompt_sections()` fixture 在 L15-39
    - .planning/phases/17-joint-section-optimization/17-PATTERNS.md §Pattern C1/C2/C3(test class + state-switch 单测样板 + mock selector)
    - .planning/phases/17-joint-section-optimization/17-RESEARCH.md §Validation Architecture(Phase Requirements → Test Map)+ §Pitfall 9(实测 14 tests,非 CONTEXT 估的 11)
    - 本 plan Task 1 的 `<behavior>` 块(8 个测试行为契约)
  </read_first>

  <files>tests/prompts/test_prompt_module.py</files>

  <behavior>
    Task 1 的 8 个 behavior 契约对应以下 7 个新测试方法 + 1 处现有测试微调(W7 revision: 数量从「5 个新测试」校正为「7 个新测试」— Task 1 behavior 块 Test 1-Test 7 实际对应 7 个测试,Test 8 是与现有 `test_forward_without_active_raises` 同语义注释不新增):
    - 现有 `TestFrozenContext::test_frozen_context_excludes_active` (L111-120):该测试之前 assert `"[memory_guidance]" not in context`(active 不在 frozen_context),但 Pitfall 1 修复后 active 也在了 frozen_context — 该测试 assert 现在错误。**调整方法:**改 assertion 为 `assert "[memory_guidance]:" in context`(active 应在 frozen_context,带 Pitfall 1 修复后的语义),并加注释 `# Phase 17 / Pitfall 1 fix: active section text now flows into frozen_context`。这是 CONTEXT 资源 1 决议允许的「测试在 assert bug 时可修改」案例。
    - 全部其他 13 现有测试(`TestPromptModule` 3 + `TestActiveSection` 3 + `TestFrozenContext::test_only_active_in_named_parameters` + `TestForward` 2 + `TestGetEvolvedSections` 4)零改动通过。
    - 7 个新测试方法在新增 `TestJointMode` 类中(W7 revision)。
  </behavior>

  <action>
    在 `tests/prompts/test_prompt_module.py` 文件末尾(L224 后)添加新测试类 `TestJointMode`,并就地修改 `TestFrozenContext::test_frozen_context_excludes_active`。

    **1. 修改 `TestFrozenContext::test_frozen_context_excludes_active`(L111-120):**

    将原方法 body:
    ```python
    def test_frozen_context_excludes_active(self):
        """_build_frozen_context() excludes active section, includes others."""
        sections = _make_prompt_sections()
        module = PromptModule(sections)
        module.set_active_section("memory_guidance")
        context = module._build_frozen_context()

        assert "[memory_guidance]" not in context
        assert "[default_agent_identity]" in context
        assert "[skills_guidance]" in context
    ```

    替换为(改名 `test_frozen_context_includes_active` + 反转 assertion + 加注释):
    ```python
    def test_frozen_context_includes_active(self):
        """_build_frozen_context() includes ALL sections (Phase 17 / Pitfall 1 fix).

        Pre-Phase-17 the active section was excluded from frozen_context, which
        meant GEPA mutations to the active section's Predict.signature.instructions
        had no path to the selector's input — mutations were effectively no-ops.
        Phase 17 fixes this: the active section's CURRENT instructions (read from
        section_predictors[active].signature.instructions) are now part of
        frozen_context. See evolution/prompts/prompt_module.py _build_frozen_context.
        """
        sections = _make_prompt_sections()
        module = PromptModule(sections)
        module.set_active_section("memory_guidance")
        context = module._build_frozen_context()

        # Phase 17 / Pitfall 1 fix: active section text now flows into frozen_context
        assert "[memory_guidance]:" in context
        # Active text content (from the Predict's signature.instructions) is present
        assert "Use memory tools to store important context." in context
        assert "[default_agent_identity]:" in context
        assert "[skills_guidance]:" in context
    ```

    **2. 在文件末尾(L224 后)添加 `TestJointMode` 类(完整 7 测试,W7 revision: 数量校正,test names 与 Task 1 behavior block Test 1-7 一对一对应):**

    ```python


    # ── TestJointMode (Phase 17) ───────────────────────────────────────────────


    class TestJointMode:
        """Tests for Phase 17 joint mode: all sections simultaneously optimizable."""

        def test_set_joint_mode_exposes_all_predictors(self):
            """set_joint_mode(True) promotes all sections to section_predictors."""
            from evolution.prompts.prompt_module import JOINT_SENTINEL

            sections = _make_prompt_sections()
            module = PromptModule(sections)
            module.set_joint_mode(True)

            assert len(module.section_predictors) == 3
            assert set(module.section_predictors.keys()) == {
                "default_agent_identity",
                "memory_guidance",
                "skills_guidance",
            }
            assert len(module._frozen_instructions) == 0
            assert module._active_section == JOINT_SENTINEL
            # Each predictor has the original instructions
            assert (
                module.section_predictors["memory_guidance"].signature.instructions
                == "Use memory tools to store important context."
            )

        def test_set_joint_mode_idempotent(self):
            """Calling set_joint_mode(True) twice is a no-op (no errors, no state change)."""
            from evolution.prompts.prompt_module import JOINT_SENTINEL

            sections = _make_prompt_sections()
            module = PromptModule(sections)
            module.set_joint_mode(True)
            module.set_joint_mode(True)  # Should not raise or duplicate

            assert len(module.section_predictors) == 3
            assert module._active_section == JOINT_SENTINEL

        def test_set_joint_mode_false_demotes_all(self):
            """set_joint_mode(False) reverses joint mode — all sections back to frozen."""
            sections = _make_prompt_sections()
            module = PromptModule(sections)
            module.set_joint_mode(True)
            module.set_joint_mode(False)

            assert len(module.section_predictors) == 0
            assert len(module._frozen_instructions) == 3
            assert module._active_section is None

        def test_joint_then_set_active_section_auto_demotes(self):
            """Calling set_active_section after set_joint_mode auto-demotes joint (Pitfall 3 fix).

            Without the auto-demote guard, set_active_section would try to
            pop JOINT_SENTINEL from section_predictors -> KeyError.
            """
            sections = _make_prompt_sections()
            module = PromptModule(sections)
            module.set_joint_mode(True)
            module.set_active_section("memory_guidance")  # Must not raise

            assert module._active_section == "memory_guidance"
            assert len(module.section_predictors) == 1
            assert "memory_guidance" in module.section_predictors
            # Other two are back in frozen
            assert "default_agent_identity" in module._frozen_instructions
            assert "skills_guidance" in module._frozen_instructions

        def test_named_predictors_in_joint_mode_excludes_selector(self):
            """In joint mode, named_predictors() yields exactly N section_predictors
            entries, NOT selector.predict (Phase 17 Resolved Decision 2 — selector freeze).

            W4 revision: tightened from substring `"selector" in n` to exact-token
            checks. The previous loose substring match would silently pass if a
            future DSPy version introduced subcomponents like `selector.reasoning`
            that happen to contain the substring `"selector"` but are NOT the
            routing Predict we intend to freeze. We now check:
              (1) `selector.predict` is not in names (the exact name that
                  super().named_predictors() yields for the ChainOfThought's
                  underlying Predict — see RESEARCH §Pattern 4 dspy 3.1.3 inspection).
              (2) total length equals exactly 3 (one per fixture section), which
                  fails loudly if any new subcomponent slips through.
            """
            sections = _make_prompt_sections()
            module = PromptModule(sections)
            module.set_joint_mode(True)

            named = list(module.named_predictors())
            names = [n for n, _ in named]
            # Must contain all 3 section_predictors entries
            for sid in ["default_agent_identity", "memory_guidance", "skills_guidance"]:
                matching = [n for n in names if sid in n]
                assert len(matching) >= 1, (
                    f"section_predictors[{sid}] missing from named_predictors(): {names}"
                )
            # W4: Tight selector exclusion check — exact-name, not substring
            assert "selector.predict" not in names, (
                f"selector.predict must be excluded in joint mode, but it's in: {names}"
            )
            # W4: Total entry count must equal section count exactly — fails loud
            # if dspy adds any new sub-predictor that bypasses _frozen_predictor_ids
            assert len(named) == 3, (
                f"Joint mode must expose exactly 3 predictors (one per section); "
                f"got {len(named)}: {names}. If dspy version added new subcomponents, "
                f"update _frozen_predictor_ids in PromptModule.__init__ accordingly."
            )

        def test_forward_in_joint_mode_uses_all_section_texts(self):
            """In joint mode, forward() builds frozen_context containing ALL N section texts."""
            sections = _make_prompt_sections()
            module = PromptModule(sections)
            module.set_joint_mode(True)

            mock_result = dspy.Prediction(output="joint mocked response")
            with patch.object(module.selector, "forward", return_value=mock_result) as mock_sel:
                result = module.forward("test input")

            assert isinstance(result, dspy.Prediction)
            assert result.output == "joint mocked response"
            mock_sel.assert_called_once()
            kwargs = mock_sel.call_args.kwargs
            assert "frozen_context" in kwargs
            ctx = kwargs["frozen_context"]
            # All 3 sections must appear in frozen_context with their texts
            assert "[default_agent_identity]:" in ctx
            assert "You are a helpful AI assistant." in ctx
            assert "[memory_guidance]:" in ctx
            assert "Use memory tools to store important context." in ctx
            assert "[skills_guidance]:" in ctx
            assert "Leverage available skills for complex tasks." in ctx
            assert kwargs["task_input"] == "test input"

        def test_forward_in_round_robin_includes_active_text(self):
            """In round-robin (single active) mode, forward() includes active section's
            CURRENT Predict instructions in frozen_context (Pitfall 1 fix)."""
            sections = _make_prompt_sections()
            module = PromptModule(sections)
            module.set_active_section("memory_guidance")

            mock_result = dspy.Prediction(output="rr mocked response")
            with patch.object(module.selector, "forward", return_value=mock_result) as mock_sel:
                module.forward("test input")

            kwargs = mock_sel.call_args.kwargs
            ctx = kwargs["frozen_context"]
            # Pitfall 1 fix: active section text flows into frozen_context
            assert "[memory_guidance]:" in ctx
            assert "Use memory tools to store important context." in ctx
            # Other sections also present
            assert "[default_agent_identity]:" in ctx
            assert "[skills_guidance]:" in ctx
    ```
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/prompts/test_prompt_module.py -v -x</automated>
  </verify>

  <done>
    - `grep -q "class TestJointMode" tests/prompts/test_prompt_module.py` 返回 0
    - `grep -c "def test_" tests/prompts/test_prompt_module.py` 返回**精确等于** 21(W7 revision: 14 现有 + 7 新 TestJointMode tests = 21 — 改名不影响计数;`>= 20` 容忍丢失一个测试,改为 exact match 防静默 drop)
    - `grep -q "test_set_joint_mode_exposes_all_predictors" tests/prompts/test_prompt_module.py` 返回 0
    - `grep -q "test_joint_then_set_active_section_auto_demotes" tests/prompts/test_prompt_module.py` 返回 0
    - `grep -q "test_named_predictors_in_joint_mode_excludes_selector" tests/prompts/test_prompt_module.py` 返回 0
    - `grep -q "test_forward_in_joint_mode_uses_all_section_texts" tests/prompts/test_prompt_module.py` 返回 0
    - `grep -q "test_forward_in_round_robin_includes_active_text" tests/prompts/test_prompt_module.py` 返回 0
    - `grep -q "test_frozen_context_includes_active" tests/prompts/test_prompt_module.py` 返回 0(改名后的现有测试)
    - `grep -q "test_frozen_context_excludes_active" tests/prompts/test_prompt_module.py` 返回非 0(旧名应已被替换)
    - `.venv/bin/python -m pytest tests/prompts/test_prompt_module.py -v -x` 全部 PASS(19+ 测试)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python class API → DSPy GEPA optimizer | `section_predictors` dict 暴露给 GEPA `named_predictors()`,joint mode 决定哪些 Predict 可被 mutate(selector 必须 frozen) |
| 测试 fixture(`_make_prompt_sections`)→ PromptModule | 测试用合成 PromptSection 输入(本地 fixture,无网络) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `set_joint_mode()` 状态机被并发或乱序调用导致 KeyError(Pitfall 3) | mitigate | `set_active_section` 入口 guard 检测 `_active_section == JOINT_SENTINEL` 时先 `set_joint_mode(False)` 自动退化;Test 4 `test_joint_then_set_active_section_auto_demotes` 覆盖 |
| T-17-02 | Tampering | GEPA 经 `component_selector="all"` 错误地 mutate `self.selector.predict.signature.instructions`(routing template,非用户可见 prompt 段) | mitigate | 覆盖 `named_predictors()`:joint mode 下排除 `selector.predict`(Resolved Decision 2);Test 5 `test_named_predictors_in_joint_mode_excludes_selector` 覆盖 |
| T-17-03 | Information Disclosure | `_frozen_predictor_ids` 集合或 JOINT_SENTINEL 字符串若被外部代码访问 / 修改 | accept | 单进程内部状态,无网络/磁盘暴露;不持久化到 metrics.json 或 hermes-agent;Python 约定下划线前缀已表明私有 |
| T-17-04 | Denial of Service | 重复调用 `set_joint_mode(True)` 导致 section_predictors 重复填充或内存增长 | mitigate | 入口 idempotent guard:`if self._active_section == JOINT_SENTINEL: return`;Test 2 `test_set_joint_mode_idempotent` 覆盖 |

低威胁面理由:Phase 17 是纯内部 Python 类扩展,无网络 / 用户输入(测试 fixture 是本地常量)/ 磁盘持久化 — 唯一攻击面是 DSPy GEPA reflection_lm 看到的 prompt 文本,该路径由 Phase 9 PromptDatasetBuilder + Phase 10 PromptRoleChecker 已有 mitigation 覆盖,本 plan 不引入新数据流。
</threat_model>

<verification>
- `grep -q "JOINT_SENTINEL = \"__JOINT__\"" evolution/prompts/prompt_module.py` 返回 0
- `grep -q "def set_joint_mode" evolution/prompts/prompt_module.py` 返回 0
- `grep -q "def named_predictors" evolution/prompts/prompt_module.py` 返回 0
- `grep -q "self._frozen_predictor_ids" evolution/prompts/prompt_module.py` 返回 0
- `grep -q "class TestJointMode" tests/prompts/test_prompt_module.py` 返回 0
- `.venv/bin/python -m pytest tests/prompts/test_prompt_module.py -v -x` 返回 exit 0,且 collected **精确等于** 21 个测试,全部 PASS(W7 revision: 14 baseline + 7 new TestJointMode)
- `.venv/bin/python -c "from evolution.prompts.prompt_module import PromptModule, JOINT_SENTINEL; assert JOINT_SENTINEL == '__JOINT__'"` 返回 exit 0
- `.venv/bin/python -m pytest tests/ -x -q` 返回 exit 0(零回归,确认本 plan 修改未破坏 Phase 1-16 任何测试)
</verification>

<success_criteria>
- ROADMAP §Phase 17 Success Criterion 1 关键基础设施就位:`set_joint_mode(True)` 后所有 N section 通过 `named_predictors()` 可被 GEPA 发现
- Pitfall 1 修复就位(active section text 现在真正流入 selector,无论 joint 还是 round-robin 模式)
- Pitfall 3 自动退化就位(joint → set_active_section 不抛 KeyError)
- selector freeze 就位(`named_predictors()` 在 joint mode 排除 selector.predict)
- 现有 14 + 7 新 TestJointMode = **精确 21** 测试全部 PASS(W7 revision: exact count,防 silent test drop)
- 全项目测试套件零回归(`pytest tests/ -x -q` 通过)
</success_criteria>

<output>
After completion, create `.planning/phases/17-joint-section-optimization/17-01-SUMMARY.md` documenting:
- 实际修改的行号区间(prompt_module.py 与 test_prompt_module.py)
- 实际通过的测试数量与名单
- 任何 Pitfall 1 修复对 Phase 8/9/10 其他测试的影响(若有)
- `JOINT_SENTINEL` 与 `_frozen_predictor_ids` 接口供 Plan 17-02 消费(它们是 plan 间契约)
</output>
