---
phase: 17-joint-section-optimization
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - evolution/prompts/evolve_prompt_sections.py
  - tests/prompts/test_evolve_prompt_sections_cli.py
autonomous: true
requirements: [PMPT-V2-01]
requirements_addressed: [PMPT-V2-01]
tags: [click, cli, gepa, joint-optimization, dspy-component-selector]

must_haves:
  truths:
    # ROADMAP §Phase 17 Success Criterion 2: GEPA can mutate multiple sections in one pass
    - "CLI `python -m evolution.prompts.evolve_prompt_sections --mode joint` 时仅调用 `dspy.GEPA(component_selector='all').compile(...)` 一次,而非按 section 13 次"
    # D-RR-04 + D-RR-01 + D-RR-02
    - "`--mode` 是 `click.Choice(['joint', 'round-robin'])` 默认 `joint`(静默切换,无 deprecation 警告);round-robin 路径保留为 legacy fallback"
    # D-RR-03
    - "`--section <sid>` 与 `--mode joint` 同时传时按 round-robin 单点处理,不报错(隐含 round-robin 单点优化)"
    # Resolved decision 4 — num_predictors-dynamic budget formula
    - "joint 分支 `max_metric_calls = max(iterations * 50, 3 * num_predictors) * num_predictors` 与 Phase 13 evolve_tool_params.py L805-807 风格对齐;`num_predictors` 运行时由 `len(list(module.named_predictors()))` 取值(joint mode 下 = section 数,selector frozen)"
    # D-IT-03
    - "CLI 启动时(非 dry-run)向 stdout 打印预算预估行,含 'Joint optimization:'、'Round-robin A/B baseline:'、'Total est. LM calls:' 三行,数值由 num_predictors 动态计算"
    # Wave 2 dry-run also prints budget
    - "`--dry-run --mode joint` 时 stdout 同样包含三行预算预估,且不进入真实 GEPA.compile()"
    # W2 revision — joint mode loud-fail invariant (D-15a parity)
    - "joint 分支 GEPA 失败时异常向上 propagate(NO try/except / NO MIPROv2 fallback,与 Phase 13 D-15a 'loud GEPA failure' 模式一致);round-robin 分支保留现有 GEPA→MIPROv2 fallback 链(legacy behavior unchanged)。这是 joint mode 与 round-robin mode 的关键行为差异,任何未来 PR 不得静默引入 joint 分支的 try/except 兜底。"
    # W1 revision — helper extraction prevents drift between dry-run and main path
    - "mode resolution(`--section` 隐式 RR 路由,D-RR-03)由单一 helper `_resolve_effective_mode(section, mode) -> str` 实现,在 dry-run gate 与 main optimization 入口都调用此 helper(避免两处重复条件表达式逻辑漂移)"
  artifacts:
    - path: "evolution/prompts/evolve_prompt_sections.py"
      provides: "--mode flag + joint pipeline 分支 + num_predictors-dynamic 预算 + stdout 预算预估 + _resolve_effective_mode helper"
      contains: "click.Choice([\"joint\", \"round-robin\"])"
      contains_2: "component_selector"
      contains_3: "Joint optimization:"
      contains_4: "_resolve_effective_mode"
    - path: "tests/prompts/test_evolve_prompt_sections_cli.py"
      provides: "新 CLI 集成测试文件(CliRunner + fake GEPA mock 风格)"
      contains: "class TestJointPipeline"
      contains_2: "class TestDryRun"
  key_links:
    - from: "evolution/prompts/evolve_prompt_sections.py:evolve (joint branch)"
      to: "dspy.GEPA"
      via: "component_selector='all' + max_metric_calls=max(iterations*50, 3*num_predictors)*num_predictors"
      pattern: "component_selector\\s*=\\s*[\"']all[\"']"
    - from: "evolution/prompts/evolve_prompt_sections.py:evolve"
      to: "evolution.prompts.prompt_module.set_joint_mode"
      via: "module.set_joint_mode(True) before GEPA.compile in joint branch"
      pattern: "set_joint_mode\\(True\\)"
    - from: "evolution/prompts/evolve_prompt_sections.py:_resolve_effective_mode"
      to: "evolution/prompts/evolve_prompt_sections.py:evolve (dry-run gate AND main path)"
      via: "single helper called from both code paths (W1: prevents mode-resolution drift)"
      pattern: "_resolve_effective_mode\\(section,\\s*mode\\)"
    - from: "tests/prompts/test_evolve_prompt_sections_cli.py"
      to: "evolution/prompts/evolve_prompt_sections.py:main"
      via: "CliRunner.invoke(main, [\"--mode\", \"joint\", ...]) + multi-patch dspy.GEPA"
      pattern: "CliRunner"
---

<objective>
扩展 `evolve_prompt_sections.py` 的 Click CLI 接入 joint mode 作为新默认。CLI 加 `--mode joint|round-robin` flag,joint 分支调用 `module.set_joint_mode(True)` 后用 GEPA `component_selector="all"` + `num_predictors`-dynamic budget 做单次 `compile()`;round-robin 分支保留现有 per-section for-loop。新建 `tests/prompts/test_evolve_prompt_sections_cli.py` 覆盖三类 CLI 调用:`--mode joint`(默认)、`--mode round-robin`(legacy)、`--section X`(任意 mode 自动 RR)、`--dry-run` 输出含预算预估。

Purpose: 把 Phase 17-01 的 PromptModule 三态状态机暴露给真实运行管道。这是 ROADMAP §Phase 17 Success Criterion 2(GEPA 单 pass mutate 多 section)的 CLI 落地。

Output:
- `evolution/prompts/evolve_prompt_sections.py` 扩展(`--mode` flag + `evolve(mode=...)` 参数 + joint pipeline 分支 + budget 预估 stdout + `--section` 隐式 RR 路由 + `_resolve_effective_mode` helper)
- `tests/prompts/test_evolve_prompt_sections_cli.py` 新建(≥ 4 测试,fake GEPA mock 风格借鉴 Phase 13 `tests/tools/test_evolve_tool_params_cli.py`)
- 现有 `tests/prompts/test_evolve_prompt_sections.py` 零回归(它的 mock 链已被默认 mode=joint 影响时,改造路径仍兼容,具体见 Task 2 的回归处理说明)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-joint-section-optimization/17-CONTEXT.md
@.planning/phases/17-joint-section-optimization/17-RESEARCH.md
@.planning/phases/17-joint-section-optimization/17-PATTERNS.md
@.planning/phases/17-joint-section-optimization/17-VALIDATION.md
@.planning/phases/17-joint-section-optimization/17-01-SUMMARY.md
@.planning/phases/13-per-parameter-description-optimization/13-08-SUMMARY.md

<interfaces>
<!-- 从 Plan 17-01 提供的新接口(由 17-01-SUMMARY 文档化) -->

From evolution/prompts/prompt_module.py (after Plan 17-01):
- `JOINT_SENTINEL: str = "__JOINT__"` (constant)
- `PromptModule.set_joint_mode(active: bool = True) -> None`
- `PromptModule.named_predictors()` overridden:joint mode 下排除 selector(返回仅 section_predictors entries)

From evolution/tools/evolve_tool_params.py(Phase 13 镜像锚点 — executor 直接复刻 CLI 装配模式):
- L579-581: `@click.option("--component-selector", default="round_robin", type=click.Choice(["round_robin", "all"]), help="...")`
- L729: `num_predictors = len(list(baseline_module.named_predictors()))` 运行时取值
- L743-751: dry-run stdout budget echo(`click.echo(f"max_metric_calls_estimate={budget_estimate}")` 风格)
- L793-808: GEPA init kwargs 字典 + `component_selector` + `max_metric_calls=max(iterations*50, 3*num_predictors)`
- L797-799: `seed=0`, `track_stats=True`

From tests/tools/test_evolve_tool_params_cli.py L64-108(Phase 13 fake-GEPA mock 风格,executor 直接复刻):
- `runner = CliRunner()`
- `with patch("evolution.tools.evolve_tool_params._load_tool_descriptions", return_value=[fake_tool]), patch("...dspy.GEPA") as mock_gepa, patch("...dspy.LM"):`
- `mock_gepa.return_value.compile.side_effect = lambda mod, trainset, valset=None: mod`(确定性返回 module 不变)
- `result = runner.invoke(evolve, [...], catch_exceptions=True)`
- `assert mock_gepa.call_args.kwargs.get("component_selector") == "all"`

From evolution/prompts/evolve_prompt_sections.py 现有 evolve() L78-97 入口签名:
```python
def evolve(
    section: Optional[str] = None,
    iterations: int = 10,
    eval_source: str = "synthetic",
    hermes_repo: Optional[str] = None,
    dry_run: bool = False,
    model: Optional[str] = None,
    api_base: Optional[str] = None,
):
```
本 plan 在此签名后追加 `mode: str = "joint"`(默认 joint 切换默认行为)。

现有 `evolve()` L213-283 的 per-section for-loop 是 round-robin 分支的完整模板,joint 分支替换为:
- `module.set_joint_mode(True)`
- `trainset = dataset.to_dspy_examples("train", section_texts=section_texts)`(不 filter section)
- `valset = dataset.to_dspy_examples("val", section_texts=section_texts)`
- 单次 GEPA init + compile,kwargs 含 `component_selector="all"`
</interfaces>

<budget_formula_note>
本 plan 使用 `num_predictors`-dynamic budget(Resolved decision 4),覆盖 CONTEXT.md D-IT-02 写死的 `× 5` 公式:

```python
num_predictors = len(list(module.named_predictors()))
# joint mode 下 = N(section 数,selector frozen 后排除),实测 PromptModule 当前 prompt_builder.py 解出 13 section
joint_budget = max(iterations * 50, 3 * num_predictors) * num_predictors
rr_per_section_budget = iterations * 50  # 单参数原始公式
rr_total_budget = rr_per_section_budget * num_predictors
```

注:`num_predictors` 在 joint mode 下应在 `set_joint_mode(True)` 后取值,否则按 round-robin 时只能拿到 0 或 1 个 entry。
</budget_formula_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 evolve_prompt_sections CLI — --mode flag + _resolve_effective_mode helper + joint pipeline 分支 + num_predictors-dynamic budget + stdout 预算预估 + --section 隐含 RR 路由</name>

  <read_first>
    - evolution/prompts/evolve_prompt_sections.py(完整,518 行)— 现有 `evolve()` L78-518 全文为修改基础
    - evolution/tools/evolve_tool_params.py L568-630, L730-815(Phase 13 CLI 装配 + budget formula 同构镜像,**直接复刻** `@click.option`、kwargs dict、`num_predictors = len(list(module.named_predictors()))` 三个模式)
    - .planning/phases/17-joint-section-optimization/17-RESEARCH.md §Code Examples Pattern 1(GEPA component_selector="all" 调用)、Pattern 6(budget pre-flight stdout)、§V7(GEPA loud-fail 决策,W2 invariant 出处)
    - .planning/phases/17-joint-section-optimization/17-PATTERNS.md §Pattern B1/B2/B3/B5(Click flag / GEPA init / round-robin 保留 / dry-run budget)
    - .planning/phases/17-joint-section-optimization/17-CONTEXT.md `<decisions>` D-RR-01, D-RR-02, D-RR-03, D-RR-04, D-IT-01, D-IT-02, D-IT-03
    - 本 plan 头部 `<resolved_decisions>` 块 + `<budget_formula_note>` 块
    - .planning/phases/17-joint-section-optimization/17-01-SUMMARY.md(Plan 17-01 实际交付的 set_joint_mode + named_predictors 接口)
    - .planning/phases/13-per-parameter-description-optimization/13-08-SUMMARY.md §D-15a(loud GEPA failure pattern — joint branch 必须遵循,不引入 try/except 兜底)
  </read_first>

  <files>evolution/prompts/evolve_prompt_sections.py</files>

  <behavior>
    Task 2 RED 测试预期以下行为:
    - `--mode joint`(默认):`dspy.GEPA` 被调用一次,kwargs 含 `component_selector="all"`,`max_metric_calls = max(iterations*50, 3*num_predictors)*num_predictors`,`seed=0`,`track_stats=True`;`module.set_joint_mode(True)` 被调用一次;`optimizer.compile()` 被调用一次
    - `--mode round-robin`:走现有 per-section for-loop(13 次调用 `set_active_section` + 13 次调用 GEPA.compile),不调 `set_joint_mode`
    - `--section memory_guidance --mode joint`:自动按 round-robin 单点处理 — 仅 1 次 `set_active_section("memory_guidance")` + 1 次 GEPA.compile,**不**调 `set_joint_mode`,不报错
    - `--dry-run --mode joint`:stdout 包含 "Joint optimization:"、"Round-robin A/B baseline:"、"Total est. LM calls:" 三行子串,且 GEPA.compile **未**被调用(早 return)
    - W2 invariant:joint 分支若 GEPA.compile 抛异常,**不** try/except 兜底,异常向上 propagate 至 Click main(然后 sys.exit 非零);round-robin 分支保留现有 GEPA→MIPROv2 fallback
    - W1 invariant:dry-run gate 与 main optimization 都通过 `_resolve_effective_mode(section, mode)` 单一 helper 取得 effective_mode,文件内无重复 `"round-robin" if section else mode` 表达式(grep 该字面量在文件中只出现在 helper 实现内一次)
    - 现有 `tests/prompts/test_evolve_prompt_sections.py` 的 `test_evolve_orchestration_order` 等测试零回归(它默认 mode 是 joint,但其 mock 链不强制 component_selector,只要 GEPA + Module 调用顺序正确即通过)
  </behavior>

  <action>
    在 `evolution/prompts/evolve_prompt_sections.py` 做以下精确修改(保留所有现有 imports / docstring / 命名风格,使用 Console / Rich markup,不引入新依赖)。

    **按 6 步顺序执行,每步完成后 verify acceptance_criteria 中对应项,再进入下一步(W5 — partial-application 风险降低):**

    <acceptance_criteria>
      | Step | 完成标志(grep / 结构检查) |
      |------|------------------------|
      | Step 1 | `grep -q "EPSILON_PP = 0.01" evolution/prompts/evolve_prompt_sections.py` 返回 0 |
      | Step 1b | `grep -q "def _resolve_effective_mode" evolution/prompts/evolve_prompt_sections.py` 返回 0 |
      | Step 2 | `grep -q "mode: str = \"joint\"" evolution/prompts/evolve_prompt_sections.py` 返回 0(signature 含 mode 参数) |
      | Step 3 | `grep -q "effective_mode = _resolve_effective_mode(section, mode)" evolution/prompts/evolve_prompt_sections.py` 返回 0(main path 使用 helper) |
      | Step 4 | `grep -q "component_selector=\"all\"" evolution/prompts/evolve_prompt_sections.py` 返回 0;`grep -q "set_joint_mode(True)" evolution/prompts/evolve_prompt_sections.py` 返回 0;**joint 分支内无 try/except 包裹 optimizer.compile**(W2 invariant);round-robin 分支 try/except GEPA→MIPROv2 fallback 完整保留 |
      | Step 5 | `grep -q "Joint optimization:" evolution/prompts/evolve_prompt_sections.py` 返回 0(dry-run 也输出 budget preview);**dry-run gate 内也调用 `_resolve_effective_mode(section, mode)`,不直接写 `"round-robin" if section else mode` 字面量** |
      | Step 6 | `grep -q "click.Choice(\\[\"joint\", \"round-robin\"\\])" evolution/prompts/evolve_prompt_sections.py` 返回 0;`main()` 签名含 `mode` 参数;`evolve(...)` 调用末尾传 `mode=mode` |
      | Final | `grep -c "\"round-robin\" if section else mode" evolution/prompts/evolve_prompt_sections.py` 返回 **正好 1**(W1: 字面量只出现在 helper 实现一处,无重复) |
    </acceptance_criteria>

    **Step 1. 顶部 imports 后(L33 之后)添加模块常量与 `_resolve_effective_mode` helper(Plan 17-03 也会用 EPSILON_PP,先在此 plan 引入便于 Plan 17-03 直接引用):**

    ```python
    # ── Module constants ────────────────────────────────────────────────────────

    # Phase 17 / D-AB-03: Soft-gate threshold — joint mode warns (no exit) when
    # joint_score regresses by more than this fraction vs round-robin baseline.
    # Used in Plan 17-03 A/B branch; defined here to keep the file self-contained.
    EPSILON_PP = 0.01


    # ── Mode resolution helper (W1: single source of truth) ────────────────────

    def _resolve_effective_mode(section: Optional[str], mode: str) -> str:
        """Return the effective optimization mode after applying D-RR-03 routing.

        D-RR-03 implicit routing: when a single `section` is targeted, the
        round-robin single-section path is forced regardless of `mode`. This
        helper is the ONLY place this conditional lives — both the dry-run
        gate and the main optimization path call it (W1 revision: prevents
        drift between two code locations).

        Args:
            section: Optional section_id from CLI --section flag.
            mode: Raw mode string from CLI --mode flag, already validated by
                click.Choice to be "joint" or "round-robin".

        Returns:
            "round-robin" when section is non-empty; otherwise mode unchanged.
        """
        return "round-robin" if section else mode
    ```

    **Step 2. 修改 `evolve()` 函数签名(L78-97)— 在 `api_base` 之后追加 `mode` 参数:**

    将原签名:
    ```python
    def evolve(
        section: Optional[str] = None,
        iterations: int = 10,
        eval_source: str = "synthetic",
        hermes_repo: Optional[str] = None,
        dry_run: bool = False,
        model: Optional[str] = None,
        api_base: Optional[str] = None,
    ):
    ```

    替换为:
    ```python
    def evolve(
        section: Optional[str] = None,
        iterations: int = 10,
        eval_source: str = "synthetic",
        hermes_repo: Optional[str] = None,
        dry_run: bool = False,
        model: Optional[str] = None,
        api_base: Optional[str] = None,
        mode: str = "joint",
    ):
    ```

    Docstring `Args:` 部分追加:
    ```python
            mode: Optimization mode — 'joint' (default, Phase 17 — all sections
                optimized simultaneously via GEPA with component_selector='all')
                or 'round-robin' (legacy, section-by-section). When section is
                also specified, round-robin single-section path is forced
                regardless of mode (D-RR-03 implicit routing). Effective mode
                is computed via _resolve_effective_mode(section, mode).
    ```

    **Step 3. 在 `evolve()` 函数体头部(L98 之后,L99 `config = ...` 之前)加入 mode resolution(W1: 调用 helper,不再 inline 写表达式):**

    ```python
        # ── 0. Mode resolution (D-RR-03 via single helper, W1 revision) ──────
        effective_mode = _resolve_effective_mode(section, mode)
        # Click.Choice already validated the input mode is "joint" or
        # "round-robin"; the helper applies the --section implicit routing.
    ```

    后续所有 mode 判断使用 `effective_mode`,不直接读 `mode`(便于测试 `--section X --mode joint` 时 effective_mode == "round-robin")。

    **Step 4. 修改 `evolve()` 内 L195-286 整段(Step 6 per-section GEPA optimization)。改造为 mode-fork:**

    将原 L195-286(从 `# ── 6. Per-section GEPA optimization ─────` 到 `console.print(f"\n  Optimization completed in {elapsed:.1f}s")`)替换为以下代码。**重要 W2 invariant:joint 分支的 `optimizer.compile(...)` 调用 NOT 包裹在 try/except 中(让 GEPA 失败 loud 抛出);round-robin 分支保留现有 GEPA→MIPROv2 fallback try/except:**

    ```python
        # ── 6. Optimization (joint vs round-robin fork) ──────────────────────
        sections_to_optimize = [section] if section else module._section_ids

        metric = PromptBehavioralMetric(config)
        section_texts = {s.section_id: s.text for s in original_sections}

        # ── 6a. Compute budget preview (D-IT-03) ─────────────────────────────
        # For budget purposes, simulate joint mode to capture num_predictors
        # without permanently mutating module state when we're going round-robin.
        if effective_mode == "joint":
            module.set_joint_mode(True)
            num_predictors = len(list(module.named_predictors()))
            joint_budget = max(iterations * 50, 3 * num_predictors) * num_predictors
        else:
            num_predictors = len(module._section_ids)
            joint_budget = 0  # not running joint
        rr_per_section_budget = iterations * 50
        rr_total_budget = rr_per_section_budget * len(module._section_ids)

        console.print(f"\n[bold]Configuring optimizer[/bold]")
        console.print(f"  Mode: {effective_mode}")
        console.print(f"  Eval model: {config.eval_model}")
        console.print(f"  Reflection model: {config.optimizer_model}")
        if effective_mode == "joint":
            console.print(
                f"  Joint optimization:        "
                f"iterations={iterations}, max_metric_calls={joint_budget}"
            )
            console.print(
                f"  Round-robin A/B baseline:  "
                f"iterations={iterations}/section × {len(module._section_ids)} sections, "
                f"max_metric_calls={rr_per_section_budget}/section"
            )
            console.print(
                f"  Total est. LM calls:       ~{joint_budget} (joint) + "
                f"~{rr_total_budget} (baseline) = ~{joint_budget + rr_total_budget}"
            )
        else:
            console.print(
                f"  Round-robin: iterations={iterations}/section × "
                f"{len(sections_to_optimize)} section(s), "
                f"max_metric_calls={rr_per_section_budget}/section"
            )

        lm = dspy.LM(config.eval_model, **config.get_lm_kwargs())
        dspy.configure(lm=lm)

        start_time = time.time()

        if effective_mode == "joint":
            # ── 6b. JOINT branch: single GEPA.compile with component_selector="all" ──
            # W2 invariant: NO try/except wrapping GEPA.compile here. Joint mode
            # follows Phase 13 D-15a "loud GEPA failure" pattern — any exception
            # propagates uncaught to Click main and exits non-zero. This is the
            # deliberate behavioral DIFFERENCE from the round-robin branch below
            # (which retains its GEPA → MIPROv2 fallback for legacy parity).
            # Future PRs MUST NOT silently introduce try/except here without a
            # CONTEXT decision revision.
            console.print(
                f"\n[bold cyan]Joint optimization across "
                f"{len(module._section_ids)} sections[/bold cyan]"
            )

            trainset = dataset.to_dspy_examples(
                "train", section_texts=section_texts
            )
            valset = dataset.to_dspy_examples(
                "val", section_texts=section_texts
            )

            if not trainset:
                console.print(
                    "  [yellow]Warning: No training data, skipping joint optimization[/yellow]"
                )
            else:
                console.print(
                    f"  Training examples: {len(trainset)}, "
                    f"Validation examples: {len(valset)}"
                )
                reflection_lm = dspy.LM(
                    config.optimizer_model, **config.get_lm_kwargs()
                )
                optimizer = dspy.GEPA(
                    metric=metric,
                    max_metric_calls=joint_budget,
                    reflection_lm=reflection_lm,
                    component_selector="all",
                    track_stats=True,
                    seed=0,
                )
                # NO try/except: loud-fail per W2 invariant / D-15a parity
                module = optimizer.compile(
                    module,
                    trainset=trainset,
                    valset=valset,
                )
        else:
            # ── 6c. ROUND-ROBIN branch: per-section for-loop (legacy preserved) ──
            # NOTE: This branch INTENTIONALLY retains the GEPA → MIPROv2 fallback
            # chain (try/except below) — that is the long-standing round-robin
            # behavior. Joint branch above does NOT have this fallback (W2 invariant).
            for active_sid in sections_to_optimize:
                console.print(
                    f"\n[bold cyan]Optimizing section: {active_sid}[/bold cyan]"
                )
                module.set_active_section(active_sid)

                # Filter dataset for this section
                section_train = [
                    ex for ex in dataset.train if ex.section_id == active_sid
                ]
                section_val = [
                    ex for ex in dataset.val if ex.section_id == active_sid
                ]

                temp_dataset = PromptBehavioralDataset(
                    train=section_train,
                    val=section_val,
                    holdout=[],
                )
                trainset = temp_dataset.to_dspy_examples(
                    "train", section_texts=section_texts
                )
                valset = temp_dataset.to_dspy_examples(
                    "val", section_texts=section_texts
                )

                if not trainset:
                    console.print(
                        f"  [yellow]Warning: No training data for {active_sid}, "
                        f"skipping[/yellow]"
                    )
                    continue

                console.print(
                    f"  Training examples: {len(trainset)}, "
                    f"Validation examples: {len(valset)}"
                )

                try:
                    reflection_lm = dspy.LM(
                        config.optimizer_model, **config.get_lm_kwargs()
                    )
                    optimizer = dspy.GEPA(
                        metric=metric,
                        max_metric_calls=rr_per_section_budget,
                        reflection_lm=reflection_lm,
                    )
                    module = optimizer.compile(
                        module,
                        trainset=trainset,
                        valset=valset,
                    )
                except Exception as e:
                    # Round-robin legacy: fall back to MIPROv2 if GEPA isn't available
                    console.print(
                        f"  [yellow]GEPA not available ({e}), "
                        f"falling back to MIPROv2[/yellow]"
                    )
                    try:
                        optimizer = dspy.MIPROv2(
                            metric=metric,
                            auto="light",
                        )
                        module = optimizer.compile(
                            module,
                            trainset=trainset,
                        )
                    except Exception as e2:
                        console.print(
                            f"  [red]MIPROv2 also failed ({e2}), "
                            f"skipping section {active_sid}[/red]"
                        )

        elapsed = time.time() - start_time
        console.print(f"\n  Optimization completed in {elapsed:.1f}s")
    ```

    **Step 5. 修改 `evolve()` 中 dry-run early-return 块(L131-150)— 把预算预估行同样打到 dry-run stdout(D-IT-03 + Phase 13 模式对齐)。W1 invariant:dry-run gate 也调用 `_resolve_effective_mode(section, mode)`,不重写表达式:**

    将原 L131-150 整段:
    ```python
        # ── 3. Dry-run gate ──────────────────────────────────────────────────
        if dry_run:
            console.print(
                "[bold green]DRY RUN -- setup validated successfully.[/bold green]"
            )
            ... (其余 console.print 行)
            return
    ```

    替换为(注意 `effective_mode_dry = _resolve_effective_mode(section, mode)` — W1 修订使用同一 helper,不再 inline 写条件):
    ```python
        # ── 3. Dry-run gate (D-IT-03: budget preview always printed) ─────────
        if dry_run:
            console.print(
                "[bold green]DRY RUN -- setup validated successfully.[/bold green]"
            )
            console.print(f"  Found {len(original_sections)} prompt section(s)")
            console.print(f"  Dataset source: {eval_source}")
            if eval_source == "load":
                dataset_path = Path("datasets") / "prompts"
                console.print(
                    f"  Dataset path: {dataset_path} (exists: {dataset_path.exists()})"
                )
            # W1 invariant: same helper as main path — single source of truth
            effective_mode_dry = _resolve_effective_mode(section, mode)
            # In dry-run we don't have module yet — use section count as proxy
            # (Phase 17 selector is frozen in joint mode so num_predictors == N sections).
            num_predictors_dry = len(original_sections)
            joint_budget_dry = max(iterations * 50, 3 * num_predictors_dry) * num_predictors_dry
            rr_per_section_dry = iterations * 50
            rr_total_dry = rr_per_section_dry * len(original_sections)
            console.print(f"  Mode: {effective_mode_dry}")
            if effective_mode_dry == "joint":
                console.print(
                    f"  Joint optimization:        "
                    f"iterations={iterations}, max_metric_calls={joint_budget_dry}"
                )
                console.print(
                    f"  Round-robin A/B baseline:  "
                    f"iterations={iterations}/section × {len(original_sections)} sections, "
                    f"max_metric_calls={rr_per_section_dry}/section"
                )
                console.print(
                    f"  Total est. LM calls:       ~{joint_budget_dry} (joint) + "
                    f"~{rr_total_dry} (baseline) = ~{joint_budget_dry + rr_total_dry}"
                )
            else:
                console.print(
                    f"  Round-robin: iterations={iterations}/section × "
                    f"{len(original_sections)} section(s), "
                    f"max_metric_calls={rr_per_section_dry}/section"
                )
            if section:
                console.print(f"  Target section: {section}")
            else:
                console.print(f"  Target: all {len(original_sections)} sections")
            return
    ```

    **Step 6. 修改 Click CLI 装饰器块(L474-518)— 添加 `--mode` flag,把它传给 `evolve()`:**

    在 `@click.option("--api-base", default=None, help="Override API base URL")` 之后(L502 之后),添加:
    ```python
    @click.option(
        "--mode",
        default="joint",
        type=click.Choice(["joint", "round-robin"]),
        help="Optimization mode: 'joint' (default, optimizes all sections "
             "simultaneously via GEPA) or 'round-robin' (legacy, optimizes "
             "section-by-section). --section <id> implicitly forces round-robin "
             "single-section even with --mode joint.",
    )
    ```

    修改 `main()` 函数签名(L503)从 `def main(section, iterations, eval_source, hermes_repo, dry_run, model, api_base):` 改为:
    ```python
    def main(section, iterations, eval_source, hermes_repo, dry_run, model, api_base, mode):
    ```

    在 `main()` 函数体内的 `evolve(...)` 调用(L505-513)末尾追加 `mode=mode`:
    ```python
        evolve(
            section=section,
            iterations=iterations,
            eval_source=eval_source,
            hermes_repo=hermes_repo,
            dry_run=dry_run,
            model=model,
            api_base=api_base,
            mode=mode,
        )
    ```

    **不修改:** Step 7 (extract evolved) 起的所有 holdout/constraint/persist 逻辑(L288-468)— Plan 17-03 在此基础上加 A/B 与软门;本 plan 仅扩展到 optimization 步骤结束。Round-robin 的 GEPA→MIPROv2 fallback 链原样保留(`--mode round-robin` 走这条);joint 分支按 W2 invariant + Phase 13 D-15a "loud GEPA failure" 模式让异常 propagate(本 plan 不引入 `--allow-miprov2-fallback`,与 CONTEXT 范围一致)。
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/prompts/test_evolve_prompt_sections.py tests/prompts/test_evolve_prompt_sections_cli.py -v -x</automated>
  </verify>

  <done>
    - `grep -q "click.Choice(\\[\"joint\", \"round-robin\"\\])" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "EPSILON_PP = 0.01" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "def _resolve_effective_mode" evolution/prompts/evolve_prompt_sections.py` 返回 0(W1 helper 存在)
    - `grep -c "effective_mode = _resolve_effective_mode(section, mode)" evolution/prompts/evolve_prompt_sections.py` 返回 ≥ 1(main path 用 helper)
    - `grep -c "effective_mode_dry = _resolve_effective_mode(section, mode)" evolution/prompts/evolve_prompt_sections.py` 返回 ≥ 1(dry-run gate 用 helper)
    - `grep -c "\"round-robin\" if section else mode" evolution/prompts/evolve_prompt_sections.py` 返回正好 1(W1: 字面量只在 helper 实现内出现一次,无重复)
    - `grep -q "component_selector=\"all\"" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "module.set_joint_mode(True)" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "Joint optimization:" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "Round-robin A/B baseline:" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "Total est. LM calls:" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "NO try/except: loud-fail per W2 invariant" evolution/prompts/evolve_prompt_sections.py` 返回 0(W2 invariant 在 joint 分支有内嵌注释)
    - `grep -c "max(iterations \\* 50, 3 \\* num_predictors)" evolution/prompts/evolve_prompt_sections.py` 返回 ≥ 1(joint budget formula 存在)
    - Task 2 测试套(`tests/prompts/test_evolve_prompt_sections_cli.py`)全部 PASS
    - 现有 `tests/prompts/test_evolve_prompt_sections.py` 测试套零回归(全部 PASS;若 mock 链假设旧 round-robin 默认行为,需调整 test 改成显式传 `--mode round-robin` — 见 Task 2 回归处理)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新建 tests/prompts/test_evolve_prompt_sections_cli.py — CliRunner + fake GEPA mock 覆盖 joint/round-robin/--section X/--dry-run 四类路径</name>

  <read_first>
    - tests/tools/test_evolve_tool_params_cli.py L1-115(完整 fake-GEPA mock 模板,直接复刻 multi-patch + assertion 风格)
    - tests/prompts/test_evolve_prompt_sections.py L1-180(现有 6 测试,其 mock 链在 mode 默认变 joint 后需要补 `--mode round-robin` argv 才能继续测旧行为 — 本任务负责确认 / 调整这些回归)
    - evolution/prompts/evolve_prompt_sections.py(Task 1 完成后的状态)
    - .planning/phases/17-joint-section-optimization/17-PATTERNS.md §Pattern D1/D2(CliRunner + multi-layer patch)
    - .planning/phases/17-joint-section-optimization/17-RESEARCH.md §Code Examples Pattern 7(fake GEPA mock 测 component_selector='all' 断言)
    - .planning/phases/17-joint-section-optimization/17-VALIDATION.md §Per-Task Verification Map(Wave 0 新文件的测试列表)
  </read_first>

  <files>tests/prompts/test_evolve_prompt_sections_cli.py</files>

  <behavior>
    新文件包含 2 个测试类共 ≥ 4 个测试:
    - `TestJointPipeline::test_joint_mode_default_calls_gepa_with_component_selector_all`:不传 `--mode`(默认 joint),`dspy.GEPA.__init__` 被传 `component_selector="all"`,`compile` 调用次数 == 1,`set_joint_mode(True)` 调用次数 == 1
    - `TestJointPipeline::test_round_robin_mode_compiles_per_section`:传 `--mode round-robin`,`dspy.GEPA.__init__` 不传 `component_selector` 或显式为 `"round_robin"`,`compile` 调用次数 == N(N=fake_sections 数),`set_joint_mode` 调用次数 == 0,`set_active_section` 调用次数 == N
    - `TestJointPipeline::test_section_flag_forces_round_robin_even_when_mode_joint`:传 `--section memory_guidance --mode joint`,`set_active_section("memory_guidance")` 被调用 1 次,`compile` 调用次数 == 1,`set_joint_mode` 调用次数 == 0(隐式 RR)
    - `TestDryRun::test_dry_run_joint_prints_budget_estimate`:传 `--dry-run --mode joint`,stdout 含 "Joint optimization:"、"Round-robin A/B baseline:"、"Total est. LM calls:" 三行子串,`dspy.GEPA` **未**被实例化(compile.call_count == 0)

    现有 `tests/prompts/test_evolve_prompt_sections.py` 的 `test_evolve_orchestration_order` 等 6 测试需保持通过:
    - 它们的 mock 链覆盖 module/dataset/metric/validator/role_checker,**不**断言 component_selector,因此默认 joint mode 下 single compile 调用对它们仍 GREEN
    - 若有任何一个现有测试假设 `mock_gepa.return_value.compile.call_count > 1`(即 per-section 多次调用),需要在该测试 argv 中添加 `--mode round-robin` 显式回到 legacy 路径。**Task 2 必须先跑现有测试一次,识别需要调整的项,再做最小修改**。
  </behavior>

  <action>
    创建新文件 `tests/prompts/test_evolve_prompt_sections_cli.py`,完整内容如下:

    ```python
    """Phase 17 — CLI integration tests for joint mode + --mode flag.

    Pattern: CliRunner + fake-GEPA mock, mirroring Phase 13
    tests/tools/test_evolve_tool_params_cli.py multi-patch style. No real LM
    calls; dspy.GEPA().compile is mocked to return the module unchanged.
    """

    from pathlib import Path
    from unittest.mock import patch, MagicMock

    import pytest


    def _make_fake_sections(n: int = 3):
        """Build n fake PromptSection instances for CLI mock tests."""
        from evolution.prompts.prompt_loader import PromptSection

        return [
            PromptSection(
                section_id=f"section_{i}",
                text=f"Section {i} original text",
                char_count=20,
                line_range=(i * 10, i * 10 + 5),
                source_path=Path("/fake/prompt_builder.py"),
            )
            for i in range(n)
        ]


    def _make_mocked_dataset():
        """Build a fake PromptBehavioralDataset with .train/.val/.holdout + .to_dspy_examples."""
        import dspy
        from evolution.prompts.prompt_dataset import (
            PromptBehavioralExample,
            PromptBehavioralDataset,
        )

        examples = [
            PromptBehavioralExample(
                section_id="section_0",
                user_message="say hi",
                expected_behavior="responds with greeting",
                difficulty="easy",
            ),
            PromptBehavioralExample(
                section_id="section_1",
                user_message="remember this",
                expected_behavior="stores memory",
                difficulty="easy",
            ),
        ]
        return PromptBehavioralDataset(
            train=examples,
            val=examples,
            holdout=examples,
        )


    def _common_patches():
        """Return a list of patch context-managers shared across CLI tests.

        Patches:
          - extract_prompt_sections -> fake sections
          - PromptDatasetBuilder -> fake dataset builder
          - PromptBehavioralMetric -> MagicMock (no real LM)
          - ConstraintValidator -> all-pass results
          - PromptRoleChecker -> all-pass results
          - dspy.GEPA -> mock_gepa with compile.side_effect = identity
          - dspy.LM, dspy.configure -> no-op
        """
        # (returned as a function building contexts in the test body)
        pass


    class TestJointPipeline:
        """Tests for --mode joint vs --mode round-robin CLI routing."""

        def _patched_run(self, argv: list[str], fake_sections=None):
            """Helper: invoke main() with full patch stack and return (result, mock_gepa,
            mock_module_instance, mock_module_cls)."""
            import dspy
            from click.testing import CliRunner
            from evolution.prompts.evolve_prompt_sections import main
            from evolution.prompts.prompt_module import PromptModule

            if fake_sections is None:
                fake_sections = _make_fake_sections(3)

            fake_ds = _make_mocked_dataset()

            mock_constraint = MagicMock()
            mock_constraint._check_growth.return_value = MagicMock(passed=True, message="ok", constraint_name="growth")
            mock_constraint._check_non_empty.return_value = MagicMock(passed=True, message="ok", constraint_name="non_empty")

            mock_role = MagicMock()
            mock_role.check_all.return_value = []

            mock_metric_instance = MagicMock(return_value=0.5)

            mock_builder_instance = MagicMock()
            mock_builder_instance.generate.return_value = fake_ds

            # We patch PromptModule at module-import location to intercept .set_joint_mode / .set_active_section
            # and proxy .named_predictors / ._section_ids to a real lightweight stand-in.
            real_module = PromptModule(fake_sections)
            spy_module = MagicMock(wraps=real_module)
            # Make MagicMock-proxied attribute access return real values
            spy_module._section_ids = real_module._section_ids
            spy_module.section_predictors = real_module.section_predictors
            spy_module.get_evolved_sections.return_value = fake_sections
            # named_predictors returns N entries to drive budget math
            spy_module.named_predictors.return_value = [
                (f"section_predictors['{sid}']", MagicMock())
                for sid in real_module._section_ids
            ]

            runner = CliRunner()

            with patch(
                "evolution.prompts.evolve_prompt_sections.extract_prompt_sections",
                return_value=fake_sections,
            ), patch(
                "evolution.prompts.evolve_prompt_sections.PromptDatasetBuilder",
                return_value=mock_builder_instance,
            ), patch(
                "evolution.prompts.evolve_prompt_sections.PromptBehavioralMetric",
                return_value=mock_metric_instance,
            ), patch(
                "evolution.prompts.evolve_prompt_sections.ConstraintValidator",
                return_value=mock_constraint,
            ), patch(
                "evolution.prompts.evolve_prompt_sections.PromptRoleChecker",
                return_value=mock_role,
            ), patch(
                "evolution.prompts.evolve_prompt_sections.PromptModule",
                return_value=spy_module,
            ) as mock_module_cls, patch(
                "evolution.prompts.evolve_prompt_sections.dspy.GEPA"
            ) as mock_gepa, patch(
                "evolution.prompts.evolve_prompt_sections.dspy.LM"
            ), patch(
                "evolution.prompts.evolve_prompt_sections.dspy.configure"
            ), patch(
                "evolution.prompts.evolve_prompt_sections.dspy.context",
                MagicMock(),
            ):
                mock_gepa.return_value.compile.side_effect = (
                    lambda mod, trainset, valset=None: mod
                )
                result = runner.invoke(main, argv, catch_exceptions=False)

            return result, mock_gepa, spy_module, mock_module_cls

        def test_joint_mode_default_calls_gepa_with_component_selector_all(self):
            """Default mode (no --mode flag) routes to joint: single GEPA.compile +
            component_selector='all' + set_joint_mode(True) called exactly once."""
            result, mock_gepa, spy_module, _ = self._patched_run(
                ["--iterations", "2", "--hermes-repo", "/fake"],
                fake_sections=_make_fake_sections(3),
            )

            assert result.exit_code == 0, f"Expected exit 0, got {result.exit_code}. Output: {result.output[:500]}"
            assert mock_gepa.called, "dspy.GEPA was never instantiated in joint mode"
            init_kwargs = mock_gepa.call_args.kwargs
            assert init_kwargs.get("component_selector") == "all", (
                f"Joint mode must call GEPA with component_selector='all', "
                f"got {init_kwargs.get('component_selector')!r}"
            )
            # Joint mode does ONE compile call (not per-section)
            assert mock_gepa.return_value.compile.call_count == 1, (
                f"Joint mode expected exactly 1 GEPA.compile() call, got "
                f"{mock_gepa.return_value.compile.call_count}"
            )
            # set_joint_mode(True) was called (proves we entered joint branch)
            spy_module.set_joint_mode.assert_called_with(True)
            # set_active_section must NOT have been called for joint pipeline
            assert spy_module.set_active_section.call_count == 0, (
                f"Joint mode should not call set_active_section; got "
                f"{spy_module.set_active_section.call_count} calls"
            )

        def test_round_robin_mode_compiles_per_section(self):
            """--mode round-robin routes to per-section for-loop: N compile calls,
            N set_active_section calls, zero set_joint_mode calls."""
            fake_sections = _make_fake_sections(3)
            result, mock_gepa, spy_module, _ = self._patched_run(
                ["--mode", "round-robin", "--iterations", "2", "--hermes-repo", "/fake"],
                fake_sections=fake_sections,
            )

            assert result.exit_code == 0, f"Output: {result.output[:500]}"
            # GEPA.__init__ should NOT receive component_selector='all' in round-robin
            init_kwargs = mock_gepa.call_args.kwargs
            assert init_kwargs.get("component_selector") != "all", (
                f"Round-robin must not pass component_selector='all'; got {init_kwargs!r}"
            )
            # One compile per section
            assert mock_gepa.return_value.compile.call_count == len(fake_sections), (
                f"Round-robin expected {len(fake_sections)} compile calls, got "
                f"{mock_gepa.return_value.compile.call_count}"
            )
            # set_active_section called per section
            assert spy_module.set_active_section.call_count == len(fake_sections)
            # set_joint_mode NEVER called
            spy_module.set_joint_mode.assert_not_called()

        def test_section_flag_forces_round_robin_even_when_mode_joint(self):
            """--section X --mode joint should still go single-section round-robin (D-RR-03)."""
            fake_sections = _make_fake_sections(3)
            target_sid = fake_sections[1].section_id
            result, mock_gepa, spy_module, _ = self._patched_run(
                ["--section", target_sid, "--mode", "joint", "--iterations", "2", "--hermes-repo", "/fake"],
                fake_sections=fake_sections,
            )

            assert result.exit_code == 0, f"Output: {result.output[:500]}"
            # Only the target section was activated
            assert spy_module.set_active_section.call_count == 1
            spy_module.set_active_section.assert_called_with(target_sid)
            # Single compile (single section)
            assert mock_gepa.return_value.compile.call_count == 1
            # set_joint_mode NEVER called (D-RR-03 implicit RR)
            spy_module.set_joint_mode.assert_not_called()


    class TestDryRun:
        """Tests for --dry-run + --mode joint stdout budget preview."""

        def test_dry_run_joint_prints_budget_estimate(self):
            """--dry-run --mode joint prints 3-line budget preview + does not invoke GEPA."""
            from click.testing import CliRunner
            from evolution.prompts.evolve_prompt_sections import main

            fake_sections = _make_fake_sections(5)  # explicit count so output is deterministic

            with patch(
                "evolution.prompts.evolve_prompt_sections.extract_prompt_sections",
                return_value=fake_sections,
            ), patch(
                "evolution.prompts.evolve_prompt_sections.dspy.GEPA"
            ) as mock_gepa:
                runner = CliRunner()
                result = runner.invoke(
                    main,
                    ["--dry-run", "--mode", "joint", "--iterations", "10", "--hermes-repo", "/fake"],
                    catch_exceptions=False,
                )

            assert result.exit_code == 0, f"Dry-run failed: {result.output[:500]}"
            # 3-line D-IT-03 budget preview
            assert "Joint optimization:" in result.output, (
                f"Missing 'Joint optimization:' in dry-run stdout. Output: {result.output[:800]}"
            )
            assert "Round-robin A/B baseline:" in result.output
            assert "Total est. LM calls:" in result.output
            # Numeric proof: 10 iterations × 5 sections, joint = max(500, 15) * 5 = 2500
            assert "max_metric_calls=2500" in result.output, (
                f"Expected joint budget=2500 in dry-run; output: {result.output[:800]}"
            )
            # GEPA never instantiated in dry-run
            assert not mock_gepa.called, "dspy.GEPA was instantiated during dry-run"
    ```

    **Additionally:** 跑现有 `tests/prompts/test_evolve_prompt_sections.py` 一次。若任何测试因为「默认 mode 现在是 joint,GEPA 调用语义改变」失败,对失败的测试 minimal-edit:在 `runner.invoke(...)` 的 argv 中添加 `["--mode", "round-robin"]` 以保留旧 round-robin 行为断言。**只**对失败的测试做这种最小调整,不删除测试,不改 mock 链结构。失败测试的修改在本 task 同 commit 内做。
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/prompts/ -v -x</automated>
  </verify>

  <done>
    - 新文件存在:`test -f tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "class TestJointPipeline" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "class TestDryRun" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "test_joint_mode_default_calls_gepa_with_component_selector_all" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "test_round_robin_mode_compiles_per_section" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "test_section_flag_forces_round_robin_even_when_mode_joint" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "test_dry_run_joint_prints_budget_estimate" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "component_selector.*all" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "Joint optimization:" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `.venv/bin/python -m pytest tests/prompts/ -v -x` 返回 exit 0,全部 PASS(现有 + 新)
    - `.venv/bin/python -m pytest tests/ -x -q` 返回 exit 0(全项目零回归)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Click CLI argv → evolve() | 用户输入 `--mode` / `--section` / `--iterations` 等;click.Choice 与 IntRange 在边界处校验 |
| evolve() → dspy.GEPA(reflection_lm) | reflection_lm = OpenAI/OpenRouter API 客户端;通过 EvolutionConfig 读取 OPENAI_API_KEY 等环境变量,本 plan 不处理凭据 |
| evolve() → PromptModule.set_joint_mode | 单进程内 Python 调用,Plan 17-01 已 mitigation 状态机攻击面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Input Validation (V5) | `--mode` 输入非法字符串(例如 `--mode joint;rm -rf /`) | mitigate | `click.Choice(["joint", "round-robin"])` 严格白名单;非法值由 Click 在 argv 解析阶段拒绝,不进入 evolve() |
| T-17-06 | Input Validation (V5) | `--iterations` 输入负数或超大整数导致 budget 溢出 | accept | 现有 `--iterations` 接受任意 int(沿用 Phase 10 决策);用户预算控制权在自身;`max_metric_calls` 是 int,Python 任意大整数无溢出 |
| T-17-07 | Denial of Service | joint mode 下 `max_metric_calls = max(iter*50, 3*N_pred) * N_pred` 在大 N 下成本爆炸 | mitigate | stdout 预算预估 3 行(D-IT-03)在跑 GEPA 前显式打印,用户可 Ctrl-C 终止;`--dry-run` 进一步允许零成本验证 |
| T-17-08 | Information Disclosure | `--hermes-repo` 路径若指向其他用户目录可读 prompt_builder.py | accept | 现有 Phase 7 实现已用 read-only 模式访问;Phase 17 不改 IO 边界,继承既有 mitigation |
| T-17-09 | Tampering | 测试 fixture(_make_fake_sections)若被恶意修改在 Wave 0 阶段引入恶意 PromptSection text | accept | 测试 fixture 是本地 Python 字面量,无外部输入;commits 走 GSD 工作流人工 review |
| T-17-16 | Repudiation | joint 分支 GEPA 失败若被未来 PR 静默引入 try/except 兜底,会掩盖真实失败,违反 W2 invariant + D-15a "loud failure" 设计意图 | mitigate | 代码内嵌注释「NO try/except: loud-fail per W2 invariant / D-15a parity」+ must_haves.truth 锚定 + Task 1 acceptance criterion 检查 joint 分支无 try/except 包裹;任何修改都触发 plan 修订 |

低威胁面理由:本 plan 是 CLI 层 + 测试,无新 IO 边界、新网络调用、新凭据处理。GEPA reflection_lm 的网络调用由 EvolutionConfig + dspy 客户端处理,本 plan 不引入额外 attack vector。
</threat_model>

<verification>
- `grep -q "click.Choice(\\[\"joint\", \"round-robin\"\\])" evolution/prompts/evolve_prompt_sections.py` 返回 0
- `grep -q "component_selector=\"all\"" evolution/prompts/evolve_prompt_sections.py` 返回 0
- `grep -q "def _resolve_effective_mode" evolution/prompts/evolve_prompt_sections.py` 返回 0(W1 helper 存在)
- `grep -c "_resolve_effective_mode(section, mode)" evolution/prompts/evolve_prompt_sections.py` 返回 ≥ 2(main + dry-run gate 都用 helper)
- `grep -c "\"round-robin\" if section else mode" evolution/prompts/evolve_prompt_sections.py` 返回正好 1(W1 invariant)
- `grep -q "NO try/except: loud-fail per W2 invariant" evolution/prompts/evolve_prompt_sections.py` 返回 0(W2 invariant 注释)
- `grep -c "effective_mode" evolution/prompts/evolve_prompt_sections.py` 返回 ≥ 3(变量在 mode resolution、optimization branch、dry-run 三处至少各 1 次出现)
- `.venv/bin/python -m pytest tests/prompts/test_evolve_prompt_sections_cli.py -v -x` 返回 exit 0,collected ≥ 4 tests
- `.venv/bin/python -m pytest tests/prompts/ -v -x` 返回 exit 0(新+旧 prompt 测试全 PASS)
- `.venv/bin/python -m pytest tests/ -x -q` 返回 exit 0(全项目零回归)
- CLI smoke:`.venv/bin/python -c "from click.testing import CliRunner; from evolution.prompts.evolve_prompt_sections import main; r = CliRunner().invoke(main, ['--help']); assert '--mode' in r.output and 'joint' in r.output and 'round-robin' in r.output"` 返回 exit 0
</verification>

<success_criteria>
- ROADMAP §Phase 17 Success Criterion 2 落地:`--mode joint` 触发 GEPA `component_selector="all"` 单 compile 调用,覆盖全部 N section
- D-RR-01/02/03/04 全部生效:joint 是默认、静默切换、`--section X` 隐式 RR、`click.Choice(["joint", "round-robin"])`
- D-IT-02 budget 公式以 `num_predictors`-dynamic 形式落地(`max(iterations*50, 3*num_predictors) * num_predictors`),非 CONTEXT 写死的 `× 5`
- D-IT-03 三行 stdout 预算预估在 dry-run 与正式 run 都打印
- W1 invariant:`_resolve_effective_mode` helper 在 dry-run 与 main 两处统一调用,字面量 `"round-robin" if section else mode` 全文件只出现一次
- W2 invariant:joint 分支 NO try/except 包裹 GEPA.compile,异常向上 propagate(D-15a parity);round-robin 分支保留现有 GEPA→MIPROv2 fallback
- 新 `tests/prompts/test_evolve_prompt_sections_cli.py` 文件 ≥ 4 测试全部 PASS
- 全项目测试套件零回归
</success_criteria>

<output>
After completion, create `.planning/phases/17-joint-section-optimization/17-02-SUMMARY.md` documenting:
- `evolve_prompt_sections.py` 实际修改的行号区间
- `--mode` flag 实际帮助文本(便于 README 引用)
- `_resolve_effective_mode` helper 的最终位置(行号 + signature)— Plan 17-03 / 17-04+ 若需要新的 mode-conditional 分支,直接复用这个 helper,不自己写条件
- 任何 `tests/prompts/test_evolve_prompt_sections.py` 因 mode 默认变 joint 而调整过的测试名单
- joint 分支与 round-robin baseline 之间的接口契约(供 Plan 17-03 inline A/B 消费)— 特别是:`fresh PromptModule(original_sections)` 作为 A/B baseline 起点的约定、`module.named_predictors()` 在 joint mode 排除 selector 的细节、W2 invariant(joint 分支 NO try/except)以防 Plan 17-03 误引入兜底
</output>
