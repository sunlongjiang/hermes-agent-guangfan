---
phase: 17-joint-section-optimization
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - evolution/prompts/evolve_prompt_sections.py
  - tests/prompts/test_evolve_prompt_sections_cli.py
autonomous: true
requirements: [PMPT-V2-01]
requirements_addressed: [PMPT-V2-01]
tags: [ab-baseline, soft-gate, metrics-schema, joint-optimization]

must_haves:
  truths:
    # ROADMAP §Phase 17 Success Criterion 3
    - "joint mode 跑完 holdout 评估后,同一 CLI 进程内 inline 跑 round-robin A/B baseline,使用 fresh PromptModule(original_sections) + 相同 dataset + 相同 metric + 相同 holdout,得到 roundrobin_baseline_score"
    # D-AB-01
    - "A/B baseline 用 fresh PromptModule 起点(Pitfall 4 — joint module 已 mutate,复用会失真)"
    # D-AB-02 + D-AB-03
    - "软门:`joint_score < roundrobin_baseline_score - EPSILON_PP (= 0.01)` 时 stdout 打 [yellow] 警告(含 joint_score / rr_score / delta_pp 三数值 + 'review before deploying'),不 exit、不阻断 constraint validation、不阻断 evolved_sections.json 写盘"
    # D-AB-04 — A/B baseline budget NOT compressed (W6 revision)
    - "A/B baseline 每个 section 使用 max_metric_calls=iterations*50 全量预算(D-AB-04 — 与 round-robin legacy 路径单参数公式对齐,不做任何压缩;保证 baseline 与 production round-robin 跑出来的 score 严格可比)"
    # D-OUT-01 — shared prefix output layout (resolved decision 3)
    - "joint run 与 A/B baseline 共用 `output/prompts/<ts>/` 目录,NO `baseline/` subdir;A/B baseline 中间产物以 `roundrobin_baseline_` 前缀文件落同级"
    # D-OUT-02 — metrics.json schema extension (W3 revision: add explicit joint-vs-rr delta to disambiguate from improvement)
    - "metrics.json 含 `mode` (=`joint` or `round-robin`)、`joint_score`、`roundrobin_baseline_score`、`epsilon_pp`、`joint_vs_roundrobin_delta_pp`(W3 revision:与现有 `improvement`(=evolved_score - baseline_score,vs unoptimized baseline)语义独立 — 此字段 = (joint_score - roundrobin_baseline_score) * 100,A/B delta)五新字段(joint mode 全填;round-robin mode 仅填 mode);保留现有 baseline_score、evolved_score、improvement、iterations、eval_model"
    # Shared-prefix files
    - "joint mode 在 output_dir 下额外落 `roundrobin_baseline_evolved_sections.json` 与 `roundrobin_baseline_diff.txt`(A/B baseline 中间产物,供 inspection 与未来 dashboard 接入)"
  artifacts:
    - path: "evolution/prompts/evolve_prompt_sections.py"
      provides: "inline A/B baseline + 软门 + metrics.json schema 扩展 + shared-prefix baseline file 落盘"
      contains: "roundrobin_baseline_score"
      contains_2: "EPSILON_PP"
      contains_3: "roundrobin_baseline_evolved_sections.json"
      contains_4: "joint_vs_roundrobin_delta_pp"
    - path: "tests/prompts/test_evolve_prompt_sections_cli.py"
      provides: "新增 TestABBaseline 测试类 — 覆盖软门触发、不触发、metrics.json schema 完整性"
      contains: "class TestABBaseline"
  key_links:
    - from: "evolution/prompts/evolve_prompt_sections.py:evolve (post-holdout joint branch)"
      to: "evolution/prompts/evolve_prompt_sections.py:evolve (A/B baseline inline block)"
      via: "fresh PromptModule(original_sections) + per-section for-loop"
      pattern: "PromptModule\\(original_sections\\)"
    - from: "evolution/prompts/evolve_prompt_sections.py (soft gate)"
      to: "stdout console.print"
      via: "[yellow] warning + delta_pp text"
      pattern: "\\[yellow\\].*joint.*rr"
    - from: "evolution/prompts/evolve_prompt_sections.py (persistence)"
      to: "output/prompts/<ts>/metrics.json"
      via: "json.dumps with mode/joint_score/roundrobin_baseline_score/epsilon_pp/joint_vs_roundrobin_delta_pp fields"
      pattern: "\"mode\":"
---

<objective>
完成 Phase 17 最后一段:在 joint mode 的 holdout 评估之后,inline 跑一次完整 round-robin baseline(用 fresh PromptModule + 相同 dataset/metric/holdout),与 joint score 做软门 1pp 比较。无论结果如何,joint 与 baseline 双方都落盘:joint 的 `evolved_sections.json` + `metrics.json` + `diff.txt` 是主交付物,baseline 的 `roundrobin_baseline_evolved_sections.json` + `roundrobin_baseline_diff.txt` 同级共存(D-OUT-01 shared-prefix layout),metrics.json 新增 5 字段(mode/joint_score/roundrobin_baseline_score/epsilon_pp/joint_vs_roundrobin_delta_pp)。软门违规仅触发 [yellow] 警告,不影响 exit code 或 evolved_sections.json 写出。

Purpose: 完成 ROADMAP §Phase 17 Success Criterion 3(joint ≥ round-robin on holdout)的可证伪机制 — 软门 + 双方落盘是 PMPT-V2-01 的核心收尾。

Output:
- `evolution/prompts/evolve_prompt_sections.py` 进一步扩展(joint 分支 post-holdout 增加 A/B baseline 块 + metrics.json 扩展 + baseline 副本文件落盘)
- `tests/prompts/test_evolve_prompt_sections_cli.py` 新增 `TestABBaseline` 测试类(≥ 3 测试覆盖软门触发、不触发、metrics 字段完整性)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-joint-section-optimization/17-CONTEXT.md
@.planning/phases/17-joint-section-optimization/17-RESEARCH.md
@.planning/phases/17-joint-section-optimization/17-PATTERNS.md
@.planning/phases/17-joint-section-optimization/17-VALIDATION.md
@.planning/phases/17-joint-section-optimization/17-01-SUMMARY.md
@.planning/phases/17-joint-section-optimization/17-02-SUMMARY.md
@.planning/phases/16-per-tool-regression-dashboard/16-CONTEXT.md

<interfaces>
<!-- 从 Plan 17-02 提供的接口(由 17-02-SUMMARY 文档化) -->

After Plan 17-02 changes, evolve_prompt_sections.py has:
- `EPSILON_PP = 0.01` module constant
- `effective_mode` variable(`"joint"` or `"round-robin"`,由 mode + section 决定)
- joint 分支结束在 `module = optimizer.compile(...)`,之后 `elapsed = time.time() - start_time`
- Step 7 (extract evolved) 起的 holdout evaluation / constraint validation / metrics.json persistence 仍是 round-robin-only 风格的现有代码

Plan 17-03 改造点:
1. 在 Step 7-10 之间(具体是 holdout evaluation 完成、`evolved_score` 计算完成后)加 A/B baseline 块
2. 修改 metrics dict 装配(L429-445)加入 mode/joint_score/rr_baseline_score/epsilon_pp/joint_vs_roundrobin_delta_pp
3. 在 metrics.json 落盘前后落 baseline 副本文件

**PromptBehavioralExample dataclass signature(运行时实测,evolution/prompts/prompt_dataset.py L33-51):**
```python
@dataclass
class PromptBehavioralExample:
    section_id: str
    user_message: str
    expected_behavior: str
    difficulty: str = "medium"
    source: str = "synthetic"  # 注意:Plan 17-02 fixture 未传该字段,沿用默认
```
`to_dspy_examples()`(L125-154)把 `user_message` 映射到 `task_input` 字段、`expected_behavior` 保持原名;dspy.Example 对象通过 `.task_input` / `.expected_behavior` 属性访问。Plan 17-03 evolve() L370 处 `ex.task_input` 引用的是 dspy.Example(holdout_examples 已经过 to_dspy_examples 转换),不是 raw PromptBehavioralExample。

From Phase 16 D-13 soft-gate pattern(evolution/tools/regression_dashboard.py L778-787):
```python
if delta <= -warning_threshold_pp:
    console.print(
        f"[yellow]WARNING: {tool} regressed by {delta:+.2f}pp in "
        f"{latest_run['path']} (threshold: -{warning_threshold_pp:.1f}pp)[/yellow]"
    )
# 不返回非零 exit code、不 raise
```

From RESEARCH §Pattern 5(A/B baseline 编排):
- fresh `PromptModule(original_sections)` 作为 baseline 起点(Pitfall 4 规避 mutation 污染)
- per-section for-loop 复刻 Plan 17-02 round-robin 分支
- 同一 holdout 评分,用 `set_active_section(first_sid)` 强制单 active 让 forward 不抛 RuntimeError
- 软门文案:`f"[yellow]Joint score ({joint:.3f}) below round-robin baseline ({rr:.3f}) by {delta_pp:.1f}pp — review before deploying[/yellow]"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 inline A/B baseline + 软门 [yellow] 警告 + metrics.json 5 新字段 + roundrobin_baseline_*.{json,txt} shared-prefix 副本落盘</name>

  <read_first>
    - evolution/prompts/evolve_prompt_sections.py(Plan 17-02 完成后的状态,关注 evolve() L288-468 区段 — Step 7 extract evolved 起到 metrics.json 落盘结束)
    - .planning/phases/17-joint-section-optimization/17-RESEARCH.md §Pattern 5(A/B baseline run 编排 — 完整代码块)+ Pitfall 4(fresh module 规避 mutation)+ §Code Examples 软门文案
    - .planning/phases/17-joint-section-optimization/17-PATTERNS.md §Pattern B3/B4/B6(round-robin for-loop 模板 + 软门 stdout-only + metrics.json schema 扩展)
    - .planning/phases/17-joint-section-optimization/17-CONTEXT.md `<decisions>` D-AB-01..D-AB-04, D-OUT-01..D-OUT-04
    - 本 plan 头部 `<resolved_decisions>` 与 must_haves(shared-prefix layout 实现细节)
    - evolution/tools/regression_dashboard.py L778-787(Phase 16 软门实现镜像)
  </read_first>

  <files>evolution/prompts/evolve_prompt_sections.py</files>

  <behavior>
    Plan 17-02 完成后 evolve() 已能跑 joint pipeline 到 Step 6 末尾,Step 7-10(extract + constraint + holdout + persist)仍是 round-robin 风格代码。Task 1 实现以下行为:

    - 在 joint mode (`effective_mode == "joint"`) 下,**Step 9(holdout evaluation)完成、`evolved_score` 计算后**,inline 跑 round-robin A/B baseline:
      - 新建 `ab_baseline_module = PromptModule(original_sections)`(fresh,Pitfall 4)
      - per-section for-loop:`for sid in ab_baseline_module._section_ids: set_active_section(sid); GEPA(component_selector="round_robin")(metric, max_metric_calls=iterations*50, reflection_lm).compile(...)`(W6 revision: per-section budget = iterations*50,与 round-robin legacy 单参数公式 1:1 对齐,D-AB-04 — 不压缩)
      - 跑同一 holdout_examples,得到 `roundrobin_baseline_score`
      - 计算 `delta_pp = (roundrobin_baseline_score - evolved_score) * 100`(W3 revision: 这是 rr_baseline-joint 视角)
      - 同时计算 `joint_vs_roundrobin_delta_pp = (evolved_score - roundrobin_baseline_score) * 100`(W3 revision: A/B delta,与 `improvement`=evolved-baseline_unoptimized 语义独立)
      - 软门:`if evolved_score < roundrobin_baseline_score - EPSILON_PP: console.print(f"[yellow]Joint score ({evolved_score:.3f}) below round-robin baseline ({roundrobin_baseline_score:.3f}) by {delta_pp:.1f}pp — review before deploying[/yellow]")`,**不 exit、不 raise**
      - 否则:`console.print(f"[green]Joint score ({evolved_score:.3f}) ≥ round-robin baseline ({roundrobin_baseline_score:.3f}) within epsilon ({EPSILON_PP*100:.0f}pp)[/green]")`
    - metrics dict 装配时根据 `effective_mode` 加字段:
      - `mode`(always)
      - joint mode 下额外加 `joint_score`(= evolved_score)、`roundrobin_baseline_score`、`epsilon_pp`(= EPSILON_PP)、`joint_vs_roundrobin_delta_pp`、`ab_elapsed_seconds`
    - joint mode 下额外落:
      - `output_dir / "roundrobin_baseline_evolved_sections.json"`:从 ab_baseline_module.get_evolved_sections() 提取
      - `output_dir / "roundrobin_baseline_diff.txt"`:`_generate_diff(original_sections, ab_baseline_evolved_sections)`
    - round-robin mode (`effective_mode == "round-robin"`) 不跑 A/B baseline,metrics dict 只加 `mode: "round-robin"`;不落 baseline 副本文件
  </behavior>

  <action>
    在 `evolution/prompts/evolve_prompt_sections.py`(Plan 17-02 完成后的状态)做以下精确修改:

    **1. 在 evolve() Step 9 holdout evaluation 块完成后(具体是 `improvement = evolved_score - baseline_score` 那一行之后,Plan 17-02 后大约在 L389 附近),插入 A/B baseline 块。**

    在 `improvement = evolved_score - baseline_score` 之后、`# ── 10. Report results` 之前,插入以下新块。注意 W3 + W6 修订:`ab_per_section_budget = iterations * 50`(D-AB-04 — 不压缩,与 round-robin legacy 路径单参数公式 1:1);W3 新增 `joint_vs_roundrobin_delta_pp` 独立计算:

    ```python
        # ── 9.5. Joint mode inline A/B baseline (D-AB-01/02/03/04) ───────────
        roundrobin_baseline_score: Optional[float] = None
        ab_elapsed: float = 0.0
        ab_baseline_evolved_sections: list[PromptSection] = []
        joint_vs_roundrobin_delta_pp: Optional[float] = None

        if effective_mode == "joint":
            # D-AB-04 (W6 revision): per-section baseline budget = iterations*50,
            # 与 round-robin legacy 路径单参数公式 1:1 对齐(NO 压缩),保证 A/B 严格可比
            ab_per_section_budget = iterations * 50

            console.print(
                f"\n[bold]Inline A/B baseline: round-robin on same dataset[/bold]"
            )
            console.print(
                f"  (fresh PromptModule, {len(original_sections)} sections × "
                f"{iterations} iter, max_metric_calls={ab_per_section_budget}/section)"
            )

            ab_baseline_module = PromptModule(original_sections)  # fresh (Pitfall 4)
            ab_start = time.time()

            for ab_sid in ab_baseline_module._section_ids:
                ab_baseline_module.set_active_section(ab_sid)
                ab_section_train = [
                    ex for ex in dataset.train if ex.section_id == ab_sid
                ]
                ab_section_val = [
                    ex for ex in dataset.val if ex.section_id == ab_sid
                ]
                if not ab_section_train:
                    console.print(
                        f"  [dim]No training data for {ab_sid}, skipping[/dim]"
                    )
                    continue

                ab_temp_ds = PromptBehavioralDataset(
                    train=ab_section_train,
                    val=ab_section_val,
                    holdout=[],
                )
                ab_trainset = ab_temp_ds.to_dspy_examples(
                    "train", section_texts=section_texts
                )
                ab_valset = ab_temp_ds.to_dspy_examples(
                    "val", section_texts=section_texts
                )

                try:
                    ab_reflection_lm = dspy.LM(
                        config.optimizer_model, **config.get_lm_kwargs()
                    )
                    ab_optimizer = dspy.GEPA(
                        metric=metric,
                        max_metric_calls=ab_per_section_budget,  # D-AB-04 full budget
                        reflection_lm=ab_reflection_lm,
                        component_selector="round_robin",
                    )
                    ab_baseline_module = ab_optimizer.compile(
                        ab_baseline_module,
                        trainset=ab_trainset,
                        valset=ab_valset,
                    )
                except Exception as e:
                    console.print(
                        f"  [yellow]A/B baseline GEPA failed for {ab_sid} ({e}); "
                        f"skipping this section[/yellow]"
                    )

            # Score A/B baseline on SAME holdout
            ab_scores: list[float] = []
            # Ensure forward() does not raise RuntimeError — set any single active
            if ab_baseline_module._section_ids:
                ab_baseline_module.set_active_section(
                    ab_baseline_module._section_ids[0]
                )
            for ex in holdout_examples:
                with dspy.context(lm=lm):
                    ab_pred = ab_baseline_module(task_input=ex.task_input)
                    ab_scores.append(metric(ex, ab_pred, trace=None))
            roundrobin_baseline_score = (
                sum(ab_scores) / max(1, len(ab_scores)) if ab_scores else 0.0
            )
            ab_elapsed = time.time() - ab_start
            ab_baseline_evolved_sections = ab_baseline_module.get_evolved_sections()

            console.print(
                f"  A/B baseline completed in {ab_elapsed:.1f}s "
                f"(score: {roundrobin_baseline_score:.3f})"
            )

            # ── 9.6. Soft gate (D-AB-02 — warning only, no exit code) ────────
            # W3 revision: explicit two-direction deltas to disambiguate semantics
            # delta_pp: rr_baseline view (positive = rr beats joint, used in warning text)
            # joint_vs_roundrobin_delta_pp: A/B delta (positive = joint wins),
            # persisted to metrics.json as a distinct field from `improvement`
            # (which is evolved_score - baseline_score vs UNOPTIMIZED baseline).
            delta_pp = (roundrobin_baseline_score - evolved_score) * 100
            joint_vs_roundrobin_delta_pp = (evolved_score - roundrobin_baseline_score) * 100
            if evolved_score < roundrobin_baseline_score - EPSILON_PP:
                console.print(
                    f"[yellow]Joint score ({evolved_score:.3f}) below "
                    f"round-robin baseline ({roundrobin_baseline_score:.3f}) "
                    f"by {delta_pp:.1f}pp — review before deploying[/yellow]"
                )
            else:
                console.print(
                    f"[green]Joint score ({evolved_score:.3f}) ≥ "
                    f"round-robin baseline ({roundrobin_baseline_score:.3f}) "
                    f"within epsilon ({EPSILON_PP * 100:.0f}pp)[/green]"
                )
    ```

    **2. 修改 metrics dict 装配(Plan 17-02 后大约在 L429-445)— 加入 5 新字段(W3 revision 添加 `joint_vs_roundrobin_delta_pp`):**

    将原:
    ```python
        metrics = {
            "timestamp": timestamp,
            "iterations": iterations,
            "eval_model": config.eval_model,
            "baseline_score": baseline_score,
            "evolved_score": evolved_score,
            "improvement": improvement,
            "section_count": len(evolved_sections),
            "train_examples": len(dataset.train),
            "val_examples": len(dataset.val),
            "holdout_examples": len(dataset.holdout),
            "elapsed_seconds": elapsed,
            "constraints_passed": True,
        }
    ```

    替换为(注意 `improvement` 注释明确区分两种 delta 语义):
    ```python
        # NOTE (W3 revision): `improvement` = evolved_score - baseline_score
        # (vs unoptimized prompt baseline_score). This is DISTINCT from
        # `joint_vs_roundrobin_delta_pp` (added below in joint mode) which is
        # the A/B delta: joint_score - roundrobin_baseline_score. Downstream
        # dashboards MUST NOT conflate the two — improvement gauges absolute
        # quality gain; joint_vs_roundrobin_delta_pp gauges joint mode's edge
        # over the round-robin legacy path on the same holdout.
        metrics = {
            "timestamp": timestamp,
            "mode": effective_mode,
            "iterations": iterations,
            "eval_model": config.eval_model,
            "baseline_score": baseline_score,
            "evolved_score": evolved_score,
            "improvement": improvement,
            "section_count": len(evolved_sections),
            "train_examples": len(dataset.train),
            "val_examples": len(dataset.val),
            "holdout_examples": len(dataset.holdout),
            "elapsed_seconds": elapsed,
            "constraints_passed": True,
        }
        # Joint-mode-only A/B baseline fields (D-OUT-02 + W3 explicit A/B delta)
        if effective_mode == "joint" and roundrobin_baseline_score is not None:
            metrics["joint_score"] = evolved_score
            metrics["roundrobin_baseline_score"] = roundrobin_baseline_score
            metrics["epsilon_pp"] = EPSILON_PP
            metrics["joint_vs_roundrobin_delta_pp"] = joint_vs_roundrobin_delta_pp
            metrics["ab_elapsed_seconds"] = ab_elapsed
    ```

    **3. 在 metrics.json 落盘后(`(output_dir / "metrics.json").write_text(...)` 那一行之后)添加 baseline 副本文件落盘(shared-prefix layout,D-OUT-01):**

    在 `(output_dir / "diff.txt").write_text(diff_text)` 之后、`console.print(f"\n  Output saved to {output_dir}/")` 之前,插入:

    ```python
        # ── 11.5. Joint mode: persist A/B baseline副本文件(shared-prefix layout, D-OUT-01)
        if effective_mode == "joint" and ab_baseline_evolved_sections:
            ab_data = [
                {"section_id": s.section_id, "text": s.text}
                for s in ab_baseline_evolved_sections
            ]
            (output_dir / "roundrobin_baseline_evolved_sections.json").write_text(
                json.dumps(ab_data, indent=2)
            )
            ab_diff_text = _generate_diff(
                original_sections, ab_baseline_evolved_sections
            )
            (output_dir / "roundrobin_baseline_diff.txt").write_text(ab_diff_text)
    ```

    **不修改:**
    - Step 7 extract evolved(L288-289)
    - Step 8 constraint validation(L292-354)— joint mode 全 13 section 自然遍历 per-section growth + non_empty + role 检查,Pattern B7 已锁定零改动
    - Step 9 holdout evaluation(L356-389)— baseline_score / evolved_score 含义与 round-robin 一致(baseline 是未优化原始 prompt,与 round-robin baseline `roundrobin_baseline_score` 是不同概念)
    - Step 10 Report results table 与 Step 11 evolved_sections.json / diff.txt 落盘 — 这些是 joint 主交付物,沿用现有逻辑
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/prompts/ -v -x</automated>
  </verify>

  <done>
    - `grep -q "roundrobin_baseline_score" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "ab_baseline_module = PromptModule(original_sections)" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "ab_per_section_budget = iterations \* 50" evolution/prompts/evolve_prompt_sections.py` 返回 0(W6: D-AB-04 全量预算)
    - `grep -q "Inline A/B baseline: round-robin on same dataset" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "review before deploying" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "within epsilon" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "roundrobin_baseline_evolved_sections.json" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "roundrobin_baseline_diff.txt" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "\"mode\": effective_mode" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "metrics\\[\"epsilon_pp\"\\] = EPSILON_PP" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `grep -q "metrics\\[\"joint_vs_roundrobin_delta_pp\"\\]" evolution/prompts/evolve_prompt_sections.py` 返回 0(W3 新字段)
    - `grep -q "component_selector=\"round_robin\"" evolution/prompts/evolve_prompt_sections.py` 返回 0
    - `.venv/bin/python -m pytest tests/prompts/ -v -x` 返回 exit 0(全部 PASS,本任务依赖 Task 2 添加的 TestABBaseline 测试驱动 RED→GREEN)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 test_evolve_prompt_sections_cli.py 中新增 TestABBaseline 测试类 — 覆盖软门触发 / 不触发 / metrics.json 字段完整性</name>

  <read_first>
    - tests/prompts/test_evolve_prompt_sections_cli.py(Plan 17-02 完成后的状态)— `_patched_run` helper 与 TestJointPipeline / TestDryRun 测试模板。**重点学习 Plan 17-02 的 `spy_module = MagicMock(wraps=PromptModule(fake_sections))` 模式**,Plan 17-03 必须复用以避免 BLOCKER-2(否则 fresh PromptModule(original_sections) 内的 `dspy.ChainOfThought` selector 会发起真 LM 调用)
    - tests/tools/test_evolve_tool_params_cli.py(Phase 13 fake-GEPA mock pattern,如果需要扩展 metric mock 控制 score 序列)
    - **evolution/prompts/prompt_dataset.py L33-51(PromptBehavioralExample 实际签名 — BLOCKER-1 修复:fixture 必须用 `section_id/user_message/expected_behavior` 三必填 + `difficulty/source` 两可选,绝不能写 `task_input` 字段)**
    - .planning/phases/17-joint-section-optimization/17-RESEARCH.md §Validation Architecture(soft-gate test 期望)
    - .planning/phases/17-joint-section-optimization/17-PATTERNS.md §Pattern D2(metric.side_effect 控制分数序列)
    - 本 plan Task 1 的 `<behavior>` 与 `<action>`(metrics.json 字段、stdout 文案、baseline 副本文件名)
    - evolution/prompts/evolve_prompt_sections.py(Task 1 完成后的状态)
  </read_first>

  <files>tests/prompts/test_evolve_prompt_sections_cli.py</files>

  <behavior>
    新增 `TestABBaseline` 类 ≥ 3 测试:
    - `test_joint_mode_runs_inline_ab_baseline`:joint mode 跑完后,metrics.json 含 `mode == "joint"` + `joint_score` + `roundrobin_baseline_score` + `epsilon_pp == 0.01` + `joint_vs_roundrobin_delta_pp`(W3 新字段);同目录下 `roundrobin_baseline_evolved_sections.json` 与 `roundrobin_baseline_diff.txt` 文件存在;`dspy.GEPA` 被调用 N+1 次(1 次 joint + N 次 A/B baseline per-section)
    - `test_soft_gate_warns_but_does_not_block`:控制 metric mock 让 joint score < rr_baseline - 0.01,断言 stdout 含 `[yellow]` 与 "review before deploying";`result.exit_code == 0`;`metrics.json` 仍写入(`constraints_passed == True`);`metrics["joint_vs_roundrobin_delta_pp"]` 为负
    - `test_round_robin_mode_skips_ab_baseline_and_extra_files`:传 `--mode round-robin`,metrics.json 只含 `mode == "round-robin"`,**不含** `joint_score` / `roundrobin_baseline_score` / `epsilon_pp` / `joint_vs_roundrobin_delta_pp` 字段;`roundrobin_baseline_evolved_sections.json` 与 `roundrobin_baseline_diff.txt` **不存在**;`dspy.GEPA` 调用 N 次(不含额外 A/B 部分)

    **BLOCKER-2 修复**:Helper `_ab_patched_run` 必须 patch `PromptModule` 类构造器为返回 spy_module(MagicMock wraps real instance),复用 Plan 17-02 `_patched_run` 的模式 — 因为 evolve() 中 `baseline_module = PromptModule(original_sections)` 与 `ab_baseline_module = PromptModule(original_sections)` 都会构造 real selector(dspy.ChainOfThought),若不 patch 会发起真 LM 调用。每次 PromptModule(...) call 必须返回新 spy(side_effect 工厂函数),且 `module(task_input=...)` 调用要可控(用 MagicMock `__call__.side_effect` 消费 score_sequence)。

    **BLOCKER-1 修复**:fixture 用 `PromptBehavioralExample(section_id=..., user_message=..., expected_behavior=..., difficulty=...)` 四字段(`source` 可选默认 "synthetic"),**不引入 task_input 字段**(那是 dspy.Example 转换后的属性,不属于 PromptBehavioralExample dataclass)。

    Helper 改造:`_patched_run` 已存在(Plan 17-02 Task 2),Task 2 需要在 fixture 中:
    - 提供 holdout 数据(`dataset.holdout` 非空)
    - 提供 `metric` mock,支持 `side_effect = [...]` 序列控制分数
    - patch `Path` / `output_dir` 用 `tmp_path` fixture(pytest 内置)替代真实 `output/prompts/`
    - **patch `PromptModule` 类构造器**(BLOCKER-2 修复,见 action 详细代码)
  </behavior>

  <action>
    在 `tests/prompts/test_evolve_prompt_sections_cli.py` 文件末尾(TestDryRun 类之后)追加 `TestABBaseline` 类。

    **完整新代码(追加在文件末尾)— 注意 BLOCKER-1(PromptBehavioralExample 正确字段)与 BLOCKER-2(patch PromptModule + spy_module 模式)两处修复点已就位:**

    ```python


    class TestABBaseline:
        """Phase 17 — inline A/B baseline + soft gate + metrics.json schema tests.

        These tests verify that joint mode:
          (1) actually runs the round-robin baseline inline after holdout eval
          (2) writes 5 new metrics.json fields
              (mode/joint_score/rr_baseline_score/epsilon_pp/joint_vs_roundrobin_delta_pp)
          (3) writes roundrobin_baseline_*.{json,txt} sibling files
          (4) warns (yellow stdout) but does not exit when joint regresses past epsilon
          (5) round-robin --mode skips A/B and the extra files

        BLOCKER fixes applied (revision pass):
          B1: PromptBehavioralExample fixture uses real dataclass fields only —
              section_id/user_message/expected_behavior/difficulty (NO task_input,
              which is a dspy.Example attribute injected by to_dspy_examples()).
          B2: PromptModule class is patched at the module import site to return
              spy modules wrapping real instances — prevents the fresh
              PromptModule(original_sections) inside the A/B branch from
              instantiating a real dspy.ChainOfThought selector and firing real
              LM calls during holdout scoring. The spy's __call__ is overridden
              to return a deterministic dspy.Prediction, so metric.side_effect
              controls the score sequence.
        """

        def _ab_patched_run(self, argv, fake_sections, score_sequence, tmp_path):
            """Like TestJointPipeline._patched_run but with:
              - dataset.holdout populated so step-9 holdout eval runs
              - metric.side_effect = score_sequence (deterministic A/B scores)
              - output dir redirected to tmp_path via cwd patching
              - PromptModule patched to return spy modules wrapping real instances
                (BLOCKER-2 fix: prevents real LM calls in evolved/baseline/AB scoring)
            """
            import os
            import dspy
            from click.testing import CliRunner
            from evolution.prompts.evolve_prompt_sections import main
            from evolution.prompts.prompt_module import PromptModule
            from evolution.prompts.prompt_dataset import (
                PromptBehavioralExample,
                PromptBehavioralDataset,
            )

            # BLOCKER-1 fix: PromptBehavioralExample uses real dataclass fields.
            # task_input is NOT a field — it appears on dspy.Example after
            # to_dspy_examples() maps user_message -> task_input.
            examples = [
                PromptBehavioralExample(
                    section_id=fake_sections[i % len(fake_sections)].section_id,
                    user_message=f"task {i}",
                    expected_behavior=f"behavior {i}",
                    difficulty="easy",
                )
                for i in range(4)
            ]
            fake_ds = PromptBehavioralDataset(
                train=examples,
                val=examples,
                holdout=examples,
            )

            mock_constraint = MagicMock()
            mock_constraint._check_growth.return_value = MagicMock(
                passed=True, message="ok", constraint_name="growth"
            )
            mock_constraint._check_non_empty.return_value = MagicMock(
                passed=True, message="ok", constraint_name="non_empty"
            )

            mock_role = MagicMock()
            mock_role.check_all.return_value = []

            mock_metric_instance = MagicMock(side_effect=score_sequence)

            mock_builder_instance = MagicMock()
            mock_builder_instance.generate.return_value = fake_ds

            # BLOCKER-2 fix: PromptModule factory returns spy modules wrapping
            # a real PromptModule (so _section_ids / set_active_section /
            # set_joint_mode / get_evolved_sections / named_predictors all work),
            # but with __call__ overridden to return a Prediction without
            # invoking the real dspy.ChainOfThought selector (which would
            # require an LM and fire a network call).
            def _make_spy_module(*args, **kwargs):
                real = PromptModule(fake_sections)
                spy = MagicMock(wraps=real)
                # Expose real attributes (MagicMock-wraps proxies methods but
                # bare attribute reads come from the spy itself)
                spy._section_ids = real._section_ids
                spy._frozen_instructions = real._frozen_instructions
                spy.section_predictors = real.section_predictors
                # get_evolved_sections returns the fake sections as-is
                spy.get_evolved_sections = MagicMock(return_value=fake_sections)
                # named_predictors yields N entries (drives joint budget formula)
                spy.named_predictors = MagicMock(return_value=[
                    (f"section_predictors['{sid}']", MagicMock())
                    for sid in real._section_ids
                ])
                # __call__ → fake Prediction (avoids real selector LM call).
                # This is what makes metric.side_effect deterministic: every
                # module(...) returns the same Prediction shape and metric
                # consumes the next score in side_effect sequence.
                spy.__call__ = MagicMock(
                    return_value=dspy.Prediction(output="mocked output")
                )
                # set_joint_mode / set_active_section delegate to the real
                # instance via wraps (MagicMock auto-proxies); no override.
                return spy

            runner = CliRunner()

            # Run in tmp_path so output/ folder is sandboxed
            orig_cwd = os.getcwd()
            try:
                os.chdir(tmp_path)

                with patch(
                    "evolution.prompts.evolve_prompt_sections.extract_prompt_sections",
                    return_value=fake_sections,
                ), patch(
                    "evolution.prompts.evolve_prompt_sections.PromptDatasetBuilder",
                    return_value=mock_builder_instance,
                ), patch(
                    "evolution.prompts.evolve_prompt_sections.PromptBehavioralMetric",
                    return_value=mock_metric_instance,
                ), patch(
                    "evolution.prompts.evolve_prompt_sections.ConstraintValidator",
                    return_value=mock_constraint,
                ), patch(
                    "evolution.prompts.evolve_prompt_sections.PromptRoleChecker",
                    return_value=mock_role,
                ), patch(
                    "evolution.prompts.evolve_prompt_sections.PromptModule",
                    side_effect=_make_spy_module,  # BLOCKER-2: factory per call
                ), patch(
                    "evolution.prompts.evolve_prompt_sections.dspy.GEPA"
                ) as mock_gepa, patch(
                    "evolution.prompts.evolve_prompt_sections.dspy.LM"
                ), patch(
                    "evolution.prompts.evolve_prompt_sections.dspy.configure"
                ), patch(
                    "evolution.prompts.evolve_prompt_sections.dspy.context",
                    MagicMock(),
                ):
                    # GEPA.compile returns the module unchanged
                    mock_gepa.return_value.compile.side_effect = (
                        lambda mod, trainset, valset=None: mod
                    )
                    result = runner.invoke(main, argv, catch_exceptions=False)

                # Find output_dir created under tmp_path
                output_root = tmp_path / "output" / "prompts"
                run_dirs = (
                    sorted(output_root.iterdir(), key=lambda p: p.stat().st_mtime)
                    if output_root.exists()
                    else []
                )
                latest = run_dirs[-1] if run_dirs else None

                return result, mock_gepa, latest
            finally:
                os.chdir(orig_cwd)

        def test_joint_mode_runs_inline_ab_baseline(self, tmp_path):
            """Joint mode: A/B baseline runs after holdout, metrics.json has 5 new fields,
            baseline副本文件 exist."""
            import json

            fake_sections = _make_fake_sections(3)
            # Score sequence: baseline_module holdout (4) + evolved_module holdout (4)
            # + ab_baseline_module holdout (4) = 12 metric calls minimum.
            # Use 0.5 for baseline, 0.8 for evolved (joint), 0.75 for AB (rr_baseline).
            # joint(0.8) > rr_baseline(0.75) → soft gate green path → metrics.json
            # joint_vs_roundrobin_delta_pp = (0.8 - 0.75) * 100 = +5.0
            scores = [0.5] * 4 + [0.8] * 4 + [0.75] * 4

            result, mock_gepa, latest = self._ab_patched_run(
                ["--mode", "joint", "--iterations", "2", "--hermes-repo", "/fake"],
                fake_sections,
                scores,
                tmp_path,
            )

            assert result.exit_code == 0, f"Joint mode failed: {result.output[:500]}"
            assert latest is not None, "Output directory was not created"
            metrics_path = latest / "metrics.json"
            assert metrics_path.exists(), f"metrics.json missing in {latest}"
            metrics = json.loads(metrics_path.read_text())

            # D-OUT-02: 5 new fields (W3 revision adds joint_vs_roundrobin_delta_pp)
            assert metrics["mode"] == "joint"
            assert "joint_score" in metrics
            assert "roundrobin_baseline_score" in metrics
            assert metrics["epsilon_pp"] == 0.01
            assert "joint_vs_roundrobin_delta_pp" in metrics  # W3 new field
            assert metrics["joint_score"] == 0.8
            assert metrics["roundrobin_baseline_score"] == 0.75
            # Delta is positive when joint wins
            assert metrics["joint_vs_roundrobin_delta_pp"] == pytest.approx(5.0, abs=0.01)

            # D-OUT-01 shared-prefix baseline files
            assert (latest / "roundrobin_baseline_evolved_sections.json").exists(), (
                f"Missing baseline副本 in {latest}"
            )
            assert (latest / "roundrobin_baseline_diff.txt").exists()
            # Joint main artifacts also present
            assert (latest / "evolved_sections.json").exists()
            assert (latest / "diff.txt").exists()

            # GEPA called once for joint + N for A/B baseline per-section
            assert mock_gepa.return_value.compile.call_count == 1 + len(fake_sections), (
                f"Expected {1 + len(fake_sections)} compile calls (1 joint + "
                f"{len(fake_sections)} A/B), got {mock_gepa.return_value.compile.call_count}"
            )

        def test_soft_gate_warns_but_does_not_block(self, tmp_path):
            """When joint_score < rr_baseline - 0.01, stdout warns but exit==0 and
            evolved_sections.json still written."""
            import json

            fake_sections = _make_fake_sections(3)
            # joint=0.50, rr_baseline=0.60 → delta = 10pp > 1pp epsilon → warn
            # joint_vs_roundrobin_delta_pp = (0.50 - 0.60) * 100 = -10.0 (negative = regression)
            scores = [0.4] * 4 + [0.50] * 4 + [0.60] * 4

            result, mock_gepa, latest = self._ab_patched_run(
                ["--mode", "joint", "--iterations", "2", "--hermes-repo", "/fake"],
                fake_sections,
                scores,
                tmp_path,
            )

            # Soft-gate must NOT exit non-zero
            assert result.exit_code == 0, (
                f"Soft gate must not exit non-zero, got {result.exit_code}. "
                f"Output: {result.output[:500]}"
            )
            # Yellow warning text must be in stdout
            output = result.output
            assert "review before deploying" in output, (
                f"Soft-gate warning text missing. Stdout: {output[:800]}"
            )
            # evolved_sections.json still written (not blocked)
            assert latest is not None
            assert (latest / "evolved_sections.json").exists()
            assert (latest / "metrics.json").exists()
            metrics = json.loads((latest / "metrics.json").read_text())
            assert metrics["constraints_passed"] is True
            assert metrics["joint_score"] == 0.50
            assert metrics["roundrobin_baseline_score"] == 0.60
            # W3: delta is NEGATIVE on regression
            assert metrics["joint_vs_roundrobin_delta_pp"] == pytest.approx(-10.0, abs=0.01)

        def test_round_robin_mode_skips_ab_baseline_and_extra_files(self, tmp_path):
            """--mode round-robin: no A/B baseline, no extra files, metrics.json has
            only mode field (no joint_score/rr_score/epsilon_pp/joint_vs_rr_delta)."""
            import json

            fake_sections = _make_fake_sections(3)
            # baseline holdout (4) + evolved holdout (4) = 8 metric calls
            scores = [0.5] * 4 + [0.7] * 4

            result, mock_gepa, latest = self._ab_patched_run(
                ["--mode", "round-robin", "--iterations", "2", "--hermes-repo", "/fake"],
                fake_sections,
                scores,
                tmp_path,
            )

            assert result.exit_code == 0, f"RR mode failed: {result.output[:500]}"
            assert latest is not None

            metrics = json.loads((latest / "metrics.json").read_text())
            assert metrics["mode"] == "round-robin"
            # No joint-mode fields
            assert "joint_score" not in metrics
            assert "roundrobin_baseline_score" not in metrics
            assert "epsilon_pp" not in metrics
            assert "joint_vs_roundrobin_delta_pp" not in metrics  # W3 new field also absent

            # No baseline副本 files in round-robin mode
            assert not (latest / "roundrobin_baseline_evolved_sections.json").exists()
            assert not (latest / "roundrobin_baseline_diff.txt").exists()

            # GEPA called N times for per-section round-robin, no extra A/B calls
            assert mock_gepa.return_value.compile.call_count == len(fake_sections)
    ```

    注:`_ab_patched_run` 用 `os.chdir(tmp_path)` 把 `output/prompts/` 沙盒在测试目录,避免污染真实 `output/`。`finally` 块保证 cwd 还原。`patch(...PromptModule, side_effect=_make_spy_module)` 是 BLOCKER-2 的核心修复 — 每次 `PromptModule(original_sections)` 调用(三次:baseline_module / module / ab_baseline_module)返回独立 spy,wraps 真实实例提供 `_section_ids`/`set_active_section`/`set_joint_mode`/`get_evolved_sections`/`named_predictors`,但 `__call__` 返回 fake Prediction 避免 selector LM 调用。
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/prompts/test_evolve_prompt_sections_cli.py::TestABBaseline -v -x</automated>
  </verify>

  <done>
    - `grep -q "class TestABBaseline" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "test_joint_mode_runs_inline_ab_baseline" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "test_soft_gate_warns_but_does_not_block" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "test_round_robin_mode_skips_ab_baseline_and_extra_files" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "review before deploying" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "roundrobin_baseline_evolved_sections.json" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0
    - `grep -q "epsilon_pp.*0.01" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0(测试断言 epsilon == 0.01)
    - `grep -q "joint_vs_roundrobin_delta_pp" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0(W3 新字段断言)
    - `grep -q "side_effect=_make_spy_module" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0(BLOCKER-2 修复锚点)
    - `grep -q "section_id=fake_sections" tests/prompts/test_evolve_prompt_sections_cli.py` 返回 0(BLOCKER-1 修复锚点:用 dataclass 真实字段)
    - `! grep -q "PromptBehavioralExample.*task_input" tests/prompts/test_evolve_prompt_sections_cli.py`(BLOCKER-1:不再用错误字段)
    - `.venv/bin/python -m pytest tests/prompts/test_evolve_prompt_sections_cli.py::TestABBaseline -v -x` 返回 exit 0
    - `.venv/bin/python -m pytest tests/prompts/ -v -x` 返回 exit 0(全部 PASS)
    - `.venv/bin/python -m pytest tests/ -x -q` 返回 exit 0(全项目零回归 — Phase 1-16 测试均通过)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evolve() joint branch → A/B baseline branch | 同进程内 Python 调用;原 module 已被 GEPA mutate,A/B 通过 `PromptModule(original_sections)` 重建 fresh 起点(Pitfall 4 mitigation) |
| metrics.json 写盘 → output_dir | 本地文件系统,write via `Path.write_text(json.dumps(metrics, indent=2))`;不含 PII/凭据 |
| stdout 软门警告 → 用户终端 | 仅文本 echo,无网络 / 文件外泄 |
| 测试 fixture cwd 切换 → output/prompts/ sandbox | `os.chdir(tmp_path)` 在 try/finally 保证还原,避免污染真实工作目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10 | Tampering | A/B baseline 误复用已 mutate 过的 joint module → A/B 失真(Pitfall 4) | mitigate | 强制 `ab_baseline_module = PromptModule(original_sections)`(新对象,从 frozen 原文起);Test `test_joint_mode_runs_inline_ab_baseline` 在 mock 中验证 `PromptModule()` 至少被实例化 2 次 |
| T-17-11 | Tampering | metrics.json 字段 schema 在 round-robin / joint mode 间不一致导致下游消费方 KeyError | mitigate | `mode` 字段 always 写入;joint-only 字段(joint_score/rr_baseline/epsilon_pp/joint_vs_rr_delta/ab_elapsed)显式 `if effective_mode == "joint"` gate;Test `test_round_robin_mode_skips_ab_baseline_and_extra_files` 验证缺字段语义 |
| T-17-12 | Denial of Service | A/B baseline 在大 N section 下额外 N × `iterations*50` LM 调用(成本 2-3x) | accept | 用户已通过 D-IT-03 stdout 预算预估行被告知;`--mode round-robin` 可显式 opt-out;CONTEXT D-AB-04 已锁定 baseline 不压缩,A/B 对称性优先 |
| T-17-13 | Information Disclosure | `roundrobin_baseline_evolved_sections.json` 与 `roundrobin_baseline_diff.txt` 落 `output/prompts/<ts>/`,与 joint 主交付物同目录,可能被误打包/上传 | accept | 路径 sandbox 在 .gitignore 下(`output/` 已 ignore,Phase 12 post-audit 7500abc);用户审查 commit diff 已能识别 |
| T-17-14 | Repudiation | 软门触发后 evolved_sections.json 仍写盘 → 部署链可能在没看 metrics.json 的情况下应用 | mitigate | metrics.json 中 `mode`、`joint_score`、`roundrobin_baseline_score`、`joint_vs_roundrobin_delta_pp` 四字段提供可审计 trail(W3 修订新增 delta 字段直接给出 A/B 差值,免去消费方自计算);部署链应在 CI 时检查 metrics.json delta(future Phase 22 dashboard 接入将自动化此步) |
| T-17-15 | Input Validation | metric mock 返回非数字导致 sum/division 抛 TypeError | mitigate | `metric` 是 `PromptBehavioralMetric` 实例,返回 float ∈ [0,1](Phase 9 已合规);测试用 0.4-0.8 区间数字 mock side_effect 验证不抛 |

低威胁面理由:Plan 17-03 是 Plan 17-02 的扩展层,新文件 IO 全部在 `output/prompts/<ts>/`(已 .gitignore)边界内,无外部接口、无凭据处理、无用户输入(除 click flag 已在 17-02 校验)。
</threat_model>

<verification>
- `grep -c "roundrobin_baseline" evolution/prompts/evolve_prompt_sections.py` 返回 ≥ 5(变量、metrics 字段、文件名、stdout)
- `grep -q "EPSILON_PP" evolution/prompts/evolve_prompt_sections.py` 返回 0
- `grep -q "ab_baseline_module = PromptModule(original_sections)" evolution/prompts/evolve_prompt_sections.py` 返回 0
- `grep -q "ab_per_section_budget = iterations \* 50" evolution/prompts/evolve_prompt_sections.py` 返回 0(W6: D-AB-04 全量预算)
- `grep -q "joint_vs_roundrobin_delta_pp" evolution/prompts/evolve_prompt_sections.py` 返回 0(W3 新字段)
- `grep -q "component_selector=\"round_robin\"" evolution/prompts/evolve_prompt_sections.py` 返回 0
- `.venv/bin/python -m pytest tests/prompts/test_evolve_prompt_sections_cli.py -v -x` 返回 exit 0,collected ≥ 7 tests(Plan 17-02 的 4 + Plan 17-03 的 3)
- `.venv/bin/python -m pytest tests/prompts/ -v -x` 返回 exit 0
- `.venv/bin/python -m pytest tests/ -x -q` 返回 exit 0(全项目零回归)
- 端到端 CLI smoke(可选,有 OPENAI_API_KEY 时):`.venv/bin/python -m evolution.prompts.evolve_prompt_sections --mode joint --iterations 1 --eval-source load --hermes-repo $HERMES_AGENT_REPO`(若环境 ready)后 inspect `output/prompts/<latest>/metrics.json`:
  - `python -c "import json; m=json.load(open('output/prompts/<ts>/metrics.json')); assert m['mode']=='joint'; assert 'joint_score' in m; assert 'roundrobin_baseline_score' in m; assert m['epsilon_pp']==0.01; assert 'joint_vs_roundrobin_delta_pp' in m"`
  - 该端到端 smoke 不强制 — pytest 套件已覆盖语义;CI 阶段可在 RECEIVING.md 标注为 manual checkpoint
</verification>

<success_criteria>
- ROADMAP §Phase 17 Success Criterion 3 落地:joint mode 落地后 metrics.json 含 `joint_score` + `roundrobin_baseline_score` + `joint_vs_roundrobin_delta_pp` 三数值可直接比较;软门触发时 stdout 警告 + 不阻断;不触发时绿色 success 消息
- D-AB-01/02/03/04 全部生效:inline A/B + 软门 [yellow] 警告 + epsilon=0.01 常量 + baseline 全量 iterations × 50 调用(W6 单参数全量预算,与 round-robin legacy 路径 1:1 对齐)
- D-OUT-01/02/03/04 全部生效:shared-prefix output layout(无 baseline/ subdir)+ metrics.json 5 新字段(W3 新增 joint_vs_roundrobin_delta_pp)+ diff.txt 零代码改动 + 不改 regression_dashboard
- 新 `TestABBaseline` 测试类 ≥ 3 测试全部 PASS(BLOCKER-1 + BLOCKER-2 修复后)
- 全项目测试套件零回归(Phase 1-16 所有测试通过)
- Phase 17 整体目标达成:PMPT-V2-01 实现完毕,joint section optimization 落地,A/B baseline 与软门构成 success criterion 3 的可证伪保护
</success_criteria>

<output>
After completion, create `.planning/phases/17-joint-section-optimization/17-03-SUMMARY.md` documenting:
- 实际修改的行号区间(evolve_prompt_sections.py 新增 A/B 块 + metrics 字段 + baseline 文件落盘)
- 实际 metrics.json schema(joint mode 与 round-robin mode 的字段对比表,含 W3 新字段 joint_vs_roundrobin_delta_pp)
- 软门 stdout 文案精确字串(便于未来 dashboard 或 README 引用)
- 全项目测试数量与通过情况(Phase 17 落地后期望 tests/ ≥ 380 tests 全 PASS)
- Phase 17 整体落地状态:三 plan 与 4 个 resolved decisions 的对账表(每个 D-XX → 实际实现锚点行号)
- 后续 Phase 18 (Personality Drift Detection) 可消费的 Phase 17 接口:`metrics.json` 中 mode/joint_score/rr_baseline/joint_vs_roundrobin_delta_pp 四字段、`output/prompts/<ts>/` shared-prefix layout
</output>
