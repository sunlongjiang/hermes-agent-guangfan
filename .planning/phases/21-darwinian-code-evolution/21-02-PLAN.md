---
phase: 21-darwinian-code-evolution
plan: "02"
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - evolution/code/__init__.py
  - evolution/code/LICENSING.md
  - tests/code/__init__.py
  - output/code/.gitkeep
autonomous: true
requirements: [V2-CODE-01]

must_haves:
  truths:
    - "evolution/code/ 包可被 Python import（无 ImportError），即使 openevolve 未安装"
    - "evolution/code/__init__.py 含 lazy-import guard 说明，不 import openevolve"
    - "evolution/code/LICENSING.md 说明 openevolve Apache-2.0 单点引用 + 本目录 MIT"
    - "tests/code/__init__.py 存在（空文件，使 tests/code/ 成为测试包）"
    - "output/code/.gitkeep 存在（output/ 已 gitignored，占位目录）"
  artifacts:
    - path: "evolution/code/__init__.py"
      provides: "包初始化 + lazy import guard（D-21；analog benchmarks/__init__.py）"
      min_lines: 15
    - path: "evolution/code/LICENSING.md"
      provides: "license boundary 文档（D-21）"
      contains: "Apache-2.0"
    - path: "tests/code/__init__.py"
      provides: "测试包初始化（空文件）"
    - path: "output/code/.gitkeep"
      provides: "output/code/ 目录占位（output/ 在 .gitignore）"
  key_links:
    - from: "evolution/code/__init__.py"
      to: "evolution/code/code_evolver_adapter.py"
      via: "explicit import 说明"
      pattern: "from evolution.code.code_evolver_adapter import"

truths:
  - "D-03: __init__.py 内无 import openevolve（单点 import 面，仅 code_evolver_adapter.py 可写）"
  - "D-21: evolution/code/LICENSING.md 说明本期 license boundary"
  - "PATTERNS.md analog: evolution/benchmarks/__init__.py lazy-import guard 精确复用"
---

<objective>
建立 `evolution/code/` 包骨架和测试包结构，为 Wave 1 并行实现的四个核心模块提供 import 基础。

包含：
1. `evolution/code/__init__.py` — lazy-import guard（analog: benchmarks/__init__.py），说明 openevolve 未安装时不 crash
2. `evolution/code/LICENSING.md` — license boundary 文档（D-21）
3. `tests/code/__init__.py` — 空文件，使测试包可被 pytest 发现
4. `output/code/.gitkeep` — 目录占位（output/ 已 gitignored）

Purpose: Wave 1 的四个核心模块（code_target_loader / code_fitness / sandbox_runner / code_evolver_adapter）需要 `evolution/code/` 包存在。所有 tests/code/ 测试文件需要 `tests/code/__init__.py` 存在。

Output: 4 个文件（2 个包 init + 1 个 doc + 1 个占位）
</objective>

<execution_context>
@/Users/slj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-darwinian-code-evolution/21-CONTEXT.md
@.planning/phases/21-darwinian-code-evolution/21-PATTERNS.md
@.planning/phases/21-darwinian-code-evolution/21-01-SUMMARY.md

<interfaces>
<!-- analog: evolution/benchmarks/__init__.py（PATTERNS.md lines 42-69）完整模板 -->
```python
# evolution/benchmarks/__init__.py  lines 1-15
"""Phase 20: Benchmark-gated validation for evolved prompt artifacts.

Lazy-import guard (Phase 20 D-Discretion-1): submodules
(tblite_runner / benchmark_gate / build_tblite_calibration) are NOT
auto-imported here. Callers must explicitly:

    from evolution.benchmarks.benchmark_gate import TBLiteBenchmarkGate
    from evolution.benchmarks.tblite_runner import TBLiteRunner

Rationale: hermes-agent or huggingface_hub may be unreachable on a
given dev machine; `evolve_prompt_sections --benchmark=none` (the
default) MUST keep working without surfacing ImportError from the
evolution package's __init__ chain. Eager imports here would cascade
failure into every CLI entrypoint that touches evolution.*.
"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>任务 1：创建 evolution/code/__init__.py（lazy-import guard）</name>
  <files>evolution/code/__init__.py</files>
  <read_first>
    - .planning/phases/21-darwinian-code-evolution/21-PATTERNS.md §evolution/code/__init__.py（lines 38-69）
    - .planning/phases/21-darwinian-code-evolution/21-CONTEXT.md §D-03（lines 52-53）
  </read_first>
  <action>
    创建 `evolution/code/__init__.py`，直接适配 PATTERNS.md 中 benchmarks/__init__.py 的模板：
    
    ```python
    """Phase 21: Darwinian code evolution for hermes-agent components.
    
    Lazy-import guard (D-21 / D-03): submodules are NOT auto-imported here.
    Callers must explicitly import what they need:
    
        from evolution.code.code_evolver_adapter import evolve_code
        from evolution.code.code_fitness import CodeFitness, score_candidate
        from evolution.code.code_target_loader import CodeTarget, find_target
        from evolution.code.sandbox_runner import run_pytest_in_sandbox
    
    Rationale: openevolve is an optional dependency (`pip install .[code]`).
    `evolve_code --dry-run` or any import of `evolution.code` MUST NOT crash
    when openevolve is not installed. Importing `evolution.code.code_evolver_adapter`
    at module level would trigger ImportError if openevolve is absent.
    
    Only `evolution/code/code_evolver_adapter.py` may contain `import openevolve`
    or `from openevolve ...` statements (D-03 single import surface).
    """
    ```
    
    文件只含 docstring，无任何实际 import 语句。
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,''); import evolution.code; print('import OK')"</automated>
  </verify>
  <done>
    - python -c "import evolution.code" 在 openevolve 未安装环境无 ImportError
    - grep -n "import openevolve\|from openevolve" evolution/code/__init__.py 输出 0 行（D-03 验证）
    - wc -l evolution/code/__init__.py 输出 ≥ 15（含完整 docstring）
  </done>
</task>

<task type="auto" tdd="false">
  <name>任务 2：创建 evolution/code/LICENSING.md 和测试/output 占位文件</name>
  <files>
    evolution/code/LICENSING.md
    tests/code/__init__.py
    output/code/.gitkeep
  </files>
  <read_first>
    - .planning/phases/21-darwinian-code-evolution/21-CONTEXT.md §D-21（lines 124-124）
    - .planning/phases/21-darwinian-code-evolution/21-PATTERNS.md §evolution/code/LICENSING.md（lines 468-478）
    - .planning/phases/21-darwinian-code-evolution/21-PATTERNS.md §output/code/.gitkeep（lines 728-734）
  </read_first>
  <action>
    **文件 1: evolution/code/LICENSING.md**
    
    内容按 D-21 决策（CONTEXT.md lines 124-124）和 PATTERNS.md adapt_notes（lines 472-476）：
    
    ```markdown
    # evolution/code/ — License Boundary
    
    ## Package Contents
    
    This directory (`evolution/code/`) is part of the `hermes-agent-self-evolution`
    project and is licensed under the **MIT License** (see `LICENSE` at the repository
    root).
    
    ## External Dependency: openevolve
    
    `evolution/code/code_evolver_adapter.py` imports `openevolve` as an **optional**
    dependency (`pip install .[code]`).
    
    - **openevolve**: Apache License 2.0 (https://github.com/codelion/openevolve)
    - **Version pinned**: openevolve>=0.2.27
    - **Import surface**: ONLY `code_evolver_adapter.py` may contain `import openevolve`
      or `from openevolve ...` statements. All other files in this directory are
      openevolve-free.
    
    ## Output Artifacts
    
    Files written to `output/code/<timestamp>/` are generated by this pipeline and
    are covered by the project MIT License. They are NOT derived works of openevolve
    (openevolve generates LLM-driven mutations; the resulting Python code belongs to
    the project).
    
    ## AGPL Status
    
    openevolve is Apache-2.0. The previous `darwinian-evolver` dependency (AGPL v3)
    has been permanently removed from `pyproject.toml`. No AGPL code is present in
    this package.
    ```
    
    **文件 2: tests/code/__init__.py** — 空文件（analog: tests/__init__.py）
    
    **文件 3: output/code/.gitkeep** — 空文件（output/ 已在 .gitignore，占位目录）
    
    注意：创建 output/code/ 目录时，output/ 已 gitignored，.gitkeep 可被 git 跟踪（!output/.gitkeep 不需要，因为 output/ 下的 .gitkeep 在 .gitignore 规则下也被忽略；直接 `mkdir -p output/code && touch output/code/.gitkeep` 即可让目录物理存在）。
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; files=['evolution/code/LICENSING.md','tests/code/__init__.py','output/code/.gitkeep']; [print(f,Path(f).exists()) for f in files]; assert all(Path(f).exists() for f in files), 'missing files'; text=Path('evolution/code/LICENSING.md').read_text(); assert 'Apache-2.0' in text and 'MIT License' in text and 'code_evolver_adapter.py' in text; print('OK')"</automated>
  </verify>
  <done>
    - ls evolution/code/LICENSING.md tests/code/__init__.py output/code/.gitkeep 全部存在
    - grep "Apache-2.0" evolution/code/LICENSING.md 输出 1 行
    - grep "MIT License" evolution/code/LICENSING.md 输出 1 行
    - grep "code_evolver_adapter.py" evolution/code/LICENSING.md 输出 1 行（单点 import 面边界说明）
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| evolution/code/ import → Python runtime | 包初始化时不触发 openevolve import |

## STRIDE 威胁登记册

| 威胁 ID | 类别 | 组件 | 处置 | 缓解方案 |
|---------|------|------|------|----------|
| T-21-IMPORT | Tampering | evolution/code/__init__.py | mitigate | D-03 __init__.py 内无 import openevolve；grep gate 在 Plan 06 pytest 测试中验证 |
| T-21-SECRET | Information Disclosure | sandbox_runner | accept（本 plan 不涉及） | Plan 05 实现 restricted_env |
| T-21-RECURSE | Elevation of Privilege | code_target_loader | accept（本 plan 不涉及） | Plan 03 实现 evolution/ 路径 reject |
| T-21-DOS | DoS | sandbox subprocess | accept（本 plan 不涉及） | Plan 05 实现 timeout |
| T-21-LEAK | Information Disclosure | eval_dir tmp | accept（本 plan 不涉及） | Plan 05 实现 cleanup |
| T-21-SUPPLY | Tampering | openevolve | accept | Apache-2.0；pinned >=0.2.27 |
| T-21-UNREVIEWED | Elevation of Privilege | output/ evolved code | mitigate | LICENSING.md 明确 output-only 策略；NOTICE.md 在 Plan 07 实现 |
</threat_model>

<verification>
```bash
# 1. evolution/code 包可 import（无 ImportError）
python -c "import evolution.code; print('package import OK')"

# 2. __init__.py 无 openevolve import（D-03）
grep -n "^import openevolve\|^from openevolve" evolution/code/__init__.py | wc -l
# 期望: 0

# 3. LICENSING.md 内容完整
grep "Apache-2.0" evolution/code/LICENSING.md && grep "code_evolver_adapter.py" evolution/code/LICENSING.md && echo "LICENSING OK"

# 4. tests/code/__init__.py 存在
ls tests/code/__init__.py && echo "test package OK"

# 5. output/code/.gitkeep 存在
ls output/code/.gitkeep && echo "gitkeep OK"
```
</verification>

<success_criteria>
- `python -c "import evolution.code"` 无 ImportError
- `grep -v "^#" evolution/code/__init__.py | grep "import openevolve"` 输出 0 行
- `grep "Apache-2.0" evolution/code/LICENSING.md` 输出 1 行
- `ls tests/code/__init__.py output/code/.gitkeep` 无错误
</success_criteria>

<output>
完成后创建 `.planning/phases/21-darwinian-code-evolution/21-02-SUMMARY.md`。
</output>
