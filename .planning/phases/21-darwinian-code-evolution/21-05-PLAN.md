---
phase: 21-darwinian-code-evolution
plan: "05"
type: execute
wave: 1
depends_on: ["21-02"]
files_modified:
  - evolution/code/sandbox_runner.py
  - tests/code/test_sandbox_runner.py
autonomous: true
requirements: [V2-CODE-01]

must_haves:
  truths:
    - "build_restricted_env() 返回的 env dict 不含 OPENAI_API_KEY / OPENROUTER_API_KEY / DASHSCOPE_KEY / EVOLUTION_API_KEY 等 API key（T-21-SECRET）"
    - "run_pytest_in_sandbox() subprocess timeout 触发 → 返回 (0, -1, [{test_name:'timeout', ...}])（T-21-DOS）"
    - "run_pytest_in_sandbox() 执行后 eval_dir 已被清理（T-21-LEAK）"
    - "PYTHONPATH=str(eval_dir) 防止 candidate 访问真实 hermes-agent import（Pitfall 3）"
    - "4 个单测全 pass（restricted_env / timeout / cleanup / hermes_import_防御）"
  artifacts:
    - path: "evolution/code/sandbox_runner.py"
      provides: "run_pytest_in_sandbox + build_restricted_env（D-09/D-20）"
      min_lines: 100
      exports: ["run_pytest_in_sandbox", "build_restricted_env"]
    - path: "tests/code/test_sandbox_runner.py"
      provides: "4 个单测覆盖安全沙箱核心路径"
      min_lines: 80
  key_links:
    - from: "sandbox_runner.build_restricted_env()"
      to: "subprocess.run env= 参数"
      via: "restricted_env dict（删去 API keys）"
      pattern: "_API_KEY_ENV_VARS"
    - from: "sandbox_runner.run_pytest_in_sandbox()"
      to: "eval_dir (~/.hermes/tmp/code_eval_<ts>_<id>/)"
      via: "shutil.rmtree finally 清理"
      pattern: "shutil.rmtree"

truths:
  - "D-20: restricted_env 删去 OPENAI_API_KEY/OPENROUTER_API_KEY/DASHSCOPE_KEY/EVOLUTION_API_KEY/ANTHROPIC_API_KEY/GEMINI_API_KEY/MODAL_TOKEN_ID/MODAL_TOKEN_SECRET"
  - "D-09: 最小 import 闭包 = tools/__init__.py（空）+ tools/ansi_strip.py（candidate）+ test_ansi_strip.py；RESEARCH.md §D-09 已验证"
  - "D-20: subprocess timeout=120；timeout 触发 → return (0, -1, [timeout_result])，不 raise"
  - "T-21-LEAK: eval_dir 在 finally 块清理（shutil.rmtree ignore_errors=True），无论是否成功"
  - "Pitfall 3: PYTHONPATH=str(eval_dir) 覆盖，不允许 candidate 跨界 import hermes"
  - "PATTERNS.md analog: tblite_runner.py（简化版，用 subprocess.run 不用 Popen + 心跳）"
---

<objective>
实现 `sandbox_runner.py`：每个 candidate 评分时的隔离运行环境——subprocess + restricted_env + timeout + eval_dir 清理。

这是 T-21-SECRET / T-21-DOS / T-21-LEAK 三大安全威胁的主要缓解点。

核心功能（D-09/D-20）：
1. `build_restricted_env(eval_dir)` — 删去所有 API key 环境变量
2. `run_pytest_in_sandbox(candidate_path, eval_dir_base, test_file_path, run_id, hermes_agent_path, timeout_seconds=120)` — 最小 import 闭包 + subprocess + 清理

Analog: `evolution/benchmarks/tblite_runner.py`（简化版，无 heartbeat，短任务用 subprocess.run 不用 Popen）

Purpose: code_fitness.py 的 score_candidate() 调用本模块；code_evolver_adapter 的 evaluator 文件也调用本模块。

Output: evolution/code/sandbox_runner.py + tests/code/test_sandbox_runner.py（4 unit tests）
</objective>

<execution_context>
@/Users/slj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-darwinian-code-evolution/21-CONTEXT.md
@.planning/phases/21-darwinian-code-evolution/21-RESEARCH.md
@.planning/phases/21-darwinian-code-evolution/21-PATTERNS.md
@.planning/phases/21-darwinian-code-evolution/21-02-SUMMARY.md

<interfaces>
<!-- restricted_env 构造（RESEARCH.md lines 613-629，已验证）-->
```python
_API_KEY_ENV_VARS = {
    "OPENAI_API_KEY", "OPENROUTER_API_KEY", "DASHSCOPE_KEY",
    "EVOLUTION_API_KEY", "ANTHROPIC_API_KEY", "GEMINI_API_KEY",
    "MODAL_TOKEN_ID", "MODAL_TOKEN_SECRET",
}

def build_restricted_env(eval_dir: Path) -> dict:
    env = os.environ.copy()
    for key in _API_KEY_ENV_VARS:
        env.pop(key, None)
    env["HERMES_AGENT_REPO"] = str(eval_dir)  # 隔离到 eval_dir
    env["PYTHONPATH"] = str(eval_dir)
    return env
```

<!-- sandbox_runner 核心（RESEARCH.md lines 571-611，已验证）-->
```python
def run_pytest_in_sandbox(candidate_path, eval_dir_base, test_file_path, run_id, restricted_env, timeout_seconds=120):
    eval_dir = eval_dir_base / run_id
    eval_dir.mkdir(parents=True, exist_ok=True)
    try:
        tools_dir = eval_dir / "tools"
        tools_dir.mkdir(exist_ok=True)
        (tools_dir / "__init__.py").write_text("")
        shutil.copy2(candidate_path, tools_dir / "ansi_strip.py")
        shutil.copy2(test_file_path, eval_dir / "test_ansi_strip.py")
        env = restricted_env.copy()
        env["PYTHONPATH"] = str(eval_dir)
        result = subprocess.run(
            [sys.executable, "-m", "pytest", "test_ansi_strip.py", "-x", "--tb=line", "-q", "--no-header"],
            cwd=str(eval_dir), env=env, timeout=timeout_seconds, capture_output=True, text=True,
        )
        return _parse_pytest_output(result.stdout, result.returncode)
    except subprocess.TimeoutExpired:
        return 0, -1, [{"test_name": "timeout", "assertion_msg": f"Timeout after {timeout_seconds}s", "traceback_one_line": ""}]
    finally:
        shutil.rmtree(eval_dir, ignore_errors=True)
```

<!-- Analog: tblite_runner.py 简化模式（PATTERNS.md §sandbox_runner.py lines 320-384）-->
<!-- Analog: subprocess check=False（evolution/core/constraints.py lines 56-93）-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：创建 evolution/code/sandbox_runner.py</name>
  <files>evolution/code/sandbox_runner.py</files>
  <read_first>
    - .planning/phases/21-darwinian-code-evolution/21-PATTERNS.md §sandbox_runner.py（lines 316-384）
    - .planning/phases/21-darwinian-code-evolution/21-RESEARCH.md §sandbox_runner 核心（lines 571-611）
    - .planning/phases/21-darwinian-code-evolution/21-RESEARCH.md §restricted_env 构造（lines 613-629）
    - .planning/phases/21-darwinian-code-evolution/21-RESEARCH.md §Pitfall 3（lines 505-509）
    - .planning/phases/21-darwinian-code-evolution/21-RESEARCH.md §Pitfall 6（lines 521-527）
    - evolution/benchmarks/tblite_runner.py（读取 subprocess + timeout + SIGTERM/SIGKILL 模式，lines 253-326；用作简化参考）
  </read_first>
  <behavior>
    - build_restricted_env(eval_dir) 删去 OPENAI_API_KEY 等 8 个 API key env vars
    - run_pytest_in_sandbox() timeout 触发 → return (0, -1, [{"test_name": "timeout", ...}])
    - run_pytest_in_sandbox() 正常运行后 eval_dir 已被 shutil.rmtree 清理
    - run_pytest_in_sandbox() 内 PYTHONPATH=str(eval_dir)（不含真实 hermes-agent）
    - _parse_pytest_output("1 passed", returncode=0) → (1, 1, [])
    - _parse_pytest_output("1 failed, 0 passed\nFAILED test_x - AssertionError: x", returncode=1) → (0, 1, [{test_name, ...}])
  </behavior>
  <action>
    实现 `evolution/code/sandbox_runner.py`，按以下规范：
    
    **1. 导入**
    ```python
    import os
    import re
    import shutil
    import sys
    import subprocess
    from pathlib import Path
    from typing import Optional
    ```
    无 openevolve import（D-03），无 dspy import（D-14）。
    
    **2. _API_KEY_ENV_VARS** — set 含 8 个 key（来自 RESEARCH.md §restricted_env）
    
    **3. build_restricted_env(eval_dir: Path) -> dict**
    - os.environ.copy()
    - pop 所有 _API_KEY_ENV_VARS
    - 设置 HERMES_AGENT_REPO=str(eval_dir)（隔离）
    - 设置 PYTHONPATH=str(eval_dir)
    - 返回 dict
    
    **4. run_pytest_in_sandbox(candidate_path, eval_dir_base, test_file_path, run_id, timeout_seconds=120) -> tuple[int, int, list[dict]]**
    - eval_dir = eval_dir_base / run_id
    - try:
      - eval_dir.mkdir(parents=True, exist_ok=True)
      - tools_dir = eval_dir / "tools"; tools_dir.mkdir()
      - (tools_dir / "__init__.py").write_text("")
      - shutil.copy2(candidate_path, tools_dir / "ansi_strip.py")
      - shutil.copy2(test_file_path, eval_dir / "test_ansi_strip.py")
      - restricted_env = build_restricted_env(eval_dir)
      - result = subprocess.run([sys.executable, "-m", "pytest", "test_ansi_strip.py", "-x", "--tb=line", "-q", "--no-header"], cwd=str(eval_dir), env=restricted_env, timeout=timeout_seconds, capture_output=True, text=True)
      - return _parse_pytest_output(result.stdout, result.returncode)
    - except subprocess.TimeoutExpired: return (0, -1, [{"test_name": "timeout", "assertion_msg": f"Timeout after {timeout_seconds}s", "traceback_one_line": ""}])
    - finally: shutil.rmtree(eval_dir, ignore_errors=True)
    
    **5. _parse_pytest_output(stdout: str, returncode: int) -> tuple[int, int, list[dict]]**
    - 解析 pytest --tb=line -q 输出
    - 提取 "X passed" → passed; "X failed" → failed；total = passed + failed
    - 失败行格式："FAILED test_id - ExcType: msg"；用 re.findall 提取
    - 返回 (passed, total, failures)
    - 若 returncode == 0 且没有提取到 passed 数量（空 stdout），返回 (0, 0, [])
    
    **Pitfall 3 防御（PYTHONPATH 覆盖）：** restricted_env["PYTHONPATH"] = str(eval_dir)（只含 eval_dir，不含真实 hermes-agent 路径）
    
    **Pitfall 6 防御（eval_dir 清理）：** finally 块 shutil.rmtree(eval_dir, ignore_errors=True) — 无论成功失败都清理 eval_dir（不清理 eval_dir_base，仅清理本次候选的目录）
  </action>
  <verify>
    <automated>pytest tests/code/test_sandbox_runner.py -x -q</automated>
  </verify>
  <done>
    - pytest tests/code/test_sandbox_runner.py 输出 "4 passed"
    - grep "shutil.rmtree" evolution/code/sandbox_runner.py 输出 ≥ 1 行（cleanup）
    - grep "OPENAI_API_KEY" evolution/code/sandbox_runner.py 输出 ≥ 1 行（_API_KEY_ENV_VARS）
    - grep "import openevolve\|from openevolve" evolution/code/sandbox_runner.py 输出 0 行
    - grep "PYTHONPATH" evolution/code/sandbox_runner.py 输出 ≥ 1 行（Pitfall 3）
  </done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：创建 tests/code/test_sandbox_runner.py（4 个单测）</name>
  <files>tests/code/test_sandbox_runner.py</files>
  <read_first>
    - .planning/phases/21-darwinian-code-evolution/21-VALIDATION.md §Per-Task Verification Map（lines 55-58）
    - .planning/phases/21-darwinian-code-evolution/21-PATTERNS.md §test_sandbox_runner.py（lines 672-681）
    - tests/benchmarks/test_tblite_runner.py（读取 mock subprocess 模式，lines 28-84）
  </read_first>
  <behavior>
    - test_restricted_env_removes_api_keys：OPENAI_API_KEY 不在 build_restricted_env 输出中
    - test_sandbox_timeout_returns_zero_fitness：mock TimeoutExpired → (0, -1, [{"test_name":"timeout",...}])
    - test_eval_dir_is_cleaned_after_run：run 后 eval_dir 不存在
    - test_candidate_with_implicit_hermes_import_fails_cleanly：含 "import hermes" 的 candidate → pytest 失败，reject_reason 含 "pytest_fail"，sandbox 不 crash
  </behavior>
  <action>
    实现 `tests/code/test_sandbox_runner.py`：
    
    **test_restricted_env_removes_api_keys(tmp_path)**
    - 强制设置 os.environ["OPENAI_API_KEY"] = "sk-test"（用 monkeypatch）
    - env = build_restricted_env(tmp_path)
    - assert "OPENAI_API_KEY" not in env
    - assert "HERMES_AGENT_REPO" in env and str(tmp_path) in env["HERMES_AGENT_REPO"]
    - assert "PYTHONPATH" in env
    
    **test_sandbox_timeout_returns_zero_fitness(tmp_path)**
    - 使用 unittest.mock.patch("evolution.code.sandbox_runner.subprocess.run", side_effect=subprocess.TimeoutExpired("pytest", 120))
    - 创建 dummy candidate_path 和 test_file_path（空文件）
    - result = run_pytest_in_sandbox(candidate_path, tmp_path/"evals", test_file_path, "test_run")
    - assert result == (0, -1, [{"test_name": "timeout", "assertion_msg": "Timeout after 120s", "traceback_one_line": ""}])
    
    **test_eval_dir_is_cleaned_after_run(tmp_path)**
    - 创建真实 candidate_path = tmp_path/"candidate.py"（写入最简 Python，无 `import hermes`）
    - 创建真实 test_file_path = tmp_path/"test_simple.py"（含 1 个 test_pass 函数，import path 指向 eval_dir）
    - 注意：此测试可能需要真实运行 pytest（较慢）；如果太慢，用 mock subprocess.run 返回 "1 passed\n"
    - eval_dir = tmp_path / "evals" / "test_cleanup"
    - run_pytest_in_sandbox(candidate_path, tmp_path/"evals", test_file_path, "test_cleanup")
    - assert not eval_dir.exists()（eval_dir 已清理）
    
    **test_candidate_with_implicit_hermes_import_fails_cleanly(tmp_path)**
    - 创建 candidate_path，内容含 `import hermes`（隐式越界 import）
    - 创建 test_file_path，导入并测试 candidate 中的函数
    - mock subprocess.run 返回 returncode=1，stdout="FAILED test_x - ImportError: No module named 'hermes'"
    - 调用 run_pytest_in_sandbox()
    - assert result[0] == 0（passed=0），assert len(result[2]) > 0（有失败记录）
    - assert sandbox 未 raise 异常（"fails_cleanly" 的核心断言）
    
    所有测试均 mock subprocess（除 test_eval_dir_is_cleaned_after_run 可选真实运行）。
    确保 `import subprocess` 可在 mock 中正确匹配（patch "evolution.code.sandbox_runner.subprocess.run"）。
  </action>
  <verify>
    <automated>pytest tests/code/test_sandbox_runner.py -x -q</automated>
  </verify>
  <done>
    - pytest tests/code/test_sandbox_runner.py 输出 "4 passed"
    - pytest tests/code/ -x -q 全部 pass（累积）
    - grep "TimeoutExpired" tests/code/test_sandbox_runner.py 输出 ≥ 1 行（T-21-DOS 验证）
    - grep "OPENAI_API_KEY" tests/code/test_sandbox_runner.py 输出 ≥ 1 行（T-21-SECRET 验证）
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| 父进程 env → restricted_env | 删去 API key；eval_dir 覆盖 HERMES_AGENT_REPO |
| candidate Python 文件 → pytest subprocess | PYTHONPATH 限制；只允许 eval_dir + stdlib |
| eval_dir → 磁盘 | finally 清理防磁盘耗尽 |

## STRIDE 威胁登记册

| 威胁 ID | 类别 | 组件 | 处置 | 缓解方案 |
|---------|------|------|------|----------|
| T-21-SECRET | Information Disclosure | build_restricted_env | mitigate | _API_KEY_ENV_VARS pop 全部 API key；test_restricted_env_removes_api_keys 机械化验证 |
| T-21-DOS | DoS | subprocess timeout | mitigate | timeout=120s；TimeoutExpired → (0, -1, [timeout])；test_sandbox_timeout 验证 |
| T-21-LEAK | Information Disclosure | eval_dir tmp | mitigate | finally shutil.rmtree(eval_dir, ignore_errors=True)；test_eval_dir_is_cleaned_after_run 验证 |
| T-21-IMPORT | Tampering | sandbox_runner.py | mitigate | 无 import openevolve；grep gate 验证 |
| T-21-RECURSE | Elevation of Privilege | candidate PYTHONPATH | mitigate | PYTHONPATH=str(eval_dir) 只含 eval_dir，不含真实 hermes-agent；test_candidate_with_implicit_hermes_import 验证 |
| T-21-SUPPLY | Tampering | openevolve | accept | 本 plan 不 import openevolve |
| T-21-UNREVIEWED | Elevation of Privilege | output/ | accept（Plan 07 实现） | — |
</threat_model>

<verification>
```bash
# 1. 4 个单测全 pass
pytest tests/code/test_sandbox_runner.py -x -q

# 2. T-21-SECRET：API key 清理
grep "_API_KEY_ENV_VARS\|OPENAI_API_KEY" evolution/code/sandbox_runner.py

# 3. T-21-LEAK：eval_dir 清理
grep "shutil.rmtree" evolution/code/sandbox_runner.py

# 4. T-21-DOS：timeout 处理
grep "TimeoutExpired" evolution/code/sandbox_runner.py

# 5. D-03：无 openevolve import
grep "^import openevolve\|^from openevolve" evolution/code/sandbox_runner.py | wc -l
# 期望: 0

# 6. Pitfall 3 防御
grep "PYTHONPATH" evolution/code/sandbox_runner.py

# 7. 全套测试回归
pytest tests/code/ -x -q
```
</verification>

<success_criteria>
- `pytest tests/code/test_sandbox_runner.py -x -q` 输出 "4 passed"
- `grep "shutil.rmtree" evolution/code/sandbox_runner.py` 输出 ≥ 1 行
- `grep "OPENAI_API_KEY" evolution/code/sandbox_runner.py` 输出 ≥ 1 行（_API_KEY_ENV_VARS 成员）
- `grep "TimeoutExpired" evolution/code/sandbox_runner.py` 输出 ≥ 1 行
- `grep "^import openevolve\|^from openevolve" evolution/code/sandbox_runner.py | wc -l` == 0
</success_criteria>

<output>
完成后创建 `.planning/phases/21-darwinian-code-evolution/21-05-SUMMARY.md`。
</output>
