---
phase: 21-darwinian-code-evolution
plan: "06"
type: execute
wave: 1
depends_on: ["21-02", "21-03", "21-04", "21-05"]
files_modified:
  - evolution/code/code_evolver_adapter.py
  - tests/code/test_import_boundary.py
autonomous: true
requirements: [V2-CODE-01]

must_haves:
  truths:
    - "evolution/code/code_evolver_adapter.py 是项目内唯一含 import openevolve 或 from openevolve 的文件"
    - "test_import_boundary.py::test_openevolve_import_only_in_adapter 用 pathlib 扫描 evolution/ 所有 .py，验证仅 code_evolver_adapter.py 中有 openevolve import"
    - "code_evolver_adapter.py 暴露窄接口：evolve(target, config, output_dir, iterations) -> EvolutionAdapterResult"
    - "adapter 动态生成 self-contained evaluator .py 文件（Pitfall 1 防御）"
    - "adapter 在 initial_program 中插入 EVOLVE-BLOCK-START/END，只包裹 strip_ansi 函数体（Pitfall 5 防御）"
    - "import openevolve 失败时（未安装）adapter 抛 ImportError + 友好提示 'pip install .[code]'"
  artifacts:
    - path: "evolution/code/code_evolver_adapter.py"
      provides: "openevolve API 单点 import facade + evolve() 接口（D-03/D-04/D-05）"
      min_lines: 150
      exports: ["evolve", "EvolutionAdapterResult"]
    - path: "tests/code/test_import_boundary.py"
      provides: "D-18 CI lint gate pytest 第二层"
      min_lines: 40
  key_links:
    - from: "code_evolver_adapter.evolve()"
      to: "openevolve.run_evolution()"
      via: "adapter 封装，不暴露 openevolve 内部类型"
      pattern: "from openevolve import.*run_evolution"
    - from: "evaluator .py 文件（动态生成）"
      to: "evolution.code.code_fitness.score_candidate()"
      via: "module-level constants（eval_dir_base / baseline_size）"
      pattern: "module-level.*eval_dir_base\|baseline_size"

truths:
  - "D-01: openevolve (Apache-2.0, >=0.2.27) 是本期唯一 evolutionary code search 库；adapter 封装其 run_evolution / Population / Archive 调度"
  - "D-03: code_evolver_adapter.py 是唯一含 import openevolve 的文件；test_import_boundary 机械化验证"
  - "D-04: adapter 用 EvolutionConfig.optimizer_model/api_base/api_key 构建 openevolve LLMModelConfig"
  - "D-05: 不复用 GEPA reflection_lm；接受 openevolve 自己的反思机制"
  - "D-16: evaluator 返回 feedback 字典含 pytest_failures / size_ratio / ruff_violations / composite_fitness / decision / reject_reason，作为下一轮 mutation prompt 的 context（openevolve 反思机制消费）"
  - "RESEARCH.md Pitfall 1: evaluator 必须是 self-contained .py 文件（非 lambda/closure）"
  - "RESEARCH.md Pitfall 5: EVOLVE-BLOCK 只包裹函数体，import re 和常量定义在块外"
  - "RESEARCH.md §openevolve API（lines 239-330）: run_evolution 接口已验证；best_code 是字符串"
---

<objective>
实现 `code_evolver_adapter.py`（项目内唯一 import openevolve 的文件）+ `test_import_boundary.py`（D-18 CI lint gate 第二层）。

这是 D-03 单点 import 面架构的实施核心。adapter 封装 openevolve 的全部调用细节，对外只暴露：
- `evolve(target, config, output_dir, iterations)` → `EvolutionAdapterResult`

adapter 内部处理：
- openevolve Config 构造（RESEARCH.md 已验证 API）
- EVOLVE-BLOCK 标记注入（Pitfall 5 防御）
- evaluator .py 动态生成（Pitfall 1 防御）
- EvolutionConfig → LLMModelConfig 的配置桥接（D-04）

同时，test_import_boundary.py 建立 D-18 pytest 第二层防御（pre-commit 是第一层，pytest 是第二层，互补）。

Purpose: evolve_code.py（Plan 07）调用 adapter 的 evolve()，不直接接触 openevolve 内部 API。

Output: evolution/code/code_evolver_adapter.py + tests/code/test_import_boundary.py（1 unit test）
</objective>

<execution_context>
@/Users/slj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-darwinian-code-evolution/21-CONTEXT.md
@.planning/phases/21-darwinian-code-evolution/21-RESEARCH.md
@.planning/phases/21-darwinian-code-evolution/21-PATTERNS.md
@.planning/phases/21-darwinian-code-evolution/21-02-SUMMARY.md
@.planning/phases/21-darwinian-code-evolution/21-03-SUMMARY.md
@.planning/phases/21-darwinian-code-evolution/21-04-SUMMARY.md
@.planning/phases/21-darwinian-code-evolution/21-05-SUMMARY.md

<interfaces>
<!-- openevolve Config 构造（RESEARCH.md §Code Examples lines 539-565，已验证）-->
```python
from openevolve import Config, run_evolution
from openevolve.config import LLMModelConfig

def _build_oe_config(evolution_config, iterations: int, sandbox_timeout: int) -> Config:
    oe_config = Config()
    model_cfg = LLMModelConfig(
        name=evolution_config.optimizer_model,
        api_base=evolution_config.api_base or "https://api.openai.com/v1",
        api_key=evolution_config.api_key,
        temperature=0.7,
        max_tokens=4096,
        timeout=sandbox_timeout,
    )
    oe_config.llm.models = [model_cfg]
    oe_config.llm.evaluator_models = [model_cfg]
    oe_config.max_iterations = iterations
    oe_config.database.population_size = 50
    oe_config.database.archive_size = 20
    oe_config.database.num_islands = 3
    oe_config.evaluator.timeout = sandbox_timeout
    oe_config.evaluator.cascade_evaluation = False
    oe_config.evaluator.parallel_evaluations = 1
    return oe_config
```

<!-- openevolve run_evolution 接口（RESEARCH.md §best candidate 获取 lines 293-311，已验证）-->
```python
result = openevolve.run_evolution(
    initial_program=str(target_path),  # 含 EVOLVE-BLOCK 的源文件路径
    evaluator=str(evaluator_file_path),  # self-contained evaluator .py
    config=oe_config,
    iterations=max_iterations,
    output_dir=str(oe_internal_dir),
    cleanup=False,
)
best_code = result.best_code  # str（代码字符串）
best_score = result.best_score  # float
```

<!-- evaluator .py 动态生成模板（RESEARCH.md §fitness function 接入接口 lines 268-286）-->
```python
# evaluator_template（写入临时文件，module-level constants = self-contained）
EVALUATOR_TEMPLATE = '''
import sys
from pathlib import Path
sys.path.insert(0, "{project_root}")

EVAL_DIR_BASE = Path("{eval_dir_base}")
BASELINE_SIZE = {baseline_size}
TEST_FILE_PATH = Path("{test_file_path}")
TRAIN_TEST_IDS = {train_test_ids!r}

def evaluate(program_path: str) -> dict:
    from evolution.code.code_fitness import score_candidate
    from evolution.code.code_target_loader import CodeTarget
    fitness = score_candidate(
        evolved_path=Path(program_path),
        eval_dir=EVAL_DIR_BASE,
        baseline_size=BASELINE_SIZE,
        train_test_ids=TRAIN_TEST_IDS,
    )
    return {{
        "combined_score": fitness.composite,
        "pytest_passed": fitness.pytest_passed,
        "pytest_total": fitness.pytest_total,
        "size_component": fitness.size_component,
        "ruff_score": fitness.ruff_score,
        "pytest_failures": fitness.pytest_failures,
        "ruff_violations": fitness.ruff_findings,
    }}
'''
```

<!-- EVOLVE-BLOCK 标记注入（RESEARCH.md §evolution markers 要求 lines 327-331）-->
<!-- PATTERNS.md adapt_notes §code_evolver_adapter.py lines 306-311 -->
```
注意：EVOLVE-BLOCK 只包裹 strip_ansi 函数体（从 def strip_ansi 行开始到文件末尾），
保留 import re 和模块常量在块外。
```

<!-- EvolutionAdapterResult dataclass（adapter 窄接口，不暴露 openevolve 内部类型）-->
```python
@dataclass
class EvolutionAdapterResult:
    best_code: str               # evolved code str
    best_score: float            # combined_score
    iterations_run: int
    output_dir: Path             # openevolve internal checkpoint dir
    optimizer_used: str = "openevolve"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：创建 tests/code/test_import_boundary.py（D-18 pytest 第二层）</name>
  <files>tests/code/test_import_boundary.py</files>
  <read_first>
    - .planning/phases/21-darwinian-code-evolution/21-VALIDATION.md §Per-Task Verification Map（line 44）
    - .planning/phases/21-darwinian-code-evolution/21-PATTERNS.md §test_import_boundary.py（lines 567-603）
    - .planning/phases/21-darwinian-code-evolution/21-CONTEXT.md §D-18（lines 98-101）
  </read_first>
  <behavior>
    - test_openevolve_import_only_in_adapter：扫描 evolution/ 所有 .py，仅 code_evolver_adapter.py 含 openevolve import，其余 0 个
  </behavior>
  <action>
    创建 `tests/code/test_import_boundary.py`：
    
    ```python
    """D-18 CI lint gate — pytest 第二层防御。
    
    验证 `import openevolve` 和 `from openevolve` 仅出现在
    `evolution/code/code_evolver_adapter.py`（单点 import 面）。
    
    不 import openevolve 本身 — 允许在 openevolve 未安装的环境中通过。
    运行时间 < 1s（纯文件读取，无 LLM 调用）。
    """
    
    import re
    from pathlib import Path
    
    
    _IMPORT_PATTERN = re.compile(r"^(?:import openevolve|from openevolve)", re.MULTILINE)
    _ALLOWED_FILE = Path("evolution/code/code_evolver_adapter.py")
    
    
    class TestImportBoundary:
        def test_openevolve_import_only_in_adapter(self):
            """pathlib 扫描 evolution/ 所有 .py，正则匹配 openevolve import，
            断言仅 code_evolver_adapter.py 中出现。"""
            evolution_dir = Path("evolution")
            violations = []
            for py_file in evolution_dir.rglob("*.py"):
                if "__pycache__" in str(py_file):
                    continue
                content = py_file.read_text(encoding="utf-8", errors="ignore")
                if _IMPORT_PATTERN.search(content):
                    if py_file != _ALLOWED_FILE:
                        violations.append(str(py_file))
            
            assert violations == [], (
                f"D-18 VIOLATION: openevolve import found outside code_evolver_adapter.py:\n"
                + "\n".join(f"  {v}" for v in violations)
            )
    ```
    
    注意：此时 code_evolver_adapter.py 可能尚未创建，test 应该 pass（无任何文件含 openevolve import）。创建 adapter 后再次运行，仅 adapter 文件命中，test 仍应 pass。
  </action>
  <verify>
    <automated>pytest tests/code/test_import_boundary.py -x -q</automated>
  </verify>
  <done>
    - pytest tests/code/test_import_boundary.py 输出 "1 passed"
    - 运行时间 < 2s
  </done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：创建 evolution/code/code_evolver_adapter.py（唯一 openevolve import 文件）</name>
  <files>evolution/code/code_evolver_adapter.py</files>
  <read_first>
    - .planning/phases/21-darwinian-code-evolution/21-RESEARCH.md §openevolve API Surface（lines 239-330）
    - .planning/phases/21-darwinian-code-evolution/21-RESEARCH.md §Pitfall 1（lines 487-495）
    - .planning/phases/21-darwinian-code-evolution/21-RESEARCH.md §Pitfall 5（lines 516-521）
    - .planning/phases/21-darwinian-code-evolution/21-PATTERNS.md §code_evolver_adapter.py（lines 251-313）
    - evolution/benchmarks/benchmark_gate.py（读取 TBLiteBenchmarkGate 单点 facade 模式，lines 95-200）
    - evolution/core/config.py（读取 EvolutionConfig 字段结构，lines 1-100）
  </read_first>
  <behavior>
    - evolve(target, config, output_dir, iterations) → EvolutionAdapterResult（不暴露 openevolve 内部类型）
    - 若 openevolve 未安装 → ImportError with message "openevolve not installed. Run: pip install .[code]"
    - adapter 生成 self-contained evaluator .py 文件（含 eval_dir_base / baseline_size 等 module-level constants）
    - EVOLVE-BLOCK 只包裹函数体（import re 保留在块外）
    - openevolve Config: population_size=50, archive_size=20, num_islands=3, parallel_evaluations=1
  </behavior>
  <action>
    实现 `evolution/code/code_evolver_adapter.py`：
    
    **关键架构约束（D-03）：此文件是项目内唯一写 import openevolve / from openevolve 的文件。**
    
    **1. 顶层 import 守卫**
    ```python
    try:
        from openevolve import Config, run_evolution
        from openevolve.config import LLMModelConfig
    except ImportError as e:
        raise ImportError(
            "openevolve not installed. Run: pip install .[code]\n"
            f"Original error: {e}"
        ) from e
    ```
    
    **2. EvolutionAdapterResult dataclass**
    字段：best_code: str, best_score: float, iterations_run: int, output_dir: Path, optimizer_used: str = "openevolve"
    含 to_dict() 方法（供 metrics.json 落盘）
    
    **3. _inject_evolve_blocks(source: str) -> str**
    - 在 `strip_ansi` 函数 def 行前一行插入 `# EVOLVE-BLOCK-START`
    - 在文件末尾（最后一行之后）追加 `# EVOLVE-BLOCK-END`
    - 如果已有这两个标记，不重复插入
    - import re 行保留在块外（不包含在 EVOLVE-BLOCK 内）
    
    **4. _generate_evaluator_file(eval_dir_base, baseline_size, test_file_path, train_test_ids, output_dir) -> Path**
    - 生成 self-contained evaluator .py 文件到 output_dir/"evaluator.py"
    - 使用 EVALUATOR_TEMPLATE（见 interfaces 区）
    - project_root = str(Path(__file__).parent.parent.parent)（evolution/ 的父目录）
    - 返回 evaluator 文件路径
    
    **5. _build_oe_config(evolution_config, iterations, sandbox_timeout=120) -> Config**
    按 RESEARCH.md §Code Examples 精确实现（见 interfaces）
    
    **6. evolve(target, config, output_dir, iterations, sandbox_timeout=120) -> EvolutionAdapterResult**
    - output_dir.mkdir(parents=True, exist_ok=True)
    - 在 target.original_source 中注入 EVOLVE-BLOCK 标记 → 写入临时 initial_program.py
    - 调用 _generate_evaluator_file(...)
    - 调用 _build_oe_config(config, iterations, sandbox_timeout)
    - oe_internal_dir = output_dir / "openevolve_internal"
    - result = run_evolution(initial_program=str(initial_program_path), evaluator=str(evaluator_path), config=oe_config, iterations=iterations, output_dir=str(oe_internal_dir), cleanup=False)
    - 返回 EvolutionAdapterResult(best_code=result.best_code, best_score=result.best_score, iterations_run=iterations, output_dir=oe_internal_dir, optimizer_used="openevolve")
    
    **注意：** tests/code/test_evolve_code_cli.py（Plan 07）的 mock 路径是 `"evolution.code.code_evolver_adapter.evolve"`，与此函数名对应。
  </action>
  <verify>
    <automated>pytest tests/code/test_import_boundary.py -x -q</automated>
  </verify>
  <done>
    - pytest tests/code/test_import_boundary.py 输出 "1 passed"（adapter 存在后仍 pass，因为 adapter 在允许列表中）
    - grep "^from openevolve\|^import openevolve" evolution/code/code_evolver_adapter.py 输出 ≥ 1 行（确认是唯一的）
    - grep "^from openevolve\|^import openevolve" evolution/code/*.py | grep -v "code_evolver_adapter.py" | wc -l 输出 0（D-03 机械化验证）
    - grep "EVOLVE-BLOCK-START" evolution/code/code_evolver_adapter.py 输出 ≥ 1 行（Pitfall 5）
    - grep "run_evolution" evolution/code/code_evolver_adapter.py 输出 ≥ 1 行
    - grep "population_size.*50\|archive_size.*20" evolution/code/code_evolver_adapter.py 输出 ≥ 1 行（PoC 保守参数）
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| evolution/ package → openevolve | 单点 import 面，D-03 架构约束 |
| evaluator .py 文件 → openevolve subprocess | self-contained，不依赖父进程闭包 |
| initial_program + EVOLVE-BLOCK → openevolve mutation | 只允许包裹函数体，不包括 import 行 |

## STRIDE 威胁登记册

| 威胁 ID | 类别 | 组件 | 处置 | 缓解方案 |
|---------|------|------|------|----------|
| T-21-IMPORT | Tampering | code_evolver_adapter.py | mitigate | test_import_boundary 机械化扫描 evolution/ 所有 .py，D-18 双层（pre-commit + pytest） |
| T-21-SECRET | Information Disclosure | evaluator .py / sandbox | mitigate | sandbox_runner.build_restricted_env() 在 Plan 05 实现；evaluator 生成时不写 API key 到文件 |
| T-21-RECURSE | Elevation of Privilege | initial_program path | mitigate | code_target_loader.find_target() 已拒绝 evolution/ 路径（Plan 03）；adapter 不二次验证（依赖上游） |
| T-21-DOS | DoS | openevolve evaluator timeout | mitigate | oe_config.evaluator.timeout = sandbox_timeout（120s）；与 sandbox_runner timeout 对齐 |
| T-21-LEAK | Information Disclosure | evaluator .py 临时文件 | mitigate | 写入 output_dir（永久产物目录），不写入 /tmp；output_dir 产物供人工审计 |
| T-21-SUPPLY | Tampering | openevolve 0.2.27 | accept | Apache-2.0；pinned >=0.2.27；ImportError guard 提供友好提示 |
| T-21-UNREVIEWED | Elevation of Privilege | best_code 写入 output/ | mitigate | Plan 07 evolve_code.py 生成 NOTICE.md（D-19）；output-only 策略 |
</threat_model>

<verification>
```bash
# 1. D-18 pytest 第二层通过
pytest tests/code/test_import_boundary.py -x -q

# 2. D-03 单点 import 面验证（机械化）
grep -rn "^import openevolve\|^from openevolve" evolution/ --include="*.py" --exclude-dir=__pycache__ | grep -v "evolution/code/code_evolver_adapter.py" | wc -l
# 期望: 0

# 3. adapter 本身含 openevolve import
grep "^from openevolve\|^import openevolve" evolution/code/code_evolver_adapter.py

# 4. EVOLVE-BLOCK 标记（Pitfall 5）
grep "EVOLVE-BLOCK-START\|EVOLVE-BLOCK-END" evolution/code/code_evolver_adapter.py

# 5. PoC 保守参数
grep "population_size\|archive_size\|num_islands" evolution/code/code_evolver_adapter.py

# 6. 全套测试回归
pytest tests/code/ -x -q
```
</verification>

<success_criteria>
- `pytest tests/code/test_import_boundary.py -x -q` 输出 "1 passed"
- `grep -rn "^import openevolve\|^from openevolve" evolution/ --include="*.py" --exclude-dir=__pycache__ | grep -v "code_evolver_adapter.py" | wc -l` == 0（D-03 机械化）
- `grep "EVOLVE-BLOCK-START" evolution/code/code_evolver_adapter.py` 输出 ≥ 1 行（Pitfall 5）
- `grep "population_size.*50" evolution/code/code_evolver_adapter.py` 输出 ≥ 1 行（保守 PoC 参数）
</success_criteria>

<output>
完成后创建 `.planning/phases/21-darwinian-code-evolution/21-06-SUMMARY.md`。
</output>
