---
phase: 21-darwinian-code-evolution
plan: "07"
type: execute
wave: 2
depends_on: ["21-03", "21-04", "21-05", "21-06"]
files_modified:
  - evolution/code/evolve_code.py
  - tests/code/test_evolve_code_cli.py
autonomous: true
requirements: [V2-CODE-01]

must_haves:
  truths:
    - "--dry-run 模式：pre-flight + baseline，不调用 code_evolver_adapter.evolve()"
    - "pre-flight 7 步检查：openevolve 可 import / hermes-agent 路径 / 目标文件 / .gitignore output/ / LICENSE 存在 / pre-commit hook / EvolutionConfig 解析。任一失败 SystemExit(1)"
    - "成功 evolve 产物落 output/code/<ts>/{component.py, NOTICE.md, metrics.json, diff.txt, eval_holdout.json}"
    - "失败产物落 output/code/FAILED_<ts>/（相同结构 + reject_reason）"
    - "NOTICE.md 含 'UNREVIEWED — DO NOT MERGE WITHOUT HUMAN REVIEW'（D-19）"
    - "NOTICE.md 写入前 _contains_secret 过滤（Pitfall 7）"
    - "CLI flags：--component / --iterations / --max-cost / --hermes-repo / --model / --api-base / --dry-run / --allow-fallback"
    - "3 个 E2E CLI 测试全 pass（dry_run_exits_0 / preflight_fails_without_license / cli_passes_model）"
  artifacts:
    - path: "evolution/code/evolve_code.py"
      provides: "Click CLI 入口 + evolve() 业务函数 + NOTICE.md 生成（D-04/D-19/D-21）"
      min_lines: 200
      exports: ["main", "evolve"]
    - path: "tests/code/test_evolve_code_cli.py"
      provides: "3 个 E2E CLI 测试（dry-run + pre-flight + model flag）"
      min_lines: 100
  key_links:
    - from: "evolve_code.py evolve()"
      to: "code_evolver_adapter.evolve()"
      via: "from evolution.code.code_evolver_adapter import evolve as adapter_evolve"
      pattern: "code_evolver_adapter"
    - from: "evolve_code.py evolve()"
      to: "output/code/<ts>/NOTICE.md"
      via: "NOTICE_TEMPLATE.format(...)"
      pattern: "UNREVIEWED"

truths:
  - "D-04: evolve_code.py 不含 import openevolve（只 adapter 才含）；用 EvolutionConfig.load() 5-param 模式"
  - "D-19: output/code/<ts>/NOTICE.md 含 LLM 生成源 + UNREVIEWED 警示 + fitness 指标"
  - "CONCERNS H1/H3: evolve_code.py 使用 EvolutionConfig.load() 多层配置链（不是 raw constructor），对齐 evolve_tool_descriptions.py 三件套"
  - "PATTERNS.md analog: evolve_tool_descriptions.py Click CLI 三件套（lines 387-464）"
  - "PATTERNS.md analog: evolve_prompt_sections.py FAILED_<ts>/ 输出拓扑（lines 430-442）"
  - "Pitfall 7（RESEARCH.md line 529）: NOTICE.md 写入前 _contains_secret 过滤"
---

<objective>
实现 `evolve_code.py`：Phase 21 的 Click CLI 入口，端到端连接 pre-flight → CodeTarget → baseline → openevolve evolve → holdout gate → output/code/<ts>/。

核心流程（CONTEXT.md §Integration Points lines 249-258）：
1. pre-flight（7 步硬检查）
2. EvolutionConfig.load(5-param)
3. CodeTarget = find_target(component)
4. find_target_tests → stratify → train/holdout
5. baseline_metrics = score_candidate(original)
6. code_evolver_adapter.evolve() → best_candidate
7. holdout_metrics = score_candidate(best_candidate, holdout_tests)
8. accept → output/code/<ts>/ + NOTICE.md + metrics.json
9. reject → output/code/FAILED_<ts>/

Analog: evolve_tool_descriptions.py（Click 三件套 + EvolutionConfig.load + dry-run + FAILED 路径）

Purpose: 这是 V2-CODE-01 的主要用户入口点。`python -m evolution.code.evolve_code --component tools/ansi_strip.py --dry-run` 必须以 exit 0 返回。

Output: evolution/code/evolve_code.py + tests/code/test_evolve_code_cli.py（3 E2E tests）
</objective>

<execution_context>
@/Users/slj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-darwinian-code-evolution/21-CONTEXT.md
@.planning/phases/21-darwinian-code-evolution/21-RESEARCH.md
@.planning/phases/21-darwinian-code-evolution/21-PATTERNS.md
@.planning/phases/21-darwinian-code-evolution/21-06-SUMMARY.md

<interfaces>
<!-- Analog：evolve_tool_descriptions.py Click CLI 三件套（PATTERNS.md lines 392-464）-->
```python
# evolution/tools/evolve_tool_descriptions.py  lines 467-490
@click.command()
@click.option("--iterations", default=10, help="Number of GEPA iterations")
@click.option("--eval-source", ...)
@click.option("--hermes-repo", default=None)
@click.option("--dry-run", is_flag=True)
@click.option("--model", default=None, help="Override model for all LLM calls")
@click.option("--api-base", default=None)
def main(iterations, eval_source, hermes_repo, dry_run, model, api_base, ...):
    """Evolve hermes-agent tool descriptions using DSPy + GEPA optimization."""
    evolve(iterations=iterations, ...)

if __name__ == "__main__":
    main()
```

<!-- Analog：EvolutionConfig.load 5-param（PATTERNS.md lines 392-403）-->
```python
# evolution/tools/evolve_tool_descriptions.py  lines 134-141
config = EvolutionConfig.load(
    iterations=iterations,
    hermes_repo=hermes_repo,
    model=model,
    api_base=api_base,
)
```

<!-- Analog：FAILED_<ts>/ + metrics.json 输出拓扑（PATTERNS.md lines 430-442）-->
```python
# evolution/prompts/evolve_prompt_sections.py  lines 1372-1375
timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
output_dir = Path("output") / "prompts" / timestamp
output_dir.mkdir(parents=True, exist_ok=True)
...
(output_dir / "metrics.json").write_text(json.dumps(metrics, indent=2))
(output_dir / "diff.txt").write_text(diff_text)
```

<!-- D-19 NOTICE.md 模板（CONTEXT.md lines 103-117）-->
```
# NOTICE — LLM-generated code
This code was generated by openevolve via Phase 21 darwinian-code-evolution pipeline.

- Source library: openevolve >=0.2.27 (Apache-2.0)
- Target: hermes-agent/<component_path>
- Generated: <timestamp>
- Status: UNREVIEWED — DO NOT MERGE WITHOUT HUMAN REVIEW
- Fitness: pytest <P>/<T>, size_component <S>, ruff_score <R>, composite <C>
- Holdout: pytest <HP>/<HT> (status: <pass|fail>)

Per project policy (PROJECT.md), evolved artifacts are output-only.
Auto-merge is explicitly out of scope.
```

<!-- pre-flight 检查顺序（CONTEXT.md §Specific Ideas lines 356-365）-->
```
1. import openevolve 成功（code_evolver_adapter 可 import）
2. hermes-agent/tools/ansi_strip.py 存在
3. hermes-agent/tests/tools/test_ansi_strip.py 存在 & AST 解析成功
4. .gitignore 含 output/
5. LICENSE 在 repo root 存在 & 非空
6. .pre-commit-config.yaml 含 openevolve-single-import-surface（失败 warn，不阻塞）
7. EvolutionConfig.load 三层链解析成功 + api_key 非空
```

<!-- _contains_secret（Pitfall 7 防御）-->
```python
from evolution.core.external_importers import _contains_secret
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：创建 evolution/code/evolve_code.py（Click CLI + evolve() + NOTICE.md）</name>
  <files>evolution/code/evolve_code.py</files>
  <read_first>
    - .planning/phases/21-darwinian-code-evolution/21-PATTERNS.md §evolve_code.py（lines 387-464）
    - .planning/phases/21-darwinian-code-evolution/21-CONTEXT.md §Integration Points（lines 249-258）
    - .planning/phases/21-darwinian-code-evolution/21-CONTEXT.md §Pre-flight check 顺序（lines 356-365）
    - .planning/phases/21-darwinian-code-evolution/21-CONTEXT.md §D-19 NOTICE.md 模板（lines 103-117）
    - evolution/tools/evolve_tool_descriptions.py（读取 Click 三件套 + dry-run 模式 + EvolutionConfig.load，lines 110-200 + 467-490）
    - evolution/prompts/evolve_prompt_sections.py（读取 FAILED_<ts>/ + metrics.json 写盘，lines 1320-1375）
  </read_first>
  <behavior>
    - main(--dry-run) → evolve() pre-flight 通过 + baseline 计算后返回，不调用 adapter.evolve()，exit 0
    - main(--component missing.py) → pre-flight 失败 FileNotFoundError → SystemExit(1)
    - main(--component ansi_strip.py, no LICENSE) → pre-flight "LICENSE not found" → SystemExit(1)
    - main(--model qwen-plus) → EvolutionConfig.optimizer_model == "qwen-plus"
    - NOTICE.md 内容含 "UNREVIEWED — DO NOT MERGE WITHOUT HUMAN REVIEW"
  </behavior>
  <action>
    实现 `evolution/code/evolve_code.py`，按 evolve_tool_descriptions.py 三件套模式：
    
    **1. 导入块**
    ```python
    import difflib
    import json
    import sys
    from datetime import datetime
    from pathlib import Path
    
    import click
    from rich.console import Console
    from rich.panel import Panel
    from rich.table import Table
    
    from evolution.core.config import EvolutionConfig
    from evolution.core.external_importers import _contains_secret
    from evolution.code.code_target_loader import find_target, find_target_tests, stratify_tests
    from evolution.code.code_fitness import score_candidate, CodeFitness
    ```
    注意：`import openevolve` 不在此文件（D-04；只有 adapter 才含）。
    code_evolver_adapter 在函数内 lazy import，防止 openevolve 未安装时 CLI help 报错：
    ```python
    # 在 evolve() 函数内部，执行 adapter 调用前：
    from evolution.code.code_evolver_adapter import evolve as _adapter_evolve, EvolutionAdapterResult
    ```
    
    **2. NOTICE_TEMPLATE 常量**（按 D-19 CONTEXT.md 模板，Python f-string 格式）
    
    **3. _run_preflight(component: str, hermes_repo: Path, config: EvolutionConfig) -> None**
    按 CONTEXT.md §pre-flight 顺序执行 7 步；任一失败 raise SystemExit(1)；第 6 步（pre-commit hook）失败只 warn，不阻塞。
    使用 Rich console.print 输出检查状态。
    
    **4. evolve(config, component, dry_run=False) -> None**
    - 调用 _run_preflight()
    - timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    - target = find_target(component, config.hermes_agent_path)
    - manifest = find_target_tests(target)
    - splits = stratify_tests(manifest, seed=42)
    - eval_dir_base = Path.home() / ".hermes" / "tmp"
    - baseline_fitness = score_candidate(target.component_path, eval_dir_base, target.baseline_size_bytes, splits["train_ids"])（传入 target.test_file_path 给 sandbox_runner）
    - Rich Panel 展示 baseline 指标
    - if dry_run: console.print("[green]DRY RUN -- pre-flight + baseline validated.[/green]"); return
    - lazy import _adapter_evolve
    - output_dir = Path("output") / "code" / timestamp; output_dir.mkdir(parents=True, exist_ok=True)
    - adapter_result = _adapter_evolve(target, config, output_dir, config.iterations)（调用 Plan 06 的 evolve()）
    - best_path = output_dir / target.component_path.name; best_path.write_text(adapter_result.best_code)
    - holdout_fitness = score_candidate(best_path, eval_dir_base, target.baseline_size_bytes, splits["holdout_ids"])
    - _check_holdout_gate(holdout_fitness) → bool
    - if gate pass: 写 metrics.json + diff.txt + NOTICE.md（过滤 secret）; Rich Panel success
    - if gate fail: output_dir.rename(Path("output")/"code"/f"FAILED_{timestamp}"); 写 metrics.json（含 reject_reason）+ NOTICE.md（含 reject）; console.print error; raise SystemExit(1)
    
    **5. _check_holdout_gate(fitness: CodeFitness) -> bool**（D-15）
    - fitness.pytest_passed == fitness.pytest_total AND fitness.size_component >= 0.7 AND fitness.ruff_score >= 0.4
    
    **6. _write_notice(output_dir, target, timestamp, holdout_fitness, reject_reason="") -> None**
    - NOTICE.md 模板渲染（含 fitness 指标）
    - 使用 _contains_secret 过滤任何来自 pytest_failures/ruff_findings 的文本（Pitfall 7）
    
    **7. @click.command() main()** — 按 CONTEXT.md §CLI signature（lines 343-353）
    flags: --component / --iterations(20) / --max-cost(5.0) / --hermes-repo / --model / --api-base / --dry-run / --allow-fallback
    调用 evolve(config, component, dry_run=dry_run)
    
    **8. if __name__ == "__main__": main()**
  </action>
  <verify>
    <automated>pytest tests/code/test_evolve_code_cli.py -x -q</automated>
  </verify>
  <done>
    - pytest tests/code/test_evolve_code_cli.py 输出 "3 passed"
    - grep "^import openevolve\|^from openevolve" evolution/code/evolve_code.py 输出 0 行（D-03/D-04）
    - grep "EvolutionConfig.load" evolution/code/evolve_code.py 输出 ≥ 1 行（CONCERNS H1/H3 修复）
    - grep "UNREVIEWED" evolution/code/evolve_code.py 输出 ≥ 1 行（D-19 NOTICE.md）
    - grep "_contains_secret" evolution/code/evolve_code.py 输出 ≥ 1 行（Pitfall 7）
  </done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：创建 tests/code/test_evolve_code_cli.py（3 个 E2E CLI 测试）</name>
  <files>tests/code/test_evolve_code_cli.py</files>
  <read_first>
    - .planning/phases/21-darwinian-code-evolution/21-VALIDATION.md §Per-Task Verification Map（lines 59-61）
    - .planning/phases/21-darwinian-code-evolution/21-PATTERNS.md §test_evolve_code_cli.py（lines 684-712）
    - tests/tools/test_evolve_tool_descriptions.py（读取 CliRunner + mock 模式，lines 40-65）
  </read_first>
  <behavior>
    - test_dry_run_exits_0_without_openevolve_call：--dry-run 下 code_evolver_adapter.evolve 未被调用，exit 0
    - test_preflight_fails_without_license：LICENSE 不存在 → pre-flight SystemExit(1)，exit_code != 0
    - test_cli_passes_model_to_evolution_config：--model qwen-plus 正确传入 EvolutionConfig
  </behavior>
  <action>
    实现 `tests/code/test_evolve_code_cli.py`：
    
    ```python
    """E2E CLI tests for evolution/code/evolve_code.py.
    
    所有测试 mock code_evolver_adapter.evolve 和 score_candidate，
    不真实调用 openevolve 或 pytest 子进程。
    """
    
    from pathlib import Path
    from unittest.mock import patch, MagicMock
    
    import pytest
    from click.testing import CliRunner
    
    from evolution.code.evolve_code import main
    ```
    
    **test_dry_run_exits_0_without_openevolve_call(tmp_path)**
    - 构建 mock hermes-agent 目录（tools/ansi_strip.py + tests/tools/test_ansi_strip.py + .git/）
    - 在 tmp_path 创建 LICENSE 文件（含 "MIT License"）
    - 创建 .gitignore 含 "output/"
    - 创建 .pre-commit-config.yaml 含 "openevolve-single-import-surface"
    - patch evolution.code.code_target_loader.find_target → 返回 mock CodeTarget
    - patch evolution.code.code_target_loader.find_target_tests → 返回 mock manifest
    - patch evolution.code.code_target_loader.stratify_tests → 返回 mock splits
    - patch evolution.code.code_fitness.score_candidate → 返回 mock CodeFitness（all pass）
    - patch "evolution.code.code_evolver_adapter.evolve" as mock_adapter（不能被调用）
    - patch EvolutionConfig.load → 返回 mock config
    - CliRunner.invoke(main, ["--component", "tools/ansi_strip.py", "--dry-run", "--hermes-repo", str(mock_hermes)])
    - assert result.exit_code == 0
    - assert mock_adapter.call_count == 0（dry-run 不调 adapter）
    
    **test_preflight_fails_without_license(tmp_path)**
    - 构建 mock hermes-agent（不创建 LICENSE 文件）
    - patch 相关函数以通过 step 1-4
    - step 5（LICENSE 存在检查）在 pre-flight 中执行时 = 真实文件系统检查（或 mock Path.exists 为 False）
    - CliRunner.invoke(main, ["--component", "tools/ansi_strip.py", "--dry-run", ...])
    - assert result.exit_code != 0（SystemExit(1)）
    - assert "LICENSE" in result.output.lower() or "license" in result.output.lower()（错误信息提及 LICENSE）
    
    **test_cli_passes_model_to_evolution_config(tmp_path)**
    - 构建全部 mock 使 pre-flight 通过
    - with patch("evolution.core.config.EvolutionConfig.load") as mock_load:
    - CliRunner.invoke(main, ["--component", "tools/ansi_strip.py", "--dry-run", "--model", "qwen-plus", "--hermes-repo", str(mock_hermes)])
    - assert mock_load.call_args.kwargs.get("model") == "qwen-plus" or mock_load.call_args[1].get("model") == "qwen-plus"
    
    注意：mock 层次较深，使用 contextlib.ExitStack 或多个 with patch(...) 嵌套。
    测试须在 < 5s 内完成（全 mock，无真实 LLM/subprocess）。
  </action>
  <verify>
    <automated>pytest tests/code/test_evolve_code_cli.py -x -q</automated>
  </verify>
  <done>
    - pytest tests/code/test_evolve_code_cli.py 输出 "3 passed"
    - pytest tests/code/ -x -q 全部 pass（累积，含 plans 03/04/05/06/07 所有 tests）
    - grep "dry_run_exits_0\|preflight_fails_without_license\|cli_passes_model" tests/code/test_evolve_code_cli.py 输出 3 行
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| CLI --component 输入 → find_target() | 用户可传任意路径；pre-flight + find_target 两层验证 |
| evolve output → NOTICE.md | _contains_secret 过滤防 API key 写入产物 |
| output/code/<ts>/ → hermes-agent | 永远不写回；output-only 策略（PROJECT.md）|

## STRIDE 威胁登记册

| 威胁 ID | 类别 | 组件 | 处置 | 缓解方案 |
|---------|------|------|------|----------|
| T-21-SECRET | Information Disclosure | NOTICE.md 写入 | mitigate | _contains_secret 过滤 fitness.pytest_failures + ruff_findings（Pitfall 7）；pre-flight 检查 api_key 非空但不写入产物 |
| T-21-IMPORT | Tampering | evolve_code.py | mitigate | 无 import openevolve（lazy import via adapter）；grep 验证 |
| T-21-RECURSE | Elevation of Privilege | --component 输入 | mitigate | find_target() 拦截 evolution/ 路径（Plan 03）；pre-flight step 3 也 AST 解析成功检查 |
| T-21-DOS | DoS | evolve() 主循环 | mitigate | --max-cost 5.0 默认；adapter 在 Plan 06 实现 cost tracking |
| T-21-LEAK | Information Disclosure | eval_dir tmp | mitigate | sandbox_runner finally cleanup（Plan 05）；evolve_code.py 不直接管理 eval_dir |
| T-21-SUPPLY | Tampering | openevolve | accept | Apache-2.0；pre-flight step 1 验证可 import |
| T-21-UNREVIEWED | Elevation of Privilege | output/code/<ts>/NOTICE.md | mitigate | NOTICE.md 含 "UNREVIEWED — DO NOT MERGE WITHOUT HUMAN REVIEW"（D-19）；output-only 策略不写回 hermes-agent |
</threat_model>

<verification>
```bash
# 1. 3 个 E2E 测试全 pass
pytest tests/code/test_evolve_code_cli.py -x -q

# 2. D-03/D-04 无 openevolve import
grep "^import openevolve\|^from openevolve" evolution/code/evolve_code.py | wc -l
# 期望: 0

# 3. CONCERNS H1/H3 修复：使用 EvolutionConfig.load()
grep "EvolutionConfig.load" evolution/code/evolve_code.py

# 4. D-19 NOTICE.md
grep "UNREVIEWED" evolution/code/evolve_code.py

# 5. Pitfall 7 防御
grep "_contains_secret" evolution/code/evolve_code.py

# 6. 全套测试回归
pytest tests/code/ -x -q

# 7. dry-run 集成测试（需要 hermes-agent + openevolve 安装）
# 可选：python -m evolution.code.evolve_code --component tools/ansi_strip.py --dry-run
```
</verification>

<success_criteria>
- `pytest tests/code/test_evolve_code_cli.py -x -q` 输出 "3 passed"
- `grep "^import openevolve\|^from openevolve" evolution/code/evolve_code.py | wc -l` == 0
- `grep "EvolutionConfig.load" evolution/code/evolve_code.py` 输出 ≥ 1 行
- `grep "UNREVIEWED" evolution/code/evolve_code.py` 输出 ≥ 1 行
- `pytest tests/code/ -x -q` 全部 pass（plans 02-07 累积 ≥ 18 tests）
</success_criteria>

<output>
完成后创建 `.planning/phases/21-darwinian-code-evolution/21-07-SUMMARY.md`。
</output>
