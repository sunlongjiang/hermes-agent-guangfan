---
phase: 21-darwinian-code-evolution
plan: "08"
type: execute
wave: 2
depends_on: ["21-02", "21-03", "21-04", "21-05", "21-06"]
files_modified:
  - tests/code/test_ansi_strip_holdout.py
autonomous: true
requirements: [V2-CODE-01]

must_haves:
  truths:
    - "tests/code/test_ansi_strip_holdout.py 含 9-10 个 edge case 测试，全部 pass"
    - "测试直接 import strip_ansi from hermes-agent（需 PYTHONPATH 或 conftest.py 设置）"
    - "覆盖 CONTEXT.md §D-07 EDGE_CASE_HOLDOUT_TESTS 列出的所有 10 个场景"
    - "这些测试同时作为 holdout gate 的 edge case 部分（D-15），与 stratified holdout 合并评分"
    - "在 evolution 仓内（tests/code/），不入 hermes-agent"
  artifacts:
    - path: "tests/code/test_ansi_strip_holdout.py"
      provides: "9-10 个 edge case holdout 测试（D-07）"
      min_lines: 80
  key_links:
    - from: "tests/code/test_ansi_strip_holdout.py"
      to: "~/.hermes/hermes-agent/tools/ansi_strip.py strip_ansi()"
      via: "from tools.ansi_strip import strip_ansi（PYTHONPATH 设置）"
      pattern: "from tools.ansi_strip import"

truths:
  - "D-07: EDGE_CASE_HOLDOUT_TESTS 10 个场景（超长/Unicode/嵌套/重叠/空串/单字符/截断CSI/未知OSC/混合无效字节/CRLF）"
  - "测试文件在 evolution 仓 tests/code/（不入 hermes-agent），符合 CONTEXT §Out of Scope 设计"
  - "测试文件顶部通过 `sys.path.insert(0, str(hermes_tools_parent))` 设置 PYTHONPATH，不依赖 conftest.py / pytest 参数"
  - "若 hermes-agent 不可达（CI 环境），pytest.skip 整个 module（不强制失败）"
---

<objective>
实现 `test_ansi_strip_holdout.py`：5-10 个 edge case 测试，直接对 `strip_ansi()` 函数进行黑盒测试，覆盖超出现有 30 个 hermes-agent 测试的边界情形。

这些测试双重用途：
1. 开发时的 regression guard（验证 ansi_strip.py 的极端情况）
2. evolve_code holdout gate 的 edge case 组成部分（D-15）——evolve 出的 best candidate 必须通过这些测试

覆盖 CONTEXT.md §D-07 EDGE_CASE_HOLDOUT_TESTS 的全部 10 个场景（Pitfall 3 防御测试 test_candidate_with_implicit_hermes_import 已在 Plan 05 中实现，此处不重复）。

Purpose: D-07 要求手补 holdout edge case，本测试文件是这批测试的 evolution 仓载体。

Output: tests/code/test_ansi_strip_holdout.py（9-10 个 edge case tests）
</objective>

<execution_context>
@/Users/slj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-darwinian-code-evolution/21-CONTEXT.md
@.planning/phases/21-darwinian-code-evolution/21-RESEARCH.md
@.planning/phases/21-darwinian-code-evolution/21-PATTERNS.md
@.planning/phases/21-darwinian-code-evolution/21-02-SUMMARY.md

<interfaces>
<!-- EDGE_CASE_HOLDOUT_TESTS 列表（CONTEXT.md §D-07 lines 314-325）-->
```python
EDGE_CASE_HOLDOUT_TESTS = [
    "test_extreme_long_input_10k_chars",
    "test_unicode_boundary_in_escape",
    "test_nested_escape_sequences",
    "test_overlapping_escapes",
    "test_empty_string",
    "test_single_char",
    "test_truncated_csi_at_eof",
    "test_unknown_osc_command",
    "test_mixed_invalid_bytes",
    "test_crlf_inside_escape",
]
```

<!-- strip_ansi 函数签名（RESEARCH.md §import 闭包分析 lines 339-342）-->
```python
# hermes-agent/tools/ansi_strip.py（44 行，1784 bytes，纯算法）
# 唯一依赖：import re
# 入口：def strip_ansi(text: str) -> str
```

<!-- ansi_strip.py 处理的转义序列类型（CONTEXT.md §D-07）-->
```
- CSI：\x1b[ ... m/A/B/C/D 等控制序列
- SGR：\x1b[0m, \x1b[31m 等颜色/样式
- OSC：\x1b]...\x07 或 \x1b]...\x1b\\
```

<!-- Analog: hermes-agent/tests/tools/test_ansi_strip.py（外部，30个测试）-->
<!-- 测试结构：直接 from tools.ansi_strip import strip_ansi -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：创建 tests/code/test_ansi_strip_holdout.py（9-10 个 edge case 测试）</name>
  <files>tests/code/test_ansi_strip_holdout.py</files>
  <read_first>
    - .planning/phases/21-darwinian-code-evolution/21-CONTEXT.md §D-07（lines 59-59）
    - .planning/phases/21-darwinian-code-evolution/21-CONTEXT.md §Specific Ideas（lines 312-325）
    - .planning/phases/21-darwinian-code-evolution/21-PATTERNS.md §test_ansi_strip_holdout.py（lines 716-727）
    - ~/.hermes/hermes-agent/tools/ansi_strip.py（必读：44 行源码，确认 strip_ansi 函数签名 + None 行为）
    - ~/.hermes/hermes-agent/tests/tools/test_ansi_strip.py（必读：30 个现有测试，确认已覆盖什么 + 找真实 gap）
  </read_first>
  <behavior>
    - test_empty_string：strip_ansi("") == ""
    - test_single_char：strip_ansi("a") == "a"
    - test_extreme_long_input_10k_chars：10K char 随机文本含 CSI，全部 ANSI 被剥离，纯文本保留
    - test_unicode_boundary_in_escape："\x1b[42m中文\x1b[0m" → "中文"
    - test_nested_escape_sequences：多重嵌套转义，最终结果为纯文本
    - test_overlapping_escapes："\x1b[31m\x1b[32m" 两个连续 SGR，均被剥离
    - test_truncated_csi_at_eof："\x1b[31" 无终止字节 → 结果不 crash，返回 str
    - test_unknown_osc_command："\x1b]99;unknown\x07" → 被剥离（不 crash）
    - test_mixed_invalid_bytes："\x80\x81\x82" C1 控制字节 → 函数不 crash，返回 str
    - test_crlf_inside_escape："\x1b[0m\r\n" → "\r\n"（换行符保留）
  </behavior>
  <action>
    创建 `tests/code/test_ansi_strip_holdout.py`：
    
    **文件头：PYTHONPATH 处理**
    ```python
    """holdout edge case tests for strip_ansi().
    
    这些测试作为 Phase 21 holdout gate 的 edge case 组成部分（D-07 / D-15）。
    测试文件在 evolution 仓内（tests/code/），不入 hermes-agent。
    
    需要 HERMES_AGENT_REPO 指向 hermes-agent 目录，或 ~/.hermes/hermes-agent 存在。
    hermes-agent 不可达时整个 module pytest.skip。
    """
    
    import os
    import sys
    from pathlib import Path
    
    import pytest
    
    # PYTHONPATH 处理：将 hermes-agent 根目录加入，使 "from tools.ansi_strip import strip_ansi" 成功
    _hermes_repo = Path(os.getenv("HERMES_AGENT_REPO", Path.home() / ".hermes/hermes-agent"))
    if not _hermes_repo.exists():
        pytest.skip("hermes-agent not found — skipping holdout tests", allow_module_level=True)
    
    if str(_hermes_repo) not in sys.path:
        sys.path.insert(0, str(_hermes_repo))
    
    from tools.ansi_strip import strip_ansi
    ```
    
    **10 个测试（对应 CONTEXT.md §D-07 EDGE_CASE_HOLDOUT_TESTS）：**
    
    读取 ansi_strip.py 源码后，为每个场景编写精确断言。注意：
    - `test_mixed_invalid_bytes`：\x80 等 C1 字节在 Python 3 字符串中是合法 Unicode 字符（U+0080 等），函数不应 crash；断言 isinstance(result, str)
    - `test_truncated_csi_at_eof`："\x1b[31" 无 m/A/B 等终止符，断言 isinstance(result, str) 且不 raise
    - `test_extreme_long_input_10k_chars`：生成含 "abc\x1b[31mred\x1b[0m" 重复的 10K 字符串，断言 "\x1b" 不在结果中
    - `test_unicode_boundary_in_escape`："\x1b[42m中文\x1b[0m"，断言 result == "中文"
    - `test_nested_escape_sequences`："\x1b[1m\x1b[31mbold_red\x1b[0m\x1b[0m"，断言 result == "bold_red"
    - `test_overlapping_escapes`："\x1b[31m\x1b[32mtext\x1b[0m"，断言 result == "text"
    - `test_empty_string`：assert strip_ansi("") == ""
    - `test_single_char`：assert strip_ansi("a") == "a"
    - `test_unknown_osc_command`："\x1b]99;payload\x07text"，断言 result == "text"（OSC 被剥离）
    - `test_crlf_inside_escape`："\x1b[0m\r\ntext"，断言 "\r\n" in result（换行符保留）
    
    每个测试前读取 ansi_strip.py 源码，确认行为与实际实现一致（现有 30 个 tests 已通过，edge case 测试的断言应基于真实函数行为，不是假设）。
    
    **conftest.py 不需要修改**（sys.path.insert 在本文件顶部处理）。
  </action>
  <verify>
    <automated>pytest tests/code/test_ansi_strip_holdout.py -x -q</automated>
  </verify>
  <done>
    - pytest tests/code/test_ansi_strip_holdout.py 输出 "9 passed" 或 "10 passed"（或 "skipped" 若 hermes-agent 不可达）
    - grep "test_empty_string\|test_extreme_long_input\|test_unicode_boundary\|test_truncated_csi\|test_unknown_osc\|test_crlf_inside" tests/code/test_ansi_strip_holdout.py 输出 ≥ 6 行
    - grep "from tools.ansi_strip import strip_ansi" tests/code/test_ansi_strip_holdout.py 输出 1 行
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| hermes-agent strip_ansi 函数 → 测试输入 | 仅读取，只需 hermes-agent 可读访问 |

## STRIDE 威胁登记册

| 威胁 ID | 类别 | 组件 | 处置 | 缓解方案 |
|---------|------|------|------|----------|
| T-21-SECRET | Information Disclosure | 测试文件 | accept | 测试不含 API key；仅对 strip_ansi 黑盒测试 |
| T-21-IMPORT | Tampering | test_ansi_strip_holdout.py | accept | 本文件不 import openevolve；grep gate 覆盖 |
| T-21-RECURSE | Elevation of Privilege | hermes-agent import | mitigate | sys.path 仅加 hermes-agent 根目录；不加 evolution/ 到 sys.path |
| T-21-DOS | DoS | test_extreme_long_input | accept | 10K chars 单次调用 strip_ansi，< 10ms，不构成 DoS |
| T-21-LEAK | Information Disclosure | tmp files | accept | 纯函数测试，无 tmp 目录创建 |
| T-21-SUPPLY | Tampering | hermes-agent strip_ansi | accept | 只读；hermes-agent 已验证可独立运行（RESEARCH.md） |
| T-21-UNREVIEWED | Elevation of Privilege | output/ | accept | 本 plan 不生成 output/ 产物 |
</threat_model>

<verification>
```bash
# 1. edge case 测试（需 hermes-agent 可达）
pytest tests/code/test_ansi_strip_holdout.py -x -q

# 2. 测试函数名覆盖 D-07 列表
grep "def test_" tests/code/test_ansi_strip_holdout.py | wc -l
# 期望: ≥ 9

# 3. D-03 无 openevolve import
grep "^import openevolve\|^from openevolve" tests/code/test_ansi_strip_holdout.py | wc -l
# 期望: 0

# 4. 全套测试回归
pytest tests/code/ -x -q
# 期望：≥ 19 passed（plans 02-08 累积）

# 5. D-18 import boundary 仍 pass
pytest tests/code/test_import_boundary.py -x -q
```
</verification>

<success_criteria>
- `pytest tests/code/test_ansi_strip_holdout.py -x -q` 输出 "≥9 passed" 或 "module-level skip"（hermes-agent 不可达时）
- `grep "def test_" tests/code/test_ansi_strip_holdout.py | wc -l` ≥ 9
- `pytest tests/code/ -x -q` 全部 pass（plans 02-08，≥ 19 tests 或含 skips）
- `grep "^import openevolve\|^from openevolve" tests/code/test_ansi_strip_holdout.py | wc -l` == 0
</success_criteria>

<output>
完成后创建 `.planning/phases/21-darwinian-code-evolution/21-08-SUMMARY.md`。

同时更新 `.planning/phases/21-darwinian-code-evolution/21-VALIDATION.md` 的 frontmatter：
- `nyquist_compliant: true`
- `wave_0_complete: true`（Wave 0 + Wave 1 + Wave 2 全部完成）
</output>
